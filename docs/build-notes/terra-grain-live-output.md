Verified again:

- `npx tsc --noEmit` — exit 0.
- `npm run regression` — all six suites passed, including `SupabaseGrainRepository`.

UI rendering remains unverified because the task explicitly prohibited starting servers.