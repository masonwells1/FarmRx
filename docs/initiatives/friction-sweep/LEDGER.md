# Initiative FS — Friction Sweep ledger

This ledger is append-only. Never edit, reorder, or delete an earlier entry. If an entry is wrong, append a correction that cites it. Times use `America/Chicago`.

## FS-000 — Initiative context and slice 1 outcome

- **Date/time:** 2026-09-13 10:45 -05:00 (`America/Chicago`), recorded after the fact.
- **Authority:** the `Initiative FS — Friction Sweep` section of the 2026-09-05 owner amendment in `docs/GOAL.md`. Mason authorized agents to push feature branches and open, update, label, and comment on their pull requests without asking (`docs/agent-delivery.md`). Merge, deploy, live migration or data, secrets/auth/permissions, customer actions, purchases, and destructive actions still require Mason's explicit approval in the current conversation. Only Mason posts `@coderabbitai review`.
- **Slice plan:** FS is one tranche split by module into three slices. Slice 1 (screen tier plus one pure helper change): replace browser `prompt`/`confirm` with in-app dialogs and derive TradingView contract months from the crop year. Slice 2 (screen tier): phone layouts for the profit matrix, cost table, cost-of-carry, and plan-comparison tables; plain-English relabel pass; one sentence and one button on every empty state. Slice 3 (full tier): persist the Grain sale limit, the cost-of-carry grid, and the U of I default badges to farm-scoped settings, and seed `usda_report_dates` by migration.
- **Slice 1 outcome:** pull request #42 (`claude/app-improvement-strategy-kzqk6s`) was reviewed by CodeRabbit (four findings, all repaired or withdrawn) and Codex (ten findings across seven rounds, all repaired), brought current with `main` after the Soil Rx merge (#37), and merged by Mason's explicit approval on 2026-09-13 as `9a9a1c54ee33f0b690ac33fc19035bcb60e69637`. Repairs proven during review included acquiring the submit lock before opening a dialog and releasing it on cancel across Programs, Grain, Field Log, Scouting, Inventory, and Equipment (`src/FieldLogModule.deleteDoubleTap.regression.tsx` pins the double-tap behavior), and rejecting crop years above 2098 in `src/components/MarketQuote.tsx` so the wheat contract year stays inside the frame allowlist.
- **Deploy state:** Vercel deploys `main` to production automatically. This session cannot see the Farm Rx Vercel project; Mason has not yet confirmed that the `9a9a1c5` production deploy is live.

## FS-001 — Slice 2 implementation and screen-tier proof

- **Date/time:** 2026-09-13 10:45 -05:00 (`America/Chicago`).
- **WHERE:** branch `claude/app-improvement-strategy-kzqk6s` restarted from `origin/main` `9a9a1c54ee33f0b690ac33fc19035bcb60e69637` (the slice 1 merge). This entry is committed together with the slice 2 code.
- **Tier:** screen tier. No migration, Row Level Security, capability check, privacy rule, money math, edge function, or write path changed. The pure-presentation boundary was checked by reading the diff: every change is JSX structure, class names, `data-label` attributes, wording, a `Link` or button that navigates, or CSS.
- **Phone layouts:** the cost table, plan-comparison table, ROI table, and cost-of-carry table now carry `phone-stack`, and each cell carries a `data-label`. At 767px and below the header row becomes screen-reader-only, each row becomes a two-column label/value grid, and category and verdict cells span the full width. The profit matrix is a true two-axis grid, so instead of stacking it keeps horizontal scroll with a sticky yield column, 72px by 48px cell buttons, and a one-line hint that only shows on phones. Desktop rendering is unchanged.
- **Relabels:** `BU TO COVER` → `Bushels to cover`; `Mo` → `Months stored`; `Basis open` / `Futures open` → `Basis not set` / `Futures not set`; `Open legs` / `manual valuation` → `Still needs a price` / `valued at your estimate`; `Insurance-backed marketing estimate` → `Bushels safe to sell (insurance floor)`; `superseded by baseline` → `replaced by a later bin count`; plan-comparison headers spelled out (`Cost per acre`, `Break-even price`, `Break-even yield`, `Extra bushels needed`); the matrix corner now reads `Yield ↓ · Price →` with a screen-reader sentence.
- **Empty states:** Field Log, Harvest, Scouting, Soil Rx, and Weather now offer `Add a field`; Equipment offers `Add a machine`; Programs offers `Add a program` (editable members) and `Assign a program to a field`; Notifications offers `Set up a grain price alert`. Each keeps one plain sentence. `tests/e2e/soil-rx.spec.ts` pinned the old Soil Rx empty-state source string and was updated to the new one.
- **Proof observed:** `npx tsc -b --force` exit 0; `npm run build` exit 0; `npm run test:agent-guidance` passed; `git diff --check` clean; focused regressions passed (Profitability equipment cost, Grain receipt, Equipment quick action, Field Log legacy recovery and delete double-tap, Harvest receipt, Programs chunk 5, Supabase Programs repository, cost of carry, Inventory compliance); the dedicated Soil Rx Playwright spec passed 36 of 36 locally with the pre-installed Chromium. `npm run regression` includes the `programInventoryCW2` lane, which needs PowerShell and cannot run in this Linux session; it runs in the Foundation workflow on the pull request.
- **Browser proof:** a scratchpad harness rendered the real `GrainCostOfCarry`, `FieldLogPage`, and `EquipmentPage` components with the real stylesheet inside a router, plus faithful replicas of the four Profitability tables (the Profitability screen needs live Supabase data, which this session does not have). At 390×844: all four stacked tables measured 388px wide, rows rendered as grids, `data-label` pseudo-content was present, the header row was visually hidden, the matrix yield column was sticky, matrix buttons measured 72×48px, the phone hint was visible, empty-state buttons measured 52px, and the page had no horizontal scroll. At 1280×800 the tables rendered as ordinary rows and the hint was hidden. Fourteen screenshots were kept in the session scratchpad. The one console error seen on the phone run was Chromium's own blocked outbound probe, not an app asset.
- **Remaining risk:** the Profitability tables were proven through replicas, not the live screen; the pull request's Vercel preview is the place to confirm them with real data. A fresh-context read-only review of the exact commit is still required for the screen tier.
- **Rollback path:** revert the slice 2 commit; it touches no data.
- **Next:** push the branch, open a draft pull request, request the Codex review, and stop at `READY FOR APPROVAL` before merge. Slice 3 starts after slice 2 has a committed, reviewed tip.

## FS-002 — Codex review repairs on slice 2

- **Date/time:** 2026-09-13 11:05 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed commit `204ef34b125a6d3da64b9291d2e165ad56b0a30a` on pull request #43 and returned three findings. All three were verified against the code and repaired in the next commit; this entry corrects FS-001 where noted.
- **Finding 1 (P1, wording):** FS-001 relabeled the insurance number `Bushels safe to sell`. That overstates it. The value is APH × coverage × acres, Revenue Protection pays money rather than bushels, and the note beside it already warns the farmer can be left exposed. Both labels now read `Insurance floor estimate` (Grain) and `Insurance floor estimate (bushels)` (Profitability). Plain English must not promise more than the math does.
- **Finding 2 (P2, permission):** the new Equipment empty-state `Add a machine` button showed for every member, but the add form only renders for owners and managers, so a worker who tapped it saw a blank page. The button now renders only when the viewer can manage equipment, matching the header button.
- **Finding 3 (P2, layout):** the editable cost line's bushels cell had no `data-label`, so a hand-entered cost row showed a bare `20 bu` on phones. The label `Bushels to cover` was added.
- **Proof:** `npx tsc -b --force` exit 0; focused regressions and `npm run build` rerun as recorded in the pull request; Foundation gate on the new head.

## FS-003 — Second Codex round: empty-state actions gated by permission

- **Date/time:** 2026-09-13 11:20 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `34ba047d94b5c5453fb83537223a25873b0a9057` and returned two findings of the same class as FS-002's Equipment finding: an empty-state button offered to members who cannot complete the action.
- **Finding 1 (P2):** the `Add a field` link on the Soil Rx, Field Log, Harvest, and Scouting empty states showed for read-only members and named reps, who land on a write-locked Fields page. Each link is now gated by that module's existing edit check (`canEdit` in Soil Rx, the owner/manager/worker role check in Field Log, Harvest, and Scouting). Weather needs no gate: its route is only reachable by members who can edit.
- **Finding 2 (P2):** the `Set up a grain price alert` button on the empty Alerts screen showed to every viewer, but Grain requires private-financial access, and even an eligible member landed on the Grain overview rather than the alerts tab. The button now renders only when `canEditFarmModule(profile, 'grain')` is true and navigates to `/grain/alerts`.
- **Rule recorded:** an empty-state action must be gated by the same permission as the action it advertises, and must land on the screen where the action is completed.
- **Proof:** `npx tsc -b --force` exit 0; `npm run build` exit 0; `git diff --check` clean; regressions passed for the Supabase Notifications repository, Field Log legacy recovery and delete double-tap, and Harvest receipt. The Soil Rx spec's pinned empty-state source string is unchanged by this edit.

## FS-004 — Third Codex round: Programs assignment action gated

- **Date/time:** 2026-09-13 11:30 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `96cc540d8dab04e7232f56f3d6450bd3e10ca294` and returned one finding: the `Assign a program to a field` button on the Programs season tracker's empty state rendered for read-only members, where the route's write lock disables it. The button is now gated by the tracker's existing `canEdit` prop, matching the `Add a program` empty-state action. This is the last empty-state action added by slice 2 that lacked a permission gate; every one now follows the rule recorded in FS-003.
- **Proof:** `npx tsc -b --force` exit 0; `npm run build` exit 0; `git diff --check` clean; Programs regressions (chunk 5, Supabase Programs repository) passed.

## FS-005 — Fourth Codex round: category subtotal labeled on phones

- **Date/time:** 2026-09-13 11:40 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `c95132dfba10d98291251e98ebf22cc3cce1f1e5` and returned one finding: on phones the cost table's category subtotal row showed a bare dollar amount beside the item count and toggle, because that cell had no `data-label`. The cell now carries `Subtotal $ per acre`, so every stacked number in the table has a visible label.
- **Proof:** `npx tsc -b --force` exit 0; `npm run build` exit 0; `git diff --check` clean; Profitability equipment-cost regression passed.

## FS-006 — Fifth Codex round: empty season tracker routes to a completable step

- **Date/time:** 2026-09-13 11:50 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `d8ec4b317e1a2c4d1a33af875391d9eafef4d85b` and returned one finding: on a farm with no unarchived program, or no field crop, the season tracker's `Assign a program to a field` button opened an assignment picker that could not be completed. The empty tracker now chooses its one button from what exists: no program yet offers `Add a program` (opens the program builder); no field crop yet offers `Add a field` (goes to Fields); otherwise `Assign a program to a field` opens the assign tab. The sentence above the button changes to match. Read-only members see the sentence only.
- **Proof:** `npx tsc -b --force` exit 0; `npm run build` exit 0; `git diff --check` clean; Programs chunk 5 and Supabase Programs repository regressions passed.

## FS-007 — Sixth Codex round: assign offered only when a program fits a crop

- **Date/time:** 2026-09-13 12:00 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `90d70413c7aaa4e9ed4f03b47c95f53fdda607aa` and returned one finding: FS-006 offered `Assign a program to a field` whenever any program and any crop existed, but the assignment picker only pairs a program with crops of a matching commodity and crop year, so a soybean program plus a corn-only farm still led to a dead picker. The empty tracker now offers Assign only when at least one unarchived program fits at least one field crop; otherwise it offers `Add a program` with a sentence explaining that no program matches a field crop's crop and year yet. The fit rule is one shared function, `programFitsCrop`, used by both the picker and the empty state so the two cannot drift.
- **Proof:** `npx tsc -b --force` exit 0; `npm run build` exit 0; `git diff --check` clean; Programs chunk 5 and Supabase Programs repository regressions passed.

## FS-008 — Slice 2 merged; slice 3 preflight

- **Date/time:** 2026-09-13 12:15 -05:00 (`America/Chicago`).
- **Slice 2 outcome:** pull request #43 was frozen at `bd36cb0d271320ef2224464b3a5c93bfaa353ed7` with Codex clean (seven rounds, nine findings, all repaired in new commits) and the Foundation gate green on every commit. Mason approved the merge in the session's `AskUserQuestion` ("Merge now"); the squash merge landed on `main` as `c56e267beb4226da5820d1e6027ca282a87b2558` on 2026-09-13. CodeRabbit was not spent on this slice by Mason's earlier standing instruction that a Codex-clean candidate may be called good. Vercel deploys `main` to production automatically; the deploy has not yet been confirmed by Mason.
- **Branch state:** `claude/app-improvement-strategy-kzqk6s` merged `origin/main` (`d430fea`) with no force-push; its tree is identical to `main`.
- **Slice 3 preflight (full tier):** the three browser-only values were located. The Grain sale limit lives only in React state keyed by the five-part position scope (`src/GrainModule.tsx`, `saleLimits`) and is lost on reload. Cost-of-carry settings live in `localStorage` keyed per farm (`src/GrainCostOfCarry.tsx`) and the per-estimate price grid lives only in React state. The U of I default badges live in a global `localStorage` map keyed by cost line id (`src/ProfitabilityModule.tsx`, `DEFAULTS_KEY`). `public.usda_report_dates` exists (global, select-only for authenticated users) with no seed rows in any migration or `supabase/seed.sql`.
- **Environment limits for the full tier:** Docker and `psql` are available, so a disposable `postgres:17` proof can be run from bash by replaying the PowerShell script's steps. PowerShell is not installed, so `scripts/verify-*.ps1` cannot run as written here. `www.usda.gov` and `www.nass.usda.gov` are blocked by the session's egress policy, so USDA report dates cannot be sourced or verified from this session; the seed needs dates supplied by Mason or a session with that access. No date will be invented.
- **Gate:** the runbook's plan gate applies to migration work. A plain-English slice 3 plan is being presented to Mason; no slice 3 product code, migration, or seed has been written.

## FS-009 — Slice 3 built and proven (full tier, items 1 to 3)

- **Date/time:** 2026-09-13 13:40 -05:00 (`America/Chicago`).
- **Authority:** Mason approved the slice 3 plan in the session's `AskUserQuestion` ("Build items 1 to 3 now"): persist the Grain sale limit, the cost-of-carry settings and grid, and the U of I default badge. Item 4, the `usda_report_dates` seed, stays blocked on report dates this session cannot source; no date was invented.
- **Migration `20260913174500_fs_persist_grain_and_budget_settings.sql`:** three private-financial tables and one additive column. `grain_sale_limits` holds one nullable limit per five-part position scope (`nulls not distinct` unique). `grain_carry_settings` holds one row per farm (mode, monthly and flat rates, interest, trucking; all non-negative). `grain_carry_grids` holds thirteen `{market_price, basis}` rows per production estimate, shape-checked by the immutable `grain_carry_rows_valid` function, cascading with the estimate. `budget_cost_lines.university_default_amount` remembers the seeded U of I amount. Every table has `set_updated_at` and `prevent_farm_id_change` triggers, row-level security that reads with `can_read_private_financials` and writes with `can_edit_farm` (the `grain_alert_settings` pattern), no anon privileges, and a covering index on every foreign key.
- **Application:** new types and validators (`src/data/grainSettings.ts`); the Grain gateway loads the three tables and reports `capabilities.persisted_settings`, treating a missing table (`42P01`/`PGRST205`) as "release not applied yet" so the screens keep their session-only behavior until the migration is live; strict row mappers, farm-binding save operations, three offline-queue kinds with exact-key validators and overlay/replay, and mock-repository parity. The Grain screen seeds sale limits from the farm and saves on blur or Enter; the cost-of-carry calculator loads the farm's settings and grid and saves each change after a 600 ms pause, one save at a time so every save carries the freshest `updated_at`. The cost-line badge now reads `university_default_amount`; seeding sets it, hand edits leave it behind, and the browser-only map is gone. Cost-line saves retry without the column on `PGRST204` until the migration is live; copies of a budget do not carry badges, matching prior behavior.
- **Compatibility note:** the offline write queue's cost-line validator accepts the optional `university_default_amount` key alongside the original exact key set; older queued entries still parse.
- **Disposable proof:** `scripts/verify-fs-persist-disposable.sh` (Linux, throwaway local PostgreSQL 16 cluster) applied all 54 migrations and ran `scripts/sql/fs-persist-disposable-assertions.sql`: schema shape and privileges, twelve authenticated policies, covering indexes, owner read and update, null-limit round trip, duplicate scope refused, negative values refused, unknown mode refused, grids with the wrong row count, a string price, an extra key, or harvest month 12 refused, farm move refused, cross-farm entity and cross-farm estimate refused, another farm's owner sees nothing and cannot insert, update, or delete, a worker without financial access sees nothing, estimate deletion cascades to the grid, and the cost-line column exists with its non-negative check. Printed `FS_PERSIST_DISPOSABLE_PASS`. `scripts/verify-fs-persist-disposable.ps1` runs the same SQL against `postgres:17` in Docker and is registered as Foundation lane 27; the static guard's lane count was raised from 26 to 27. Docker's daemon is not available in this session, so the PowerShell twin's first run is the Foundation workflow on the pull request.
- **Ladder:** `npx tsc -b --force` exit 0; regressions passed for cost of carry, marketing alerts, grain alerts, queued operation context, grain repair, mock and Supabase grain repositories (the Supabase one extended with strict mapping, farm binding, rejection, and offline replay cases for the three new kinds), Grain receipt, mock and Supabase profitability repositories, Profitability equipment cost, and the new `grainSettings.regression.ts` (validators, queue parsing, mock round trip; registered in `npm run regression`); `npm run build` exit 0; `npm audit --audit-level=high` zero findings; `node scripts/foundation-static-guards.mjs` PASS; `node scripts/verify-foundation-mutations.mjs` PASS, 184 of 184 controlled mutations turned the gate red; `tests/e2e/foundation-shell.spec.ts` 52 passed and 14 intentional skips on desktop and phone with the three tables added to its exact-query mock; `git diff --check` clean; credential-like scan of the diff found nothing.
- **Browser proof:** a scratchpad harness rendered the real `GrainCostOfCarry` with a fake persistence layer at 1280×800 and 390×844. It loaded the farm's flat-rate settings and the saved grid (harvest month 8, harvest price 4.20), a mode toggle produced one settings save with the workspace's `updated_at`, typing a price produced one thirteen-row grid save with the existing id, changing the harvest month produced a second grid save carrying the refreshed `updated_at`, the phone page had no horizontal scroll, no page errors, and with `persisted_settings` off the component wrote only to `localStorage` and showed the device-only note.
- **Remaining risk and unrun lanes:** the full `npm run regression` (PowerShell-only `programInventoryCW2` lane) and the PowerShell disposable twin run in the Foundation workflow; the sale-limit blur commit on the Grain overview was proven by code reading and TypeScript only, not in a browser, because the overview needs the live Supabase workspace; the live migration is a separate hard gate that Mason applies; existing cost lines have no badge until re-seeded. A fresh-context read-only review of the exact commit follows as its own ledger entry.
- **Rollback:** revert the commit; the migration is additive and the application tolerates its absence.

## FS-010 — Correction to FS-006: archived-field crops no longer count as assignable

- **Date/time:** 2026-09-13 13:55 -05:00 (`America/Chicago`).
- **What happened:** when pull request #43 was marked ready for review, Codex ran once more on `bd36cb0` and posted one further P2 finding at 16:47 UTC, after the review threads had been checked and while the merge was being performed. The merge of #43 therefore carried an open finding; this entry records it and its repair rather than rewriting FS-008.
- **Finding:** the Programs crop-assignment read joined `fields` without filtering `is_active`, so a farm whose only crops sit on archived fields still counted as "has a field crop". The FS-006 empty tracker then offered `Assign a program to a field`, and the picker listed the archived field's crop.
- **Repair:** the Programs gateway now selects `fields.is_active` and filters the inner join to active fields (`fields.is_active=eq.true`), so both the empty-tracker prerequisite and the assignment picker see only crops on active fields. The `assign_program` RPC's own acceptance of archived-field crops is a pre-existing server-side gap outside the Friction Sweep scope and is noted for a later tranche.
- **Proof:** `npx tsc -b --force` exit 0; Programs chunk 5 and Supabase Programs repository regressions passed. The change rides pull request #44 as its own commit because the designated branch is the only push target; it is unrelated to the slice 3 migration and reviewable on its own.

## FS-011 — Foundation repair: access-epoch guard on the three new tables

- **Date/time:** 2026-09-13 14:15 -05:00 (`America/Chicago`).
- **Failure:** the Foundation workflow on `1927e99` failed in the `verify-0040-disposable.ps1` lane with `3 farm-scoped tables lack the access epoch guard`. Migration 0040 requires every `public` table with a `farm_id` column to carry the `farm_access_epoch_guard` trigger; the slice 3 migration mirrored `grain_alert_settings` (which predates 0040 and received its guard there) but did not add the trigger. The local bash proof did not check this rule, which is why it passed.
- **Repair:** the migration now creates `farm_access_epoch_guard` (`before insert or update or delete`, `guard_row_farm_access_epoch()`) on `grain_sale_limits`, `grain_carry_settings`, and `grain_carry_grids`. The migration file is edited in place because it has never been applied anywhere. The assertions SQL now checks that all three guards exist, and its two cross-farm insert probes accept the guard's `FARM_ACCESS_EPOCH_CHANGED` refusal as well as a row-level-security refusal, since the guard fires first.
- **Proof:** `scripts/verify-fs-persist-disposable.sh` printed `FS_PERSIST_DISPOSABLE_PASS` again on the throwaway cluster; the Foundation workflow re-runs on the new head.

## FS-012 — Fresh-context read-only review of `1927e99` and the repair round

- **Date/time:** 2026-09-13 14:45 -05:00 (`America/Chicago`).
- **Review:** a fresh-context, read-only reviewer (Claude, Explore profile, opus model) reviewed the exact tranche commit `1927e9941a7e404aa7313a7ff4472a53f48f110d` against the FS scope and the repository standards. Verdict: **REPAIR**, fourteen findings, all confirmed by tracing; it also recorded what it checked and found clean (all twelve RLS policies, anon/public revoked, composite FK targets, PG15 compatibility, cascade behavior, offline replay re-stamping, strict cross-farm rejection, read-only members fenced by the route's write lock, e2e mock queries exact, no writes to bins/contracts/deliveries/on-hand, badge and copy semantics, plain-English text).
- **Dispositions (every finding repaired in the commit carrying this entry unless noted):**
  - F1 P0, missing `farm_access_epoch_guard` on the three tables: repaired earlier in `b125293` (FS-011).
  - F2 P2, every debounced carry save ran a full workspace `refresh()` (seventeen queries plus alert-transition writes per keystroke pause): the three save methods now return the server row through the Supabase, queued, and mock repositories, and the screen adopts that row into the workspace locally; `refresh()` runs only after a failed save so the screen learns what another device may have written.
  - F3 P2, a refresh could overwrite a sale limit still being typed on another position card: scopes being edited are tracked and skipped by the refresh merge until committed.
  - F4 P2, unrecoverable stale-version paths (fabricated `updated_at` after a failed refresh, a memoized client id colliding with another device's grid, a regenerated sale-limit id on scope collision): the local adoption of the returned row removes the fabricated stamp, and a failed save now refreshes once so the next attempt uses the existing row's id and version.
  - F5 P2, `grain_carry_rows_valid` lacked `set search_path`: added, with execute revoked from `public`/`anon` and granted to `authenticated`/`service_role`.
  - F6 P2, no automated coverage of the missing-table (`42P01`/`PGRST205`) and missing-column (`PGRST204`) compatibility paths: the decisions were lifted into exported pure helpers (`settingsSlicesFromResults`, `saveCostLineWithBadgeFallback`) and `grainSettings.regression.ts` now proves both, including that other errors still fail closed.
  - F7 P3, exact echo comparison on values the database rounds: sale limits and carry numbers are rounded through `boundedDecimal` to the column scales before sending.
  - F8 P3, save-receipt key collision between alert settings and carry settings: carry settings receipts are namespaced.
  - F9 P3, mock accepted duplicate scope or estimate rows: the mock now matches on the natural key and refuses a second row like the database does.
  - F10 P3, Enter plus blur could commit a sale limit twice: a submit lock guards the commit and Enter prevents its default.
  - F11 P3, the grid validator relied on boolean short-circuiting for scalar input: rewritten with explicit `case` guards; the assertions now cover a scalar and an array of scalars.
  - F12 P3, the PowerShell lane checked only the exit code: it now requires the `FS_PERSIST_DISPOSABLE_PASS` token like the bash twin.
  - F13 P3, an unstable empty-grid fallback defeated the calculation memo: one stable empty grid per mount.
  - F14 P3, unreferenced `unique (id, farm_id)` indexes on two tables: dropped.
- **Proof after repairs:** `npx tsc -b --force` exit 0; fourteen regressions passed (the grain, profitability, and Programs sets plus the extended `grainSettings` regression); `npm run build` exit 0; static guards PASS; `git diff --check` clean; `scripts/verify-fs-persist-disposable.sh` printed `FS_PERSIST_DISPOSABLE_PASS` on the revised migration; the browser harness reproduced the same desktop and phone results (loads, one settings save, two grid saves with the refreshed version, no horizontal scroll, legacy mode device-only).
- **Note on the reviewer's process finding:** the tranche's own proof had not covered the 0040 guard rule, so the "full verification tier" claim in FS-009 was overstated for that rule. FS-011 corrected the proof; this entry records the overstatement rather than editing FS-009.

## FS-013 — Codex findings on `b125293`: version-paired drafts and farm-safe flushing

- **Date/time:** 2026-09-13 15:20 -05:00 (`America/Chicago`).
- **Findings (Codex, on `b125293`):** (P1) the calculator did not resync when a refresh brought carry rows changed on another device, yet its next save took the refreshed `updated_at`, so a stale draft could overwrite the newer row without an optimistic-lock conflict; (P1) the unmount-time flush started an asynchronous save that could run after the farmer switched farms, where the queued repository rebinds the row's farm; (P2) Enter plus blur could commit the same sale limit twice, already repaired by the submit lock in `fc86073`.
- **Repairs:** the calculator now remembers the row version each displayed draft came from and sends that version, so a row changed elsewhere conflicts and, after the one recovery refresh, replaces the pristine draft; pristine drafts resync automatically when the workspace's rows change, drafts being edited keep their original version; every rate, price, basis, and harvest-month control flushes on blur so leaving the screen saves while the original farm is still selected; the page's save wrappers confirm the live selected farm still matches the row's farm before any save and otherwise do nothing.
- **Proof:** `npx tsc -b --force` exit 0; Grain receipt, Supabase grain, mock grain, and grainSettings regressions passed; `npm run build` exit 0; `git diff --check` clean; the browser harness at 1280×800 and 390×844 now also proves that a simulated other-device change replaces the displayed values (monthly mode, rate 9, harvest month 3, harvest price 5.50) and that the following saves carry the new version `R1`, and that tabbing out of a grid cell produces a save at once instead of after the 600 ms pause.

## FS-014 — Codex findings on `fc86073`: per-position sale-limit lock; version race already closed

- **Date/time:** 2026-09-13 15:40 -05:00 (`America/Chicago`).
- **Findings (Codex, on `fc86073`):** (P1) one shared submit lock across every position card let a slow save on one card silently drop a commit on another, leaving that limit dirty with no retry; (P1) a second debounced carry edit could start its save before the returned `updated_at` reached the calculator, so it would send the old version and conflict.
- **Dispositions:** the lock is now keyed per position scope, the commit reads the workspace through a ref that is updated synchronously with the saved row, and when the farmer kept typing while a save was in flight the newest value is committed once more after the lock releases. The carry version race was already closed in `65943b1`, where the calculator updates its remembered version from the returned row inside the chained save itself, before the next queued save can start; recorded here as already repaired rather than reopened.
- **Proof:** `npx tsc -b --force` exit 0; Grain receipt and Supabase grain regressions passed; `npm run build` exit 0; `git diff --check` clean.

## FS-015 — Codex findings on `b3f3a2b`: financial access required to write; mode buttons flush at once

- **Date/time:** 2026-09-13 15:55 -05:00 (`America/Chicago`).
- **Findings (Codex, on `b3f3a2b`):** (P1) the write policies required only `can_edit_farm`, so an active worker without financial access could insert or change these private grain settings through the Data API even though the screen is closed to them; (P1) the storage-mode buttons had no flush of their own, so a mode change made within the pause and followed by a farm switch was lost.
- **Repairs:** every insert, update, and delete policy on the three tables now requires `can_edit_farm(farm_id) and can_read_private_financials(farm_id)`; the assertions prove the worker without financial access cannot insert a sale limit or a carry grid and updates zero carry-settings rows. The mode buttons now save as soon as React has committed the click. While re-proving, the harness showed that this immediate flush also fired in legacy (device-only) mode, so both flush functions now return early unless the farm's tables are live; the legacy run again records zero farm saves and one `localStorage` key.
- **Proof:** `scripts/verify-fs-persist-disposable.sh` printed `FS_PERSIST_DISPOSABLE_PASS`; `npx tsc -b --force` exit 0; Grain receipt, Supabase grain, and grainSettings regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness unchanged on desktop and phone (five saves in the expected order, remote resync, blur flush within 60 ms, no errors) and legacy mode back to zero saves.

## FS-016 — Codex finding on `809335e`: in-flight settings work is visible to the farm switcher

- **Date/time:** 2026-09-13 16:15 -05:00 (`America/Chicago`).
- **Finding (Codex, P1):** a carry edit made while another save was in flight waited in the calculator's in-memory chain; the farm switcher's `hasPendingFarmWork` could not see it, so a farm switch in that window let the live farm check discard the edit.
- **Repair:** a small in-memory registry (`src/data/pendingSettingsWork.ts`) counts settings saves that are queued, in flight, or still unflushed per farm; `hasPendingFarmWork` consults it first, so the existing "Switch away from this farm?" confirmation now covers those edits until every chained save has run. The soil-cleanup lines the static guard pins are unchanged.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; farm-context regression (new case: in-flight work warns for that farm only and clears when done), Grain receipt, Supabase grain, and grainSettings (registry counting, idempotent completion) regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness unchanged (five saves in order, remote resync, blur flush, no errors, legacy mode zero saves).

## FS-017 — Codex findings on `e6698dd`: settings edits are sent and awaited before a confirmed farm switch

- **Date/time:** 2026-09-13 18:55 -05:00 (`America/Chicago`).
- **Findings (Codex, on `e6698dd`):** (P1) a sale-limit commit started by the blur that a farm-selector click causes never registered with the pending-work registry, so on a slow connection the switch proceeded without a warning and the live-farm guard rejected the typed limit; (P1) when a farmer confirmed the "Switch away from this farm?" dialog while a carry save was queued or slow, the registry only knew about an in-memory closure, so the switch changed the farm context and reloaded the page before the save had reached the server or the durable queue, and the save was then rejected or cut off.
- **Repairs:** the registry now also lets a screen register how to send its unflushed edits, and offers `settlePendingSettingsWork(farmId)`, which sends them and waits until every pending save for that farm has finished (each save either returns from the server or lands in the durable grain queue before its token clears; the wait is bounded so a save that keeps re-queuing itself cannot hold the switch forever). The farm switch in `App.tsx` now awaits that settle after the farmer confirms (and harmlessly when nothing is pending) and before the farm selection changes, so every save still runs under the original farm. The sale-limit commit holds a pending token synchronously from the moment it starts until the save (and any follow-up commit for a value typed in flight) has finished, an edit still being typed holds one until its commit starts, and the Grain page registers a flush that commits every position still being typed. The carry calculator registers its two flush functions the same way.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; grainSettings regression (new cases: settle sends unflushed edits first, waits for a save and for one chained behind it, leaves other farms alone, ignores an unregistered screen, and is bounded), farm-context, Supabase grain, and Grain receipt regressions passed; `npm run build` exit 0; `git diff --check` clean; the browser harness at 1280×800 and 390×844 keeps its earlier facts (five saves in order, remote resync, blur flush, no errors, legacy mode zero saves) and now also shows a grid value typed without leaving the cell as pending, a settle that sends it and waits about 400 ms for a deliberately slow save (`5->6` saves, last row carries 5.1), and no pending work afterwards.
- **Limit of this proof:** the switch path itself (`chooseFarm` in `App.tsx`) was exercised by the unit regression of the settle function and by reading the code, not by a signed-in two-farm browser session.

## FS-018 — Codex findings on `40cddf5`: a failed or stalled settings save refuses the farm switch; copied cost lines drop the U of I badge

- **Date/time:** 2026-09-13 19:20 -05:00 (`America/Chicago`).
- **Findings (Codex, on `40cddf5`):** (P1) the switch-time settle observed only that each save had finished, not that it had succeeded: the page's save wrappers caught the error, the token cleared, and the switch went ahead and reloaded, discarding the failed in-memory edit despite the dialog's promise; (P2) the settle's round limit did not bound time, so a save that never resolved on a poor connection could leave the farm selector stuck; (P2) copying a budget spread the source line into the new one, so a U of I-seeded line carried its badge into the copy, contrary to the stated copy semantics.
- **Repairs:** a save now reports its failure to the registry (the calculator's chain passes the error to the token; the sale-limit commit records its failure; the page's carry wrappers rethrow after their one recovery refresh), and `settlePendingSettingsWork` rejects with `SETTINGS_SAVE_FAILED` when any awaited save failed, or `SETTINGS_SAVE_STILL_RUNNING` when saves are still running after a 20-second limit (or the round limit). The farm switch stops before the selection changes, the switcher shows a plain-language reason (added to `farmerError`), and the unsent work stays with the farm that is still selected. The three budget-copy paths (Supabase, queued, mock) now set `university_default_amount` to null on every copied line.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; grainSettings (new cases: a failed save refuses the switch; a stalled save refuses it after the limit and stays pending; the round limit refuses rather than resolves), Supabase and mock profitability (copied lines carry no badge while the source keeps it), farm-context, Supabase grain, Grain receipt, and Profitability equipment-cost regressions passed; `npm run build` exit 0; `git diff --check` clean; the browser harness keeps every earlier fact at both sizes and adds: with the fake persistence set to fail, a value typed into a grid cell makes settle reject with `SETTINGS_SAVE_FAILED`, the typed value stays on screen, and no pending work is left behind.
- **Limit of this proof:** as in FS-017, the switch path in `App.tsx` and the switcher's error display were proven by unit regression and code reading, not by a signed-in two-farm browser session.

## FS-019 — Codex finding on `7ef9856`: a draft whose save failed stays dirty and pending until retried or replaced

- **Date/time:** 2026-09-13 19:35 -05:00 (`America/Chicago`).
- **Finding (Codex, P1):** when a carry save failed on its own (a blur, with no switch attempt), the token recorded the failure but left the registry at once, and the flush had already cleared the draft's dirty flag before the save started; a later farm switch therefore saw neither pending work nor a failure, its flush had nothing to send, and the visible draft was discarded.
- **Repair:** a failed carry save re-marks its draft dirty, records it as failed, and holds the unflushed token again, so the farm switcher warns and the next flush (a blur, or the switch-time settle) retries it; if the retry fails too, the switch is refused as in FS-018. A newer row arriving from the recovery refresh replaces a failed draft the way it replaces a pristine one (and clears its pending state), keeping the FS-013 conflict rule; a fresh edit after a failure counts as the farmer's newer intent and is kept. The sale-limit commit holds the unflushed token again after a failure for the same reason. Switching farms resets the failed flags with the rest of the draft state.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; grainSettings, Supabase grain, Grain receipt, farm-context, and cost-of-carry regressions passed; `npm run build` exit 0; `git diff --check` clean; the browser harness keeps every earlier fact at both sizes and adds: a failed save with a value typed and tabbed out leaves the farm pending; once the cause clears, settle sends the retained value (row 4 = 6.2) and clears the farm; a second failed draft is replaced when a newer row arrives from elsewhere and stops being pending; no page errors; legacy mode still zero saves.

## FS-020 — Codex findings on `b0a5e6d`: settings saves refuse a farm changed in another tab; a newer sale-limit row replaces a failed draft

- **Date/time:** 2026-09-13 19:50 -05:00 (`America/Chicago`).
- **Findings (Codex, on `b0a5e6d`):** (P1) a sibling tab switching the same account to another farm bypasses this tab's registry; the sale-limit commit never checked that its farm was still selected, and the queued repository rebinds the row to the current farm, so a limit typed for one farm could be saved to another; (P1) in the same situation the carry wrappers returned quietly, which the calculator took as a successful save, so the draft was dropped instead of kept for retry; (P1) after an optimistic-lock conflict on a sale limit, the recovery refresh skipped the dirty scope, so the stale draft survived and its retry would overwrite the other device's change with the refreshed version.
- **Repairs:** the sale-limit commit and both carry wrappers now confirm the live selected farm still matches the row's farm and otherwise fail with `SETTINGS_CONTEXT_CHANGED` (a farmer-facing notice, "The selected farm changed in another tab. Reload this page before saving again."; no refresh runs under the other farm), so the draft stays dirty and pending and a confirmed switch is refused as in FS-018. Sale-limit scopes whose save failed are tracked like the carry drafts: the recovery refresh replaces such a draft when it brings a newer row (a different id or version; the FS-013 conflict rule) and clears its dirty and pending state; when the row is unchanged the draft stays for the next retry; a fresh edit clears the failed mark.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; grainSettings, Supabase grain, Grain receipt, and farm-context regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness unchanged from FS-019 at both sizes (no errors, legacy zero saves). Foundation is green on `b0a5e6d`.
- **Limit of this proof:** the Grain page's sale-limit commit, its refresh merge, and the cross-tab fence were proven by code reading and TypeScript; the regression harness renders the page's sub-components, not the page, and the browser harness renders the calculator with fake persistence.

## FS-021 — Codex findings on `438e863`: the account is part of the context guard; failed drafts survive leaving the screen; legacy carry rates are adopted

- **Date/time:** 2026-09-13 20:15 -05:00 (`America/Chicago`).
- **Findings (Codex, on `438e863`):** (P1) the context guard compared only the farm, so a save queued behind a slow request could run after a sign-out and sign-in by another account with access to the same farm and persist the first account's draft under the second; (P1) the route boundary remounts the page per path, so a carry save that failed after the farmer left the Carry screen re-marked refs of a destroyed component and left an orphan pending token: the draft was lost on the next visit and a later farm switch waited out the time limit before refusing; (P1) when the migration goes live, a farm with no `grain_carry_settings` row started from the defaults and ignored the rates this device had stored per farm before, so customised storage, interest, and trucking rates disappeared until the next edit.
- **Repairs:** the page remembers the account and farm it loaded for and every settings save requires both to match the live selection. A save that fails after its screen was left is retained in the registry (`retainFailedSettingsDraft`) with a retry: the farm stays pending, a confirmed switch retries it through the flush and is refused at once if it fails again (a failed token now surfaces without waiting for the others or the time limit), and the next visit to the screen takes the draft back (`takeRetainedSettingsDrafts`) as dirty, failed, and pending, where a newer row replaces it by the FS-013 rule and the usual flush saves it otherwise; the sale-limit commit retains its draft the same way and the page adopts it after its load unless the farm's row moved on. Where the farm has no settings row yet, the calculator starts from the rates this device stored before the table was live and saves them for the farm at once; a device that stored nothing starts from the defaults without a save.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; grainSettings (new cases: a failure surfaces at once beside an open token; a retained draft keeps the farm pending, a failing retry refuses the switch at once and stays retained, a succeeding retry clears it, drafts are taken back by prefix and the hold releases with the last one), Supabase grain, Grain receipt, farm-context, and cost-of-carry regressions passed; `npm run build` exit 0; `git diff --check` clean; the browser harness keeps every earlier fact at both sizes and adds: a value typed and then the screen unmounted with the save failing leaves the farm pending, a settle while the screen is gone is refused with `SETTINGS_SAVE_FAILED`, the remounted screen shows the value again and saves it once the cause has cleared; a farm with no row and legacy device rates shows those rates (flat, 0.33) and produces exactly one settings save carrying them; a farm with no row and no legacy rates shows the defaults and saves nothing.
- **Limit of this proof:** the account check, the sale-limit retention, and the page's adoption of a retained sale limit were proven by code reading and TypeScript, not in a signed-in browser session.

## FS-022 — Codex findings on `4bc66f0`: settings drafts live in the browser per account and farm until their save is confirmed

- **Date/time:** 2026-09-13 20:45 -05:00 (`America/Chicago`).
- **Findings (Codex, on `4bc66f0`):** (P1) the in-memory store of drafts retained after leaving a screen was keyed by farm only, so after a sign-out and a sign-in by another account with the same farm, that account's screen would take the first account's unsaved values and could save them; (P1) a carry edit made inside the 600 ms save pause existed only in React state and the in-memory registry, so a reload, a closed tab, or a crash in that window lost it, and cleanup on page unload is neither guaranteed nor awaitable.
- **Repairs:** the in-memory retained-draft store from FS-021 is replaced by a browser draft (`src/data/settingsDrafts.ts`) written synchronously on every carry or sale-limit edit under a key that names the project, account, and farm, and cleared only after the server or the durable queue confirmed the save (and nothing newer was typed), or when a newer row from elsewhere replaced the draft. Because the key carries the account and farm and the value carries a non-empty `entries` array, the farm switcher's existing scan (`hasPendingFarmWork`) counts the draft as work waiting for that farm and that account only, so the "Switch away from this farm?" dialog's promise ("they will stay with that farm and send when you come back") now holds for these drafts too. On the next visit the calculator and the Grain page take the drafts back as dirty and pending (the calculator marks them like a failed draft, so a newer row replaces them by the FS-013 rule; the page drops a sale-limit draft whose row moved on). The account-aware context guard from FS-021 stays. Legacy device rates adopted for a farm without a row are written to the draft too, so they survive a reload before their first save.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS (the pinned `hasPendingFarmWork` lines are unchanged); grainSettings (new cases: drafts are written per account and farm on a key the switcher scan recognises, read back by prefix with the latest payload, invisible to another account, cleared one at a time, and the key is removed with the last), farm-context (new case: a kept draft counts as pending work for that account and farm only), Supabase grain, Grain receipt, and cost-of-carry regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness at both sizes keeps every earlier fact and now shows: after the screen is unmounted with a failing save the farm stays pending through the browser draft, a settle while the screen is gone proceeds and leaves the draft for the farm, the remounted screen shows the value again and saves it and the draft key disappears; an edit followed at once by a reload comes back from the browser draft, is saved once, and the draft key disappears; legacy adoption and the no-row default are unchanged.
- **Limit of this proof:** the page's sale-limit draft write, adoption, and clearing were proven by code reading and TypeScript; the harness renders the calculator with fake persistence.

## FS-023 — Codex findings on `d929202`: a refused draft write saves at once; a save clears only the draft revision it covered

- **Date/time:** 2026-09-13 21:05 -05:00 (`America/Chicago`).
- **Findings (Codex, on `d929202`):** (P1) the draft helper swallowed every storage failure (private mode, blocked or full storage), so the screen treated an edit as kept when it lived only in memory, and a reload inside the save pause could still lose it; (P1) the same account and farm open in two tabs: a tab finishing an older save cleared the draft key unconditionally and could remove the other tab's newer draft, which was then lost if that tab closed before its own save.
- **Repairs:** every draft write now carries a revision and reports whether the browser accepted it; when it did not, the screen clears any stale entry and saves the edit at once instead of waiting out the pause (the pending token still warns the farm switcher). A save, a no-change commit, or a resync that replaces a draft clears the draft only when the stored revision is the one that save covered, so another tab's newer draft stays; drafts adopted on mount take over their stored revision.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; grainSettings (new cases: each write has its own revision; clearing with an older revision leaves the newer draft and clearing with the current one removes it; a refused write returns null), farm-context, Grain receipt, and Supabase grain regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness at both sizes keeps every earlier fact and adds: with the browser's storage write made to throw, a typed grid value is saved within 120 ms and no draft key is left behind.
- **Limit of this proof:** the two-tab race itself was proven by the unit regression of the conditional clear and by code reading, not with two live tabs.

## FS-024 — Codex finding on `9202181`: a refused sale-limit draft write commits the value at once

- **Date/time:** 2026-09-13 21:25 -05:00 (`America/Chicago`).
- **Finding (Codex, P1):** when the browser refused the sale-limit draft write, the page recorded the refusal and cleared any stale draft but still waited for blur or Enter to save, so a tab closed or reloaded before that lost the typed limit, unlike the carry calculator, which saves at once in that case.
- **Repair:** on a refused write the sale-limit change handler now schedules `commitSaleLimit` for right after React has committed the keystroke (the per-position lock and its follow-up commit absorb rapid typing), so the value reaches the server or the durable queue without waiting for blur.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; Grain receipt and grainSettings regressions passed; `npm run build` exit 0; `git diff --check` clean. The path was proven by code reading and TypeScript, as the regression harness renders the page's sub-components rather than the page.

## FS-025 — Codex finding on `95e6261`: a newer draft is rebased onto the version a predecessor save produced

- **Date/time:** 2026-09-13 21:45 -05:00 (`America/Chicago`).
- **Finding (Codex, P1):** an edit made while an earlier save was in flight wrote its browser draft with the row version from before that save; once the save advanced the server version, a reload before the second save read the draft back as failed, saw the newer server version, treated the predecessor save as another device's change, and replaced and dropped the newer edit.
- **Repair:** when a carry save confirms and the farmer has typed more meanwhile, the newer draft is rewritten at once with the saved version as its base (settings and grids alike); the sale-limit commit does the same when its follow-up commit is due. A reload then finds a draft whose base matches the server row and keeps it dirty and pending for the usual flush.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; grainSettings, Grain receipt, and cost-of-carry regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness at both sizes keeps every earlier fact and adds: with a deliberately slow save in flight and a second cell typed meanwhile, the grid draft first carries the old base and the new value, then after the first save completes it carries the saved version `T2` and the new value, the second save carries `T2` and the new value, and no draft remains.
- **Limit of this proof:** the sale-limit rebase was proven by code reading and TypeScript.

## FS-025 addendum — the browser-harness proof for FS-025 was re-established with a corrected harness

- **Date/time:** 2026-09-13 21:55 -05:00 (`America/Chicago`).
- **Correction:** when `7822446` was committed, the harness check for the rebase had not passed: the harness's fake persistence returned no saved row, so the calculator never learned a saved version inside its chain and every save reported the same version, which made the "old base then saved base" claim unobservable (the check failed on that assertion alone; every other fact matched). The commit went out before that failure was noticed. The fake persistence now returns the saved row with a fresh version per save, as the real page wrappers do.
- **Re-proof on `7822446` (unchanged code):** at both sizes, while a deliberately slow save is in flight and a second cell is typed, the grid draft carries the previous version (`T2`) and the new value; after the first save confirms it carries the saved version (`T3`) and the new value; the second save carries `T3`; no draft remains; every earlier harness fact holds, and the chained saves now visibly carry each predecessor's version (the third save in the run carries `T3`, the two after the simulated remote change carry `R1`).

## FS-026 — Codex finding on `13fc616`: a queued save's replayed version reaches the screen; a draft being edited is rebased onto its own replayed write

- **Date/time:** 2026-09-13 22:25 -05:00 (`America/Chicago`).
- **Finding (Codex, P1):** a save queued offline (or behind an earlier queue entry) returns the client row with its old version; replay later advances the version on the server, but the page never learned it, so the next carry edit carried the stale version, conflicted, and the recovery treated the replayed row as another device's change and replaced the new edit.
- **Repair:** the Grain page now subscribes to the grain module's sync state (`getModuleSyncStatus` added to `src/data/syncStatus.ts`) and refetches once the queue has drained, so the rows carry their replayed versions. The calculator remembers the content of the last row each save sent; when a refreshed row has the same content and a newer version while a draft is being edited (and not failed), it is this screen's own write coming back, so the draft is kept and rebased onto that version (and the browser draft rewritten with it) instead of being replaced. Sale limits already read the row version from the refreshed workspace at commit time.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; grainSettings, Grain receipt, cost-of-carry, and Supabase grain regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness at both sizes keeps every earlier fact and adds: with the fake persistence in "queued" mode (returns the client row unchanged), a cell saved and a second cell typed meanwhile leave a draft with the old base; a simulated replay (same content, version `Q1`) rebases the draft to `Q1` while the typed value stays on screen, and the following save carries `Q1` and the typed value; no draft remains.
- **Limit of this proof:** the page's refetch after the queue drains was proven by code reading and TypeScript.

## FS-027 — Codex findings on `99a5005`: lineage travels with the draft; a typed sale-limit draft is rebased, not cleared; settings drafts are quarantined with a revoked farm

- **Date/time:** 2026-09-13 23:05 -05:00 (`America/Chicago`).
- **Findings (Codex, on `99a5005`):** (P1) the lineage that lets the calculator recognise its own replayed write lived only in a React ref, so a queued edit, a newer draft, and a reload before the replay left the reloaded screen unable to recognise the replayed row, which then replaced the newer draft; (P1) when the drain refetch brought a replayed sale-limit row while the farmer was typing a newer value, the page cleared the newer browser draft for the version mismatch before checking that the scope was being typed, so a reload before blur lost it; (P1) when access to a farm was removed, the revoked-farm quarantine did not recognise the settings-draft key, so private unsaved values stayed in active browser storage and could be restored and saved if access was granted again.
- **Repairs:** every carry draft now carries the content of the last row the screen sent (`sent`), the reloaded screen restores it, and the resync's lineage check runs before the failed-draft rule, so a replayed row with the same content rebases the draft instead of replacing it; a sale-limit scope still being typed keeps its draft, rewritten onto the refreshed row, while an idle scope's older draft is dropped as before; `quarantineRevokedFarmWork` recognises the settings-draft key, captures a non-empty envelope into the recovery vault as a `settings_drafts` record (shown as "Unsaved Grain settings" on the review screen), removes it from active storage, removes an empty one, and refuses to clear anything when the envelope is unreadable.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; revoked-farm recovery (new cases: capture and removal for the revoked farm only, empty key removed without a record, unreadable key stops the quarantine), grainSettings, farm-context, Grain receipt, and cost-of-carry regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness (its fake server now survives a reload within the tab) at both sizes keeps every earlier fact and adds: a cell saved while queued, a second cell typed, and a reload before the replay leave the draft with the queued row's lineage; the reloaded screen shows the typed value, a simulated replay (same content, version `Q2`) rebases the draft to `Q2` while the value stays on screen, the following save carries `Q2`, and no draft remains.
- **Limit of this proof:** the sale-limit rebase and the quarantine's place in the revocation flow were proven by unit regression, code reading, and TypeScript.

## FS-028 — Codex finding on `0f1909b`: sale limits send the version their editing session started from and rebase only onto their own replayed write

- **Date/time:** 2026-09-13 23:30 -05:00 (`America/Chicago`).
- **Finding (Codex, P1):** the sale-limit path rebased a draft still being typed onto any refreshed row, and its commit took the version from the refreshed workspace, so a change made on another device while the farmer was typing was silently overwritten on blur instead of conflicting (the guide's offline-and-mutation-safety rule).
- **Repair:** each sale-limit editing session now remembers the row it started from (`saleLimitBases`, fixed at the first keystroke and kept in the browser draft as `base`) and the commit sends that version, so a row changed elsewhere conflicts and the recovery refresh replaces the draft as in FS-020. The page also remembers the last value it saved per position (`saleLimitSent`, kept in the draft as `sent`); on a refetch, a draft still being typed is rebased only when the refreshed row carries that value (this browser's own queued write coming back), otherwise it keeps its base so its commit conflicts. A successful save with nothing newer typed ends the session; a save with a newer value typed meanwhile carries the saved row forward as the base and lineage.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; Grain receipt, grainSettings, and Supabase grain regressions passed; `npm run build` exit 0; `git diff --check` clean. The path was proven by code reading and TypeScript, as the regression harness renders the page's sub-components rather than the page.

## FS-029 — Codex findings on `3e9c4d3`: one storage key per draft; every draft rewrite handles a refused write; pending work is keyed by account and farm

- **Date/time:** 2026-09-13 23:55 -05:00 (`America/Chicago`).
- **Findings (Codex, on `3e9c4d3`):** (P1) all of a farm's drafts shared one storage envelope, so two tabs editing different drafts could each read the envelope, and the later write dropped the other tab's entry; (P1) the draft rewrite after a predecessor save ignored a refused write, leaving the durable draft on the old base with no immediate save; (P2) the in-memory pending-work registry was keyed by farm only, so a stalled save by one account could make another account's farm switch wait on it or refuse.
- **Repairs:** each draft now has its own storage key, named in the offline-queue shape (`farm-rx-settings-draft-<encoded draft key>:v1:<project>:<account>:<farm>`), so tabs never overwrite each other's drafts, the farm switcher's scan and the revocation scope discovery still find them, and the quarantine captures each as its own record; every draft write in the calculator (edits, the rewrite after a predecessor save, the rebase on a replayed row, the legacy adoption) goes through the refusal-aware helper that clears the stale entry and saves at once when the browser refuses, and the sale-limit rewrite after a save clears its stale entry on refusal; the registry's tokens and flushers are keyed by account and farm, and every caller (calculator, Grain page, farm switcher, farm context) passes both.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS (the pinned `hasPendingFarmWork` lines are unchanged); grainSettings (per-draft keys, the key resolves through the revocation scope discovery, account isolation of the registry), revoked-farm recovery (two drafts captured as two records, other farm untouched), farm-context (another account on the same farm is not blocked by the first account's in-flight save), Grain receipt, cost-of-carry, and Supabase grain regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness at both sizes keeps every earlier fact with the per-draft keys (drafts, reloads, unmount retention, refused writes, replays, and the reload-before-replay lineage).
- **Limit of this proof:** the two-tab race itself and the account transition were proven by unit regression and code reading, not with two live tabs or two sign-ins.

## FS-030 — Codex finding on `1b653be`: a successor edit already queued behind a slow save is rebased onto that save's version

- **Date/time:** 2026-09-14 00:20 -05:00 (`America/Chicago`).
- **Finding (Codex, P1):** when a first carry save took longer than the 600 ms pause, a second edit's flush ran meanwhile, cleared the dirty flag, and queued the successor behind the first save; the rewrite after the first save confirmed looked only at the dirty flag, so the queued successor's browser draft kept the old base, and a reload while the successor was pending restored it as a stale draft that the newer server version then replaced.
- **Repair:** after a save confirms, the calculator decides whether a newer draft exists by content (the part is dirty, or the current draft differs from the snapshot just saved) instead of by the dirty flag or by the draft's revision, so a queued successor's draft is rewritten onto the saved version and lineage, and when the successor's own save confirms with nothing newer typed, its draft is cleared under the revision this tab last wrote (the cross-tab guard stays).
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; cost-of-carry and grainSettings regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness at both sizes keeps every earlier fact and adds: with a deliberately slow first save (1.5 s) and a second cell typed whose pause runs out while the first save is in flight, the successor's draft carries the old base while the first save runs, the saved version once it confirms, the successor's save carries that version and the typed value, and no draft remains afterwards.

## FS-031 — Codex findings on `386cd66`: a refused rebase write re-sends a queued successor; legacy carry adoption only for writers; the legacy U of I badge map moves into the column

- **Date/time:** 2026-09-14 00:55 -05:00 (`America/Chicago`).
- **Findings (Codex, on `386cd66`):** (P1) when the browser refused the rebase write for a successor edit already queued behind an earlier save, the refusal path removed the old draft and scheduled a flush that did nothing because the part was no longer dirty, so a reload before the queued save ran lost the edit; (P2) the calculator adopted this device's legacy carry rates for a farm with no row even for a member who may read but not write, whose automatic save the write policy refuses, leaving a pending draft that made every confirmed farm switch fail; (P2) the "U of I default" badge provenance that budgets created before this release kept in the browser (`farm-rx.profitability.university-defaults`) was no longer read, so those badges and their source note would vanish at deployment.
- **Repairs:** the refusal path now re-sends the part from its newest snapshot explicitly (marking it dirty first, so the flush is never a no-op), for settings and grids alike; the Grain page tells the calculator whether the member may write, and the legacy adoption is skipped otherwise; the Profitability page performs a one-time move of the legacy badge map into `university_default_amount` for a member who may edit (each remembered line written once, the browser entry dropped only after the refreshed row confirms the amount, entries for other farms' lines left for those farms), so existing badges survive the release. Screens rendered outside the farm-access provider (regression harnesses) see no profile and skip these writes.
- **Foundation note:** Foundation failed on `1b653be` in the built-browser lane's `a delayed old farm read cannot overwrite the cache after revoke and regrant` (first run and retry), which times out waiting for a stale tab's reload to show the fields list; the same test passed locally on `386cd66` three times in a row with the same built-browser flow, and the changes in `1b653be` (per-key drafts, the account-scoped registry, the quarantine's key recognition) do not touch the fields read, the cache fence, or the revocation flow the test exercises. The failure was reported once on the PR; Foundation's run on the next head is the re-check, and a repeat on the same test will be root-caused from its trace artifact rather than dismissed.
- **Proof:** `npx tsc -b --force` exit 0; static guards PASS; Profitability equipment-cost, mock and Supabase profitability, Grain receipt, grainSettings, and cost-of-carry regressions passed; `npm run build` exit 0; `git diff --check` clean; browser harness at both sizes keeps every earlier fact and adds: with a 1.5 s first save, a successor typed whose pause runs out meanwhile, and the browser's storage write made to throw before the first save confirms, the successor's value is still saved (the queued save and the explicit resend both carry it) and no draft key remains.
- **Limit of this proof:** the read-only viewer gate and the badge migration were proven by code reading and TypeScript (the regression harnesses render these screens without a farm-access provider, which skips both paths by design).

## FS-032 — Codex findings on `d3c55e4`: a refused sale-limit rebase write commits at once; kept drafts stay untouched for a read-only member

- **Date/time:** 2026-09-13 16:55 -05:00 (`America/Chicago`).
- **Findings (Codex, on `d3c55e4`):** (P1) when an earlier sale-limit save replayed while a newer value was still being typed, the refresh's rebase of that draft could run after browser storage had become full or blocked; the refused write returned no revision, but the path neither removed the stale draft nor committed the newer value at once, so a reload could restore the older draft over the newer value; (P2) the calculator's reset effect and the Grain page's refresh restored every kept draft, marked it dirty, and let the debounce and the flush try to save it even for a member since downgraded to read-only, whose saves the write policy refuses, leaving the farm stuck as pending.
- **Repairs:** the sale-limit rebase now behaves like the calculator's `keepDraft` path: a refused write clears the stale draft and commits the value being typed at once for that position (found by its scope key among the farm's production estimates). The Grain page computes whether the member may write the farm's grain settings once (read through a ref inside `refresh`, which older closures keep calling), passes it to the calculator as before, and skips sale-limit draft adoption when it is false; the calculator's reset effect likewise skips draft adoption when `persistence.writable === false`, and `writable` is now one of its dependencies, so drafts left untouched while read-only are adopted when edit access returns. The Grain page refreshes once when access flips from read-only to writable for the same reason. In both cases the kept drafts stay in storage, untouched and unsent; the farm switcher still warns about them (they are unsaved work the member cannot yet send), and a confirmed switch proceeds because nothing is dirty in memory.
- **Proof:** `npx tsc -b --force` exit 0; grainSettings, cost-of-carry, farm-context, and revoked-farm-recovery regressions passed; `npm run build` exit 0; static guards PASS; `git diff --check` clean; browser harness at both sizes keeps every earlier fact unchanged (results compared field by field with the previous run) and adds: a kept grid draft loaded by a writer is shown, saved once, and its draft key removed; loaded by a read-only member it is not shown, nothing is saved, the draft key remains, and the registry holds no token; when that page is switched to writable, the draft is shown, saved once, and its key removed.
- **Limit of this proof:** the sale-limit refusal path lives on the Grain page, which the browser harness does not mount; it was proven by code reading and TypeScript against the same pattern the calculator's harness case C14 exercises.

## FS-033 — Codex finding on `d700b66`: a kept sale-limit draft whose own queued save replayed is adopted after a reload, not dropped

- **Date/time:** 2026-09-13 17:03 -05:00 (`America/Chicago`).
- **Finding (Codex, on `d700b66`, P1):** after a reload nothing is dirty in memory while the kept sale-limit drafts are inspected, so a draft whose base no longer matched the farm's row (because this browser's own earlier queued limit had replayed and advanced the row's version) took the "idle scope, older draft" path and was dropped, losing the newer uncommitted value. The rebase-onto-own-write branch ran only for a scope still being typed.
- **Repair:** the adoption loop now decides from the draft's own lineage first, regardless of what is dirty in memory: a moved row that carries the value this browser last saved (the draft's persisted `sent`, or the in-memory one while typing) is this browser's own queued write coming back, so the draft is rebased onto it and, when nothing is being typed, adopted dirty and pending like any kept draft; a moved row with a different value is another device's change, kept as a conflicting draft while typing and dropped when idle, as before; a refused rebase write still clears the stale draft and commits the value at once. The sale-limit path therefore matches the calculator, whose lineage check has travelled with the draft since FS-027.
- **Proof:** `npx tsc -b --force` exit 0; grainSettings, farm-context, and cost-of-carry regressions passed; `npm run build` exit 0; static guards PASS; `git diff --check` clean. The calculator and its browser harness are untouched by this commit (Grain page only).
- **Limit of this proof:** the Grain page is not mounted by the browser harness; the repair was proven by code reading and TypeScript. The reload case it fixes is the sale-limit twin of the calculator's harness case "queued replay after a reload" (FS-027), which still passes.

## FS-034 — Codex finding on `95526f4`: settings rows are queued, returned, and saved in the shape the database stores

- **Date/time:** 2026-09-13 17:11 -05:00 (`America/Chicago`).
- **Finding (Codex, on `95526f4`, P1):** when a settings save fell back to the offline queue, the queued repository handed back the client row exactly as typed, while the later replay through the Supabase repository rounds sale limits to two decimals and carry rates and prices to four. A screen that had recorded the unrounded row as "last sent" would then not recognise its own replayed write (the refreshed row differs in the rounded digits), treat it as another device's change, and could drop a newer draft typed before the replay.
- **Repair:** `src/data/grainSettings.ts` gains `normalizeGrainSaleLimit`, `normalizeGrainCarrySettings`, and `normalizeGrainCarryGrid`, one shared rounding step per row (numeric scale 2 for bushels and 4 for rates and prices, PostgreSQL ties-away-from-zero, a value too large for its column refused). The Supabase repository's three save operations use them in place of their inline rounding; the queued repository normalizes the row before the queue entry is built, so the queued entry, the offline overlay, and the row handed back on fallback all equal the row a replay produces; the mock repository saves the same shape. The screens' lineage comparison is therefore exact on every path without a second comparison rule.
- **Proof:** `npx tsc -b --force` exit 0; grainSettings (new checks: two- and four-decimal rounding with ties away from zero, blanks preserved, an oversize sale limit refused before queueing, normalization idempotent), Supabase grain, mock grain, cost-of-carry, farm-context, grain repair, and queued operation-context regressions passed; `npm run build` exit 0; static guards PASS; `git diff --check` clean. The calculator and the Grain page are untouched by this commit, so the browser harness (last run on `d700b66`) still stands.
- **Limit of this proof:** the queued fallback's returned row was proven through the shared normalizer's regression and code reading; the queued repository's own regression lanes do not exercise the three settings kinds offline.

## FS-035 — Codex findings on `165b373`: the sale-limit commit adopts the rounded value; seeded badge amounts survive the pre-migration window

- **Date/time:** 2026-09-13 17:23 -05:00 (`America/Chicago`).
- **Findings (Codex, on `165b373`):** (P1) after FS-034 a sale limit typed with more than two decimals (`1234.565`) came back from the save as `1234.57`, while the value on screen stayed `1234.565`; the "typed again meanwhile" check compared the screen against the saved value and so scheduled another commit, forever, and offline each pass could add a queue entry; (P2) while this app version runs before the migration is applied, a U of I budget's lines save through the `PGRST204` fallback without their badge amount, and since the page no longer wrote the browser badge map, that provenance was gone for good.
- **Repairs:** the sale-limit commit rounds the typed value with the shared normalizer before comparing it with the farm's row and sending it; "typed again meanwhile" now compares the screen against the value the commit started from (not the saved one), and unless the farmer typed again the rounded value is shown on screen, so the screen, the draft lineage, and the saved row agree and no follow-up loops. A number too large for the column is refused before any save (the failure path shows the notice and keeps the draft). The badge provenance map moves into `src/data/universityDefaultProvenance.ts` (read, remember, forget; safe without browser storage); the `PGRST204` fallback in the profitability gateway remembers the seeded amount for the line it could not store it for (a `retain` parameter, defaulting to that store, so the regression can observe it); the Profitability page shows those remembered amounts as badges while the column is missing and, as since FS-031, writes them into the column once it exists and forgets them once the refreshed row carries them.
- **Proof:** `npx tsc -b --force` exit 0; grainSettings (new check: the fallback retains a seeded amount dropped on `PGRST204`, nothing for a hand-entered line or a stored badge), Supabase and mock profitability, Profitability equipment-cost UI, Supabase and mock grain, farm-context, and cost-of-carry regressions passed; `npm run build` exit 0; static guards PASS; `git diff --check` clean. The calculator is untouched (harness of `d700b66` still stands).
- **Limit of this proof:** the sale-limit commit and the badge display on the Profitability page were proven by code reading and TypeScript (neither screen is mounted by a harness). The page still attempts once per session to write a remembered amount into the column; before the migration that attempt takes the same fallback (the amount stays remembered) and costs one save per remembered line per session.

## FS-036 — Codex findings on `6f07949`: a refused draft write removes only this tab's revision; badge provenance kept per line and its loss reported

- **Date/time:** 2026-09-13 17:34 -05:00 (`America/Chicago`).
- **Findings (Codex, on `6f07949`):** (P1) when the browser refused a draft write, the calculator's `keepDraft` and the Grain page's sale-limit paths removed whatever revision was stored under the key, which could be a newer draft another tab had just written, so that tab's edit could be lost if it closed before its save; (P2) the browser badge-provenance store silently dropped a seeded amount when storage was blocked or full while the line save reported success, so the badge was lost for good without a word; (P2) the store was one JSON map rewritten whole, so two tabs seeding different lines at the same time could overwrite each other's entry.
- **Repairs:** every refusal path now clears only the revision this tab itself wrote before (the calculator looks it up from its per-key revision refs; the Grain page's three paths capture it before the write, and the adoption loop uses the revision it read), so another tab's newer draft stays. `src/data/universityDefaultProvenance.ts` keeps one storage key per line (`farm-rx.profitability.university-default:<line id>`), still reads the older single-map key and prunes it as entries are confirmed, returns false and records the line when a write is refused, and exposes the recorded lines once; the Profitability page reports them after the next refresh in plain words (the numbers are saved, the badge is not, how to restore it after the update is live).
- **Proof:** `npx tsc -b --force` exit 0; grainSettings (new checks: per-line keys, the older map read together with them, forgetting prunes both, a refused write returns false and is reported once by line, no storage reads nothing and reports the write), Supabase and mock profitability, Profitability equipment-cost UI, cost-of-carry, farm-context, and revoked-farm-recovery regressions passed; `npm run build` exit 0; static guards PASS; `git diff --check` clean; browser harness at both sizes keeps every fact of the `d700b66` run (compared field by field; one wait time differs by a millisecond), including the two refused-write cases.
- **Limit of this proof:** the two-tab refusal case itself is not exercised by the harness (one tab); it was proven by code reading against the conditional clear the drafts module already pins in its regression. The Profitability notice was proven by code reading and TypeScript.

## FS-037 — Codex findings on `1a80814`: lost edit access releases pending sale-limit work; badge provenance scoped for the quarantine; kept drafts validated before use

- **Date/time:** 2026-09-13 17:49 -05:00 (`America/Chicago`).
- **Findings (Codex, on `1a80814`, three P2):** (1) a member downgraded to read-only while a sale limit was still being typed kept its in-memory pending token and its registered flusher, so the next confirmed farm switch tried the save, the write policy refused it, and the switch was refused until reload; (2) the badge provenance keys named only the line, so `listFarmRevocationScopes` and `quarantineRevokedFarmWork` could not tie them to a revoked farm and the financial provenance stayed in active storage; (3) `readSettingsDrafts` returned any entry with valid metadata, and the screens cast the payload, so a malformed or older-shaped draft could crash the calculator.
- **Repairs:** (1) the Grain page's write-access effect now also handles the loss: every dirty sale-limit's pending token is settled and the in-memory dirty, failed, base, and revision state cleared, while the browser drafts stay for a later visit with edit access; the sale-limit flusher is registered only while the member may write; the calculator's flusher likewise (its reset effect already settled the registry on the change). (2) `src/data/universityDefaultProvenance.ts` keys are now `farm-rx-university-default-<line id>:v1:<project>:<account>:<farm>`, the offline-queue shape, with the value `{ version: 1, amount }` (no `entries`, so the farm switcher's unsaved-work scan ignores it); the gateway fallback receives the scope from the operation context and the bound farm, the Profitability page reads and forgets with the scope from the farm-access profile and the workspace farm (and reads none outside the provider); the revoked-farm quarantine recognises the keys, captures each as a `university_defaults` record ("Budget line seeded from the U of I defaults" in the recovery list), removes it from active storage, and stops on an unreadable one; the older unscoped map is still read and pruned. (3) `readSettingsDrafts` takes an optional payload guard and removes and skips entries that fail it; the calculator validates carry-settings and carry-grid drafts (mode, four finite rates, thirteen rows of strings, harvest month 0 to 11, estimate id matching the key, base and lineage shapes) and the Grain page validates sale-limit drafts (key, finite or null value, base row shape, finite or null lineage).
- **Proof:** `npx tsc -b --force` exit 0; grainSettings (new checks: scoped keys read together with the older map and not across farms, key shape recognised by the revocation scope discovery, value without `entries`, an unusable draft skipped and its key removed while a usable one stays), revoked-farm recovery (new checks: a scoped provenance entry captured and removed while another farm's stays; an unreadable one stops the capture), Supabase and mock profitability, Profitability equipment-cost UI, cost-of-carry, farm-context, and Supabase grain regressions passed; `npm run build` exit 0; static guards PASS; `git diff --check` clean; browser harness at both sizes keeps every fact of the previous run (compared field by field, wait times excluded), including the kept-draft and read-only cases.
- **Limit of this proof:** the downgrade-while-typing release on the Grain page and the recovery list label were proven by code reading and TypeScript (the harness mounts the calculator only, and its read-only case starts read-only rather than losing access mid-edit).

## FS-038 — Codex findings on `4b50577`: queued carry saves re-check write access; the kept lineage row is validated by kind

- **Date/time:** 2026-09-13 18:01 -05:00 (`America/Chicago`).
- **Findings (Codex, on `4b50577`, two P2):** (1) a carry save already waiting in the calculator's chain when edit access was lost still ran with the now read-only persistence: offline it could be queued and replayed after access returned, online it was refused and its catch re-marked the farm pending with no flusher left to send it, so farm switches were refused; (2) the draft guard from FS-037 accepted any object as the lineage row (`sent`), so a damaged grid draft with `{}` there reached `sentRows`, and the resync's content comparison would read `rows` of nothing and crash the screen.
- **Repairs:** (1) the calculator's flushes and every queued save callback first check the live `writable` flag (the persistence ref is refreshed each render): a read-only member's flush is a no-op, a queued callback returns without sending, and the failure path does not re-mark pending work for a member who may not write; the draft stays in storage for a later visit with edit access. (2) The lineage is validated by kind: a settings draft accepts only a complete `GrainCarrySettings` row (farm, mode, four finite rates, version) or none, a grid draft only a complete `GrainCarryGrid` row (ids, integer harvest month, finite default basis, thirteen rows of finite-or-null prices and bases, version) or none. `isCarryDraft` is exported so the regression pins it.
- **Proof:** `npx tsc -b --force` exit 0; grainSettings (new checks: settings and grid drafts accepted with a complete lineage row or none, dropped with an empty or short lineage, a row of the other kind, an incomplete rate set, twelve rows, or another estimate's key), cost-of-carry, farm-context, and revoked-farm-recovery regressions passed; `npm run build` exit 0; static guards PASS; `git diff --check` clean; browser harness at both sizes keeps every fact of the previous run (compared field by field, wait times excluded).
- **Limit of this proof:** the loss of access while a save is queued was proven by code reading and TypeScript (the harness's read-only case starts read-only rather than losing access mid-queue).
