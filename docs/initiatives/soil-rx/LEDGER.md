# Soil Rx initiative ledger

This file is append-only. Corrections must be recorded as later entries; prior entries are never edited, reordered, or deleted.

## SRX-000 — Initiative preflight and authority

- **Date/time:** 2026-08-10 17:25:19 (`America/Chicago`, UTC-05:00).
- **Owner authority:** Mason Wells authorized local build, test, and commit work for the three sequenced Soil Rx tranches SRX-1 through SRX-3. No push, pull-request mutation, merge, deployment, live migration, live data, secret/auth/permission change, customer action, destructive action, or other outward action is authorized.
- **Controlling scope:** `docs/GOAL.md`, section `Owner scope amendment — 2026-08-10`, and `docs/season-readiness/ORCHESTRATOR-RUNBOOK.md`.
- **Base and branch:** current `origin/main` exact SHA `e723c509f65fc82abbeafba625071207425892e0`; local branch `codex/farmrx-soil-rx`; isolated writer worktree `C:\Users\mason\.codex\worktrees\farmrx-soil-rx`.
- **Preflight result:** PR #23 was verified merged at the exact base SHA. The amendment contains SRX-1 soil test storage, SRX-2 descriptive interpretation, and SRX-3 nutrient removal. The branch and worktree were newly created from that exact `origin/main` with no pre-existing Soil Rx branch or worktree.
- **Safety boundary:** all database proof will use a disposable local backend built from repository migrations and synthetic fixtures. The Farm Rx live Supabase project and CRX Manager project are out of bounds. The Grain, Harvest, bin, contract, delivery, Weather, Programs, and Inventory implementation surfaces are fenced from writes by this initiative; SRX-3 may consume Harvest actuals read-only through existing interfaces.
- **Next gate:** inspect existing architecture and present a plain-English SRX-1 plan to Mason. No SRX-1 product code may be written until Mason approves that plan.

## SRX-001 — SRX-1 plan-to-build handoff

- **Date/time:** 2026-08-10 17:30:24 (`America/Chicago`, UTC-05:00).
- **WHERE:** `C:\Users\mason\.codex\worktrees\farmrx-soil-rx`, branch `codex/farmrx-soil-rx`, local head `c6111877748c59a8da66ebc682c588ca9b2cfcb6`, based on `origin/main` `e723c509f65fc82abbeafba625071207425892e0`.
- **GOAL:** build SRX-1 as a plain Soil Rx screen where a farmer selects a field, saves a dated lab result, optionally attaches one lab report, and reviews newest-first multi-year history. Done means the exact SRX-1 commit passes the full runbook ladder, proves cross-farm denial and private attachment access on a disposable backend, and receives fresh-context read-only Sol `ACCEPT`.
- **PROVEN:** PR #23 is merged at the exact base and the controlling amendment contains SRX-1 through SRX-3. The required writer worktree is isolated. Existing source provides the closest safe patterns: Scouting's field-scoped history, Gateway/Supabase/queued repository stack, guarded private attachment storage, farm-context replay fencing, and responsive screen; the farm privacy setting already gates named-rep access through `share_with_rep` plus an explicit rep grant.
- **WRITTEN, NOT PROVEN:** only the ledger-only preflight commit exists. This planning entry is uncommitted. No SRX table, migration, bucket, RLS policy, screen, repository, queue, route, or test has been written.
- **NOT STARTED:** SRX-1 implementation and proof; all SRX-2 and SRX-3 work.
- **APPROVAL STATE:** Mason approved local build, test, and commit work for SRX-1 through SRX-3, but required a plain-English plan and separate OK before SRX-1 product code. No outward or live action is authorized.
- **PLAN:** create a new Soil Rx screen and isolated data stack; store each test under its farm and field with lab name/date plus optional reported measurements; keep one optional lab-report attachment in a dedicated private bucket; mirror existing offline save/retry and farm-switch custody guards; add only the smallest shell registrations; update the farm-privacy wording so the existing share toggle truthfully includes Soil Rx; prove desktop/phone behavior, save/replay, attachment cleanup, rep-toggle behavior, and denial to another farm.
- **RECOMMENDED INPUT RULES:** require lab name and sample date; allow every measurement to be left blank and display `Not reported`; accept one PDF, JPEG, PNG, HEIC, or HEIF lab report up to 20 MB; show each field's newest test expanded and older tests collapsed.
- **GATES AND BLOCKERS:** waiting only for Mason's OK on the plan and recommended input rules. No live Farm Rx or CRX Manager Supabase access is permitted. Grain, Harvest, bin/contract/delivery, Weather, Programs, and Inventory implementation surfaces remain write-fenced. Any output or schema outside SRX-1 stops the tranche.
- **FIRST ACTION:** after Mason replies `OK`, assign exactly one bounded writer to implement the approved SRX-1 slice, then run focused proof before the full verification ladder.

Verify current state from Git, disk, and connected services before trusting this handoff; it may be stale when read.

## SRX-002 — Schema/data slice proof and local-only migration side effect

- **Date/time:** 2026-08-10 17:59:11 (`America/Chicago`, UTC-05:00).
- **Implemented, not committed:** the SRX-1 migration, typed data contract, Gateway/Supabase/Mock/queued repositories, guarded private-report storage, durable cleanup outbox, revoked-farm recovery registration, focused regression, and disposable database proof harness are present in the writer worktree. No farmer-facing screen or app-shell registration exists yet.
- **Focused proof:** `npx tsc -b --force` exited `0`; `npx tsx src\data\SupabaseSoilRxRepository.regression.ts` exited `0` with `SOIL_RX_REPOSITORY_REGRESSION_PASS`; `npx tsx src\data\revokedFarmRecovery.regression.ts` exited `0`; a fresh PID-scoped `postgres:17` run of `scripts\verify-soil-rx-disposable.ps1` exited `0` with `SOIL_RX_DISPOSABLE_RLS_STORAGE_PASS`; a separate fresh disposable `scripts\verify-0043-disposable.ps1` run exited `0` with the database-advisor hardening probe passing. The disposable containers removed themselves in their `finally` blocks.
- **Local-only side effect:** before the orchestrator's fresh-container warning reached the writer, `supabase migration list --local; supabase migration up --local` applied migration version `20260810223508` to the already-running local development container `supabase_db_farmrx-farmer-simplicity-2027-local`. The command exited `0`. It did not access a linked or live project. The migration file was later improved, so that local application is not proof of the final migration bytes.
- **Preservation decision:** the shared local development instance was not reset, rolled back, or otherwise changed afterward. A read-only query on this entry's date returned migration ledger version `20260810223508`. Any future use of that local instance must account for this provenance; SRX-1 proof must continue to use fresh isolated disposable containers.
- **Scope boundary:** no Grain, Harvest, bin/contract/delivery, Weather, Programs, or Inventory implementation file changed. No stage, commit, push, pull-request mutation, deployment, live migration/data/auth/permission/secret change, customer action, or destructive cleanup occurred.

## SRX-003 — SRX-1 implementation and full product proof

- **Date/time:** 2026-08-11 01:01:25 (`America/Chicago`, UTC-05:00).
- **Implemented, not committed:** a `/soil-rx` screen now provides field selection, newest-first multi-year history, collapsible tests, required lab/sample date, nullable typed measurements rendered as `Not reported`, and one optional private PDF/JPEG/PNG/HEIC/HEIF report up to 20 MB. Read-only users retain interactive history/report access while the add form is absent. The selected history field synchronizes the form, and saved dates use the farm-local calendar date.
- **Database and custody:** `soil_tests` and single-attachment metadata enforce farm/field/test integrity, immutable identity, bounded measurements, and RLS through the existing farm access/edit functions. The private `soil-test-reports` bucket validates the full `farm/field/test/file` path. Farm-access epoch changes fence both database and Storage paths. Text-only offline saves use deterministic queue/replay/needs-attention custody; attachments require online access and failed cleanup enters a durable outbox.
- **Focused proof:** TypeScript, Soil Rx repository regression, revoked-farm recovery regression, Soil Rx disposable RLS/Storage proof, access-epoch disposable proof, database-advisor proof, `git diff --check`, and the dedicated desktop/phone Soil Rx Playwright spec passed. The browser proof covers collapse/re-expand, field/form synchronization, signed report opening, read-only report access without write controls, Privacy navigation, no horizontal overflow, blocked service workers, and zero unknown Supabase requests.
- **Full non-season gates:** `npm run regression` passed; `npm run build` passed; `npm audit --audit-level=high` reported zero vulnerabilities; the static guard plus all 25 mutation cases passed; the full standard browser suite passed 64 tests with 14 intentional skips and zero failures; and `npm run verify:foundation` exited `0` with `Farm Rx foundation gate: PASS`, including all existing disposable migration and RLS lanes plus Soil Rx.
- **Repairs proven during the ladder:** removed the broad disabled fieldset that blocked read-only history/report access; made the newest card collapsible; synchronized field history and form selection; replaced UTC date derivation with the farm-local helper; aligned pH precision with the database; hardened attachment filename traversal denial; registered access-epoch triggers and revoked-farm recovery; and updated the existing Privacy wording/browser expectations to name grain, financial, and Soil Rx data.
- **Scope:** shell edits are limited to route, navigation, repository/replay registration, sync status, capability, Privacy wording, and responsive CSS. No protected Grain, Harvest, bin/contract/delivery, Weather, Programs, or Inventory implementation file changed. SRX-2 and SRX-3 remain unstarted.

## SRX-004 — Season proof, generic harness repairs, and shared-backend release

- **Date/time:** 2026-08-11 01:01:25 (`America/Chicago`, UTC-05:00).
- **Season contract and independent scenarios:** `npm run verify:season` passed 101 fixtures, all six scenario contracts, and isolation mutations. Prairie Spray passed desktop/phone/database proof. Cedar Creek passed desktop/phone/database proof after its static contract was changed from an impossible permanent migration-head assertion to exact existence plus immutable blob proof, and its frozen callback used direct read-only SQL rather than an out-of-scope script helper. North Fork passed all eight desktop and eight phone role/privacy scenarios plus exact SQL after its stale Privacy selectors were updated to the approved Soil Rx wording.
- **Continuous Maple proof:** the January-through-December run passed, including governed clock phases for July, August, September, October, November, and December and `MAPLE_2027_AUGUST_DECEMBER_DISPOSABLE_PASS`. The generic repair removes `.GetNewClosure()` from the three August-December frozen callbacks so their script-local snapshot, browser, and SQL helpers remain resolvable.
- **Harvest Ridge proof:** all nine canonical phases, canonical SQL, both reverse-order phases, reverse SQL, and `HARVEST_RIDGE_2027_DISPOSABLE_PASS` passed. Its generic local harness now waits for an authenticated synthetic farm read after reset/token. Only `authenticated API read failed` plus the exact owned journal may trigger a local gateway restart, governed `-ResumeRecovery`, readiness re-proof, and one idempotent phase retry; every other or second failure propagates.
- **Pine Hill proof:** desktop and phone canonical sequences, corrupt-active, corrupt-vault, exact SQL, replay/no-op guarantees, and `PINE_HILL_2027_DISPOSABLE_PASS` passed. Pine now refuses a reset if any clock residue exists, waits for the authenticated synthetic farm read, and applies the same exact journal-gated, one-retry gateway recovery to PH2/PH4.
- **Failure/recovery history:** an initial Maple August swap was recovered through the repository-owned journal path. Later outer command timeouts did not interrupt owned child recovery. A transient Windows browser crash left one Pine Vite listener on port 4181; its exact `npm -> cmd -> node` process chain was verified, stopped, and the port re-proved free. A later Pine reset recreated the ordinary DB after an earlier PH2 recovery journal survived, making that journal intentionally unrecoverable by identity. Its snapshot and derived images were label-attested to the stale original, verified unused, and removed; the journal evidence was moved non-destructively to `C:\Users\mason\AppData\Local\Temp\farmrx-recovery-quarantine\soil-rx-stale-desktop-ph2-b6269a561a50.json`. The new pre-reset refusal prevents recurrence.
- **Coordination:** two other Farm Rx tasks remained backend-paused while Soil owned the shared stack, except one already-running V2 Maple chain that was discovered and fenced before its next farm lane. Soil resumed only after that owner proved its process exit and clean ordinary topology. No live, linked, or customer backend was contacted.
- **Release proof:** after Pine passed, scenario processes were `0`; governed listeners across ports `4174-4186` and `4274-4280` were `0`; clock journals, clock images, and parked containers were each `0`; the ordinary DB used `public.ecr.aws/supabase/postgres:17.6.1.134`, was running healthy with `unless-stopped`, and `pg_isready` reported accepting connections. `SOIL_RX_SHARED_BACKEND_RELEASE_PASS` was sent to the queued V2 and Connect Workflows tasks with the exact Maple/Harvest/Pine harness handoff.
- **Remaining gate:** append-only evidence is current, but SRX-1 is not yet committed and has not received the required fresh-context exact-SHA Sol `ACCEPT`. No push, PR mutation, merge, deployment, live migration/data/auth/permission/secret change, or customer action occurred.

## SRX-005 — Exact-SHA review BLOCK and bounded custody repair

- **Date/time:** 2026-08-11 09:12:04 (`America/Chicago`, UTC-05:00).
- **Reviewed commit:** fresh-context, read-only Sol review of immutable local commit `9e803159ff750e3c7fcce7c636fc5b0270e48eb7` returned `BLOCK` with zero P1, six P2, and two P3 findings. The review made no file, ref, backend, or live-service mutation. The blocked commit was not amended.
- **Accepted findings:** attachment upload/metadata failures could strand or duplicate a `soil_tests` row; a post-upload access change could escape cleanup custody; offline and permanent-failure state was not visible or actionable; form/history field selection could diverge; a saved newest test did not exclusively expand; mandatory application write/non-write proof was absent; picker limits diverged from the data boundary; and the unexposed delete API removed Storage before database metadata.
- **Repair design, implemented but not committed:** the form now creates one stable test UUID and preserves it through retries. Attachment saves durably register unified row-and-path custody before remote work. A retry keeps old custody until cleanup is complete and one verified write durably replaces it; successful work releases custody only after the exact row, attachment, access context, and retained UI state are verified. Rollback deletes the database row first, captures any cascaded attachment path, then removes Storage idempotently. The public non-atomic delete API was removed. Matching-context drains clear stranded row/path work, while a stale access context leaves custody untouched.
- **Farmer-visible repair:** Soil Rx publishes pending/syncing/blocked state, renders its existing Needs-attention list, and provides exact-queue guarded Retry and Dismiss. Field buttons and the form selector now share one selection action. A successful save opens only the new newest card. UI and service share the zero-byte, exact 20 MB, MIME, and 255-character filename boundary.
- **Focused mutation proof:** forced TypeScript passed. `QueuedSoilRxRepository.regression.ts` passed while injecting ambiguous metadata acceptance after upload, access-epoch change after upload, interruption of custody replacement, retry cleanup failure, matching-context drain, permanent replay failure, wrong-key retry, retry/dismiss, and same-ID retry. `SupabaseSoilRxRepository.regression.ts` passed with the database-first internal rollback contract. Source-order mutation guards require custody before the first remote write, cleanup before retry replacement, and retained state before custody release.
- **Application proof:** the dedicated Soil Rx Playwright packet passed all six desktop/phone tests. It exercises text save, attachment save, metadata-failure row/path cleanup, same-ID retry with one final row, offline queue/replay, visible permanent-failure Needs attention with Retry and Dismiss, synchronized field selection, newest-only expansion, read-only denial, signed report opening, and zero unknown or unrelated Supabase writes.
- **Current gate:** backend-dependent disposable proof and the complete runbook ladder have not yet been rerun for these uncommitted repair bytes. Soil made no Docker, local Supabase, scenario, linked, or live call because Connect Workflows owns the shared backend. A new immutable repair commit and fresh exact-SHA Sol review remain required. No push, PR mutation, merge, deployment, live migration/data/auth/permission/secret change, or customer action occurred.

## SRX-006 — Custody repair proof, clean backend release, and review handoff

- **Date/time:** 2026-08-11 10:10:56 (`America/Chicago`, UTC-05:00).
- **SRX-005 evidence correction:** the final dedicated Soil Rx Playwright packet contains eight tests across the desktop and phone projects, not the earlier six-test count. The exact command exited `0` with `8 passed`; the complete ordinary browser suite later exited `0` with `70 passed` and `14` intentional skips.
- **Repair result:** all six P2 and two P3 review findings are addressed in the bounded 17-file repair. Stable draft identity plus durable unified row/object custody prevents duplicate or hidden tests. Custody survives post-upload access changes, interrupted replacement, and failed retry cleanup until a matching context drains it. Permanent replay failures publish honest blocked state and guarded Needs-attention Retry/Dismiss actions. Form/history field selection is bidirectional, a successful save expands only the new test, picker limits match the service boundary, and the public non-atomic delete API is removed in favor of internal database-first rollback.
- **Focused and mutation proof:** `npx tsc -b --force`, `SupabaseSoilRxRepository.regression.ts`, `QueuedSoilRxRepository.regression.ts`, and `revokedFarmRecovery.regression.ts` each exited `0`. Fault injection covers upload success plus metadata failure, access-epoch change after upload, interruption during custody transfer, retry cleanup failure, matching-context drain, stable same-ID retry, permanent replay failure, and final single-row/single-object convergence. Static foundation guards passed, and all `25/25` foundation mutation cases passed.
- **Offline ladder:** `npm run regression`, `npm run build`, and `npm audit --audit-level=high` each exited `0`; the audit found zero vulnerabilities. The dedicated two-project Soil Rx browser packet exited `0` with `8 passed`. `npm run test:e2e` exited `0` with `70 passed` and `14` intentional skips. Changed-file credential scanning passed across all 17 files, protected Grain/Harvest/Weather/Programs/Inventory/bin/contract/delivery scope scanning found zero matches, the index remained empty, and `git diff --check` exited `0`.
- **Backend ownership and proof:** Connect Workflows released the shared backend after its six-lane packet and a zero-residue audit. Soil independently re-attested zero scenario processes, journals, clock images, parked replacement container, and governed listeners before continuing. `scripts\verify-soil-rx-disposable.ps1` exited `0` in `14.5s` with `SOIL_RX_DISPOSABLE_RLS_STORAGE_PASS`. A foreground wrapper timed out around the first foundation launch and its completed process exited before an exit monitor attached, so that launch received no credit; a clean residue audit preceded the one credited proof-capture rerun. The credited `npm run verify:foundation` rerun exited `0` with `Farm Rx foundation gate: PASS`, another Soil disposable pass, and browser proof of `70 passed` plus `14` intentional skips. `npm run verify:season` exited `0` with `Farm Rx season contract gate: PASS`; as its own output states, that command proves the 101-fixture closed contract and isolation boundary, including rejection of nine contract and fourteen isolation mutations, and does not claim a new workflow-scenario run.
- **Final release:** scenario processes, clock journals, clock images, proof-owned or parked containers, and governed listeners on ports `4174-4187` and `4274-4280` were each `0`. Ordinary database container `0c770fdb6b184f0643a891c0351d0db1a95650d4dd3c8d38586ee27333a5e05a` was running healthy with restart policy `unless-stopped` on `public.ecr.aws/supabase/postgres:17.6.1.134`, exact image digest `sha256:ba10e934f0a59990379f78ab9ed93926f1c291dd61a12fe4026f4202f1b89770`; `pg_isready` reported accepting connections. Soil released the backend to the queued V2/CW owners and will make no further backend or topology call in this tranche.
- **Remaining gate:** create one new immutable local repair commit, then obtain a fresh-context, read-only Sol review of that exact SHA. No push, pull-request mutation, merge, deployment, live migration/data/auth/permission/secret change, customer action, or other outward action is authorized.

## SRX-007 — Repaired-byte workflow matrix and Cedar harness correction

- **Date/time:** 2026-08-11 11:22:21 (`America/Chicago`, UTC-05:00).
- **SRX-006 evidence correction:** the final repair contains 19 changed files, not the earlier 17-file count. The two additional files are the narrowly repaired Cedar proof harness and its new mandatory native-stderr regression. The Soil Rx product/data/browser repair bytes described by SRX-006 remained frozen throughout the workflow matrix.
- **Credited workflow matrix:** Maple Ridge January-through-December plus the August-through-December governed phases exited `0` with `MAPLE_2027_AUGUST_DECEMBER_DISPOSABLE_PASS`; North Fork exited `0` after eight desktop and eight phone cases with `NORTH_FORK_2027_DISPOSABLE_PASS`; Prairie Spray exited `0` with `PRAIRIE_SPRAY_2027_DISPOSABLE_PASS`; Harvest Ridge exited `0` after canonical and reverse-order proof with `HARVEST_RIDGE_2027_DISPOSABLE_PASS`; repaired Cedar Creek exited `0` in `205.0s` after desktop and phone Playwright exits `0`, exact SQL-marker enforcement, and `CEDAR_CREEK_2027_DISPOSABLE_PASS`; Pine Hill exited `0` in `774.8s` after canonical, revoked-work, account-switch, corrupt-active, corrupt-vault, repeated SQL verification, and `PINE_HILL_2027_DISPOSABLE_PASS` proof.
- **Preserved Cedar failure/no-credit attempt:** the first Cedar invocation exited `1` after only `CEDAR_CREEK_2027_STATIC_CONTRACT_PASS`; Pine was not started. `%TEMP%\soil-rx-cedar-20260811-105209.out.log` and `.err.log` show routine `npm notice run farm-rx@0.0.0 npx` native stderr followed by the clock adapter's primary rethrow before Playwright output, exit capture, SQL, or final markers. No journal survived, governed recovery completed, and the ordinary database was healthy before adjudication. No unchanged retry occurred.
- **Harness classification and repair:** the failure was deterministic Windows PowerShell harness behavior, not a Soil Rx product, browser, SQL, or transient backend failure. With global `ErrorActionPreference=Stop`, redirected native stderr terminated the Cedar action before its explicit fail-closed checks. Read-only comparison showed the same repair in accepted V2 commit `01f98a9ae9a4208c17a1c4002e91f21f7ec5875b` and CW commit `0ea4b5c8f5584689799c8ba14effee698e0131f0`. Only the native-capture hunk was transplanted: Cedar saves the prior preference, sets `Continue` solely around Playwright capture and `LASTEXITCODE`, restores in `finally`, then retains the existing output, nonzero-exit, failed-summary, SQL-marker, and final-marker guards. No V2/CW product or migration bytes were imported.
- **Mandatory harness proof:** `scripts\verify-cedar-native-stderr.regression.ps1` is included in `npm run regression`. PowerShell parse passed for both Cedar scripts; the focused regression emitted `CEDAR_NATIVE_STDERR_REGRESSION_PASS`; Cedar `-StaticOnly` emitted `CEDAR_CREEK_2027_STATIC_CONTRACT_PASS`. The regression proves exact save/set/capture/exit/finally/restore/output/guard order, executable retention of routine native stderr with exit `0`, and rejection of stderr plus nonzero exit and an exit-`0` failed-test summary. Six controlled source mutations were rejected: no saved preference, `Stop` instead of `Continue`, falsified exit capture, no restore, removed nonzero guard, and removed failed-summary guard.
- **Backend-free post-repair gates:** `npm run regression`, `npx tsc -b --force`, `npm run build`, `npm audit --audit-level=high`, and `git diff --check` each exited `0`; the audit found zero vulnerabilities. The changed-file scope scan passed for exactly 19 files, the credential scan found zero matches, the index remained empty, and no generated test/build artifact entered Git status.
- **Durable scenario logs:** credited logs are `%TEMP%\soil-rx-maple-augdec-20260811-101318.out.log`, `soil-rx-north-fork-20260811-102315.out.log`, `soil-rx-prairie-20260811-103651.out.log`, `soil-rx-harvest-20260811-103750.out.log`, `soil-rx-cedar-repaired-20260811-110443.out.log`, and `soil-rx-pine-20260811-110840.out.log`, each with a same-prefix `.err.log` companion.
- **Final shared-backend release:** scenario processes, clock journals, clock images, Farm Rx parked/replacement/residue containers, and governed listeners on ports `4174-4187` and `4274-4280` were each `0`. Ordinary database container `7dfaa26c4c6d0139bfaebd64e5a2863b0d2967254d27e7b225392229ad89c48d` was running healthy with restart policy `unless-stopped` on `public.ecr.aws/supabase/postgres:17.6.1.134`, exact image digest `sha256:ba10e934f0a59990379f78ab9ed93926f1c291dd61a12fe4026f4202f1b89770`; `pg_isready` reported accepting connections. Soil sent the evidence-backed clean release to the orchestrator and queued V2 owner and will make no further backend/topology calls in this tranche.
- **Remaining gate:** create one new immutable local repair commit, then obtain a fresh-context, read-only Sol review of that exact SHA. No push, pull-request mutation, merge, deployment, live migration/data/auth/permission/secret change, customer action, or other outward action is authorized.

## SRX-008 — Queue-custody review repair and accepted shared-harness transplant

- **Date/time:** 2026-08-12 22:11:20 (`America/Chicago`, UTC-05:00).
- **Immutable base and review history:** local Soil head remains blocked commit `070c3097fcbf91a6d2e49b114a847da9b9d3adab`; it was not amended. The subsequent bounded, uncommitted Soil repair closes review findings SRX1-009 and SRX1-010 by binding Retry and Dismiss to exactly one byte-identical parked operation under one queue transaction, revalidating active and parked custody before dismissal, fencing same-farm revoke/regrant races, and making application-level unrelated-write and cleanup-failure proof fail closed. Two fresh read-only reviews accepted the final queue transaction/order, contention, post-capture context, and mutation evidence. The six accepted Soil repair files remained byte-identical throughout the later harness transplant.
- **Accepted harness provenance:** shared Windows proof-harness files were copied only from immutable V2 commit `8d589f1530f7d14388500e80a6c90ff142bf5194`, parent `850d8e2986baea065c4241ce32608d8ce5afe13b`, after its exact-commit Sol review returned `ACCEPT` with zero P1/P2/P3 and its serialized Foundation proof exited `0`. The V2 ledger and all V2 product bytes were excluded. Direct/new imports are the native Foundation helper and owner regression, Maple browser Job Object helper, Maple timeout regression, Maple July wiring regression, and Maple browser wrapper. Manual additive merges are limited to Soil's Foundation static/mutation/wrapper integrations and its small `verify-season.ps1` shared-harness bridge; Soil route, disposable lane, queue regressions, Cedar native capture, package wiring, and protected Harvest/Pine paths were preserved.
- **Scope-correction failure/no-credit:** the first complete backend-free Stage A stopped at `npm run verify:season` because the intentional Soil bridge changed `scripts/verify-season.ps1` while its owning normalized SHA pin still named the prior wrapper. That attempt receives no Stage A credit. No command after the failure ran until diagnosis and approval. The bounded correction added only `scripts/verify-season-contract.mjs` to scope, updated `EXPECTED_POWERSHELL_SHA256` to independently recomputed normalized SHA-256 `c60b8a74434742c005d5a49a45e12180775745c65102c1606b252fe356bb4e02`, and made the exact bridge count and placement load-bearing. The affected contract, isolation, shared-harness, Foundation-static, and Foundation-mutation checks then passed before Stage A restarted from command one.
- **Credited backend-free Stage A:** PowerShell parsing and C# `MapleSeasonOwnedJob` load passed; focused Soil queue, queue-transaction, Supabase repository, and revoked-farm recovery regressions passed; Maple timeout, Maple July wiring, shared-harness/Soil-bridge, Foundation native-owner, and Cedar native-stderr regressions passed. The dedicated Soil Playwright command ran desktop and phone projects and exited `0` with `10 passed`, covering text and attachment saves, offline replay, guarded Retry/Dismiss, permanent failure, cleanup-removal failure with visible custody and later matching-context drain, denied/read-only actors, and exact unrelated-table non-writes. Forced TypeScript, full `npm run regression` plus postregression, build, and high-severity audit each exited `0`; the audit reported zero vulnerabilities.
- **Contract and mutation evidence:** final Foundation static guards passed and all merged `79/79` mutations were rejected, retaining V2's 77 accepted cases plus Soil's route and disposable-lane cases. `npm run verify:season` exited `0` as contract/isolation/shared-harness proof only: 101 fixtures and six scenario contracts passed while nine contract and fourteen isolation mutations were rejected. It is not claimed as disposable Foundation or workflow-scenario proof.
- **Frozen pre-ledger packet:** before this append-only entry, Git reported exactly 18 intended modified/untracked paths, empty index, clean `git diff --check`, zero generated build/test-report paths, and zero credential-like matches. The canonical aggregate over sorted `path|sha256` lines with a final newline was `5ed3466f31b153dceb0e31865d50fd8a9238869119384ef45bee467d4f774a3e`. No Docker, local or linked Supabase, shared topology, Foundation disposable, workflow scenario, live service, stage, commit, ref, push, pull-request, merge, deployment, migration, secret/auth/permission, customer, or other outward action occurred in this tranche.
- **Remaining gate:** freeze the final 19-file packet including this ledger entry and obtain a genuinely fresh, backend-free, read-only Sol pre-backend review. A green review permits only a request for the serialized disposable/backend ladder; it does not authorize commit or outward action.

## SRX-009 — SRX1-011 request-guard correction and restarted backend-free proof

- **Date/time:** 2026-08-12 22:38:16 (`America/Chicago`, UTC-05:00).
- **Preserved review-block evidence:** the prior frozen 19-path aggregate `fc5aae8d1a8f3818fb857a3dbab6d2211d7d1f490e67d8cc99d3c5999ff53f7a` remains immutable evidence of the fresh pre-backend `BLOCK` (zero P1, one P2, zero P3). SRX1-011 correctly identified that early browser read stubs for `farms`, `farm_memberships`, `farm_rep_access`, `entities`, and `fields` could fulfill a non-GET REST request before the later unrelated-write recorder observed it. No backend or real data action was taken while awaiting Mason's explicit synthetic-proof approval.
- **Bounded repair:** after Mason approved local synthetic request-guard work only, `tests/e2e/soil-rx.spec.ts` gained an explicit method gate before every early REST-table read stub. Every non-GET REST-table request is recorded and rejected with `405` unless its exact, case-sensitive operation is one of `POST soil_tests`, `DELETE soil_tests`, or `POST soil_test_attachments`; Storage operations remain outside this REST-table gate. There is no broad table allowlist, implicit fallthrough, or method normalization. The protected state now includes the five early tables as well as the pre-existing unrelated tables.
- **Load-bearing application proof:** desktop and phone each ran a controlled synthetic `POST`, `PATCH`, and `DELETE` to the early `fields` stub. Each request returned `405`, was recorded in order, and left protected snapshots and write state byte-identical. The same dedicated two-project command also retained text/attachment saves, offline replay, guarded Retry/Dismiss, cleanup-failure custody/recovery, read-only, denied-actor, and existing unrelated-nonwrite coverage; it exited `0` with `14 passed`.
- **Controlled mutation proof:** the browser spec runs six exact source mutations and requires all to turn red: move the method gate after early stubs, omit the `fields` early stub, allow `DELETE`, broaden the Soil-write exception, drop recording, and fulfill before refusal. The mutation oracle itself confines marker matching to the interceptor's early-read-stub region, so an explanatory marker outside that region cannot false-green the omission case.
- **Restarted credited backend-free Stage A:** after focused browser and Foundation static/mutation checks passed, the full ladder restarted at command one. PowerShell parsing and C# load passed; queued Soil, queue-transaction, Supabase Soil, and revoked-farm recovery regressions passed; Maple timeout, Maple July, shared-harness/Soil bridge, Foundation-native owner, and Cedar-native stderr regressions passed; forced TypeScript, `npm run regression`, `npm run postregression`, build, and high-severity audit each exited `0`, with zero audit vulnerabilities. Final `npm run verify:season` passed its contract/isolation/shared-harness boundary (101 fixtures, six scenarios, nine contract and fourteen isolation mutations rejected), and final Foundation static/mutation proof passed with merged `79/79` mutations. No Docker, Supabase, shared topology, Foundation disposable, workflow scenario, stage, commit, ref, push, pull-request, merge, deployment, live service, or real data action occurred.
- **Remaining gate:** freeze the same 19 intended paths with this appended ledger correction, run final scope/generated/credential/hash checks, and obtain a genuinely fresh backend-free read-only Sol review. Only a new `ACCEPT` permits requesting—not starting—the serialized backend ladder.

## SRX-010 — SRX1-011 serialized disposable RLS/storage proof credited

- **Date/time:** 2026-08-14 (`America/Chicago`). Connect Workflows first completed its accepted full disposable proof and released shared-backend custody with zero proof process, governed listener, clock/journal/temp residue, and a healthy pinned ordinary database. Soil then independently re-attested all 19 accepted leaf hashes byte-for-byte. The prior aggregate discrepancy was only PowerShell sort behavior: the accepted culture-aware terminal-LF recipe remains `3660149e9d44d67f2d8ba44c491921788e4198a8c7afd13401640e4c25b18516`, while true ordinal terminal-LF order is `5468e031f346369030c8d9a0a6f5258d1c780a7baf86f128b51d7f83268bd290`. Missing, extra, and changed leaves were each zero; index remained empty and `git diff --check` clean. The fresh read-only Sol reviewer confirmed the prior `ACCEPT` still bound and authorized exactly one serialized Soil disposable invocation.
- **Credited proof:** exactly one `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-soil-rx-disposable.ps1` invocation exited `0` after `19.9s` with `SOIL_RX_DISPOSABLE_RLS_STORAGE_PASS`. Its PID-scoped `postgres:17` container applied the full migration chain and proved owner/cross-farm/rep access, access-epoch denial, Soil test identity protection, private attachment storage paths, traversal denial, and cleanup semantics using synthetic UUIDs and `.example.test` identities only. The runner's `finally` block removed its disposable container.
- **Post-run custody:** zero Soil/CW proof process, zero port `4187` listener, and zero Soil/CW/clock/journal/temp residue remained. The ordinary `supabase_db_farmrx-farmer-simplicity-2027-local` container remained healthy on `public.ecr.aws/supabase/postgres:17.6.1.134`, and `pg_isready` reported accepting connections. No Foundation/scenario, commit, ref, push, pull-request, merge, production, linked/live database, customer-data, secret/auth/permission, or other outward action ran.
- **Remaining gate:** freeze this append-only evidence and obtain a new exact-byte read-only review before requesting the next serialized Foundation rung. This credited Soil disposable invocation grants no automatic retry or broader backend/scenario authority.

## SRX-011 — Foundation wrong-server reuse diagnosed and fenced

- **Date/time:** 2026-08-14 (`America/Chicago`). Fresh exact-byte review accepted SRX-010 aggregate `ba5239985d0d34f9712929b6b8eb16fc68805c4cd13e364885b19aae5a4cf1fc`, zero P1 / zero P2 / zero P3, and authorized exactly one serialized `npm run verify:foundation` invocation. That invocation exited `1` after `704.6s` and remains **NO CREDIT**. All native-owner, regression, build, audit, static, `81`-predecessor mutation, disposable migration/RLS, and Soil disposable lanes preceding the built-browser phase passed. The final browser lane reported `10 passed`, `14 skipped`, and `66 failed`; durable native log `C:\Users\mason\AppData\Local\FarmRx\foundation-native-lanes\20260814T065524145Z-built-browser-f34b923543fd49de995a666a3a4197a6.log` is `214132` bytes with SHA-256 `7b11bb3a35acd24f8be8fd29037edded5e3e934b8774685d12dff96848ca6fef`.
- **Root cause:** an unrelated CRX Manager below-cost UI Vite process, PID `22224` with parent `75576`, has owned `127.0.0.1:4173` since 2026-08-13 16:31 local time. Farm Rx Playwright used the same fixed port with `reuseExistingServer: !process.env.CI`, so it silently attached to the CRX server instead of the just-built Farm Rx app. Failed-page evidence showed only `Loading Crop RX...`; static, header, and source-only browser cases passed while nearly every rendered Farm Rx case failed. This is one shared wrong-server collision, not 66 independent product defects. Post-failure cleanup left zero Farm Rx proof process, zero governed listener/residue, and the pinned ordinary database healthy.
- **Fail-closed repair:** `playwright.config.ts` now sets `reuseExistingServer: false`. `scripts/verify-foundation.ps1` attempts an exclusive loopback bind on port `4173` before any long Foundation lane and throws `FOUNDATION_BROWSER_PORT_4173_OCCUPIED` instead of reusing or waiting behind an unknown server. The CRX process was inspected only and was not stopped, modified, or adopted.
- **Load-bearing proof:** Foundation parsing, forced TypeScript, and static guards pass. Two new controlled mutations remove the port preflight call or restore existing-server reuse; both turn the static contract red. The merged Foundation mutation drill now passes `81/81` controlled mutations.
- **Remaining gate:** the exact unrelated CRX preview process must stop or release port `4173`; this requires owner coordination because it is outside Farm Rx scope. After the port is free, restart the complete backend-free gate, freeze exact bytes, obtain a fresh read-only Sol review, and only then request one new serialized Foundation invocation. No retry, scenario, commit, ref, push/PR, live service, production, or customer-data action is authorized on these bytes.

## SRX-012 — Port-collision repair proved and backend-free gate restarted

- **Date/time and authority:** 2026-08-14 13:44:29 (`America/Chicago`, UTC-05:00). Mason explicitly approved stopping the stale CRX preview if it still owned port `4173`, running Foundation exactly once after a fresh review, and then advancing Soil scenarios. The port was independently re-resolved before any stop: it was already free, so no CRX process was killed or modified.
- **Executable denial proof:** a controlled local listener was bound to `127.0.0.1:4173`, then `scripts\verify-foundation.ps1` was invoked. It exited `1` immediately with `FOUNDATION_BROWSER_PORT_4173_OCCUPIED`; the probe listener was removed in `finally`, and no Foundation lane, Docker, Supabase, shared-backend, or scenario action began. This proves the new preflight rejects an occupied browser port instead of reusing an unrelated server.
- **Restarted backend-free gate:** PowerShell parsing and C# `MapleSeasonOwnedJob` load passed. Focused queued Soil, queue-transaction, Supabase Soil, and revoked-farm recovery regressions passed. Maple timeout, Maple July clock wiring, shared-harness bridge, Foundation native-owner, and Cedar native-stderr regressions each passed independently. The dedicated Soil Playwright command built and served Farm Rx itself on the newly free port and exited `0` with `14 passed` across desktop and phone. Forced TypeScript, full `npm run regression` plus automatic postregression, production build, and high-severity audit each exited `0`; the audit reported zero vulnerabilities.
- **Final contracts:** `npm run verify:season` exited `0` at its contract/isolation boundary with 101 fixtures, six scenarios, nine contract mutations, and fourteen isolation mutations. Foundation static guards passed, and the final mutation drill rejected all `81/81` controlled mutations, including removal of the browser-port preflight call and restoration of existing-server reuse.
- **Remaining gate:** freeze the complete current packet, verify scope/generated/credential/hash identity, and obtain a genuinely fresh backend-free, read-only Sol review. Only an exact-byte `ACCEPT` may authorize the one approved serialized `npm run verify:foundation` rerun. Soil scenarios remain blocked until that rerun passes and shared-backend custody is re-attested; no commit, ref, push, pull-request, merge, deployment, linked/live database, production, customer-data, secret/auth/permission, or other outward action is authorized.

## SRX-013 — Foundation preflight-order review repair

- **Fresh review result:** the exact SRX-012 20-path packet, ordinal terminal-LF aggregate `5bdd673719994bfa5142984ea7c0dfd3bcdf2986617ea28c6505f5ef824e6685`, was rejected with zero P1, one P2, and zero P3. The reviewer confirmed the runtime preflight was first, stopped its listener in `finally`, and paired with `reuseExistingServer: false`, but correctly found the static contract proved only call presence/count. Moving the call after the browser lane could preserve all prior checks and false-green. The one approved Foundation rerun was not started.
- **Bounded proof repair:** the Foundation static contract now requires the exact normalized entry sequence `Push-Location` → main `try` → browser-port preflight → controlled intermediate-failure probe. The executable mutation matrix adds a source-real relocation of the sole preflight call to after the built-browser lane and a separate corruption of the exact occupied-port refusal marker. Each must turn the owning static guard red; the prior removal and existing-server-reuse mutations remain.
- **Owning proof:** the repaired Foundation static guard exited `0`, and the expanded executable mutation drill exited `0` with all `83/83` controlled mutations turning the gate red, including the new relocation and marker-corruption cases. No runtime, browser, port, Foundation, Docker, Supabase, or scenario command was invoked for this proof-only repair.
- **Remaining gate:** re-freeze the complete packet and obtain another genuinely fresh read-only Sol review. No Foundation, scenario, Docker, Supabase, shared-backend, commit, ref, push, pull-request, merge, deployment, linked/live database, production, customer-data, secret/auth/permission, or other outward action is authorized before that exact-byte review accepts the repair.

## SRX-014 — Accepted CW artifact packet imported for Soil proof

- **Date/time and source custody:** 2026-08-20 (`America/Chicago`). The exact CW commit `dde53a962951126ee6a5e79d376bd663ca695869` received a genuinely fresh exact-commit Sol review with `ACCEPT`, zero P1 / zero P2 / zero P3. Soil copied portable bytes only from that immutable commit object, never from the mutable CW worktree. The imported set is six artifact-owner/adapter/topology/spike scripts, two append-only historical evidence suffixes, the replacement-manifest JSON, and its regression. The V2 harness, Soil route, queue repair, Cedar capture, Foundation native lane, browser port fence, and season bridge remain Soil-owned and preserved.
- **Portable identity:** the accepted reusable artifact is token `b9ad08aeb66ed961e8426b2cce527365`, tag `maple-faketime-artifacts-b9ad08aeb66ed961e8426b2cce527365:synthetic`, and immutable ref/ID `sha256:7cbc0a183ba33c4318a9784dae376104e55282e8e0c716511336afaf924f3302`, with the exact five-label contract. The ten portable source/evidence leaves have canonical aggregate `fa6f9f9be624def8b7b1e6932ff2a113da82e2cb85837e0f0ab598500f9effef`. The copied manifest separately retains the accepted derived-image and post-cleanup evidence identities; prior retired-artifact facts remain append-only historical evidence.
- **Soil integration:** `package.json` adds exactly the adapter, topology, and replacement-manifest regressions between the existing Chunk 5 and due-items lanes. Maple July now binds the replacement ref/tag/ID and five labels before its port proof. Soil's Foundation static/mutation owners add 34 discovery, 19 replacement, and three omission cases to the prior 83, for exact `139/139` accounting. The portable manifest regression's own four-case Git AST/trace child proof remains separately load-bearing and is not double-counted in 139.
- **Narrow backend-free proof:** JavaScript and PowerShell parsing passed. Foundation static guards passed; every `139/139` controlled mutation turned the real guard red. The replacement-manifest regression emitted its Git trace, error-preference restoration, failure-capture, AST-child, clean-fallback, and canonical-manifest PASS markers. The adapter regression, topology-plan regression, and Maple July wiring regression each exited `0` with their exact terminal PASS marker.
- **CW runtime provenance and Soil credit boundary:** CW-118 records one accepted focused disposable, one Foundation pass, and six governed scenarios on its accepted pre-entry CW packet, followed by a clean release; the portable artifact bytes subsequently remained unchanged through exact commit `dde53a9`. Those facts establish CW provenance only. Soil claims **no** disposable, Foundation, Docker, Supabase, workflow-scenario, backend, or live-runtime credit for this imported packet.
- **Current gate:** restart the complete Soil backend-free Stage A from command one on the final 31-path packet including this entry, then freeze exact bytes and obtain a genuinely fresh read-only exact-byte Sol review before requesting any serialized backend lane. No stage, commit, ref, push, pull request, merge, deployment, linked/live database, customer data, secret/auth/permission, or other outward action occurred.

## SRX-015 — Stage A browser-project invocation correction

- **Date/time:** 2026-08-20 (`America/Chicago`). The first post-import Stage A attempt is **NO CREDIT**. Commands 1–3 completed, but command 4 used obsolete Playwright project names `desktop` and `phone`. Playwright exited `1` before running a test and reported that the configured projects are `chromium-desktop` and `chromium-phone`. This is an operator invocation error, not a product, browser, artifact, or harness failure.
- **Disposition:** no backend, Docker, Supabase, Foundation, governed scenario, stage, commit, ref, or outward action occurred. Restart the entire backend-free Stage A from command one on the packet containing this append-only correction, using the exact configured project names. No credit carries from the interrupted attempt.

## SRX-016 — Forced-Git live-span proof repair

- **Fresh review BLOCK:** the exact 31-path aggregate `1faca2940610ef55c66f0b20471566a7df3aee58f0adcf126fe81c8b5fd1914c` received a genuinely fresh read-only review with `BLOCK`, zero P1 / one P2 / zero P3. The two controlled mutations that deadened the forced Git call or injected a synthetic accepted result were reported as red by the JavaScript mutation drill, but the static oracle relied on separable source substrings/order rather than independently pinning the live executable evidence span. The prior Stage A remains historical backend-free evidence only and is superseded for acceptance; it grants no runtime credit.
- **Bounded repair:** the Foundation static owner now LF-normalizes the portable regression, requires one unique contiguous forced-Git evidence span from the null-cause initialization through the exact scoped-EAP refusal, and pins its exact SHA-256 `14651a7e62810c19660b6376aa9051031cfa90a54d5f11d6206272faeba1d1c1`. The existing PowerShell AST contract continues to require the one real forced call inside the exact top-level trace `try`, exact caught message/exit assignments, and executable child cases. A split-constructed outer owner needle independently requires the live-span guard, so removing that guard cannot self-describe itself green.
- **Mutation accounting:** the two existing dead-call/synthetic-result mutations now fail the exact span contract. One redundant comparator case was replaced by a source-real mutation removing the live-span guard and requiring the outer owner error. The denominator therefore remains exactly `139/139`: 83 prior Soil/V2/Foundation cases, 34 manifest-discovery cases, 19 artifact cases, and three omission cases. Static baseline and all `139/139` mutations passed after repair.
- **Current gate:** restart complete backend-free Stage A from command one on the post-entry bytes using a durable external owner that binds the exact start/final manifest and retains paired stdout/stderr files with lengths and SHA-256 for all ten commands. No backend, Docker, Supabase, Foundation runtime, governed scenario, stage, commit, ref, or outward action is authorized.

## SRX-017 — Durable Stage A wrapper host correction

- **NO CREDIT construction attempt:** the first durable owner bound the post-SRX-016 manifest and then stopped before Stage A command 1. It was launched under Windows PowerShell 5.1, whose `ProcessStartInfo` implementation does not expose the `ArgumentList` collection used by the prepared direct-process capture. The wrapper threw on its first argument append; no child command, test, browser, backend, Docker, Supabase, or scenario action began. Its partial manifest-only evidence remains under `%TEMP%\farmrx-soil-artifact-stage-a-d03f90cd5b40`.
- **Correction and gate:** run the same bounded direct-process owner under installed PowerShell 7, preserving concurrent stdout/stderr drains, process disposal before readback/hash, exact command order, fail-closed markers, and start/final source custody. Restart from command 1 on the packet containing this entry. The failed construction grants no credit and no retry of a completed command occurred.

## SRX-018 — Durable Stage A npm executable-resolution correction

- **NO CREDIT wrapper attempt:** the PowerShell 7 owner captured Stage A commands 1–3 with exit `0`, disposed streams, lengths, and SHA-256 evidence, then command 4 failed before Playwright loaded. Direct `ProcessStartInfo` resolution selected the repository-local `node_modules/.bin/npx.cmd`; that shim looked for nonexistent repository-local npm internals and exited `1`. The wrapper recorded the nonzero exit, absent marker, paired streams, and `fail_closed`. Its evidence remains under `%TEMP%\farmrx-soil-artifact-stage-a-386b7b9fbe13`. No browser test, backend, Docker, Supabase, Foundation runtime, or scenario began.
- **Correction and gate:** resolve `npm.cmd` and `npx.cmd` once from their installed absolute application paths and pass those exact paths to the direct child owner; also retain a syntactically valid fail-closed throw. Restart all ten commands from command 1 on the new ledger aggregate. Prior partial commands grant no credit.

## SRX-019 — Final accepted-packet runtime ladder and clean local release

- **Date/time and exact source:** 2026-08-20 17:21:00 (`America/Chicago`, UTC-05:00). A genuinely fresh read-only Sol review had accepted the exact 31-path packet with zero P1 / zero P2 / zero P3. Immediately before runtime, branch `codex/farmrx-soil-rx`, HEAD `070c3097fcbf91a6d2e49b114a847da9b9d3adab`, empty index, clean diff, and canonical ordinal UTF-8/LF-final aggregate `e1d8ad54b01d02800c27b8d403e6ab5b6566f3cfa7070999f88f66260e3a9fa6` were re-attested. The exclusive local boundary was clean and the ordinary Supabase database used exact image ID `sha256:ba10e934f0a59990379f78ab9ed93926f1c291dd61a12fe4026f4202f1b89770`, was running healthy with restart `unless-stopped`, and accepted `pg_isready`.
- **Serialized runtime result:** every authorized rung ran exactly once, in order, with no retry. `scripts\verify-soil-rx-disposable.ps1` exited `0` in `16.1s` with `SOIL_RX_DISPOSABLE_RLS_STORAGE_PASS`. `CI=1 npm run verify:foundation` exited `0` in `413.3s`; Foundation native custody, full regression/build/audit/static proof, the `139/139` mutation drill, embedded Soil disposable proof, built-browser native capture (`76 passed`, `14` expected skips), and terminal `Farm Rx foundation gate: PASS` all passed. Its durable built-browser log is `C:\Users\mason\AppData\Local\FarmRx\foundation-native-lanes\20260820T211213590Z-built-browser-61b705949c7844c5a7ac776ddd3104c8e.log`.
- **Scenario result:** the six governed scenarios then ran strictly one at a time with a clean custody audit between each. Maple exited `0` in `450.1s` after one continuous January-through-December narrative with `MAPLE_2027_AUGUST_DECEMBER_DISPOSABLE_PASS`. North Fork exited `0` in `806.8s` after complete desktop and phone NF1-NF8 proof with `NORTH_FORK_2027_VERIFY_PASS` and `NORTH_FORK_2027_DISPOSABLE_PASS`. Prairie Spray exited `0` in `39.2s` with `PRAIRIE_SPRAY_2027_DISPOSABLE_PASS`. Harvest Ridge exited `0` in `777.0s` with `HARVEST_RIDGE_2027_VERIFY_PASS`, `HARVEST_RIDGE_2027_REVERSE_VERIFY_PASS`, and `HARVEST_RIDGE_2027_DISPOSABLE_PASS`. Cedar Creek exited `0` in `183.0s` after desktop and phone Playwright exits `0` with `CEDAR_CREEK_2027_DISPOSABLE_PASS`. Pine Hill exited `0` in `727.5s` after canonical, replay, corrupt-active, and corrupt-vault custody proof with `PINE_HILL_2027_VERIFY_PASS` and `PINE_HILL_2027_DISPOSABLE_PASS`.
- **Final clean release:** immediately after Pine, the same 31 source paths still reproduced aggregate `e1d8ad54b01d02800c27b8d403e6ab5b6566f3cfa7070999f88f66260e3a9fa6`; index remained empty and `git diff --check` was clean. Governed listeners, Soil proof/scenario processes, exact clock journals, controlled temp roots, clock/derived images, parked/replacement/residue containers, prepared transactions, and owned database sessions were each `0`. The ordinary database was exact container `c7e3542403561157773735fd4014c2f3e152a6bf2f81bcd6df9d9361767d5d6f` on the pinned image above, running healthy with restart `unless-stopped`; `pg_isready` exited `0` and reported accepting connections.
- **Boundary and remaining gate:** all evidence is synthetic and local. No linked/live database, customer data, secret/auth/permission change, stage, commit, ref, push, pull request, merge, deployment, or other outward action occurred. This runtime evidence does not itself complete the tranche: the full backend-free Stage A must restart on the ledger-final bytes, then the exact 31-path packet must freeze and receive a genuinely fresh read-only review.

## SRX-020 — SRX-019 evidence-metadata correction

- **Date/time and review disposition:** 2026-08-20 17:41:18 (`America/Chicago`, UTC-05:00). A genuinely fresh read-only review of exact aggregate `c78c29ecf0a0255720b79b3e5d86a4b9ed73623176770ebb90aaf4feb8ae488b` returned `BLOCK` with zero P1, zero P2, and one P3. The reviewer accepted every runtime outcome, terminal marker, serialization boundary, and clean-release fact in SRX-019, but found two evidence-metadata inaccuracies corrected here without rewriting that historical entry.
- **Exact Foundation log correction:** the retained built-browser log is `C:\Users\mason\AppData\Local\FarmRx\foundation-native-lanes\20260820T211213590Z-built-browser-61b705949c7844c5a7ac776dd3104c8e.log`, not the SRX-019 filename containing an extra `d`. The existing file is `2010` raw bytes with SHA-256 `4810c89f65a76b67b66e83035b2979cf5be2992f926b33d3e27bb6aa22313fe3`.
- **Duration correction:** every per-rung duration printed in SRX-019 is retracted and receives **NO AUTHORITATIVE DURATION CREDIT**. Specifically, `16.1s`, `413.3s`, `450.1s`, `806.8s`, `39.2s`, `777.0s`, `183.0s`, and `727.5s` were coordinator tool-wait estimates, not durably captured launch-to-exit elapsed times. They must not be reused as exact evidence and are not replaced by guesses here.
- **Credit retained:** the exactly-once ordered exits, named PASS markers, browser counts, mutation count, runtime behavior, inter-rung custody checks, and final clean local release recorded in SRX-019 remain credited and unchanged. This correction adds no runtime, backend, scenario, or outward action. The full backend-free Stage A must restart from command one on the packet containing this append-only correction, then the final exact bytes require a new genuinely fresh read-only review.
