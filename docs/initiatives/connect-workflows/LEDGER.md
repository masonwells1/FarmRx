# Initiative CW — Connect Workflows ledger

This ledger is append-only. Never edit, reorder, or delete an earlier entry. If an entry is wrong, append a correction that cites it. Times use `America/Chicago`.

## CW-000 — Preflight and CW-1 plan gate

- **Date/time:** 2026-08-10 17:30:47 -05:00 (`America/Chicago`).
- **Authority:** Mason approved local build, test, and commit work for CW-1 and CW-2 on 2026-08-10, bounded by the `## Owner scope amendment — 2026-08-10` section of `docs/GOAL.md` and the operating loop in `docs/season-readiness/ORCHESTRATOR-RUNBOOK.md`. Mason requires a plain-English tranche plan and his explicit OK before application code is written. No push, pull-request mutation, merge, deploy, live migration/data change, secret/auth/permission change, customer action, or destructive action is authorized.
- **Repository/base:** `C:\FarmRx`; refreshed `origin/main` exact SHA `e723c509f65fc82abbeafba625071207425892e0` (`docs: owner scope amendment — Connect Workflows and Soil Rx initiatives (#23)`). The required amendment heading and both CW tranche definitions were verified from that exact commit.
- **Writer isolation:** branch `codex/farmrx-connect-workflows` in `C:\Users\mason\.codex\worktrees\farmrx-connect-workflows`, created from the exact base above and tracking `origin/main`. `C:\FarmRx` remains the non-writer main checkout.
- **Preflight state:** the writer worktree was clean before this ledger was created. No application code, test, migration, database, service, or production state was changed. This new ledger is intended to be included in the first immutable CW-1 local commit after proof succeeds.
- **CW-1 proposed boundary:** retain the existing blank manual-spray action and add a separate farmer-visible action on a fresh Weather field forecast that opens the existing Inventory spray form with that field, forecast-local date, temperature, wind speed, and compass direction prefilled. The farmer can review or change every value. Navigation/prefill performs no repository or database write; only the existing explicit **Save spray record** action may write. Stale Weather exposes no prefill action. No weather/provenance table, provider link, background write, vendor, migration, or new save path is planned.
- **Expected CW-1 writes:** after explicit farmer save only, the existing application save path may create the normal `application_records` and `application_products` rows and perform its already-governed completed-application Inventory draw-down. No other write is allowed.
- **Expected CW-1 non-writes:** opening Weather, clicking the prefill action, reviewing/editing values, navigating away, or abandoning the form must create zero application, product, Inventory, notification, field-location, Program, Grain, Harvest, scouting, task, provenance, or repository-receipt changes. No live service or database is reachable by proof.
- **Expected implementation area:** `src/WeatherModule.tsx`, `src/InventoryModule.tsx`, `src/data/weatherSprayHandoff.ts`, and focused regressions. A local-only Playwright/disposable-backend proof lane may add narrowly scoped files under `tests/e2e/season`, `tests/season`, `scripts`, and a Playwright config. `docs/initiatives/connect-workflows/LEDGER.md` is the only governance file in the tranche. Exact allowlisting occurs before commit.
- **Hard fence:** do not modify the Grain module, Harvest module, or any bin/contract/delivery code, repository, migration, table, fixture, or proof owned by the separate in-flight initiative. No such dependency was found in preflight.
- **Reconnaissance:** a bounded Terra Medium read-only worker inspected the Weather/Inventory UI seam and returned exact source/proof candidates without writing. A bounded Sol High read-only data-contract worker was launched without delegation or service access but did not return within the bounded preflight window and was interrupted; no output from that worker is credited. The root Sol independently verified the existing application save path, current Cedar Creek local fixture/browser seam, and no-migration boundary from source.
- **Verification plan after approval:** focused route/parser and no-write regressions; deterministic 2027 disposable-backend browser proof at representative desktop and 390×844 phone sizes; pre-action/no-save/post-save database assertions; then `npx tsc -b --force`, `npm run regression`, `npm run build`, `npm audit --audit-level=high`, verified foundation proof, the complete season scenario ladder, and final `git diff --check`, scope/untracked/credential review. Failures remain explicit and are not weakened.
- **Current gate:** **WAITING FOR MASON — CW-1 plan approval.** No CW-1 application or test code may be written until Mason explicitly approves this plan.

## CW-001 — Mason approved the CW-1 plan

- **Date/time:** 2026-08-10 17:34:44 -05:00 (`America/Chicago`).
- **Authority:** Mason replied `yes i approve` to the plain-English CW-1 plan in the owning Codex task. This authorizes the bounded local CW-1 implementation, tests, complete verification ladder, and one immutable local commit already covered by the initiative authority. Mason also directed the task to continue without routine check-ins while he is away.
- **Approval boundary:** this approval does not authorize push, pull-request creation or mutation, merge, deploy, live migration/data change, secret/auth/permission change, customer action, destructive action, or any Grain/Harvest/bin/contract/delivery work. Those gates remain closed.
- **Execution:** root Sol remains orchestrator. Exactly one bounded Terra Medium worker is the CW-1 application/test writer; it may not delegate or perform outward actions. Root Sol owns verification, scope review, ledger evidence, commit identity, and the review packet.
- **Current gate:** **APPROVED FOR LOCAL CW-1 BUILD AND PROOF.**

## CW-002 — CW-1 implementation and pre-commit review repairs

- **Date/time:** 2026-08-10 19:32:41 -05:00 (`America/Chicago`).
- **State:** uncommitted CW-1 working tree on base/parent `e723c509f65fc82abbeafba625071207425892e0`; no push, pull request, deployment, live database, live data, customer action, or other outward action occurred.
- **Implementation:** Weather retains the original **Open blank spray record** path and adds the separate **Start spray record with this weather** action only while the forecast is actionably fresh. The new versioned route contract copies and freezes exactly field UUID, forecast-local date, temperature, wind speed, and one eight-point compass direction. Inventory validates the route state, validates the field against the current farm workspace, opens the existing Spray record form, and leaves every value editable. Crop, acres, time, pest, applicator, license, humidity, and product facts remain blank/unprefilled. No migration, provenance record, provider link, background write, or new repository save path was added.
- **Explicit-save boundary:** route creation, navigation, form initialization, edits, abandonment, and stale-weather rendering contain no repository/database write. Only the existing **Save spray record** path can persist an application and perform its existing completed-application Inventory draw-down. Successful confirmed or queued saves replace the browser history entry with null route state so reload cannot reapply an accepted handoff. Failed or needs-attention saves retain the farmer's values for review.
- **Pre-commit Sol review findings:** a bounded read-only Sol High review found four P2 issues and no P1 issue: consumed prefill lived only in React state and could return on reload; forecast freshness was not re-evaluated when an open page crossed the two-hour ceiling; same-path route-state changes could reuse old controlled/uncontrolled form values; and the parser accepted numbers beyond the destination database bounds.
- **Repairs:** successful/queued consumption now uses history `replace`; Weather schedules an expiry re-render and rechecks freshness in the click handler; the Spray form remounts on each route location key; and route plus form bounds match the database contract (`-100..160` °F, `0..250` mph). Focused malformed-state regressions cover exact-key rejection, invalid UUID/date/direction, non-finite numbers, out-of-range values, and the payload-free manual intent.
- **Changed-file allowlist at this state:** `src/InventoryModule.tsx`; `src/WeatherModule.tsx`; `src/data/weatherNoWrite.regression.ts`; `src/data/weatherSprayHandoff.regression.ts`; `src/data/weatherSprayHandoff.ts`; `tests/e2e/season/cedar-creek.spec.ts`; `playwright.connect-workflows-cw1.config.ts`; `scripts/verify-connect-workflows-cw1-disposable.ps1`; `tests/season/connect-workflows-cw1.verify.sql`; and this ledger. No Grain, Harvest, bin, contract, delivery, migration, package, lockfile, fixture, repository, or live-system file changed.
- **Current gate:** **IMPLEMENTED; VERIFICATION IN PROGRESS.**

## CW-003 — CW-1 focused proof and repository ladder status

- **Date/time:** 2026-08-10 19:32:41 -05:00 (`America/Chicago`).
- **Focused source proof:** `npx tsx src/data/weatherSprayHandoff.regression.ts`, `npx tsx src/data/weatherNoWrite.regression.ts`, and `npx tsc -b --force --pretty false` each exited `0`. The full `npm run regression` later exited `0` and included both CW-1 focused regressions plus the repository post-regression chain.
- **Focused disposable/browser proof:** `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-connect-workflows-cw1-disposable.ps1` completed from repository migrations and exited `0`. Exact markers were `CONNECT_WORKFLOWS_CW1_PLAYWRIGHT_EXIT:desktop:0`, `CONNECT_WORKFLOWS_CW1_PLAYWRIGHT_EXIT:phone:0`, and `CONNECT_WORKFLOWS_CW1_DISPOSABLE_PASS`. Browser sizes were `1440x900` and `390x844`; each ran the isolated `@connect-workflows-cw1` test only. The existing `@cedar-creek` default selection remains separate.
- **Expected write proof:** after the one explicit farmer save, exact SQL proved one Cedar application record and one application product with edited accepted weather `75` °F, `9` mph, `W`, `40` acres, and `5` gal of the known synthetic product; target Inventory moved exactly `20` to `15` gal. The route and form caused no write before Save.
- **Expected non-write proof:** a normalized full public-table snapshot proved zero unrelated write. Named assertions included application/product rows outside the deterministic target, Inventory outside the target product and target Inventory metadata, notifications, fields/location, Programs, tasks, scouting, repository receipts outside the authorized operation, Grain, Harvest, bins, contracts, and deliveries. Abandoning the blank path and reviewing/editing the prefill produced zero target RPC. Reload after success did not restore the prefill. Stale Weather removed the prefill action. The denied-actor lane passed. Browser traffic remained loopback-only.
- **Focused failed-attempt accounting:** one local Supabase reset failed before migrations due the known Realtime duplicate synthetic-tenant startup race and received no proof credit. A first browser attempt exposed PowerShell stderr capture as a terminating harness error; stage-only, credential-free diagnostics and actual exit-code handling repaired the harness. The next browser attempt correctly rejected an inconsistent synthetic rate (`qt/acre` paired with a gallon total); the fixture input was corrected to its real `gal/acre` label unit. The following attempt saved successfully but the proof looked for an unmounted Spray form on Inventory's default post-reload shelf; the proof now explicitly reopens the Spray tab before asserting blank history state. Only the final clean two-viewport run above receives proof credit.
- **Repository ladder passed:** `npx tsc -b --force` exit `0`; `npm run regression` exit `0`; `npm run build` exit `0`; `npm audit --audit-level=high` exit `0` with zero vulnerabilities; `npm run verify:season` exit `0` with 101 fixtures, six scenarios, nine expected contract rejections, and fourteen expected isolation rejections; `CI=1 npm run verify:foundation` exit `0` with 25/25 controlled mutations detected, disposable migration/provenance/RLS probes passed, and Playwright `62 passed`, `14` intentional skips.
- **All-scenario ladder status:** the first continuous Maple Ridge attempt passed January through March, then an April login received HTTP `500` because local Auth briefly could not connect to the restarting disposable database; it received no full-scenario credit. An immediate retry refused an unhealthy stack. The parent command left one exact proof-owned runner continuing in the background; root identified and terminated only that process tree, then used the repository retained-journal recovery lane. The runner had reached the governed August swap. Exact `maple-august` recovery ultimately emitted `MAPLE_AUGUST_RETAINED_RECOVERY_PASS`; final inspection showed the original database healthy with `unless-stopped`, zero parked containers, zero clock images, zero journals, and zero proof-owned processes. A subsequent Maple retry was interrupted because a separate Harvest Ridge proof began using the same shared disposable backend; no Maple credit is claimed for that attempt. Root will not run shared-backend scenarios in parallel.
- **Current gate:** **FULL SCENARIO LADDER PENDING SHARED-BACKEND AVAILABILITY.** No commit has been created.

## CW-004 — Shared disposable-backend coordination and prerequisite harness findings

- **Date/time:** 2026-08-11 01:01:23 -05:00 (`America/Chicago`).
- **CW process boundary:** CW started no disposable scenario after the shared-backend lock was established. The CW working tree and product implementation remained unchanged while Soil Rx owned the stack. No live service, live database, customer data, secret, deployment, push, pull request, merge, or destructive action occurred.
- **Soil Rx handoff:** Soil's final Pine Hill run emitted `PINE_HILL_2027_DISPOSABLE_PASS` after desktop canonical, phone canonical, corrupt-active, corrupt-vault, and exact SQL proof. Its release audit reported zero scenario processes, governed listeners, clock journals, clock images, and parked containers; the ordinary local database was healthy on the pinned PostgreSQL image with `unless-stopped` and accepted connections. Soil preserved stale journal evidence in its local recovery quarantine and removed only ownership-attested unused Pine images.
- **Uncredited cross-task collision:** while Soil Pine was paused with an owned recovery journal, read-only process inspection found PIDs `40104` and `58796` running Maple scripts from `C:\Users\mason\.codex\worktrees\farmrx-v2-backlog`, not from CW. CW did not stop them. The owning V2 task fenced its exact aggregate parent, allowed that Maple tree to exit, launched no next farm lane, preserved Soil's artifacts, and reported zero scenario/tracked processes, parked containers, and governed listeners plus a healthy ordinary database. Its Maple attempt passed through July, then correctly failed closed on the deliberately preserved Soil residue; CW claims no proof credit from that run.
- **Queue:** after Soil's clean release, V2 received one strictly bounded exact-SHA aggregate window. It may not start a second aggregate or repair rerun; on failure it must use governed recovery and release. CW remains backend-paused until V2 explicitly reports clean topology. This queue does not transfer any proof credit to CW.
- **Generic harness evidence from Soil:** continuous Maple passed January through December after removing `.GetNewClosure()` from three Maple frozen-action callbacks. Standalone Harvest Ridge passed canonical and reverse-order proof after authenticated farm-read readiness plus an exact journal-gated, one-retry stale-gateway recovery contract. Pine Hill passed after pre-reset residue refusal, authenticated readiness, and the same one-retry recovery contract for PH2/PH4.
- **Hard-fence adjudication:** the proven Harvest prerequisite changes `scripts/verify-harvest-ridge-disposable.ps1`, a Harvest-owned verification surface. CW has not copied, edited, staged, or run that repair. The owner fence prohibits CW from modifying the Harvest module or bin/contract/delivery code and requires returning to Mason if the tranche needs such a touch. Root will exhaust read-only/externally landed alternatives before requesting a decision; no protected repair will enter the CW tranche silently.
- **Current gate:** **FULL SCENARIO LADDER PAUSED FOR THE BOUNDED V2 WINDOW; HARVEST-HARNESS FENCE UNRESOLVED.** No commit has been created.

## CW-005 — Correction: read-only Soil query overlapped the V2 ownership window

- **Date/time:** 2026-08-11 01:24:54 -05:00 (`America/Chicago`).
- **Correction to CW-004 release narrative:** after Soil's final clean release and after V2 acquired its bounded backend window, Soil's read-only wrap audit inadvertently completed one `supabase migration list --local` query against the ordinary local backend. It read only the local migration ledger. Soil reported no write, reset, restart, listener, container, journal, clock image, topology change, or scenario command, and committed to make no further backend call.
- **Adjudication:** root immediately disclosed the overlap to the V2 owner. V2 owns the decision whether its exact-SHA aggregate remains creditable or must fail closed under its ownership contract. CW made zero backend calls and claims no evidence from the overlapping V2 run.
- **Current gate:** unchanged: **CW BACKEND WORK PAUSED; V2 AGGREGATE AND HARVEST-HARNESS FENCE PENDING.**

## CW-006 — CW full scenario attempt, clean recovery, and hard-fence decision gate

- **Date/time:** 2026-08-11 03:35:29 -05:00 (`America/Chicago`).
- **Ownership preflight:** after V2's bounded window failed closed at Cedar and released, root independently proved zero Farm Rx scenario processes, clock journals, clock images, parked containers, and governed listeners plus one healthy ordinary local database on `public.ecr.aws/supabase/postgres:17.6.1.134`; `pg_isready` accepted connections. CW then owned the backend exclusively. Soil and V2 launched no scenario during the CW run.
- **PASS — Continuous Maple:** `scripts/verify-maple-august-december-disposable.ps1` exited `0` in 1,143.7 seconds. Exact markers covered January through June, governed July, governed August through December, and `MAPLE_2027_AUGUST_DECEMBER_DISPOSABLE_PASS`. Post-run topology was clean before the next packet.
- **PASS — North Fork:** `scripts/verify-north-fork-disposable.ps1` exited `0` in 1,080.6 seconds. All eight desktop lanes, exact SQL marker, all eight phone lanes, second exact SQL marker, clean recovery, and `NORTH_FORK_2027_DISPOSABLE_PASS` passed. Post-run topology was clean before the next packet.
- **PASS — Prairie Spray:** `scripts/verify-prairie-spray-disposable.ps1` exited `0` in 53.5 seconds. Desktop explicit-save browser proof, phone read-only/overflow proof, exact database assertions, and `PRAIRIE_SPRAY_2027_DISPOSABLE_PASS` passed. Post-run topology was clean before the next packet.
- **FAIL CLOSED — Harvest Ridge:** unmodified `scripts/verify-harvest-ridge-disposable.ps1` exited `1` in 312.1 seconds. HR1 browser proof and `HARVEST_RIDGE_CLOCK_PHASE_PASS hr1` passed; HR2 browser phase then failed before its required marker. The wrapper restored the ordinary database and left zero scenario processes, journals, clock images, parked containers, or Farm Rx governed listeners. CW claims no Harvest credit, did not retry, and did not edit the protected Harvest proof surface.
- **FAIL CLOSED — Cedar Creek:** unmodified `scripts/verify-cedar-creek-disposable.ps1` emitted `CEDAR_CREEK_2027_STATIC_CONTRACT_PASS`, then exited `1` in 157.8 seconds before the required runtime marker. This independently reproduced the exact Cedar failure preserved by V2. The wrapper restored a clean ordinary topology. CW claims no Cedar credit and did not retry or repair the harness.
- **PASS — Pine Hill:** `scripts/verify-pine-hill-disposable.ps1` exited `0` in 1,725.4 seconds. Desktop canonical, phone canonical, corrupt-active, corrupt-vault, exact SQL markers, and `PINE_HILL_2027_DISPOSABLE_PASS` passed. Final inspection showed zero Farm Rx scenario processes, clock journals, clock images, parked containers, and Farm Rx-owned governed listeners; the ordinary database was healthy with `unless-stopped` and accepted connections. One listener on port `4174` belongs to a separate `C:\Users\mason\.codex\worktrees\0ba1\CRX_Manager` Vite process; CW did not stop or alter it and does not classify it as Farm Rx residue.
- **Scenario verdict:** **4 PASS / 2 FAIL.** The runbook requires all six. CW-1 cannot be committed or sent for exact-SHA acceptance while Harvest and Cedar remain red.
- **Immutable prerequisite provenance:** Soil's proven product/harness commit is `9e803159ff750e3c7fcce7c636fc5b0270e48eb7`. V2 later isolated the accepted shared-harness repair in clean commit `41e2fe3aad982008a5cb16ba54774cd13acee12b` (parent failed commit `0434d80f7c455d8fc6404bbf07e6ed7a1741f092`) and reported `SEASON_SHARED_HARNESS_REPAIR_REGRESSION_PASS` plus the 101-fixture closed contract pass. That repair includes Cedar exact-marker SQL reconciliation, Maple callback scope, Harvest authenticated readiness and exact one-retry journal-gated gateway recovery, Pine readiness/recovery, and a static guard.
- **Hard fence:** the isolated prerequisite changes `scripts/verify-harvest-ridge-disposable.ps1`. CW has not cherry-picked it because the owner directive forbids CW from modifying Harvest-owned proof/code and requires returning to Mason when the tranche needs that touch. No proof-safe alternative remains: the unmodified protected harness was run once under clean exclusive ownership and failed.
- **Final scope/credential state:** exactly ten CW allowlisted files are changed/untracked; zero changed path matches Grain/Harvest/bin/contract/delivery; zero file is outside the allowlist; credential-like scan count `0`; `.env*` changed-file count `0`; `git diff --check` exit `0`; HEAD remains base `e723c509f65fc82abbeafba625071207425892e0`. No commit exists.
- **Current gate:** **NEEDS MASON — authorize one separate local shared-harness prerequisite commit from exact repair `41e2fe3aad982008a5cb16ba54774cd13acee12b`, explicitly waiving the Harvest-proof fence for that prerequisite only, or direct CW to wait until the repair lands on `origin/main`.** No product, migration, live, push, PR, merge, or deployment action is requested.

## CW-007 — Correction: Harvest passed unmodified and Cedar's pre-browser harness failure was repaired

- **Date/time:** 2026-08-11 05:27:17 -05:00 (`America/Chicago`).
- **Correction to CW-006 Harvest gate:** after V2's final clean release, root independently re-proved clean topology and ran the original, unmodified `scripts/verify-harvest-ridge-disposable.ps1` exactly once. It exited `0` in 1,216.2 seconds. All canonical HR1-HR5, extension-bin, extension-in, extension-contract, phone, exact SQL, reverse HR5/HR4, clock-phase, and final `HARVEST_RIDGE_2027_DISPOSABLE_PASS` markers passed. The earlier HR2 failure was environmental. No Harvest/Grain/bin/contract/delivery file was changed, no fence exception is required, and the Mason decision requested in CW-006 is withdrawn.
- **Read-only overlap disclosure:** while CW owned the backend and Harvest was running, V2 mistakenly invoked `scripts/harvest-ridge-db-clock.regression.ps1` after classifying it as offline. V2 reported that it performed only read-only `docker inspect` calls against the ordinary database/rest container names, then failed its expected-topology assertion and stopped. It did not reset, restart, write, launch a browser/scenario, or create/remove a listener, container, journal, or image. Root adjudicated the overlap as non-mutating; it does not invalidate Harvest's write/non-write result or CW's exclusive mutation ownership. V2 committed to zero further backend/topology calls until CW releases.
- **Cedar primary failure captured:** a direct unmodified rerun emitted `CEDAR_CREEK_2027_STATIC_CONTRACT_PASS`, reset successfully, began the Playwright launch, then exited `1` in 113 seconds. The first native output was the routine npm notice for `npx`; under the wrapper's global `$ErrorActionPreference = 'Stop'`, merged native stderr became a terminating `NativeCommandError`/`ParentContainsErrorRecordException`. The clock adapter rethrew that primary exception at line 131 before the harness could print its captured Playwright output or exit marker. This explains CW's and both V2 Cedar failures; they were harness-output failures, not credited browser assertion failures.
- **Narrow Cedar repair:** only `scripts/verify-cedar-creek-disposable.ps1` changed. Around the native Playwright capture it saves the prior error-action preference, uses `Continue` so routine native stderr can be captured, and restores the prior strict preference in `finally`. The existing explicit nonzero-exit and failed-summary checks remain unchanged. The action also reads the existing verifier SQL before the clock phase and requires its exact `CEDAR_CREEK_2027_VERIFY_PASS` marker through direct local `psql`, retaining V2 repair provenance while avoiding function lookup across the frozen clock boundary. No product, fixture, migration, assertion, expected write/non-write, or protected module changed.
- **Cedar focused proof:** StaticOnly contract, PowerShell parse, and `git diff --check` passed. The repaired full disposable packet then exited `0` in 226.2 seconds: desktop Playwright `1 passed`, `CEDAR_CREEK_PLAYWRIGHT_EXIT:desktop:0`; phone Playwright `1 passed`, `CEDAR_CREEK_PLAYWRIGHT_EXIT:phone:0`; exact SQL accepted after each viewport; and `CEDAR_CREEK_2027_DISPOSABLE_PASS` emitted. Recovery left zero clock journals, clock images, parked containers, or governed listeners and a healthy ordinary database.
- **Current gate:** **FOCUSED HARVEST AND CEDAR BLOCKERS RESOLVED; COMPLETE SIX-SCENARIO RERUN PENDING.** No commit has been created.

## CW-008 — Complete-ladder attempt stopped on an operating-system Maple browser crash

- **Date/time:** 2026-08-11 05:38:06 -05:00 (`America/Chicago`).
- **Attempt:** root started the six scenario packets sequentially from the current CW bytes with fail-fast exit handling. Maple Ridge was first. Its credential and request-classifier regressions, start packet, January, February, and March passed. The April browser process then exited `-1073740791`; the nested Maple prerequisites propagated exit `1`, and the aggregate stopped after 544.7 seconds. No North Fork, Prairie Spray, Harvest Ridge, Cedar Creek, or Pine Hill lane started in this attempt, and no proof credit is claimed for the incomplete Maple run.
- **Classification:** Windows exit `-1073740791` is a native process crash rather than a Farm Rx browser assertion or database verifier result. The failure is preserved; no test, assertion, product code, fixture, or expected outcome was weakened.
- **Recovery proof:** after exit, root found zero scenario runners, Playwright/browser processes tied to the CW worktree, clock journals, clock images, parked containers, or governed listeners. The ordinary database was healthy with `unless-stopped`, used `public.ecr.aws/supabase/postgres:17.6.1.134`, and accepted connections. A single exact Maple retry from this clean topology is permitted before the remaining lanes continue.
- **Current gate:** **MAPLE CLEAN RETRY PENDING; COMPLETE SIX-SCENARIO LADDER STILL RED.** No commit has been created.

## CW-009 — Five final-byte scenario passes; Pine failed closed and recovered cleanly

- **Date/time:** 2026-08-11 07:03:12 -05:00 (`America/Chicago`).
- **PASS — Maple retry:** the exact retry exited `0` in 1,032.1 seconds with January through June, governed July, every August-December clock phase, and `MAPLE_2027_AUGUST_DECEMBER_DISPOSABLE_PASS`. The native April crash from CW-008 did not recur.
- **PASS — North Fork:** exited `0` in 1,267.9 seconds. All eight desktop lanes, exact SQL, all eight phone lanes, second exact SQL, and `NORTH_FORK_2027_DISPOSABLE_PASS` passed.
- **PASS — Prairie Spray:** exited `0` in 42.0 seconds. Both desktop/phone tests, exact SQL/non-write checks, and `PRAIRIE_SPRAY_2027_DISPOSABLE_PASS` passed.
- **PASS — Harvest Ridge:** the unmodified protected packet exited `0` in 1,284.7 seconds with all canonical, extension, phone, exact SQL, reverse-order, clock-phase, and `HARVEST_RIDGE_2027_DISPOSABLE_PASS` markers.
- **PASS — Cedar Creek:** the repaired packet exited `0` in 267.3 seconds. Desktop and phone each passed with exact `CEDAR_CREEK_PLAYWRIGHT_EXIT:<viewport>:0` markers, exact SQL accepted, and `CEDAR_CREEK_2027_DISPOSABLE_PASS` emitted.
- **FAIL CLOSED — Pine Hill:** Pine desktop canonical and exact SQL passed. Phone init, PH1, PH2, and PH3 passed, but the phone PH4 recovery left one exact journal and the two owned clock images; final `Assert-PineNoClockResidue` threw `PINE_HILL_CLOCK_RECOVERY_INCOMPLETE`. Pine exited `1` in 824.5 seconds, corrupt-active/corrupt-vault did not run, and no Pine final marker or proof credit is claimed from this attempt.
- **Governed recovery:** root identified only the exact `phone-ph4` journal and its matching snapshot/derived images; the ordinary database was already canonical/healthy and no parked/replacement container or listener remained. The module's exact `-ResumeRecovery` path first refused final authenticated farm-read readiness, then removed the exact owned journal/images while retaining the ordinary database. Because the one-time synthetic child credential had been erased, root rotated only the disposable local Pine owner password through the loopback local Auth admin API and used a fresh synthetic token; no key/password/token was printed or persisted. A read-only loopback farm query then returned HTTP `200` with exactly Pine Hill. A final audit proved zero journals, clock images, parked containers, listeners, or scenario processes; exact ordinary database id `ec219dae3d40721aab2e5977d0b97ac094eb1e23f0c407910a06f06474a40f37`, pinned image id, `unless-stopped`, healthy, and `pg_isready` accepting.
- **Current gate:** **FIVE SCENARIOS PASS; ONE CLEAN PINE RETRY PENDING.** No commit has been created.

## CW-010 — CW-1 complete local verification ladder passed; commit gate ready

- **Date/time:** 2026-08-11 07:47:57 -05:00 (`America/Chicago`).
- **PASS — Pine clean retry:** `scripts/verify-pine-hill-disposable.ps1` exited `0` in 1,803.8 seconds. Desktop canonical, phone canonical, corrupt-active, corrupt-vault, exact SQL, all governed clock/recovery phases, and `PINE_HILL_2027_DISPOSABLE_PASS` passed. Post-run topology was clean.
- **Complete scenario verdict:** **6 PASS / 0 FAIL on the final CW bytes.** Credited exact passes are Maple Ridge from the clean retry in CW-009; North Fork, Prairie Spray, Harvest Ridge, and repaired Cedar Creek from CW-009; and Pine Hill from this entry. Every lane ran sequentially against the disposable local backend. Failed/incomplete attempts remain recorded in CW-003, CW-006, CW-008, and CW-009 and receive no credit.
- **Final focused/source gates:** Weather handoff regression exit `0`; Weather no-write regression exit `0`; Cedar StaticOnly contract exit `0`; `npx tsc -b --force` exit `0`; `npm run regression` exit `0`; `npm run build` exit `0`; `npm audit --audit-level=high` exit `0` with zero vulnerabilities; and `npm run verify:season` exit `0` with 101 fixtures, nine expected contract rejections, and fourteen expected isolation rejections.
- **Final foundation gate:** `CI=1 npm run verify:foundation` exited `0` in 595 seconds. Static guards passed; all 25/25 controlled mutations turned the gate red; disposable migration, provenance, service-log, advisor, and RLS probes passed; production build and zero-vulnerability audit passed; Playwright reported `62 passed` and `14` intentional skips; final marker was `Farm Rx foundation gate: PASS`.
- **Final backend release audit:** zero Farm Rx scenario processes, clock journals, clock images, parked/replacement containers, or governed listeners. The ordinary local database is healthy, uses the pinned `public.ecr.aws/supabase/postgres:17.6.1.134` image, has restart policy `unless-stopped`, and `pg_isready` accepts connections. No live Supabase project or customer data was accessed.
- **Final scope:** eleven exact CW-1 files: `src/WeatherModule.tsx`; `src/InventoryModule.tsx`; `src/data/weatherSprayHandoff.ts`; its two focused regressions; `tests/e2e/season/cedar-creek.spec.ts`; `playwright.connect-workflows-cw1.config.ts`; `scripts/verify-connect-workflows-cw1-disposable.ps1`; `tests/season/connect-workflows-cw1.verify.sql`; the narrow `scripts/verify-cedar-creek-disposable.ps1` native-capture repair; and this ledger. No Grain, Harvest, bin, contract, delivery, migration, package, lockfile, fixture, repository, live-system, `.env`, or credential file changed. `git diff --check` passed.
- **Expected write/non-write verdict:** only the existing explicit farmer Save path wrote the exact application/product and drew target Inventory from `20` to `15` gal. Weather open, manual blank open, prefill navigation, review/edit, abandon, stale Weather, reload after accepted save, and denied actor paths wrote nothing. Named unrelated tables and rows—including Inventory outside the target and all protected Grain/Harvest/bin/contract/delivery surfaces—were unchanged.
- **Current gate:** **READY FOR ONE IMMUTABLE LOCAL CW-1 COMMIT AND FRESH-CONTEXT EXACT-SHA SOL REVIEW.** No push, PR, merge, deploy, live action, or CW-2 implementation is authorized by this gate.

## CW-011 — Exact-SHA CW-1 review BLOCK and approved bounded repair

- **Date/time:** 2026-08-11 08:16:31 -05:00 (`America/Chicago`).
- **Reviewed commit:** fresh-context, read-only Sol review examined exact commit `43292d013a650b1a6c3fc05fcf2adcf5a7bf8ecd`, parent `e723c509f65fc82abbeafba625071207425892e0`, on `codex/farmrx-connect-workflows`. The reviewer independently verified the exact branch, parent, eleven-file diff, hashes, hard fences, credential/scope results, and recorded proof. The reviewer task exposed the Sol role but did not expose a configured effort label, so no effort is inferred. No file, ref, service, or database state was changed by review.
- **Verdict:** **BLOCK** with two P2 findings and no P1 or P3 finding. `CW1-R01` found that the Weather-prefilled form implicitly selected the first Inventory product even though product facts were required to remain blank; the browser proof also saved that implicit selection. `CW1-R02` found that exported `SprayForm` unconditionally required React Router context, causing `npx tsx src/data/inventorySpraySaveReceipt.regression.ts` to exit `1`, while that focused regression was absent from `npm run regression` and therefore allowed a false-green full regression result.
- **Authority and immutability:** Mason explicitly approved the recommended local repair. Commit `43292d013a650b1a6c3fc05fcf2adcf5a7bf8ecd` remains immutable and was not amended. The repair must be a new local commit and receive its own fresh-context exact-SHA review. No push, pull-request mutation, merge, deploy, live database/data, secret/auth/permission, customer, destructive, or hard-fenced action occurred.
- **Bounded repair:** the router-aware `InventoryPage` now owns location/navigation and keys `SprayForm` by the route entry; the reusable form no longer calls router hooks. A valid Weather prefill initializes Product to an explicit required blank **Choose product** value, while the existing manual path retains its prior first-product default. CW browser proof asserts blank Product, explicitly selects `Synthetic Cedar Herbicide 41`, and then exercises the same explicit-save write/non-write matrix. The focused receipt regression remains router-independent and is now mandatory inside `npm run regression`.
- **Repair allowlist at this state:** `src/InventoryModule.tsx`; `src/data/inventorySpraySaveReceipt.regression.ts`; `src/data/weatherSprayHandoff.regression.ts`; `tests/e2e/season/cedar-creek.spec.ts`; `package.json`; and this append-only ledger. No Grain, Harvest, bin, contract, delivery, migration, fixture, lockfile, environment, credential, or live-system file changed.
- **Focused/offline proof:** `npx tsx src/data/inventorySpraySaveReceipt.regression.ts`, `npx tsx src/data/weatherSprayHandoff.regression.ts`, and `npx tsx src/data/weatherNoWrite.regression.ts` each exited `0`. `npx tsc -b --force` exited `0`. `npm run regression` exited `0` and visibly included `Inventory spray save receipt regression passed`. `npm run build` exited `0`; `npm audit --audit-level=high` exited `0` with zero vulnerabilities; and contract-only `npm run verify:season` exited `0` with 101 fixtures, nine expected contract rejections, and fourteen expected isolation rejections.
- **Shared-backend boundary:** CW cleanly released the disposable backend to Farm Rx V2 before this repair. No Docker, Supabase, topology, disposable-backend, foundation, or browser command was run during V2's exclusive window. Focused disposable database/browser proof, foundation proof, and the six-scenario ladder remain uncredited for the repaired bytes until V2 explicitly clean-releases.
- **Current gate:** **REPAIR IMPLEMENTED; BACKEND-DEPENDENT VERIFICATION PAUSED FOR V2'S EXCLUSIVE WINDOW.** No repair commit has been created.

## CW-012 — CW-1 repair verification passed after governed outage recovery

- **Date/time:** 2026-08-11 10:00:38 -05:00 (`America/Chicago`).
- **Power-loss and ownership disclosure:** the workstation lost power while Farm Rx V2 owned one authorized exact-SHA aggregate at `01f98a9ae9a4208c17a1c4002e91f21f7ec5875b`. V2 found no durable aggregate evidence and claims no pass/fail credit. Its retained `farmrx-harvest-ridge-clock-maple-december.json` showed interruption at `stop_original`; repository-supported adapter recovery restored the exact ordinary database, its `unless-stopped` policy, local API readiness, authenticated synthetic Maple route, and removed the owned journal last. V2 then reported zero scenario processes, journals, clock images, parked/replacement containers, and governed listeners plus a healthy ordinary database and explicitly released the backend. CW independently re-attested the same clean facts before taking exclusive ownership. The V2 interruption is an environmental no-credit event, not a CW product result.
- **Focused repaired-byte proof:** `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-connect-workflows-cw1-disposable.ps1` exited `0` in `301.4` seconds. Exact markers included `CONNECT_WORKFLOWS_CW1_STATIC_CONTRACT_PASS`, desktop Playwright `1 passed` and `CONNECT_WORKFLOWS_CW1_PLAYWRIGHT_EXIT:desktop:0`, phone Playwright `1 passed` and `CONNECT_WORKFLOWS_CW1_PLAYWRIGHT_EXIT:phone:0`, per-viewport `no-unexpected-writes` and `focused-sql` stages, and `CONNECT_WORKFLOWS_CW1_DISPOSABLE_PASS`.
- **CW-1 write/non-write result:** both representative viewports proved the Weather-prefilled Product begins blank, the farmer explicitly selects `Synthetic Cedar Herbicide 41`, and only the existing explicit **Save spray record** action writes the exact application/product and changes target Inventory from `20` to `15` gal. Weather open, blank-record handoff, prefill navigation, review/edit/abandon, stale Weather, accepted-save reload, and denied actor paths wrote nothing. The normalized public-table snapshot proved zero unrelated write, including Inventory outside the target and every protected Grain, Harvest, bin, contract, and delivery surface.
- **Foundation proof:** `CI=1 npm run verify:foundation` exited `0` in `417.5` seconds. The intermediate-failure probe and static guards passed; all `25/25` controlled mutations turned the gate red; forced TypeScript, full regression (including `Inventory spray save receipt regression passed`), build, zero-vulnerability high audit, disposable migration/provenance/advisor/RLS probes, and browser proof passed. Playwright reported `62 passed`, `14` intentional skips; final marker was `Farm Rx foundation gate: PASS`.
- **PASS — Maple Ridge (1/6):** the continuous exact January-through-December packet exited `0` in `660.8` seconds. Start, January through June, governed July, every August-December clock phase, and `MAPLE_2027_AUGUST_DECEMBER_DISPOSABLE_PASS` passed. No V2 Maple result was carried.
- **North Fork failed-attempt accounting:** the first North Fork attempt reset successfully, then exited `1` in `47.1` seconds at the wrapper's first retained exception `North Fork browser phase failed: nf1` before any `nf1` marker. No new Playwright report or `test-results` artifact existed; the only HTML report was the older successful foundation report. The repair diff changes no North Fork source, configuration, fixture, or test, and post-failure proof showed zero processes/journals/images/parked containers/listeners plus a healthy ordinary database. The event was classified as transient browser/web-server bootstrap, preserved as no-credit, and the supervising orchestrator authorized exactly one unchanged full retry.
- **PASS — North Fork (2/6):** the single unchanged clean retry exited `0` in `891.6` seconds. Desktop `nf1`-`nf8`, exact `NORTH_FORK_2027_VERIFY_PASS`, phone `nf1`-`nf8`, a second exact SQL marker, clean recovery, and `NORTH_FORK_2027_DISPOSABLE_PASS` passed. No source or harness edit was made.
- **PASS — Prairie Spray (3/6):** exited `0` in `44.2` seconds. Desktop explicit-save, phone compliance/read-only/no-overflow, exact database assertions, and `PRAIRIE_SPRAY_2027_DISPOSABLE_PASS` passed.
- **PASS — Harvest Ridge (4/6):** the unmodified hard-fenced packet exited `0` in `903.3` seconds. Canonical HR1-HR5, extension-bin/in/contract, phone, exact SQL, reverse HR5/HR4, reverse exact SQL, all clock phases, and `HARVEST_RIDGE_2027_DISPOSABLE_PASS` passed. No Harvest, Grain, bin, contract, or delivery file changed.
- **PASS — Cedar Creek (5/6):** exited `0` in `213.3` seconds. Static contract, desktop and phone Playwright exit markers, exact SQL, and `CEDAR_CREEK_2027_DISPOSABLE_PASS` passed.
- **PASS — Pine Hill (6/6):** exited `0` in `770.2` seconds. Desktop and phone canonical weak-signal/reconnect/revocation/recovery flows, two exact `PINE_HILL_2027_VERIFY_PASS` results, corrupt-active, corrupt-vault, and `PINE_HILL_2027_DISPOSABLE_PASS` passed.
- **Complete repaired-byte scenario verdict:** **6 PASS / 0 FAIL.** Every credited scenario ran sequentially under CW's exclusive disposable-backend ownership. Failed or interrupted attempts above receive no proof credit and remain explicit.
- **Fixture/migration identity:** `tests/season/season-2027.manifest.json` SHA-256 `dbecd5ffb5c6d9a155ee296ca1b96fbe95423ad91ea104b0fd8ef73c0de5259c`; 48 ordered migration files through `20260725213142_pine_hill_removed_farm_epoch.sql`; ordered `name|file-sha256` migration-set SHA-256 `3acc43c65c3df71bc30eeaa4cb30f381feb6b59fb710d46c23c52216928e0d69`.
- **Clean backend release:** after Pine, CW independently proved scenario processes `0`, clock journals `0`, clock images `0`, parked/replacement containers `0`, and governed listeners across `4174`-`4187` plus `4274`-`4280` equal `0`. Ordinary database exact id `0c770fdb6b184f0643a891c0351d0db1a95650d4dd3c8d38586ee27333a5e05a` was running healthy with restart `unless-stopped`, pinned image `public.ecr.aws/supabase/postgres:17.6.1.134`, and `pg_isready` accepting. CW sent this evidence-backed release to Soil Rx and the supervising orchestrator and will perform zero further backend/topology calls while Soil owns it.
- **Current gate:** **FULL REPAIR PROOF PASSED; FINAL OFFLINE SCOPE/CREDENTIAL/DIFF AUDIT, NEW LOCAL COMMIT, AND FRESH-CONTEXT EXACT-SHA SOL REVIEW REMAIN.** No push, pull-request mutation, merge, deploy, live database/data, secret/auth/permission, customer, destructive, or hard-fenced action occurred.
