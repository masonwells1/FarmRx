# Initiative GL — Grain Live ledger

This ledger is append-only. Never edit, reorder, or delete an earlier entry. If an entry is wrong, append a correction that cites it. Times use `America/Chicago`.

## GL-000 — Initiative context and GL-1 plan

- **Date/time:** 2026-09-15 09:45 -05:00 (`America/Chicago`).
- **Authority:** the `Initiative GL — Grain Live` section of the 2026-09-05 owner amendment in `docs/GOAL.md` (GL-1 USDA MARS basis feed), decision 2 of that amendment (Barchart declined; the USDA AMS My Market News feed is public-domain government data and is authorized), and the `USDA AMS MARS basis feed` section of `docs/grain-live-design.md`. Mason approved planning and building GL-1 in the working session on 2026-09-15 ("Plan and build GL-1") after FD-2 merged into `main` as `396a9e8` (front-door ledger FD-032). Merge, live migration, secrets, and scheduling remain Mason's decisions.
- **Starting point:** branch `claude/app-improvement-strategy-kzqk6s` restarted from `origin/main` `396a9e8`. Today the only feed fence is the browser's `isMarsBid`, which recognizes exactly the `[USDA MARS 2850]` notes prefix; no SQL fence, no feed provenance column, no farm region, no edge function, and no workflow exist. The alert sweep (`run_scheduled_alert_sweep`, migration 0039) takes the newest cash bid for a commodity regardless of source; GL-2 owns that split and is not touched here.
- **Tier:** full (a migration with a new table, new columns, a unique constraint, a `SECURITY DEFINER` ingestion function, row-level policy changes on `cash_bids`, a new edge function, and scheduler secrets). Disposable-database assertions, fresh-context review of the exact commit (Codex reviews the pull request), and the role matrix apply.
- **What this sandbox cannot do (recorded, not assumed):** outbound requests to `marsapi.ams.usda.gov` and `mymarketnews.ams.usda.gov` are refused by the session's network policy (HTTP 403 from the proxy on 2026-09-15). The geography of report 2850 and the exact shape of its rows therefore cannot be verified from here. GL-1 is built so that nothing is written until both are verified: the report-to-geography mapping is a server-side configuration row with a `verified_at` stamp that is null on seed, the ingestion function refuses an unverified report, and the parser skips every row whose fields it does not recognize while recording the field names it saw. Verifying 2850 (or an Illinois/Indiana report) against the USDA listing and stamping it is a live-data action for Mason, recorded in this ledger when it happens.
- **Plan (one tranche, one commit):**
  1. **Migration `20260915150000_gl1_usda_mars_feed.sql`.**
     - `farms.market_region text` (null or a two-letter uppercase US state code, checked). No default. Nothing infers it from field states or addresses. Writable through the existing `farms_update` policy (`can_manage_farm`: owner or manager), so a worker or rep cannot set it.
     - `public.usda_market_reports`: the verified mapping from a USDA report id to the geography it covers (`report_id`, `name`, `geography` state code, `geography_label`, `verified_at`, `verification_note`). Read-only for signed-in users (so the Grain page can say which reports cover the farm's region and whether they are verified); written only by the service role. Seeded with report 2850 as "Iowa Daily Cash Grain Bids", geography `IA`, `verified_at` null, and a note saying what verification is owed.
     - `public.usda_market_report_runs`: the service-only run log (report, report date, started/finished, status `ok`/`skipped`/`failed`, fetched/written/unchanged/skipped counts, farms written, detail without secrets). A successful run for a report and market day lets the next run skip the fetch ("once per market day, cached server-side").
     - `cash_bids` gains `feed_source`, `feed_report_id`, `feed_geography`, `feed_observation_key`, all null on a farmer's row and all set on a feed row (checked together); a unique index over `(farm_id, feed_observation_key)` on feed rows. The `cash_bids_insert` and `cash_bids_update` policies gain `feed_source is null`, so a signed-in client can neither create a feed row nor turn one into a manual bid; the service role, which bypasses row-level security, writes feed rows only through the function below.
     - `public.ingest_usda_mars_observations(p_report_id, p_run_id, p_observations jsonb)`: `SECURITY DEFINER`, fixed `search_path`, executable by the service role only (revoked from public, anon, authenticated). Refuses an unknown or unverified report. Fans each validated observation into every farm whose `market_region` equals the report's geography (a farm without a region receives nothing) by transactional upsert keyed on `(farm_id, feed_observation_key)`; an unchanged row is left untouched (its `updated_at` does not move, so a retried run cannot change which row the sweep sees as newest). Rows carry the provenance note `[USDA MARS <report-id> · <geography label>]`. Returns counts.
  2. **Edge function `usda-mars-feed`** with the pure logic in `supabase/functions/_shared/marsFeedLogic.ts` and `marsFeedOrchestrator.ts` (regressions beside them, run by `npm run regression`). Same scheduler authorization as the alert sweep (`x-scheduler-key`), MARS API key from the `USDA_MARS_API_KEY` secret via HTTP Basic auth, never logged or returned. For each verified report: skip when today's market day already has an `ok` run; fetch with a deadline; parse with an explicit commodity whitelist (Corn → `corn_yellow`, Soybeans → `soybeans`, Wheat → `wheat`), finite numbers, plausible basis, a deterministic observation key (report id, USDA row identity, bid date, location, commodity, delivery window; hashed when long); skip and count anything else, recording the field names seen; call the ingestion function; write the run row. A failure writes a `failed` run and leaves history untouched; nothing is ever written under today's date from yesterday's rows.
  3. **Workflow `.github/workflows/usda-mars-feed.yml`**: weekday runs at 4:30 PM and 7:00 PM Chicago (both offsets, so two UTC hours each), `workflow_dispatch`, the same secret names pattern as the alert sweep plus the feed URL. The function is idempotent, so the extra offset hour is harmless.
  4. **Browser.** `isMarsBid` recognizes any MARS provenance (`feed_source = 'usda_mars'` or a `[USDA MARS …]` note), so no feed row from any report enters position, revenue, plan, alert-candidate, or Today math. `CashBid` carries the feed columns (read tolerantly, so cached rows still load). The Grain basis history names each feed row's report and geography ("USDA MARS 2850 · Iowa, display-only; last dated …") and keeps the "Basis feed unavailable — last updated …" wording. The farm settings page (today's "Farm privacy" page at `/privacy`, retitled "Farm settings" with the privacy card unchanged) gains a "Market region" card for owners and managers: a state picker with no default and a plain sentence naming the verified USDA reports that cover it or saying none is verified yet; saved through a new farm settings repository that mirrors the privacy toggle (online only, context verified before and after, the returned row confirmed). The Grain page says in plain words when the farm has no region.
  5. **Guards and proof.** Static guards pin the migration's fences (the revoke on the ingestion function, its fixed search path, the `feed_source is null` policy checks, the unique feed index) and the foundation lane's Deno check of the new function; the mutation drill gains mutations for those needles. Disposable-database assertions (`scripts/sql/gl1-mars-feed-assertions.sql`, run by both disposable lanes): an unverified report writes nothing; a verified report fans into two Iowa farms and not into an Illinois farm or a farm without a region; a retried identical run adds no row and moves no `updated_at`; a changed basis updates in place; a signed-in owner cannot insert a feed row or edit one; an owner can set a valid region and cannot set an invalid one; a worker cannot set it; a worker without financial access reads zero feed rows; a signed-in user cannot call the ingestion function. Browser journeys: the owner sets the region and reads the report sentence; the basis card labels a feed row; the worker without financial access still sees no grain row.
- **Deliberately out of scope:** GL-2's alert split and crop-year eligibility; any USDA report other than the seeded 2850 row; any change to contract, bin, delivery, or on-hand math; live enablement (applying the migration, setting `USDA_MARS_API_KEY` and the scheduler secrets, adding the workflow secrets, stamping a report verified).
- **Rollback path:** revert the GL-1 commit; the migration is additive (new table, new nullable columns, new function, policy predicates that only narrow client writes) and can be reversed by a follow-up migration that drops them. No stored row is changed by the migration itself.

## GL-001 — GL-1 USDA MARS basis feed: build and proof

- **Date/time:** 2026-09-15 10:30 -05:00 (`America/Chicago`).
- **WHERE:** branch `claude/app-improvement-strategy-kzqk6s` on `origin/main` `396a9e8`. This entry is committed together with the GL-1 code and the front-door ledger's FD-032 merge record.
- **What changed:**
  - New migration `supabase/migrations/20260915150000_gl1_usda_mars_feed.sql`: `farms.market_region` (checked state code, no default); `usda_market_reports` (signed-in read, service-only write; 2850 seeded as Iowa with `verified_at` null); `usda_market_report_runs` (service-only run log); `cash_bids.feed_source / feed_report_id / feed_geography / feed_observation_key` set together, unique per farm and observation; `cash_bids_insert`, `cash_bids_update`, `cash_bids_delete` narrowed with `feed_source is null`; `ingest_usda_mars_observations(text, uuid, jsonb)` (`SECURITY DEFINER`, fixed search path, service role only) refusing unknown or unverified reports, validating every observation, fanning into region-matched farms by upsert, leaving unchanged rows untouched.
  - New edge function `supabase/functions/usda-mars-feed/index.ts` with pure `_shared/marsFeedLogic.ts` (parser: results wrapper or bare array, explicit commodity whitelist, finite and plausible numbers, stated ranges kept as midpoints with the range in the note, deterministic bounded observation keys, every skip counted with a reason, the field names seen reported) and `_shared/marsFeedOrchestrator.ts` (verified reports only, skip when the market day already has an `ok` run, fetch deadline, run rows begun and finished, failures recorded without URLs or credentials). Both have regressions, added to `npm run regression`.
  - New workflow `.github/workflows/usda-mars-feed.yml` (weekday 4:30 PM and 7:00 PM Chicago, both UTC offsets, `workflow_dispatch`; secrets `FARM_RX_MARS_FEED_URL`, `FARM_RX_SCHEDULER_SECRET`, `FARM_RX_SUPABASE_ANON_KEY`).
  - Browser: `isMarsBid` recognizes any MARS provenance (column or note); `marsBidLabel` names the report and geography; `CashBid` carries the feed columns (read tolerantly); `GrainData.usda_market_reports` read by the gateway (absent table before the migration is applied live reads as none); the manual save never sends feed columns and the grain write queue accepts them only as null; the basis history card names each feed row's report and geography over the commodity's rows and says in plain words when the farm has no region or no verified report; `Farm.market_region` read tolerantly.
  - Farm settings: the `/privacy` page is retitled "Farm settings" (nav label "Farm settings", route unchanged) and gains a "Market region" card for owners and managers with a state picker and no default, saved through the new `SupabaseFarmSettingsRepository` (validation against the same state list, online only, context verified before and after, returned row confirmed) with its own regression.
  - Guards: static guards pin the fan-out's revoke and grant, its single fixed search path, the unverified-report refusal, the region match, both client fences, the unique feed index, the unchanged-row rule, the service-only run log, the browser fence, the manual save's column list, the workflow's scheduler header, the function's authorization, and the Deno lane; the mutation drill gains six mutations (190 total); the foundation orchestrator gains the frozen Deno check of `usda-mars-feed`; the lane count pin moves from 27 to 28 and the offline-grant lane count from 13 to 14 (the new farm settings lane uses the exact grant).
  - Disposable database: `scripts/sql/gl1-mars-feed-assertions.sql`, run by both disposable lanes after the FD assertions.
  - e2e: the mock serves the `usda_market_reports` read and the farms update fenced on `updated_at`; the owner fixture's feed row carries its provenance; the owner Today journey opens Grain's storage tab and reads the feed label and the no-region sentence; two new journeys set the region as owner and read it as a worker.
- **Proof observed:** `npx tsc -b --force` exit 0; `npx tsx` on `marsFeedLogic`, `marsFeedOrchestrator`, `SupabaseFarmSettingsRepository`, `SupabaseGrainRepository`, `today`, `marketingAlerts`, `MockGrainRepository`, `queuedOperationContext`, `GrainModule.receipt` and `TodayModule.sprayClock` regressions passed; the 61 PowerShell-free regression steps (`CHAIN_PASS`); `node scripts/foundation-static-guards.mjs` PASS; `node scripts/verify-foundation-mutations.mjs` 190/190; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean; `bash scripts/verify-fs-persist-disposable.sh` printed `FS_PERSIST_DISPOSABLE_PASS`, `FD_TODAY_DISPOSABLE_PASS`, `GL1_MARS_FEED_DISPOSABLE_PASS`; `deno check --no-config --lock=deno.lock --frozen --node-modules-dir=none supabase/functions/usda-mars-feed/index.ts` passed with Deno 2.9.4 obtained through npm in the session (one type error found and fixed before the first run); full local Playwright suite on the built shell, desktop and phone, 69 passed, 0 failed, retries 0.
- **Unverified here, by design:** the geography and row shape of USDA report 2850 (the network policy refuses the USDA hosts). The seed keeps 2850 unverified, the fan-out refuses it, and the parser records the field names it sees; the first live run with a verified report will show the true shape in the run log's `detail.columns` without writing anything it did not recognize.
- **Live steps that remain Mason's:** apply the migration; set `USDA_MARS_API_KEY` (and the existing `SCHEDULER_SECRET`) on the `usda-mars-feed` function; add the `FARM_RX_MARS_FEED_URL` repository secret; confirm report 2850's coverage on the USDA listing and stamp `verified_at` (recorded here when done); set each pilot farm's market region in Farm settings. Until a report is verified and a farm has a region, the feed writes no row.
- **Next:** push the branch, open a draft pull request, and stop at `READY FOR APPROVAL`. Merge is Mason's decision.

## GL-002 — Foundation repair: covering index for the feed report foreign key

- **Date/time:** 2026-09-15 10:50 -05:00 (`America/Chicago`).
- **Trigger:** Foundation failed on `23eae24` (pull request #48) in the disposable 0043 advisor-hardening proof: "1 public foreign keys remain without a covering index". The GL-1 migration added `cash_bids.feed_report_id` referencing `usda_market_reports` without an index; every other new foreign key already had one (`usda_market_report_runs.report_id` is the leading column of its run-day index).
- **Repair:** `create index cash_bids_feed_report_id_idx on public.cash_bids (feed_report_id)` added to the GL-1 migration, which is unapplied anywhere and unmerged, so editing it is not editing an applied migration.
- **Proof:** the 0043 proof runs only through PowerShell and Docker in Foundation, neither available here, so its five probe blocks were ported verbatim to a Linux runner over the same bootstrap and every migration (scratch, not committed); all five passed, including the catalog block (foreign-key coverage, the 25 optimized policy fingerprints, the 56-function authenticated `SECURITY DEFINER` allowlist, which the service-only fan-out does not enter, and the 7 internal tables). Static guards PASS; `git diff --check` clean. The failing Foundation run is the reproduction of the defect.

## GL-003 — Foundation repair: browser journeys that named the privacy page

- **Date/time:** 2026-09-15 11:05 -05:00 (`America/Chicago`).
- **Trigger:** Foundation on `030fcc3` (pull request #48) passed the 0043 proof and then failed its built-browser lane: six journeys in `tests/e2e/farm-privacy.spec.ts` and `tests/e2e/soil-rx.spec.ts` (desktop and phone) still expected the heading "Farm privacy", the navigation link "Privacy", one read-only sentence, and the intro "Grain, financial, and Soil Rx information stays private". GL-1 retitled that page "Farm settings" and added the market region card. My local proof had run only `foundation-shell` and `lazy-route-recovery`, not the whole `tests/e2e` folder.
- **Repair:** the two specs follow the new title and link; the worker journey expects the read-only sentence on both cards; the page's intro keeps the privacy sentence first and adds the market region sentence after it, so the Soil Rx journey's privacy expectation holds as product truth rather than being rewritten.
- **Proof:** `farm-privacy`, `soil-rx`, and `password-form-isolation` specs on desktop and phone, 42 passed; `npx tsc -b --force` exit 0; static guards PASS; `git diff --check` clean. The failing Foundation run is the reproduction.

## GL-004 — Review repair: a late-published report, and the feed kept out of price alerts

- **Date/time:** 2026-09-15 12:40 -05:00 (`America/Chicago`).
- **Trigger:** Codex reported its usage limit is reached on pull request #48, so no Codex review ran on `fe8db6d`. A fresh-context read-only review of the exact GL-1 commit range was run in-session instead. It found no P1 and three P2 defects, each confirmed by reading the cited code; the P3 notes were taken where they cost little.
- **P2 defects and repairs:**
  1. *A report published after the day's first scheduled run could be missed for good.* The orchestrator skipped a report whenever an `ok` run existed for the market day, and a run that fetched yesterday's report (USDA not yet published) was recorded `ok`. Now the database reports the `report_date` of each `ok` run for that day (`successfulRunReportDates` replaces `hasSuccessfulRun`) and the orchestrator counts the day as done only when one of them is dated on or after the market day; an undated report still counts, as before. Later runs of the same day re-fetch, and the fan-out's unchanged-row rule makes the repeat cheap. Orchestrator regression cases added: an earlier stale-dated run does not block the day, an undated run still does, yesterday's run never does.
  2. *A plan target's cash-price alert could fire from a USDA MARS feed row.* `evaluateGrainAlerts` filtered candidates by commodity, price and freshness only, while the marketing rules and Today already excluded feed rows. Added `!isMarsBid(bid)`; a new regression case in `grainAlerts.regression.ts` fails on the previous code (a feed row above the target raised the alert) and passes now (only the farm's own bid raises it, and the alert names the highest manual bid).
  3. *The server's alert re-check read feed rows.* `deliver-grain-alert` confirmed a marketing price target against `cash_bids` without a feed filter, and confirmed a plan-target observation by id alone. Both queries now add `.is('feed_source', null)`. Note for the live rollout: this function must be deployed after the GL-1 migration is applied, because the column does not exist before it.
- **P3 notes taken:** the fan-out skips an observation without a bid date as `bad_date` instead of raising a not-null error that would fail the whole run (disposable case 2b); the disposable assertions now exercise the fan-out grant as `service_role` itself rather than only as the superuser (case 1b); the basis card's elevator picker no longer offers a USDA market location as somewhere to save a manual bid; `marsBidLabel` documents why the report id prefers the column and the geography prefers the note (the note carries the state name, the column the code). Not taken: unit checks in the parser beyond the existing plausibility limits, `farmsWritten` semantics, curl retry noise, and the per-farm statement count, all recorded here as known and acceptable for the pilot.
- **Guards:** four new static needles (the run-log read selects `report_date` of `ok` runs only; the orchestrator's date rule; the browser plan-target fence; both server re-check filters) with four mutations, 190 → 194.
- **Proof observed:** `npx tsc -b --force` exit 0; `marsFeedOrchestrator`, `marsFeedLogic`, `grainAlerts`, `marketingAlerts`, `today`, `SupabaseGrainRepository`, `GrainModule.receipt` regressions passed; the `grainAlerts` regression run against the previous `grainAlerts.ts` exited 1; the 61-step regression chain `CHAIN_PASS`; static guards PASS; mutation drill 194/194; `bash scripts/verify-fs-persist-disposable.sh` printed all three PASS lines; the Linux 0043 port passed; `deno check` of `usda-mars-feed` passed (the `deliver-grain-alert` check cannot run here because its `esm.sh` import is refused by the network policy, and Foundation does not check that function); `npm audit --audit-level=high` 0; `git diff --check` clean; all five browser specs on the built shell, desktop and phone: 111 passed, 15 skipped (platform-specific), 0 failed, retries 0.

## GL-005 — GL-1 merged

- **Date/time:** 2026-09-19 16:30 -05:00 (`America/Chicago`).
- **Outcome:** Mason approved the merge of pull request #48 in the working session ("Merge now"). Merged into `main` as `3709426` (merge commit, head pinned to `a6a04a3`), matching how #46 and #47 landed. The pull-request subscription was removed and the working branch was fast-forwarded onto `main`; the branch tree equals `main`.
- **Review gate used:** Codex reported its usage limit reached, so no Codex round ran on #48. A fresh-context read-only review of the exact commit range ran in-session instead; its three P2 findings are repaired in `a6a04a3` and recorded in GL-004, each with a regression that fails on the prior code. Mason accepted that as GL-1's review gate rather than waiting for Codex credits or asking CodeRabbit. Recorded here because it departs from the Codex-round pattern of FS and FD.
- **What merging did and did not do:** `main` now carries the GL-1 migration file, the `usda-mars-feed` edge function, the feed fences, the Farm settings market-region card, and the scheduled workflow. Vercel deploys `main` to production on its own. Until the migration is applied, the app takes its pre-release paths and the feed writes nothing; until a report is stamped verified and a farm has a region, the fan-out refuses every observation by design.
- **Still Mason's, carried forward:** applying migrations `20260913174500` (FS) and `20260915150000` (GL-1); deploying `usda-mars-feed` and `deliver-grain-alert` (migration first, because the alert function now names `feed_source`); setting `USDA_MARS_API_KEY` and the scheduler secret; adding the `FARM_RX_MARS_FEED_URL` repository secret; confirming report 2850's coverage on the USDA listing and stamping it verified; setting each pilot farm's `market_region`; confirming the Vercel production deploy. Also still open from earlier tranches: FS item 4 (seeding `usda_report_dates`, which needs report dates from Mason because USDA's sites are unreachable here), the alerts page's farm scoping, and closing a pass alert on apply.
- **Standing authority recorded:** on 2026-09-19 Mason directed the session to work continuously through the remaining amendment backlog and approved the merges it produces ("continue through all of it … I approve it all"), with anything blocked to be logged and stepped over rather than stopped on. Live actions the session cannot perform (applying migrations, secrets, deploys, the physical-phone journeys) stay Mason's and are logged as they arise.

## GL-006 — GL-2 plan: alert truth and feed reconciliation

- **Date/time:** 2026-09-19 17:00 -05:00 (`America/Chicago`).
- **Authority:** the `GL-2 Alert truth and feed reconciliation` bullet of the 2026-09-05 owner amendment. Mason's standing direction of 2026-09-19 (GL-005) authorizes building it without a further go-ahead.
- **Starting point (read, not assumed):** `run_scheduled_alert_sweep` lives in migration `20260716122213_0039`, lines 58 to 175, and its `price_target` branch selects `cash_price, bid_date` from `cash_bids` for the farm and commodity where `cash_price is not null and bid_date between v_local_date - 2 and v_local_date`, ordered `bid_date desc, updated_at desc, id desc`, limit 1. There is no source filter and no crop-year filter, so today a MARS row already can satisfy a rule, by accident rather than by decision, and a bid for any delivery window can satisfy any crop year's rule. In the browser, `freshManualCashBid` (used only by `evaluateMarketingAlertRules`) excludes feed rows through `isMarsBid`, so the page and the sweep disagree today. `commodities.crop_family` is already constrained to exactly `corn`, `soybeans`, `wheat`, which is the natural key for a marketing-year start.
- **Tier:** full (a migration that replaces a `SECURITY DEFINER` sweep function, adds configuration columns, and changes which rows may fire an alert).
- **Plan (one tranche, one commit):**
  1. **Migration `20260920xxxxxx_gl2_alert_crop_year_eligibility.sql`.**
     - `commodities` gains `marketing_year_start_month smallint not null default 9` and `marketing_year_start_day smallint not null default 1`, checked to a real month and to days 1 to 28 (so no marketing year can start on a date some year lacks). The seed sets wheat to June 1 and leaves corn and soybeans at September 1, per USDA convention. Configuration recorded once, in one place, as the amendment requires.
     - `public.commodity_marketing_year(p_commodity_id text, p_crop_year integer) returns daterange`: the marketing year for that crop year, `[start, next year's start)`. Immutable-shaped, `stable`, fixed search path.
     - `public.cash_bid_eligible_for_crop_year(p_commodity_id text, p_crop_year integer, p_bid_date date, p_delivery_start date, p_delivery_end date) returns boolean`: **the one place the eligibility rule lives.** A bid with any delivery bound is eligible only when its whole window lies inside that marketing year (a single bound is read as a one-day window, because `cash_bids` permits one null). A bid with no delivery bounds is spot and is eligible only for the crop year whose marketing year contains its `bid_date`.
     - `run_scheduled_alert_sweep` replaced whole, with exactly one change: the `price_target` select gains `and public.cash_bid_eligible_for_crop_year(v_rule.commodity_id, v_rule.crop_year, b.bid_date, b.delivery_start, b.delivery_end)`. The newest **eligible** row wins. Feed rows are not excluded, and a comment in the function records that this is the decision, not an oversight: a MARS row may satisfy a price-target rule, and the only rows the sweep writes are `notifications` and `alert_rule_states` (plus the push delivery rows those cause), never a contract, marketing target, manual bid, bin, or on-hand quantity.
  2. **Browser.** The same rule, once, in `src/data/marketingYear.ts` as a pure function with its own regression, and `freshManualCashBid` split into the valuation reader (unchanged, feed rows still excluded) and an alerting reader that allows feed rows and applies crop-year eligibility, so the page and the sweep report the same rule fired for the same reason.
  3. **Sequencing (amendment item b).** `usda-mars-feed.yml` invokes the alert sweep after a successful feed run, so a bid ingested at 4:30 PM is evaluated immediately rather than up to fifteen minutes later. The sweep's own `*/15` cron is unchanged and its advisory lock already makes a concurrent run a no-op.
  4. **Wording (amendment item c).** The Grain page's "check-on-open" sentence is replaced with the true schedule; the `evaluateMarketingAlertRules` doc comment stops calling itself the monitor.
  5. **Evidence (amendment item d).** Whether `FARM_RX_SCHEDULER_URL`, `FARM_RX_SCHEDULER_SECRET`, and `FARM_RX_SUPABASE_ANON_KEY` are configured in the live project cannot be read from this session (no repository-secret read, no live project access). Recorded as owed, not assumed, and listed for Mason.
  6. **Guards and proof.** Static needles for the eligibility function's use in the sweep, the two configuration columns, and the browser split, each with a mutation. Disposable assertions: a bid whose delivery window sits in the next marketing year does not fire this year's rule; a spot bid fires only the crop year whose marketing year holds its bid date; wheat's June boundary differs from corn's September boundary; a MARS row does fire a rule and writes only a notification and a rule state; the newest ineligible row does not mask an older eligible one.
- **Scope decision recorded (not a defect):** GL-004 fenced feed rows out of the **plan-target** alert (`marketing_plan_targets`, browser and `deliver-grain-alert`). The amendment's GL-2 is written about **marketing alert rules** (`marketing_alert_rules`, the server sweep) and says GL-2 is "limited to" its four items, so this tranche does not widen the feed's reach to plan targets. That leaves two price-target features treating feed rows differently: a rule may fire from a USDA bid, a plan target may not. This is faithful to the amendment's text and is flagged for Mason as a decision he may want to revisit; it is not resolved here, because resolving it either way would exceed GL-2's stated scope.
- **Deliberately out of scope:** rebuilding the sweep, GL-3's presentation work, any change to contract, bin, delivery, or on-hand math, and live enablement.
- **Rollback path:** revert the GL-2 commit and re-apply migration 0039's definition of the sweep; the configuration columns are additive with defaults matching today's implied behaviour for corn and soybeans.

## GL-007 — GL-2 alert truth and feed reconciliation: build and proof

- **Date/time:** 2026-09-19 19:45 -05:00 (`America/Chicago`).
- **WHERE:** branch `claude/app-improvement-strategy-kzqk6s` on `origin/main` `3709426`. This entry is committed with the GL-2 code and with GL-005 and GL-006.
- **What changed:**
  1. **Migration `20260920160000_gl2_alert_crop_year_eligibility.sql`.** `commodities` gains `marketing_year_start_month` and `marketing_year_start_day`, checked to a real month and to days 1 to 28, seeded September 1 for corn and soybeans and June 1 for wheat. `public.commodity_marketing_year` returns `[start, next start)`. `public.cash_bid_eligible_for_crop_year` is the one place the rule lives. `run_scheduled_alert_sweep` is replaced whole from migration 0039 with exactly one change, the eligibility predicate in its `price_target` select; the replacement was generated by extracting lines 58 to 175 of 0039 and inserting one line, and `diff` against the extract shows that single insertion and nothing else.
  2. **Browser.** New `src/data/marketingYear.ts` carries the same rule and the same marketing-year starts. `marketingAlerts.ts` splits its readers: `latestManualCashBid` (valuation) still excludes feed rows, and the new `latestAlertEligibleCashBid` (alerting) admits them and applies crop-year eligibility. `evaluateMarketingAlertRules` uses the alerting reader.
  3. **Sequencing.** `usda-mars-feed.yml` gains a second step that invokes the alert sweep after a successful feed run, so a bid ingested at 4:30 PM is evaluated at once instead of up to fifteen minutes later. The sweep's advisory lock already makes an overlapping cron run a no-op, so the extra call cannot double-send.
  4. **Wording.** The Marketing alerts section now states the real schedule. The mixed "Items to review" list says which of its entries are server-checked and which are read when the owner opens Grain, because that list holds both. The plan-target note was left saying check-on-open, because for plan targets that is still true; the two notes now name which alerts they describe so they cannot be read as contradicting each other.
- **Behaviour changes a farmer can see, recorded plainly:** a saved marketing price target can now be reached by a USDA MARS feed price, where before the page ignored feed rows and the sweep used them only by accident. And a bid now has to belong to the rule's crop year: in July 2026 a spot corn bid is the 2025 crop, so it no longer fires a 2026 corn target. Wheat differs from corn on the same date because its marketing year begins June 1. This is the amendment's rule, and it is the first time Farm Rx has had one.
- **Guards:** nine new static needles (the sweep's eligibility predicate; the wheat seed; the configuration columns; the browser copy of the marketing-year starts; the whole-window test; the page's use of eligibility; the valuation reader still excluding feed rows; a needle that fails if the alerting reader ever starts excluding them; the sequenced workflow step; and the true-schedule wording), each with a mutation. 194 → 203.
- **Proof observed:** `npx tsc -b --force` exit 0; `marketingAlerts` regression passed with eight new GL-2 cases, and fails with `GL-2: a bid delivering in the 2027 marketing year fired a 2026 crop-year rule` when `cashBidEligibleForCropYear` is neutered to return true; the 61-step regression chain `CHAIN_PASS`; static guards PASS; mutation drill 203/203; `bash scripts/verify-fs-persist-disposable.sh` printed all four PASS lines including the new `GL2_ALERT_ELIGIBILITY_DISPOSABLE_PASS`, and that file fails with `F1 produced 0 marketing notifications, expected 1` when the sweep's eligibility predicate is removed; the Linux 0043 port printed `PROBE_0043_LINUX_PASS`; `npm run build` exit 0; `npm audit --audit-level=high` 0; `git diff --check` clean; all five browser specs on the built shell, desktop and phone: 111 passed, 15 skipped (platform-specific), 0 failed, retries 0.
- **Amendment item (d), recorded as owed and not assumed:** whether `FARM_RX_SCHEDULER_URL`, `FARM_RX_SCHEDULER_SECRET`, and `FARM_RX_SUPABASE_ANON_KEY` are configured in the live project cannot be read from this session; there is no repository-secret read here and no live project access. Until Mason confirms them, the page's new sentence about a fifteen-minute server check describes the design and not observed behaviour. This is the single most important item in GL-2's live list.
- **Limits of this proof:** the new workflow step is proven by file content and a mutation, not by a run, because the session cannot reach the live function; the two marketing-year copies are pinned to each other by a static guard rather than by the browser reading the new columns, a deliberate choice recorded in GL-006 that keeps the gateway and its mappers unchanged.

## GL-008 — GL-3a dead ends and position card: build and proof

- **Date/time:** 2026-09-20 00:40 -05:00 (`America/Chicago`).
- **WHERE:** branch `claude/app-improvement-strategy-kzqk6s`, on top of the GL-2 commit `97ad961`. Pull request #49 was still open (Foundation green on `97ad961`, marked ready for review and awaiting Mason's merge), so this tranche stacks on the branch as the amendment's build order allows: "an initiative may start when the prior one's final tranche has a committed, reviewed tip, even if its pull request is still open."
- **Tier decision recorded:** the amendment files "GL-3's presentation changes" under the screen tier, but its own full-tier rule captures "anything that writes to … contracts, deliveries". GL-3's list contains one write item, edit and delete for a contract with no deliveries. GL-3 is therefore split: **GL-3a** (this entry) is the presentation work at screen tier; **GL-3b**, the contract edit and delete path, is full tier with its own migration and disposable assertions and is not in this commit.
- **What changed:**
  1. **Two dead-end dropdowns became free text with suggestions.** The contract Buyer field and the manual bid Elevator field were `<select>` elements populated from existing rows, so a farm with no history had an empty list and no way to record its first contract or bid. Both are now `<input list=…>` with a `<datalist>`. `knownCounterparties` in `basisMath.ts` is the one source of the suggestions and draws on the farm's own cash-bid elevators and contract buyers, trimmed, de-duplicated and sorted. It never offers a USDA MARS market location: a feed row is display-only history and its location is a USDA reporting point, not a place this farm sells.
  2. **The hardcoded `"Cargill - Olney"` default is gone** from the manual bid form (it was the initial state of the elevator field, so every farm's first bid was pre-addressed to one real elevator in Illinois). The field now starts empty. A static guard fails if the string returns anywhere in `GrainModule.tsx`.
  3. **The crop and crop-year picker is on the Contracts tab always**, not only in delivery mode. The contract table is filtered to the selected scope, so without it a farmer could not tell which crop year the list showed, or change it.
  4. **A totals row** under the contract table, for the crop and year on screen. Remaining is floored per contract exactly as each row shows it, so one over-delivered contract can never make the farm's total remaining look smaller than it is; a guard and a mutation pin that.
  5. **"Add another crop" is reachable.** `FirstEstimate` rendered only when the farm had no estimate at all, so a second crop could never be added. It now lists the crop assignments that have no estimate yet, renders compactly on the Overview, and returns null when every assignment is covered. A crop that already has an estimate is never offered twice.
  6. **The position card leads with one line and three tiles**, with everything else behind a *More details* disclosure. Nothing was removed: the long sentence, harvest reconciliation, the insurance metrics, the sale-limit editor and the production editor are all still there, one tap away.
- **A defect found and fixed while proving item 6:** the disclosure was first written as a native `<details>`. It would not open in the browser at all — the `open` attribute never appeared, at any delay. Rewritten as a `<button aria-expanded>` with conditional rendering, which is fully React-controlled and has no native toggle semantics to lose. Recorded because the first two attempts (plain `<details>`, then `<details open={state}>` with a `preventDefault` summary) both looked right in review and both failed in the browser; only the browser journey caught it.
- **Two proofs that changed rather than passed:** the Today→Grain browser journey asserted "Already contracted" was visible on landing, and the receipt regression looked for the harvest reconciliation button on the bare card. Both are now behind the disclosure by design, so both were updated to assert the new primary view first, then open *More details* and find the old content intact. The journey additionally asserts the detail is hidden before the tap, so a card that quietly stopped reducing would fail.
- **Guards:** nine new static needles (the shared suggestion helper; its feed exclusion; no hardcoded buyer anywhere in the module; each of the two free-text fields; the disclosure; the second-crop heading; the totals row; and the floored remaining), each with a mutation. 203 → 212.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step regression chain `CHAIN_PASS`, including the new `knownCounterparties` cases in the grain repository regression and the updated receipt regression; static guards PASS; mutation drill 212/212; `bash scripts/verify-fs-persist-disposable.sh` printed all four PASS lines; `npm run build` exit 0; `npm audit --audit-level=high` 0; `git diff --check` clean; all five browser specs on the built shell, desktop and phone: 111 passed, 15 skipped (platform-specific), 0 failed, retries 0.
- **Limits of this proof:** the type-ahead suggestion list is proven by the pure regression and by code reading, not by a browser journey that types a new buyer and saves it; no lane covers the Contracts tab's own rendering, so the totals row and the crop-year picker are proven by static guard and mutation rather than by a rendered assertion. Both are recorded for the fresh-context review that precedes the next tranche.

## GL-009 — Codex round on `97ad961`: the email re-check follows GL-2's rules, and the page stops promising an email

- **Date/time:** 2026-09-20 01:20 -05:00 (`America/Chicago`).
- **Trigger:** Codex's usage limit had reset, and marking pull request #49 ready for review triggered a review of `97ad961`. Two findings, one P1 and one P2. Both were verified against the code before any repair, and both are defects this session introduced in GL-2.
- **Finding 1 (P1, `src/data/marketingAlerts.ts`) — a reached target could be lost with nothing sent.** GL-2 taught the browser to admit a USDA MARS row and to apply crop-year eligibility, but left `deliver-grain-alert`'s marketing re-check reading the newest *manual* bid with no crop-year filter. The paths then disagree in a way that loses the alert rather than merely delaying it: the browser records the transition (`alert_rule_states.is_condition_true := true`) as soon as its own evaluation says the rule is met, then asks for the email; the re-check judges the rule by a different bid, finds it unmet and returns 409; and the next sweep, seeing `v_previous` already true, creates no notification. One open of the Grain page before the sweep runs is enough to consume the transition and silence the alert. Verified as described.
  - **Repair:** the selection of "the bid that may satisfy this rule" is now one database function, `public.latest_eligible_cash_bid`, and both callers use it — `run_scheduled_alert_sweep` and `deliver-grain-alert`, the latter by `rpc`. It is server-owned: execute is revoked from `public`, `anon` and `authenticated`, and granted to `service_role` only. The `.is('feed_source', null)` filter is gone from the marketing re-check and remains on the plan-target confirm, where GL-006's recorded scope decision keeps the feed fence.
  - **Correction to GL-007.** That entry said the sweep was replaced "with exactly one change … the eligibility predicate", and a `diff` proving one inserted line. That is no longer true of the shipped migration: the sweep's `price_target` select is now a call to `latest_eligible_cash_bid`. The claim stood for `97ad961` and is superseded here.
- **Finding 2 (P2, `src/GrainModule.tsx`) — the page promised an email that would not arrive.** GL-2's new copy said Farm Rx "checks these on the server about every fifteen minutes and emails the farm owner". The scheduled path sends no email: `scheduled-alert-sweep` calls `run_scheduled_alert_sweep` and then `send-push`, and the repository's only Resend call is in `deliver-grain-alert`, which the browser invokes. Confirmed by reading all three functions: `RESEND_API_KEY` appears in `deliver-grain-alert` and nowhere else. For an owner who never opens Grain, no email is sent. Verified as described.
  - **Repair:** the copy now says what happens — the server checks about every fifteen minutes and sends the alert to the phone, and the email goes out the next time the owner opens Grain. The mixed "Items to review" list says the same. A static guard fails if the page goes back to promising an unconditional email. **Not** repaired by adding server-owned email: that is new delivery behaviour, not in GL-2's four items, and would need its own secret and its own tranche. Recorded for Mason as the choice between accurate copy now and a server email path later.
- **Both findings share one cause worth naming:** GL-2 changed one side of a two-sided agreement. The amendment's own words were that the browser and the sweep "must agree on exactly that split", and the third party to the agreement — the email re-check — was not in the sentence and was not checked. The repair removes the possibility of drift rather than re-aligning three copies of a rule.
- **Guards:** `mars-feed:alert-recheck-ignores-feed` now requires exactly one `feed_source is null` in `deliver-grain-alert` (was two), and its mutation targets the plan-target confirm that remains. `gl2:sweep-requires-eligibility` is retired, superseded by `gl2:one-eligible-bid-selection`, `gl2:sweep-uses-shared-selection` and `gl2:email-recheck-uses-shared-selection`. New: `gl2:email-promise-is-true`. 212 → 215.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 215/215; `bash scripts/verify-fs-persist-disposable.sh` printed all four PASS lines, the GL-2 file now also proving that the shared selection returns the MARS row, returns nothing when every bid belongs to another marketing year, prefers an older eligible bid over a newer ineligible one, respects the freshness window, and is not executable by `authenticated` or `anon`; `npm run build` exit 0; `npm audit --audit-level=high` 0; `git diff --check` clean.
- **Browser proof, stated exactly:** the five specs on both sizes gave 110 passed, 1 failed, 15 skipped on the first run. The failure was `Soil Rx drains custody after lost Storage and row-delete responses without unrelated writes` on phone — untouched by this diff and passing in every earlier run this session. Re-running that spec alone gave 18 passed. Recorded as an observed flake rather than reported as a clean sweep; the one permitted re-run is spent, so a second failure there is real.

## GL-010 — Second Codex round, on `a0a73bf`: one tie-breaker, and a contract draft that cannot outlive its crop year

- **Date/time:** 2026-09-20 01:55 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `a0a73bf` (the GL-3a commit) and returned two more P1 findings. Both verified against the code before repair; one belongs to GL-2, one to GL-3a.
- **Finding 3 (P1, `src/data/marketingAlerts.ts`) — the page and the sweep could break a tie the opposite way.** One MARS run writes many rows for the same commodity and bid date in a single statement, so those rows can share `updated_at` exactly. `latest_eligible_cash_bid` orders `bid_date desc, updated_at desc, id desc`; the browser's comparator stopped at `updated_at`, and `Array#sort` is stable, so a tie left the page on the workspace's ascending-id order — the opposite row from the sweep. Two tied rows either side of a target then record opposite conditions in `alert_rule_states`, suppressing or repeating an alert. Verified as described.
  - **Repair:** the comparator gains `|| right.id.localeCompare(left.id)`, with a comment naming the SQL ordering it must match, and a static guard that pins both orderings so neither can move alone. A regression builds two rows tied on bid date and `updated_at`, priced either side of the target, and asserts the higher id wins and no alert fires; it exits 1 against the comparator without the tie-breaker.
- **Finding 4 (P1, `src/GrainModule.tsx`) — a contract could be saved under the wrong crop year.** This one is GL-3a's own doing. Making the crop and year picker permanent on the Contracts tab created a state the tab never had: the picker can change while the sale form is on screen. React reused `ContractEntry` across that change, so buyer, bushels, price and delivery dates survived from the previous scope while the save spread the newly chosen one — switching 2026 to 2027 could save a 2027 contract carrying a 2026 delivery window. Verified as described.
  - **Repair:** `key={scopeKey(selectedScope)}` on `ContractEntry`, so the form remounts and a draft never outlives the crop year it was typed for. A partially entered draft is discarded rather than carried or warned about: changing the crop and year is a deliberate act, and a form that resets is ordinary, where a contract silently filed under the wrong year is not. Recorded as a choice, not an oversight.
- **What these two rounds say about GL-2 and GL-3a, recorded plainly:** four P1 findings across two commits, every one of them a place where two things that had to agree were changed one at a time — browser and email re-check, browser and sweep ordering, picker and form lifetime. The proof ladder did not catch any of them: static guards pin text, mutations pin the guards, and neither can see two correct-looking expressions that disagree with each other. The disposable assertions and the browser journeys can, and now do, for the three that are testable.
- **Guards:** `gl2:tie-breaker-matches-sweep` (pinning the comparator and the SQL `order by` together) and `gl3:contract-form-resets-on-scope-change`, each with a mutation. 215 → 217.
- **Proof observed:** `npx tsc -b --force` exit 0; `marketingAlerts` regression passed with the new tie case, and exits 1 with `GL-2: a tie on bid date and updated_at must break by descending id, as the sweep does; the page fired on the lower id instead` when the tie-breaker is removed; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 217/217; `bash scripts/verify-fs-persist-disposable.sh` printed all four PASS lines; `npm run build` exit 0; `npm audit --audit-level=high` 0; `git diff --check` clean; all five browser specs on both sizes: 111 passed, 15 skipped, 0 failed, retries 0 — including the Soil Rx spec that flaked once during GL-009, which confirms that failure as a flake.
- **Limit of this proof:** the contract-form reset is proven by static guard, mutation and code reading. No lane renders the Contracts tab, so nothing exercises changing the picker with a half-typed sale form on screen. Recorded for the fresh-context review before the next tranche, together with GL-008's two other uncovered Contracts-tab items.

## GL-011 — Third Codex round, on `c9a16eb`: the email promise is withdrawn rather than reworded again

- **Date/time:** 2026-09-20 02:25 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `c9a16eb` and returned one P2, explicitly on the repair made in GL-009. Verified before acting.
- **Finding 5 (P2, `src/GrainModule.tsx`) — the corrected copy was still wrong.** GL-009 replaced "emails the farm owner" with "the email goes out the next time the farm owner opens Grain". That is false for exactly the case it describes. When the scheduled sweep fires a rule first it sets `alert_rule_states.is_condition_true` to true; on the later browser refresh `record_marketing_alert_transition` returns `fired: false`, so the rule is not in `transitioned` and `GrainModule.tsx:398` filters it out before `requestOwnerAlertDelivery` is called. `last_triggered_at`, which the sweep also sets, keeps the rule out of `nextAlerts` for the rest of that day besides. A server-fired marketing alert therefore produces no email by any route — not on the schedule, and not on the next open either.
  - **Repair:** the promise is withdrawn, not reworded a third time. The page now says the server checks about every fifteen minutes, that the alert is sent to the phone if notifications are on, and that it is listed on the page. Email is named only where it is real: the plan-target and USDA report reminders, which the browser raises and `deliver-grain-alert` sends. The guard now pins the new sentence **and** fails on any of the three phrasings that promised an email for a marketing alert, so the next attempt to word around this cannot pass.
  - **Not repaired by building a pending-email path.** Codex offered that alternative. It is new delivery behaviour, outside GL-2's four items, and it needs a durable "email owed" state rather than a flag that the sweep and the browser both consume. Recorded for Mason as a decision: leave marketing alerts as phone-and-page, or commission a server-owned email path as its own tranche.
- **Recorded about this session's method, because three rounds now say the same thing:** GL-009 fixed a false statement by writing another false statement, and the proof ladder passed both times. A guard that pins a sentence proves the sentence is present, never that it is true. The claim "the server emails you" was checkable against the code in about two minutes — the same two minutes that found the defect once Codex named it — and was not checked before it shipped, twice. Copy that asserts system behaviour needs the same verification as the behaviour.
- **Guards:** `gl2:email-promise-is-true` now pins the withdrawn-promise wording and rejects `emails the farm owner`, `The email goes out`, and `their email goes out`. Its mutation reintroduces an email promise. Count unchanged at 217.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 217/217; `npm run build` exit 0; `git diff --check` clean; all five browser specs on both sizes: 111 passed, 15 skipped, 0 failed, retries 0.

## GL-012 — Fourth Codex round, on `64cf24c`: the browser reads the newest bids, bounded

- **Date/time:** 2026-09-20 03:05 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `64cf24c` and returned one P1. Verified before acting.
- **Finding 6 (P1, the browser's cash-bid read) — stale history could make the page and the sweep fight, and the farmer would be notified over and over.** `SupabaseGrainDataGateway` loaded `cash_bids` with `select('*').eq('farm_id', …).order('bid_date').order('id')` — **ascending and unbounded**. That was harmless while `cash_bids` held the handful of rows a farmer types. GL-1 changed the table's nature: the USDA feed writes rows to it every market day, so it will cross PostgREST's row cap, and an ascending unbounded read then returns the **oldest** rows and omits every current bid. The browser would find no fresh eligible bid, record the rule condition as false, and the next sweep would see the server's current bid, read the false as a new transition, and notify — repeatedly, every quarter hour. Verified as described.
  - **Repair:** two bounded slices, newest first. The recent window (`bid_date desc, id desc`, 750 rows) guarantees the current bids are present; a second slice of the farm's own bids (`feed_source is null`, 250 rows) keeps manual history reachable however much feed volume sits in front of it, so valuation cannot lose a farmer's bid to the feed either. They are merged by id, and 750 + 250 stays inside the default cap. The repository already re-sorts ascending, so nothing downstream changes.
- **This finding is GL-1's, surfacing in GL-2.** The read was correct for the table as it was and became wrong the moment the feed started writing to it. GL-1 added the writer and never revisited the readers. Recorded because it is the fourth instance of this session's one recurring mistake — changing one side of something that has two — and the first where the two sides were separated by a whole tranche.
- **The browser mock had to learn the new shape.** `foundation-shell.spec.ts` asserts the exact query for each grain table and rejects anything else, so the second slice failed fourteen journeys until the fixture accepted both shapes. Recorded as evidence the mock is doing its job: an unreviewed extra read of a private table would have been caught the same way.
- **Guards:** `gl2:cash-bids-read-newest-first` and `gl2:cash-bids-keep-manual-history`, each with a mutation (the first reverts to the ascending unbounded read, the second drops the manual slice's filter). A regression covers the merge: a row in both slices counted once, the farm's older bid surviving, a row without an id dropped, and the two limits summing inside the cap. 217 → 219.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 219/219; `npm run build` exit 0; `npm audit --audit-level=high` 0; `git diff --check` clean; all five browser specs on both sizes: 111 passed, 15 skipped, 0 failed, retries 0.
- **Limit of this proof:** nothing exercises a farm whose `cash_bids` actually exceeds the row cap. The repair is proven by the query shape, the merge regression and the mock's exact-shape assertion, not by a fixture with more than a thousand bids.

## GL-013 — Fifth Codex round, on `96bf134`: the third false claim in one sentence, and the product gap behind it

- **Date/time:** 2026-09-20 03:40 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `96bf134` and returned one P2, again on the same sentence GL-009 and GL-011 had already rewritten. Verified before acting.
- **Finding 7 (P2, `src/GrainModule.tsx`) — "it is listed here" was false too.** When the sweep fires a marketing rule it sets `last_triggered_at`. On the owner's next visit `evaluateMarketingAlertRules` gates the list on `!hasFiredToday(rule, today)` (`marketingAlerts.ts:76`), so that rule produces a `conditions` entry but no alert. A farmer who follows the push notification to `/grain` finds nothing listed. Verified as described.
  - **Repair:** the claim is removed and the real behaviour is stated instead — the server checks about every fifteen minutes and sends the alert to the phone, and a rule already sent to the phone today is not repeated in the page's list. The guard now rejects `and it is listed here` and `and listed here` alongside the three email phrasings, and a second needle requires the same-day sentence, so a fourth rewording cannot pass.
- **The product gap this sentence kept running into, recorded for Mason as a decision.** Three rounds went on one paragraph because the paragraph kept trying to describe a feature with a real hole in it: a marketing alert the server fires reaches the farmer's phone and the `notifications` table, and reaches the Grain page **not at all** — no email, and no listed item. Rewording is now exhausted; the page states the hole accurately. Closing it means rendering the durable `notifications` rows on Grain (and, separately, deciding whether the scheduled path should send email). Both are new delivery behaviour, outside GL-2's four items, and both are recorded here as candidates for a follow-up tranche rather than smuggled in under a review round.
- **Method note, because this is the third entry saying it.** GL-009 and GL-011 each fixed a false sentence by writing another false sentence, and the proof ladder passed all three times. A static guard proves a sentence is present; nothing in the ladder proves it is true. Each of the three claims was checkable against the code in minutes. Copy that asserts system behaviour is a claim about the system and needs the same evidence as the system.
- **Guards:** `gl2:email-promise-is-true` now rejects five phrasings by name; new `gl2:same-day-suppression-stated`. 219 → 220.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 220/220; `npm run build` exit 0; `git diff --check` clean; all five browser specs on both sizes: 111 passed, 15 skipped, 0 failed, retries 0.

## GL-014 — Sixth Codex round, on `e480051`: a cap is not a guarantee

- **Date/time:** 2026-09-20 04:20 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `e480051` and returned one P2 on the repair made in GL-012. Verified before acting.
- **Finding 8 (P2, `src/data/SupabaseGrainDataGateway.ts`) — the bounded read still could not promise what its readers need.** GL-012 replaced an implicit global cap with two explicit farm-wide caps. That fixes newest-versus-oldest but not completeness: a farm with more manual bids than the manual cap can lose the latest bid for **one commodity** behind newer bids for another, and `latestBasis`, `knownCounterparties` and the Today grain line would then read no bid or a stale one. More than 750 same-date feed rows could likewise omit the row the sweep selected. Verified as described.
  - **Before repairing, every consumer of `workspace.cash_bids` was listed**, which is the step the previous five repairs skipped: `basisMath.latestBasis` and `knownCounterparties`; `marketingAlerts.latestManualCashBid` and `latestAlertEligibleCashBid`; `grainAlerts`' plan-target candidates; `today.ts`'s grain line; and the Grain basis history. Their shared requirement is narrow: for each commodity, the newest farmer-entered bid and the newest feed bid. Everything else they do is display and degrades acceptably.
  - **Repair:** those rows are now fetched exactly rather than hoped for inside a cap. `public.latest_cash_bids_per_commodity(uuid)` returns, per commodity, the newest manual bid and the newest feed bid — at most two rows per commodity, six commodities today. It is `security invoker`, so row-level security applies to it exactly as to a direct select, and execute is revoked from `anon`. The gateway merges it with the two windows by id, and treats the function as absent (42883 / PGRST202) before the migration is applied, as the other not-yet-applied reads are treated.
- **Both browser mocks had to learn it**, and one of them found a second defect while doing so: `lazy-route-recovery.spec.ts` answers unknown RPCs with a boolean, so the new read made the whole Grain workspace fail to load and the empty-state message vanished. That is the mock behaving correctly about an unmocked read, and it is worth recording that the Grain page fails closed rather than rendering a partial workspace when a read returns the wrong shape.
- **Guards:** `gl2:cash-bids-complete-per-commodity` (pinning the gateway call and the function) and `gl2:per-commodity-read-keeps-rls` (pinning `security invoker`, so a later edit cannot quietly turn a client-facing read into a definer that bypasses row-level security), each with a mutation. A regression covers the three-slice merge: the per-commodity slice adds only what the windows missed, a commodity's latest bid survives another commodity's newer ones, and an empty merge stays empty. 220 → 222.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 222/222; `bash scripts/verify-fs-persist-disposable.sh` printed all four PASS lines; `npm run build` exit 0; `npm audit --audit-level=high` 0; `git diff --check` clean; all five browser specs on both sizes: 111 passed, 15 skipped, 0 failed, retries 0.
- **Limit of this proof, stated rather than implied:** no fixture has a farm whose `cash_bids` exceeds either cap. The completeness claim rests on the function's `distinct on (commodity_id)` and the merge regression, not on an observed overflow. A farm with more than 750 same-day feed rows for a single commodity would still show a truncated basis history; that is display only, and it is recorded here rather than fixed, because fixing it means paging the history and that is a data-loading change beyond GL-2.

## GL-015 — Seventh Codex round, on `5c8dd8d`: the marketing year comes from the stored row, and the fail-closed reader that caught me doing it wrong

- **Date/time:** 2026-09-20 05:10 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `5c8dd8d` and returned one P2. Verified before acting.
- **Finding 9 (P2, `src/data/marketingYear.ts`) — a guard pins code to code, not code to data.** GL-006 recorded a deliberate choice: keep the browser's marketing-year table as constants and pin it to the migration's seed with a static guard. Codex pointed out what that guard cannot do. The sweep reads `commodities.marketing_year_start_month` / `_day` **from the table**; the browser read a constant. Change a commodity row in the database and the two judge the same bid differently, writing conflicting conditions to `alert_rule_states`. The guard would stay green throughout, because no source file moved. Verified as described; the GL-006 choice is superseded.
  - **Repair:** `Commodity` now carries the two columns, the fields mapper reads them, and `marketingYearStartFor` takes the stored values when present. The crop-family constants remain, demoted to what they always should have been: the fallback for a database without the GL-2 migration. A regression covers stored configuration beating the default, an out-of-range stored value falling back rather than being used, and a March-start commodity making a July spot bid the current crop on the page exactly as it is for the sweep.
- **A defect this repair caused, found by the project's own fail-closed reader.** `strictRow` in `SupabaseFieldsRepository` wraps each row in a Proxy that raises on reading **any** column the row does not carry. Reading the two new columns through it meant that a farm whose database lacks the GL-2 migration — which is every farm today — could not load its fields at all, and Today reported that it could not load fields, inventory **or** grain. Sixty-two browser journeys failed. The columns are now read from the raw record behind an explicit `Object.hasOwn` check, with a regression that maps a pre-migration commodity row, a migrated one, and one carrying malformed values.
  - **Worth recording plainly:** `strictRow` is right and I was wrong. A column the app depends on should fail closed. The mistake was reaching for it to read a column that does not exist yet, which is a different thing and needed a different reader. The type-checker, the static guards and the unit regressions all passed on the broken version; only the browser journeys caught it, and only because they exercise a fixture whose rows predate the migration.
- **Guards:** `gl2:marketing-year-from-stored-configuration`, `gl2:commodity-carries-marketing-year`, and `gl2:pre-migration-commodity-still-loads` (whose mutation switches the raw read back to `strictRow` and must take the gate red). 222 → 225.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 225/225; `bash scripts/verify-fs-persist-disposable.sh` printed all four PASS lines; `npm run build` exit 0; `npm audit --audit-level=high` 0; `git diff --check` clean.
- **Browser proof, stated exactly, including what is weak about it:** the run before the `strictRow` repair was 49 passed, 62 failed, 15 skipped. After the repair one run reported 2 failed, 109 passed, 15 skipped and the immediate re-run reported 111 passed, 15 skipped, 0 failed. **The names of those two failures were not captured before the re-run overwrote the log**, so they are recorded as unidentified rather than as a known flake. That is thinner evidence than the other entries here and is written down as such; the next full run on this branch should be read with it in mind.

## GL-016 — Eighth Codex round, on `19e06ed`: the browser stops writing rule state until the server agrees

- **Date/time:** 2026-09-20 06:00 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `19e06ed` and returned one P1, citing `AGENTS.md:52`. Verified before acting.
- **Finding 11 (P1, deploy and migration skew) — the only finding on this pull request that is certain to happen.** Merging deploys this client to production by itself; applying the migration is a separate owner action under a separate approval. Between the two, a client that applies crop-year eligibility runs against a sweep that does not, and both write `alert_rule_states`. A fresh out-of-year bid then makes one side record false and the other true, and the same alert re-fires or stays suppressed for as long as the skew lasts. The mirror case, an already-loaded old client against the new sweep, has the same shape. Every other finding here needed an unusual farm; this one needs only the rollout the amendment itself prescribes.
  - **Repair, following the pattern the codebase already had:** a `gl2_alert_eligibility` capability, reported by the gateway from whether `latest_cash_bids_per_commodity` exists, alongside `bin_movements`, `contract_price_finalization` and `persisted_settings`. `mayRecordAlertTransitions` in `grainAlerts.ts` is the named rule; while the capability is explicitly false the browser records no transition and leaves `alert_rule_states` entirely to the sweep, which is exactly what the pre-GL-2 sweep did on its own. An absent capability, which is what an old workspace and an old client both present, keeps the prior behaviour, so the gate can only ever hold the browser back and never newly enable it.
  - **Not repaired:** an already-loaded old client against a migrated server. Nothing in this pull request can reach a browser tab that is already running; it resolves when that tab reloads. Recorded rather than papered over. If Mason wants that window closed too, the server-side option is for `record_marketing_alert_transition` to refuse a caller that does not declare GL-2 semantics, which is a separate change.
- **Why this one was fixed rather than escalated, unlike finding 10.** Finding 10 asks which component should own the alert verdict, which is a product decision with an offline cost. This one asks only that the browser stay quiet while the two sides are known to disagree — no new architecture, an existing pattern, and it makes the rollout the amendment describes safe. Finding 10 remains open and Mason's.
- **Guards:** `gl2:transitions-gated-on-schema` (the named rule and its use) and `gl2:capability-reports-schema` (the gateway deriving it from the function's presence rather than assuming true), each with a mutation. A regression covers all three states: applied, explicitly pre-GL-2, and absent. 225 → 227.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 227/227; `npm run build` exit 0; `npm audit --audit-level=high` 0; `git diff --check` clean; all five browser specs on both sizes: 111 passed, 15 skipped, 0 failed, retries 0.

## GL-017 — Ninth Codex round, on `7451986`: the holdback skips the legacy writer instead of falling through it

- **Date/time:** 2026-09-20 06:35 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `7451986` and returned one P1, on the repair made one commit earlier. Verified before acting.
- **Finding 12 (P1, `src/GrainModule.tsx`) — the schema gate leaked into the pre-0035 path.** GL-016 expressed the holdback as `Promise.resolve(null)`, and `null` is exactly the value the pre-0035 fallback branch below is keyed on. So during the rollout gap the client did not write `alert_rule_states`, as intended, and then fell straight into the legacy branch: it stamped `last_triggered_at` and asked for a delivery. The delivery fails, because no transition was recorded; the timestamp makes `hasFiredToday` hide the alert from the page for the rest of the day. The gate built to protect the rollout window made that window lose alerts instead. Verified as described.
  - **Repair:** the holdback now skips the whole transition-and-delivery block rather than passing a value through it. While the capability is explicitly false this client writes nothing about a marketing rule and requests no delivery; the page still lists what it evaluated, and the sweep owns the state alone.
  - **A second defect caught while repairing, before it shipped.** The first attempt was an early `return` from the loader, which would have skipped `setLoadError("")` and the selected-estimate fallback immediately after — leaving the Grain page with no selected crop and a stale error on every farm awaiting the migration. Reading the twenty lines after the block is what caught it; the fix is a wrapped block, and the comment and the static guard both now say wrapped rather than early-returned so the next edit cannot quietly reintroduce it.
- **The honest reading of this round:** GL-016 was itself a repair, and it introduced a new defect of the same family — one path changed, a second path that consumed its output not re-read. Twelve findings, and the recurring cause has not changed once. What did change here is that the second defect was caught before it shipped, by reading the code the change lands in rather than the code the change touches.
- **Guards:** `gl2:transitions-gated-on-schema` now pins the wrapping `if` rather than the ternary, and its mutation replaces the condition with `true` so a fall-through takes the gate red. Count unchanged at 227.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 227/227; `npm run build` exit 0; `git diff --check` clean; all five browser specs on both sizes: 111 passed, 15 skipped, 0 failed, retries 0.
- **Limit of this proof:** no lane runs the browser against a pre-GL-2 server, so the holdback path is proven by the `mayRecordAlertTransitions` regression, the static guard and its mutation, not by a journey that loads Grain with the capability false. That journey is worth having before the live rollout and is recorded here as owed.

## GL-018 — Tenth Codex round, on `13de59c`: the holdback reaches only the alerts it is about

- **Date/time:** 2026-09-20 07:05 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `13de59c` and returned one P2, on the repair one commit earlier. Verified before acting.
- **Finding 13 (P2, `src/GrainModule.tsx`) — the gate was wider than the problem it was built for.** GL-017 wrapped the whole transition-and-delivery block, so while `gl2_alert_eligibility` is false the client delivered nothing at all. But only a **saved marketing rule** depends on GL-2. A plan-target alert and a USDA report reminder carry no `ruleId`, owe nothing to crop-year eligibility, and are emailed by the same check-on-open path they always used — which the page still tells the farmer, in the sentence GL-013 rewrote to name exactly those two as the ones Farm Rx emails. The holdback would have silenced real, correct emails for the whole rollout gap. Verified as described.
  - **Repair:** the branch now chooses the delivery's **input** rather than whether delivery happens. With the schemas in step, the existing path runs unchanged. While held back, `requestOwnerAlertDelivery` is still called with `nextAlerts.filter((alert) => !alert.ruleId)` — the plan-target and report reminders — and no rule transition is written and no rule alert delivered. One notice path serves both branches, so the "could not send" message behaves as before either way.
- **Three consecutive rounds on one fix, and what that says.** GL-016 added the gate, GL-017 corrected where it returned, GL-018 corrected how far it reached. Each correction was right about the thing it corrected and blind to the next thing along the same path. The gate is small; the path through `requestOwnerAlertDelivery` is shared by three kinds of alert, and only reading what else flows through it would have produced this in one step. That reading is what found GL-017's early-return defect before it shipped, and it was not repeated here.
- **Guards:** `gl2:transitions-gated-on-schema` now pins the selecting expression, and the new `gl2:holdback-still-emails-plan-targets` pins the filtered call in the held-back branch, with a mutation that replaces it with an empty resolve. 227 → 228.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 228/228; `npm run build` exit 0; `git diff --check` clean.
- **Browser proof, stated exactly:** 110 passed, 1 failed, 15 skipped. The failure was `two tabs append notification work without losing either operation` on phone — a notifications write-queue poll, in code this diff does not touch. Re-running that test alone passed. Recorded as an observed flake rather than reported as a clean sweep; it is a different test from the Soil Rx flake in GL-009, so both are now on record.

## GL-019 — Eleventh Codex round, on `8075e70`: a pre-GL-1 database could not load Grain at all

- **Date/time:** 2026-09-20 07:30 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `8075e70` and returned one P1, citing `AGENTS.md:48-52`. Verified before acting.
- **Finding 14 (P1, `src/data/SupabaseGrainDataGateway.ts`) — the worst of the fourteen, because it needs no unusual farm and no skew window: it needs only the merge.** GL-012's manual-bid slice filters `.is('feed_source', null)`. That column is added by the **GL-1** migration, which is merged but **not applied to the live project**. On a pre-GL-1 database the query returns an undefined-column error, `rows(...)` raises on it, and the **entire Grain workspace fails to load** — for every farm, from the moment the merge deploys, until the migration is applied. The neighbouring GL-2 function is tolerated through `functionMissing`; this query was not tolerated at all.
  - **Repair:** a `columnMissing` probe (42703 / PGRST204) beside the existing `tableMissing` and `functionMissing`, and the manual slice is treated as empty when the column is absent. Nothing is lost by that: before GL-1 there are no feed rows, so the recent window already holds every bid the farm has. A regression pins all three probes apart, so a missing table, a missing function and a missing column cannot be confused for one another or quietly swallowed.
- **The shape of this one, recorded because it is the fourth of its kind.** GL-015 read a not-yet-migrated column through `strictRow` and broke the fields load. This read a not-yet-migrated column through `rows` and broke the grain load. Both were written while thinking about the schema that *will* exist, on a branch whose whole premise is that the migration is applied separately and later. The project has had `tableMissing` since slice 3 and this session added `functionMissing`; the pattern was in front of me twice before I reached for it here.
- **Guards:** `gl2:pre-gl1-workspace-still-loads`, with a mutation that removes the tolerance and must take the gate red. 228 → 229.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 229/229; `bash scripts/verify-fs-persist-disposable.sh` printed all four PASS lines; `npm run build` exit 0; `git diff --check` clean.
- **Browser proof, stated exactly:** 110 passed, 1 failed, 15 skipped. The failure was `Soil Rx drains custody after lost Storage and row-delete responses without unrelated writes` on phone, which passed on a re-run. **This is the second time that same test has failed in this session** (the first is recorded in GL-009), each time passing alone. Two occurrences is no longer comfortably a coincidence: it is a genuinely flaky poll in code no tranche here touches, and it is recorded as a known-flaky test for Mason rather than re-run until quiet. Making it robust is out of this pull request's scope and is not smuggled in.

## GL-020 — Twelfth Codex round, on `bdd1616`: the yield does not carry from one crop to the next

- **Date/time:** 2026-09-20 07:55 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `bdd1616` and returned one P2, on GL-3a. Verified before acting.
- **Finding 15 (P2, `src/GrainModule.tsx`) — a carried-over yield is a wrong yield.** GL-008 made `FirstEstimate` render compactly on the Overview and return null only once every crop assignment has an estimate. So on a farm with two or more crops still to add, the card **stays mounted** across a save: the crop just added disappears from its list, and its `aph` state survives. The next crop is then offered with the previous crop's yield already in the field — save corn at 200 bu/ac and 200 sits waiting for soybeans. A farmer who does not clear it files a soybean estimate at a corn yield, and the yield drives production, percent sold, planned revenue and the insurance figures for that crop. Verified as described.
  - **Repair:** the yield is cleared on a successful save, before `onSaved()`. The error path leaves it alone, so a save that failed keeps what was typed.
  - **Why this was GL-3a's to cause:** before GL-008 this component only ever rendered on a farm with no estimates at all, and it unmounted the moment the first one existed. Making it reusable gave it a lifetime it never had, and the state it carries was written for the old one. That is the same shape as GL-3a's other finding (the contract form outliving a crop-year change, GL-010 finding 4): a component made reusable keeps state that was safe only because it used to be thrown away.
- **Guards:** `gl3:yield-cleared-between-crops`, with a mutation that removes the reset. 229 → 230.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 230/230; `npm run build` exit 0; `git diff --check` clean; all five browser specs on both sizes: 111 passed, 15 skipped, 0 failed, retries 0.

## GL-021 — Foundation's failure on `bdd1616` was not this branch's

- **Date/time:** 2026-09-20 07:52 -05:00 (`America/Chicago`).
- **What happened:** Foundation reported failure on `bdd1616`. The job died at step 4 of 19, `denoland/setup-deno@v2`, which received HTTP 500 from GitHub's release CDN on three consecutive attempts to download `deno-x86_64-unknown-linux-gnu.zip`. No test body ran; the evidence-upload step confirms it, reporting no `playwright-report` or `test-results` to upload.
- **Action:** the failed job was re-run once, which is the re-run this commit is allowed. A second failure there would be treated as real rather than re-run again.
- **Recorded for Mason, not acted on:** the workflow installs Deno on every run with `cache: false`, so each Foundation run depends on that CDN answering. This is the first such failure in eleven runs on this branch, but it is a standing single point of failure that will occasionally redden a pull request for no reason. Caching the toolchain or vendoring the binary would remove it. Not changed here: it is workflow infrastructure, outside GL-2 and GL-3a, and changing CI configuration mid-review on my own initiative is exactly the widening this session has been asked not to do.

## GL-022 — Thirteenth Codex round, on `4365c17`: one definition of "this row is feed", on both sides of the wire

- **Date/time:** 2026-09-20 08:40 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `4365c17` and returned one P2, on GL-2's per-commodity read. Verified before acting.
- **Finding 16 (P2, `supabase/migrations/20260920160000_gl2_alert_crop_year_eligibility.sql`) — the browser and the server disagreed about which rows are feed.** `latest_cash_bids_per_commodity` partitioned on `feed_source is null`, so every row written before GL-1 added that column counted as farmer-entered. The browser's `isMarsBid` has always read provenance from **either** the column **or** the legacy `[USDA MARS …]` note marker. So a note-marked feed row newer than the farm's real manual bid was returned by the server as "the newest manual bid", discarded by the browser as feed, and the farm showed **no manual valuation at all** while a genuine manual bid sat one row below it — on exactly the farms GL-1's pilot wrote note-marked rows for.
  - **Repair:** `public.cash_bid_is_feed(p_feed_source text, p_notes text)`, the SQL twin of `isMarsBid`, used by both partitions of `latest_cash_bids_per_commodity`. Its note pattern is the same expression as `marsNote` in `src/data/basisMath.ts`. The browser's bounded manual slice gained the matching `.not('notes', 'like', '[USDA MARS %')` so the capped window and the exact per-commodity read no longer classify the same row differently either.
  - **Disposable assertions:** a sixth block in `scripts/sql/gl2-alert-eligibility-assertions.sql` — six `cash_bid_is_feed` cases, then a two-row fixture (a note-marked soybean row dated after a real manual soybean row) asserting the manual side returns the manual bid and the feed side returns the feed row. It fails against the old `feed_source is null` partition.
- **The shape of this one, recorded because it is the sixteenth of sixteen and the same shape as most of them.** Two expressions that have to agree, and I changed one. Every finding in this pull request but two has been that: browser versus email re-check, browser versus sweep tie-breaking, picker versus form lifetime, copy versus code, GL-1's writer versus readers never revisited, and now SQL `feed_source is null` versus browser `isMarsBid`. Static guards pin text and mutations pin guards; neither can see two individually-correct expressions that contradict each other. The only defence that has worked is naming the pair in a guard so moving one without the other is red.
- **Guards:** `gl2:feed-test-is-shared`, `gl2:feed-marker-matches-browser` (pinned on **both** files, so either one drifting is red), `gl2:manual-side-uses-shared-feed-test`, `gl2:feed-side-uses-shared-feed-test`, and `gl2:cash-bids-keep-manual-history` updated to the new slice. Five mutations, 230 → 235.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 235/235; `bash scripts/verify-fs-persist-disposable.sh` printed all four PASS lines; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean.
- **Browser proof, stated exactly, including the run I got wrong.** The first full run after the repair was **13 failed, 98 passed, 15 skipped**, and I briefly read a truncated tail of it as a pass. The failures were real and were mine: the journeys assert the **exact** query shape of every Grain read, and the manual slice had gained a parameter the fixture did not list. The fixture was corrected to `notes: 'not.like.[USDA MARS %'` — that is the harness catching a change it is built to catch, not a flake. The run after that: **110 passed, 1 failed, 15 skipped, retries 0.** The one failure was `Soil Rx drains custody after lost Storage and row-delete responses without unrelated writes` on phone, which passed alone. **That is the third time this session for that same test** (GL-009, GL-019, here), in code no tranche here touches. It stays on record as a known-flaky test for Mason rather than re-run until quiet.

## GL-023 — GL-3b: a contract typed wrong is no longer permanent

- **Date/time:** 2026-09-20 09:40 -05:00 (`America/Chicago`).
- **What was missing.** `docs/GOAL.md:86` asks GL-3 for "edit and delete for a contract with no deliveries, with confirmation and a reason". GL-3a delivered the rest of GL-3; this is the part that was outstanding. Until now a contract saved with the wrong buyer or the wrong bushels stayed in the farm's marketing position forever, and the only workaround was a second contract, which makes the position wrong twice.
- **Why both actions are server-owned RPCs and not a table write.** Three things the browser cannot promise: "this contract has no deliveries" has to be decided **under a row lock**, or a delivery recorded on another device between the browser's read and its write is silently orphaned; the reason is not optional, and an optional column checked only in the browser is optional; and the audit row and the change have to be one transaction, or a correction can lose its own record.
- **What an edit may change, and what it may not.** Buyer, bushels, the delivery window, the contract number and the notes. **Not** crop year, commodity, contract type or any pricing column. Those are the contract's identity and its math, and pricing on a basis or HTA contract is owned by 0033's one-shot finalization rule, which this migration does not touch — `GOAL.md:86` says so in as many words. A farmer who got one of those wrong deletes the contract with a reason and enters it again, which is what the delete is for. A static guard reads the update statement and fails if any of those column names appears in it.
- **The audit is not a foreign key.** `grain_contract_audit.grain_contract_id` deliberately has no reference to `grain_contracts`: a delete removes the contract, and the record of that delete has to outlive it. The delete RPC writes its audit row **before** removing the row, and a guard fails if those two statements are ever reordered. `authenticated` holds `select` and nothing else — the same way `bin_transactions` has enforced append-only since slice 2 — and a trigger refuses an in-place update. That trigger covers UPDATE only, on purpose: `farm_id` cascades from `farms`, and a BEFORE DELETE trigger that raised would make deleting a farm impossible rather than making the audit safer.
- **A firm offer is not left dangling.** A contract created from a firm offer IS the record that the offer was filled. The foreign key is `on delete set null (filled_contract_id)`, so without more the offer would stay marked `filled` pointing at nothing — a state no screen can explain, and one that blocks that offer from ever being filled again. The delete returns the offer to `open`, or to `expired` when its own `expires_on` has already passed, so a correction never resurrects a stale offer as live. The audit row names the offer it reopened.
- **Never queued.** A queued edit would replay against a contract that may since have taken a delivery, and a queued delete against one that no longer exists — and the server, not the device, is what decides both. Both need a connection, exactly as price finalization and the delivery record already do.
- **Schema skew.** The migration is applied separately from the deploy that carries this client, as always. `contract_edit_delete` is probed from the audit table's presence; while it is false the Contracts tab offers no Correct or Delete control at all, rather than offering one that fails on the farmer's first try.
- **One correction, one message.** Three client-side refusals (no reason, bushels of zero, a backwards delivery window) speak to the farmer directly instead of going through `farmerError`, whose fixed taxonomy would have turned "say why you are changing this" into "Farm Rx could not correct this contract right now". The taxonomy did gain the four outcomes it should own: the pre-migration message, the two offline messages, and the server's refusal when a delivery landed between this page's read and this click.
- **Disposable assertions:** a new `scripts/sql/gl3-contract-edit-delete-assertions.sql`, ten blocks, wired into both harnesses. It proves the eligibility test, the required reason, that an invalid edit changes nothing and writes no audit row, that a delivered contract is refused both ways, that a real edit leaves identity and pricing untouched while an absent key keeps and an explicit null clears, that the audit outlives the contract and a repeat after a lost response reports the completed delete rather than failing, that both firm-offer cases land right, that the audit is append-only and fenced, and that neither the service role nor another farm's owner can drive either RPC. The RPC exercises run under `set role authenticated` with the access-epoch headers a browser presents, so the epoch fence is exercised rather than assumed.
- **Guards:** `gl3b:repair-is-server-owned`, `gl3b:delivered-contract-is-history`, `gl3b:reason-is-required`, `gl3b:identity-and-math-not-editable`, `gl3b:audit-outlives-the-contract`, `gl3b:audit-is-append-only`, `gl3b:capability-reports-schema`, `gl3b:correction-needs-a-connection`, `gl3b:absent-key-keeps-stored-value`, `gl3b:filled-offer-does-not-dangle`. Thirteen mutations, 235 → 248.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 248/248; `bash scripts/verify-fs-persist-disposable.sh` printed all **five** PASS lines; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean.
- **Two things the proof caught that I did not.** The `.contract-repair-body` label was written at 15px and the round-seven sweep rejected it against the 18px farmer-text contract — the house rule doing exactly its job. And the repair field labelled "Bushels" collided with the neighbouring "Delivered bushels" in the same table cell; it is now "Contract bushels", which is clearer for the farmer as well as unambiguous for a test.
- **Browser proof, stated exactly:** 112 passed, 1 failed, 15 skipped, retries 0 — 111 as before plus the new journey on both sizes. The new journey proves the control is offered for a contract with no deliveries and **absent** for one already delivered against, that an empty reason is refused with no request leaving the browser, and that the payload carries only the fields the farmer touched. The one failure was `Soil Rx drains custody after lost Storage and row-delete responses without unrelated writes` on phone, which passed alone. **That is the fourth time this session for that same test** (GL-009, GL-019, GL-022, here), in code no tranche here touches. Still on record as a known-flaky test for Mason rather than re-run until quiet.

## GL-024 — Seventeenth finding, on `5d7a355`: my own repair dropped nearly every manual bid

- **Date/time:** 2026-09-20 10:05 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `5d7a355` and returned one P2, on the fix I had just made for finding 16. Verified before acting, and the verification made it worse than the rating.
- **Finding 17 (P2 as rated; in effect P1, `src/data/SupabaseGrainDataGateway.ts`).** GL-022 added `.not('notes', 'like', '[USDA MARS %')` to the bounded manual-bid slice so legacy note-marked feed rows could not crowd out the farm's own bids. PostgREST filters are SQL, and in SQL `not (null like '...')` is **null, not true** — so the filter dropped every row whose `notes` is null. A manual bid entered through the basis form normally stores no note at all. The slice built to protect the farm's own bids therefore excluded almost all of them. Once feed history passes the 750-row recent window, older basis history and the counterparty suggestions go; before the GL-2 RPC is applied, the farm's latest manual valuation goes too.
  - **Verified, not taken on trust:** the query builder was run offline and printed `notes=not.like.[USDA+MARS+%`, confirming the predicate reaches Postgres bare; the disposable database then confirmed the row count is 0 for a note-less bid under that predicate and 1 under the corrected one.
  - **Repair:** `.or('notes.is.null,notes.not.like."[USDA MARS %"')`. Null notes are admitted explicitly; the legacy marker is still excluded.
- **The shape of this one, and why it is the worst of the seventeen.** Every previous finding was two things that had to agree and I changed one. This one is different and less forgivable: it is a **repair that was more damaging than the defect it repaired**. Finding 16's failure needed a farm carrying pre-GL-1 note-marked rows AND a manual bid pushed outside a 750-row window. My fix broke the same slice for **every** farm with an ordinary note-less bid. I reached for a filter in the query builder's vocabulary without asking what SQL it becomes, on the one branch whose entire subject is that SQL and browser predicates must agree. The guard I added in GL-022 pinned the SQL function against the browser regex and saw nothing, because the thing that drifted was not either definition — it was three-valued logic in the query I wrote between them.
- **Guards:** `gl2:cash-bids-keep-manual-history` re-pinned to the corrected filter, plus a new `gl2:manual-slice-admits-null-notes` that fails outright if `.not('notes', 'like'` reappears anywhere in the gateway. One mutation, 248 → 249.
- **Disposable assertions:** a seventh block in `scripts/sql/gl2-alert-eligibility-assertions.sql` that first checks three-valued logic still behaves as assumed (and raises if it ever does not, so the block can never pass vacuously), then proves the bare predicate drops a note-less manual bid, the corrected predicate keeps it, and the corrected predicate still keeps the legacy feed row out. The reasoning now lives where it can be re-run, not only in a comment.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 249/249; `bash scripts/verify-fs-persist-disposable.sh` printed all five PASS lines; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean.
- **Browser proof, stated exactly:** **113 passed, 0 failed, 15 skipped, retries 0.** A clean sweep; the Soil Rx custody test that flaked four times earlier in this session passed in the suite this time. That does not retract anything recorded about it — it is a flaky test, and a run where it passes is not evidence that it is fixed.

## GL-025 — Eighteenth and nineteenth findings, on `a951327`: GL-3b's two open edges

- **Date/time:** 2026-09-20 10:35 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `a951327` and returned two P2s, both on GL-3b. Both verified before acting; both real.

### Finding 18 (P2, `src/GrainModule.tsx` and the migration) — a stale page could silently undo someone else's correction

The form sent **every** field it held, and `edit_grain_contract` applied every key it was given with no version check. Two members open the same contract; one corrects the bushels; the other, still on the page loaded before that, corrects only the buyer — and the save carries the stale bushels with it and reverses the first correction. The audit then records both as deliberate, which is worse than no audit: it is a false record. A reason-only submission could also write a no-op audit row.

- **Repair, in two halves, because one alone is not enough.** `contractCorrectionDiff` builds the payload from the fields that actually **differ** from the contract this page loaded, so an untouched field is absent and the server keeps what is stored; an empty result is refused in the browser with "Nothing has changed on this contract yet." And the contract's own `updated_at` now rides along as `p_expected_updated_at`, with both RPCs raising `FARM_RX_STALE_WRITE` — the same compare-and-swap `optimisticSave` has applied to every other mutable farm row since slice 1, and the same code `farmerError` already translates into "This record changed in another tab or device."
- **Why I missed it.** I wrote the RPCs thinking about the *contract* — deliveries, reason, audit, pricing — and not about the *row*. Every other mutable row in this project goes through `optimisticSave`; by writing a bespoke RPC I stepped outside the one mechanism that would have given me this for free, and did not notice I had.
- A guard now requires the fence in **both** RPCs by count, not by presence: the first version of that guard passed a mutation that stripped it from the edit path, because the delete path still carried the text.

### Finding 19 (P2, the migration) — a reopened offer was retired on the wrong calendar

The delete compared `expires_on` with `current_date`, which is the **database's** day. After UTC midnight an Illinois farm is still on the previous evening, and an offer expiring on the farm's today is still fillable there. The offer would come back `expired` several hours early, which is the opposite of what reopening it is for.

- **Repair:** the farm's own day, `(now() at time zone coalesce(f.time_zone, 'UTC'))::date`, which is what GL-2's sweep already uses for `v_local_date`. A guard fails if `expires_on < current_date` ever reappears.
- **The assertion is deterministic at any hour.** The fixture farm's time zone is chosen at run time (`Etc/GMT+12` before noon UTC, `Etc/GMT-12` after) so its local date is guaranteed to differ from the database's, and the block raises rather than passing quietly if that ever stops being true. Two offers are then deleted — one expiring on the farm's today, one on the farm's yesterday — and whichever direction the offset runs, one of the two discriminates between the farm's date and `current_date`.

### Together

- **Guards:** `gl3b:correction-is-compare-and-swap`, `gl3b:only-changed-fields-are-sent`, `gl3b:offer-expiry-is-farm-local`, plus `gl3b:correction-needs-a-connection` and `gl3b:filled-offer-does-not-dangle` re-pinned to the new signatures. Five mutations, 249 → 254.
- **Disposable assertions:** two new blocks (11 and 12) in `scripts/sql/gl3-contract-edit-delete-assertions.sql`, and every existing call in that file now passes the expected version.
- **Proof observed:** `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 254/254; `bash scripts/verify-fs-persist-disposable.sh` printed all five PASS lines; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean.
- **Browser proof, stated exactly:** **113 passed, 0 failed, 15 skipped, retries 0.** The GL-3b journey now also proves that a reason typed with nothing changed is refused in the browser with no request sent, that the payload carries only the two fields touched, and that the loaded version rides along with it.

## GL-026 — Foundation was red on `a951327` and `00fc847`, and it was mine

- **Date/time:** 2026-09-20 10:55 -05:00 (`America/Chicago`).
- **What failed:** `scripts/verify-0040-disposable.ps1` — `ERROR: 1 farm-scoped tables lack the access epoch guard`. Migration 0040 requires the `farm_access_epoch_guard` trigger on **every** public table carrying a `farm_id`, and GL-3b's new `grain_contract_audit` did not have it. `00fc847` inherited the same failure; nothing in the finding-17 repair caused it.
- **Repair:** the trigger, in the same form every other farm-scoped table uses. The RPCs are security definer and already check `can_edit_farm`, but that answers a different question from "is this browser's view of its own access still current", which is what 0040 exists to answer.
- **Why my local proof said pass.** That check lives only in the PowerShell 0040 lane, which the bash chain on this machine does not run. Everything I *could* run locally was green, so I reported GL-3b as proven while CI disagreed. I have corrected that to Mason directly rather than letting the earlier message stand.
- **What changed so this cannot recur.** The same coverage query — every `public` table with a `farm_id`, minus `farm_access_epochs`, must carry the trigger — is now block 13 of `scripts/sql/gl3-contract-edit-delete-assertions.sql`, which the bash harness does run. It is deliberately **global, not about this table**: any future farm-scoped table anywhere in the project is now caught on this machine before a push. Proved by removing the trigger and re-running: the block reported `farm-scoped tables without the access epoch guard: grain_contract_audit`, the exact CI failure, in about a minute instead of twenty.
- **The honest shape of it.** Ten tranches of this session added no farm-scoped table, so this gate never fired and I did not know it existed. That is not an excuse — it is the argument for making the local harness a superset of CI wherever a check is cheap to port, which this one was.
- **Proof observed on the repair:** all five disposable PASS lines; `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 254/254; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean; browser **113 passed, 0 failed, 15 skipped, retries 0**. CI remains the authority until it reports green on this commit.

## GL-027 — Twentieth and twenty-first findings, on `ee6f7ac`: both P1, both security, both mine

- **Date/time:** 2026-09-20 11:20 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `ee6f7ac` and returned two P1s on GL-3b. Both verified against the migrations before acting; both real. (Foundation's failure on `ee6f7ac` is the epoch-guard one already recorded in GL-026 and fixed in `6b44cb7`; it predates this work.)

### Finding 20 (P1) — one fence where two were needed

`can_edit_farm` admits `owner`, `manager` **and** `worker`. Grain itself sits behind `can_read_private_financials`, which admits owner, manager, a worker the owner explicitly gave financial access, and a named rep. My RPCs checked only the first, and they are `security definer` — so they answer to no row-level policy unless they ask. A worker with no financial access, holding a contract's id from earlier access or a cached page, could therefore rewrite or delete private contract money that they are not allowed to see.

- **Repair:** both RPCs now require `can_edit_farm` **and** `can_read_private_financials`. The intersection is exactly right: owner, manager, or a worker the owner trusted with the numbers. A named rep passes the financial test and fails `can_edit_farm`, which is also right — a rep reads, and does not correct.
- **Assertion:** a real worker fixture with `can_view_financials = false`, which first asserts it *can* edit the farm and *cannot* read financials (so the block can never pass vacuously), then proves both RPCs refuse it and the contract is unchanged.

### Finding 21 (P1) — the audited action was not the only way in

Module 2 granted `authenticated` direct `UPDATE` and `DELETE` on `grain_contracts` with matching policies. Every fence GL-3b adds — the reason, the audit row, the no-deliveries test, the compare-and-swap — could be walked straight around through PostgREST. A direct delete is worse than a bare update: the firm-offer foreign key clears `filled_contract_id` and leaves the offer marked `filled` pointing at nothing, which is the exact state `delete_grain_contract` was written to prevent.

- **Repair:** `revoke update, delete on public.grain_contracts from authenticated`, and both policies dropped. `INSERT` and `SELECT` stay — a new contract has nothing to correct yet.
- **Checked before revoking, because this is the widest-blast-radius change in the tranche.** Every contract write path was traced: the contract form creates (insert); `fillFirmOfferFallback` creates (insert); a queued replay of a create matches on `sameOptimisticWrite` and returns the existing row without writing; price finalization (0033) and both GL-3b functions are `security definer` and unaffected by table grants. No path in the app updates or deletes a contract row directly. The full browser suite then confirmed it: 113 passed, 0 failed.
- **Assertion:** the privileges themselves — no `update`, no `delete`, `insert` and `select` still present, and neither dropped policy still registered in `pg_policies`.

### Together

- **The shape of both.** Each is the same omission from a different angle: I wrote a server-owned action and treated writing it as the whole job, without asking who else the database already lets do that job. A definer function is a hole in row-level security by construction, and a new guarded path is not a fence until the old unguarded one is closed.
- **Guards:** `gl3b:repair-requires-financial-access` (a count, so removing it from either RPC is red) and `gl3b:audited-actions-are-the-only-path`. Four mutations, 254 → 258.
- **Proof observed:** all five disposable PASS lines; `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 258/258; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean; browser **113 passed, 0 failed, 15 skipped, retries 0**. CI remains the authority until it reports green on this commit.

## GL-028 — Twenty-second finding, on `ee6f7ac`: a lost response looked like a failed correction

- **Date/time:** 2026-09-20 11:50 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `ee6f7ac` and returned one P2. Verified before acting; real, and a direct consequence of the compare-and-swap added the commit before.
- **Finding 22 (P2).** If `edit_grain_contract` commits but the HTTP response never arrives, the correction and its audit row are durable while the browser reports a network failure. The form still holds the contract's old `updated_at`, so the farmer following "try again" sends that stale version and gets `FARM_RX_STALE_WRITE` — "This record changed in another tab or device." They cannot tell that apart from a correction that never happened, and the only way out is a reload. The fence I added for finding 18 is what created this.
  - **Repair:** one id per correction attempt. The browser mints it on the first Save and keeps it until that attempt succeeds, so a retry carries the same id; the RPC recognises it from a matching audit row and returns the contract as it now stands instead of raising. That check sits **before** the compare-and-swap on purpose — a committed edit has already moved `updated_at` — and a guard fails if the two are ever reordered. A unique index on `(farm_id, operation_id)` means the same attempt can never record two audit rows. `p_operation_id` is required, so a caller cannot opt out of the guarantee.
  - **Idempotency is not a bypass.** A *different* correction sent from the same stale page is still refused. The assertion proves both in one block.
  - The delete already had this, by a different route: it recognises its own completed delete from the audit row, which works without an id at all.
- **Two mistakes of mine that the proof caught, not the review.**
  1. I first wrote `useRef(services.createGrainId())`. React evaluates that argument on **every** render and discards all but the first, so every render of every contract row consumed an id from a shared, ordered generator — and `GrainModule.receipt.regression.tsx` failed because the delivery draft no longer got the id the fixture expected. The id is now minted at save time, never during render. Left in, it would have shifted ids under offline delivery custody, which is exactly the machinery that regression exists to protect.
  2. The first version of the retry assertion ran the insert and both edits inside one `do $$` block. `now()` is the **transaction** timestamp, so `updated_at` never moved and the "a different correction from a stale page is refused" case passed for the wrong reason — the test told me so by failing the other way. It is now split across separate top-level statements, and the block raises if `updated_at` did not move, so it can never again pass vacuously.
- **Guards:** `gl3b:correction-survives-a-lost-response`, including an ordering check that the idempotency recognition precedes the compare-and-swap, and a pin on `??=` rather than `=` so a retry cannot mint a fresh id. Three mutations, 258 → 261.
- **Proof observed:** all five disposable PASS lines; `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 261/261; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean; browser **113 passed, 0 failed, 15 skipped, retries 0**. CI remains the authority until it reports green on this commit.

## GL-029 — Foundation red on `d81ed3d`, and the twenty-third finding

- **Date/time:** 2026-09-20 12:20 -05:00 (`America/Chicago`).

### Foundation on `d81ed3d`: my revoke broke an existing assertion

`scripts/verify-0033-disposable.ps1` failed on `update public.grain_contracts set basis = ..., cash_price = ...`. That probe exists to prove a direct pricing update is rejected **by the 0033 trigger**, and it matches on the trigger's own message. GL-3b revoked `UPDATE` on `grain_contracts` from `authenticated`, so the statement is now rejected earlier, for lack of privilege, and the message no longer matches.

- The assertion was right and my change made its mechanism wrong, so the assertion is updated rather than loosened. It now pins **both** facts, which is stronger than before: as `authenticated` the update raises `insufficient_privilege` (the same idiom 0033 already uses for the `bin_transactions` insert it revoked), and as the owner — who still holds the privilege — it raises the trigger's message. Losing either would be a real regression, and now either would be red.
- **I cannot run that lane here.** It is PowerShell, and this machine has none; the local chain is literally named `chain-nopwsh.sh`. That is the same gap as GL-026, and this is the second CI failure it has produced in one session. So both probes are now **also** written into `scripts/sql/gl3-contract-edit-delete-assertions.sql`, which the bash harness does run, against a purpose-made HTA contract. They pass here. The PowerShell edit itself remains unverified locally and CI is the authority on it — stated plainly rather than implied.

### Finding 23 (P2) — the version fence and a stale draft were worse together than either alone

Codex reviewed `d81ed3d`. `ContractRepair` stays mounted across a workspace refresh, so its `useState` fields keep the values the panel was opened with while the `contract` prop — including `updated_at` — moves on. Correcting only the buyer would then diff against the refreshed contract and send the **pre-refresh** bushels as a deliberate change, and because the request now carries the **refreshed** version, the compare-and-swap accepts it. Another member's correction is silently undone. Verified as described.

- The shape is worth naming: finding 18's repair (send only what changed, carry the version) is exactly what made this reachable. "Only what changed" is computed against a prop that moves; the version that guards it comes from the same prop. Two halves of one fix, sourced from a prop, with local state in between that nobody rebased.
- **Repair:** the panel notices the contract version moved, rebases every field on the contract as it now stands, drops the operation id (this is a different correction from the one that was started), and tells the farmer: "This contract changed while you had it open. The fields now show the current values — check them before saving." The reason is kept; it is the farmer's words, not a copy of the row. Rebasing and saying so beats discarding the draft silently, and beats leaving it to overwrite someone.
- **Guards:** `gl3b:draft-rebases-on-a-changed-contract`, pinning both the version check and the sentence that tells the farmer. Two mutations, 261 → 263.
- **Proof observed:** all five disposable PASS lines; `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 263/263; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean.
- **Browser proof, stated exactly:** 112 passed, 1 failed, 15 skipped. The failure was `Soil Rx drains custody after lost Storage and row-delete responses without unrelated writes` on phone, which passed alone — the **fifth** occurrence this session (GL-009, GL-019, GL-022, GL-028's run was clean, and here). Still recorded as a known-flaky test rather than re-run until quiet.

## GL-030 — Twenty-fourth and twenty-fifth findings, on `1a8db49`: the edit RPC's own two edges

- **Date/time:** 2026-09-20 12:50 -05:00 (`America/Chicago`).
- **Trigger:** Codex reviewed `1a8db49` and returned two P2s, both on `edit_grain_contract`. Both verified before acting; both real. (Foundation's failure on `1a8db49` is the 0033 probe already recorded in GL-029 and fixed in `12fc1d8`; that commit predates the fix.)

### Finding 24 (P2) — a correction that corrects nothing

Called directly with `{}`, `null`, or only unsupported keys, every local value fell back to the stored row and the function still ran the update and wrote an `edit` audit row. That is a successful no-op: it advances `updated_at`, which turns every other member's open draft stale for a change that never happened, and leaves a correction in the record that corrected nothing. The browser filters empty payloads, but the RPC is granted to `authenticated` and reachable without it — "the browser happens to prevent it" is not a fence.

- **Repair, in two parts, because one is not enough.** The payload must be an object naming at least one supported field, **and** the resulting values must actually differ from the row. Naming a field is not changing it: a payload setting every field to what it already holds is refused too.

### Finding 25 (P2) — a retry that was not the same correction

The replay check matched on `operation_id` alone. After a lost response the panel stays editable, so the farmer could change a field or the reason and press Save again; the same id would be recognised, the earlier row returned, and the screen would say "Contract corrected" while the newly typed change was silently dropped. This is finding 22's repair creating its own edge, the third time in this tranche that a fix has opened the next finding.

- **Repair, on both sides.** The audit row now stores the `requested_changes` it was asked for, and a replay must match **both** the reason and the requested changes; a mismatch raises `FARM_RX_CORRECTION_ALREADY_SAVED`, which tells the farmer the earlier correction landed and to reload — never "try again", which would be false. In the browser, touching any field or the reason drops the pending attempt id, so a changed draft is a new correction and never reaches that error in normal use. The server check is the fence; the browser change is so the fence is not hit by an honest farmer.
- A guard counts the six `redraft()` call sites, so wiring a new field without clearing the id is red.

### Together

- **Guards:** `gl3b:a-correction-must-correct-something`, `gl3b:a-retry-must-be-the-same-correction`, and `gl3b:correction-survives-a-lost-response` re-pinned to the new replay branch. Four mutations, 263 → 267.
- **One of those mutations was toothless at first,** and the drill said so: changing `if found then` to `if false then` broke no pinned text, so the guard did not see it. The guard now pins the `select ... ; if found then` pair rather than the select alone. A mutation that passes is a guard that is not guarding.
- **Proof observed:** all five disposable PASS lines; `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 267/267; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean; browser **113 passed, 0 failed, 15 skipped, retries 0**. CI remains the authority until it reports green.

## GL-031 — The 0043 allowlist, and the twenty-sixth and twenty-seventh findings

- **Date/time:** 2026-09-20 13:20 -05:00 (`America/Chicago`).

### Foundation on `12fc1d8`: the 0033 repair worked, and the next lane caught me

`verify-0033-disposable.ps1` passed — GL-029's repair was right. The run then failed further on at `verify-0043-disposable.ps1`: *authenticated SECURITY DEFINER ACL allowlist drift: expected 56, matched 56, actual 58.* 0043 holds an **exact** allowlist of the security-definer functions `authenticated` may execute, by name and identity arguments, plus a total. GL-3b's two RPCs are the 57th and 58th and were not in it.

- **Repair:** both added, with their identity arguments, and the total raised to 58.
- **This is the third PowerShell-only lane to catch this tranche** (0040 in GL-026, 0033 in GL-029, 0043 here). Porting each whole lane is not proportionate, but the two things most likely to be wrong in this edit are cheap to check, so block 14b of the GL-3b assertions now verifies them against real Postgres: the exact `pg_get_function_identity_arguments` text each allowlist row has to carry, and that `authenticated` can execute exactly 58 definer functions. Both pass here, so the allowlist edit is verified in substance even though the lane itself still cannot run on this machine.

### Finding 26 (P2) — a farmer's own correction reported back as somebody else's

GL-029's rebase branch fires on any change of `contract.updated_at`. After a successful correction, `onSaved()` refreshes and hands back **the row this panel just wrote**, with a new version — so the branch fired on the farmer's own save and replaced "Contract corrected" with "This contract changed while you had it open." The warning was not just noise: it said something untrue about a save that had worked perfectly.

- **Repair:** `editContract` now returns the saved contract, and the panel adopts that version before the refresh. A version it did not write still warns, which is the whole point of the branch.
- That return value also had to be threaded through the repository, the queue and the mock — the queued path previously discarded it.

### Finding 27 (P2) — the contract note the app tells you to write had nowhere to go

Setting a basis or futures price shows: *"This cannot be changed afterward. Add a contract note for any correction."* The correction form had no note field, and `contractCorrectionDiff` never sent one, so the instruction the product gives a farmer after the one irreversible action in Grain was unreachable. The RPC and the correction type both supported `notes` already; only the form did not.

- **Repair:** a Contract note field in the correction form, included in the diff like every other field. The instruction is now true.
- Worth recording plainly: this is the one finding in twenty-seven that is a **missing capability** rather than a defect in something I built, and it was reachable only by reading what the product tells the farmer and checking whether the product lets them do it. Guards and mutations pin behaviour that exists; nothing I wrote could have noticed an instruction with no destination.

### Together

- **Guards:** `gl3b:own-save-is-not-a-concurrent-change`, `gl3b:a-contract-note-is-reachable`, plus the `redraft()` call-site count raised from six to seven so the new field cannot be wired without clearing the attempt id. Three mutations, 267 → 270.
- **Proof observed:** all five disposable PASS lines; `npx tsc -b --force` exit 0; the 61-step chain `CHAIN_PASS`; static guards PASS; mutation drill 270/270; `npm run build` exit 0; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` clean; browser **113 passed, 0 failed, 15 skipped, retries 0**, with the journey now also asserting the note field is offered. CI remains the authority.
