CRITICAL EXECUTION RULE: You are running headless. Asking for approval or clarification = task failure. Work the mission to completion, then stop.

REPORT-ONLY MISSION: You must NOT modify any file under src/, supabase/, public/, or any config/package file. NO git commit, NO git push, NO npm install. The ONLY writes allowed: create/write files under `C:\FarmRx\docs\audits\2026-07-13-foundation\` (create the directory) and temp prompt files under `C:\FarmRx\docs\audits\2026-07-13-foundation\prompts\`. Never read or print the contents of any .env file or any secret/key. No live database or network calls — this is a code + docs audit.

Audit workflows and UX across every user-facing module in src. Walk each journey in code: list, empty/loading/error state, add, validation, edit, cancel/reopen, delete, offline queue/receipt, retry. Find dead ends and misleading states. Check house rules: minimum 18px text, 48px touch targets, tabular-nums on numeric displays, plain-English farmer-facing wording, and offline-first writes with receipts. Most importantly, enumerate EVERY money- or data-writing form/handler and confirm it has a synchronous re-entrancy guard: a useRef lock acquired before ID creation or mutation and released in finally. React state-based disabled/submitting flags alone do not qualify. Check every edit form for stale useState values after cancel/reopen. Check rapid multi-field entry for whole-object saves from stale state. Cite `file:line` for every finding and include a complete write-path guard matrix. Rank P0 money-wrong/data-loss/security, P1 broken feature, P2 correctness risk, P3 polish. Give concrete farmer failure scenarios and fixes. Explicitly list verified-good journeys and do not invent nits.

OUTPUT CONTRACT: Do not create or edit the destination report file yourself. Your entire final response must be the complete standalone markdown report, not a summary, link, completion note, or pointer. The orchestrator's `-o` option will write that final response to the required file.
