CRITICAL EXECUTION RULE: You are running headless. Asking for approval or clarification = task failure. Work the mission to completion, then stop.

REPORT-ONLY MISSION: You must NOT modify any file under src/, supabase/, public/, or any config/package file. NO git commit, NO git push, NO npm install. The ONLY writes allowed: create/write files under `C:\FarmRx\docs\audits\2026-07-13-foundation\` (create the directory) and temp prompt files under `C:\FarmRx\docs\audits\2026-07-13-foundation\prompts\`. Never read or print the contents of any .env file or any secret/key. No live database or network calls — this is a code + docs audit.

You are the contract-inventory analyst. Read docs/GOAL.md, docs/farm-rx-handoff.md, docs/profitability-grand-plan.md, docs/ship-checklist.md, every docs/*-design.md, every docs/schema-*.md, docs/foundation-design.md, and docs/profitability-upgrade-spec.md. Also include the numbered docs/design/*.md files when they define UI/experience contracts. Produce an authoritative, numbered contract inventory of every promised feature, behavior, business rule, data requirement, and UI house rule. Every item must cite exact source as `file:line` (or a narrow line span). De-duplicate repeated promises while preserving all source citations and note contradictions explicitly. Organize by module and cross-cutting concern. Capture these house rules explicitly: 18px minimum text, 48px touch targets, tabular-nums for numbers, plain-English farmer-facing wording, and offline-first writes with receipts. Do not inspect implementation except when needed to understand a referenced contract; this report is the yardstick, not a built-state review.

OUTPUT CONTRACT: Do not create or edit the destination report file yourself. Your entire final response must be the complete standalone markdown report, not a summary, link, completion note, or pointer. The orchestrator's `-o` option will write that final response to the required file.
