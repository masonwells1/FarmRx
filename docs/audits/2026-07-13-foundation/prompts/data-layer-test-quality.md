CRITICAL EXECUTION RULE: You are running headless. Asking for approval or clarification = task failure. Work the mission to completion, then stop.

REPORT-ONLY MISSION: You must NOT modify any file under src/, supabase/, public/, or any config/package file. NO git commit, NO git push, NO npm install. The ONLY writes allowed: create/write files under `C:\FarmRx\docs\audits\2026-07-13-foundation\` (create the directory) and temp prompt files under `C:\FarmRx\docs\audits\2026-07-13-foundation\prompts\`. Never read or print the contents of any .env file or any secret/key. No live database or network calls — this is a code + docs audit.

Perform a mechanical but deep data-layer and regression-quality audit. Inspect every *DataGateway.ts and each Mock/Supabase/Queued repository triple. Check seam consistency: method sets, behavior, defaults, errors, ordering, identity, null semantics. Inspect writeQueue.ts, every per-module queue, and syncStatus.ts for lost-write, duplicate-write, retry, permanent-failure, dependency, ordering, conflict, and receipt risks. Cross-reference all migrations 0001-0030 against TypeScript row/input/output types and repository select/insert/update mapping for exact column name, type, scale/precision, and nullability drift. Evaluate all 23 regression suites: identify tautological fake tests, missing distinct-sentinel coverage, and whether dropping/misnaming a selected column would actually fail. Produce a suite-by-suite quality matrix. Cite `file:line` for every issue. Rank P0 money-wrong/data-loss/security, P1 broken feature, P2 correctness risk, P3 polish. Give concrete failure scenarios and fixes. Explicitly list verified-good seams/tests.

OUTPUT CONTRACT: Do not create or edit the destination report file yourself. Your entire final response must be the complete standalone markdown report, not a summary, link, completion note, or pointer. The orchestrator's `-o` option will write that final response to the required file.
