CRITICAL EXECUTION RULE: You are running headless. Asking for approval or clarification = task failure. Work the mission to completion, then stop.

REPORT-ONLY MISSION: You must NOT modify any file under src/, supabase/, public/, or any config/package file. NO git commit, NO git push, NO npm install. The ONLY writes allowed: create/write files under `C:\FarmRx\docs\audits\2026-07-13-foundation\` (create the directory) and temp prompt files under `C:\FarmRx\docs\audits\2026-07-13-foundation\prompts\`. Never read or print the contents of any .env file or any secret/key. No live database or network calls — this is a code + docs audit.

Audit Fields and land economics deeply. Scope: src/FieldsModule.tsx; all field/land data gateway, Mock/Supabase/Queued repository and type files needed to trace values; arrangement types cash rent, crop share, and flex including migration 0014 methods; landlord input-share percentages; src/data/profitabilityCalculations.ts; src/ProfitabilityReport.tsx; relevant migrations and design docs. Verify landlord settlement math end-to-end: every editor field maps to the intended stored column and correct expense bucket; shares apply to the right costs; cash-rent, crop-share, and every flex method include/zero the correct components; per-acre and whole-field conversions are coherent; null/blank/zero handling cannot silently alter money. Judge against standard US crop-share practice but clearly label any practice-dependent judgment. Trace concrete numerical examples. Cite `file:line` for every finding. Rank P0 money-wrong/data-loss/security, P1 broken feature, P2 correctness risk, P3 polish. For each issue give a concrete farmer failure scenario and suggested fix. Explicitly list what you checked and found GOOD. If an area has zero real issues, say so rather than inventing nits.

OUTPUT CONTRACT: Do not create or edit the destination report file yourself. Your entire final response must be the complete standalone markdown report, not a summary, link, completion note, or pointer. The orchestrator's `-o` option will write that final response to the required file.
