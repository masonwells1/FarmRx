CRITICAL EXECUTION RULE: You are running headless. Asking for approval or clarification = task failure. Work the mission to completion, then stop.

REPORT-ONLY MISSION: You must NOT modify any file under src/, supabase/, public/, or any config/package file. NO git commit, NO git push, NO npm install. The ONLY writes allowed: create/write files under `C:\FarmRx\docs\audits\2026-07-13-foundation\` (create the directory) and temp prompt files under `C:\FarmRx\docs\audits\2026-07-13-foundation\prompts\`. Never read or print the contents of any .env file or any secret/key. No live database or network calls — this is a code + docs audit.

Audit grain marketing deeply. Scope: src/GrainModule.tsx, src/GrainCostOfCarry.tsx, contracts, firm offers, src/data/binLedger.ts and its callers, cash bids/quotes, safe-to-forward logic including aggregate revenue-protection interaction, src/data/marketingAlerts.ts, src/data/grainAlerts.ts, src/data/basisMath.ts, src/data/costOfCarry.ts, supabase/functions/deliver-grain-alert/index.ts, repositories/migrations/design docs needed for full tracing. Verify bushel movement and capacity math, moisture effects, futures + basis = cash conventions, contract quantities/deliveries, carry calculations and units/time basis, safe-to-forward limits, aggregate insurance interaction, and every alert rule including edge dates and missing quotes. Trace representative numerical scenarios. Cite `file:line` for every finding. Rank P0 money-wrong/data-loss/security, P1 broken feature, P2 correctness risk, P3 polish. Give a concrete farmer failure scenario and suggested fix for every issue. Explicitly list what was checked and found GOOD. Say plainly when an area has zero real issues.

OUTPUT CONTRACT: Do not create or edit the destination report file yourself. Your entire final response must be the complete standalone markdown report, not a summary, link, completion note, or pointer. The orchestrator's `-o` option will write that final response to the required file.
