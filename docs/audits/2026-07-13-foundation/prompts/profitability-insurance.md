CRITICAL EXECUTION RULE: You are running headless. Asking for approval or clarification = task failure. Work the mission to completion, then stop.

REPORT-ONLY MISSION: You must NOT modify any file under src/, supabase/, public/, or any config/package file. NO git commit, NO git push, NO npm install. The ONLY writes allowed: create/write files under `C:\FarmRx\docs\audits\2026-07-13-foundation\` (create the directory) and temp prompt files under `C:\FarmRx\docs\audits\2026-07-13-foundation\prompts\`. Never read or print the contents of any .env file or any secret/key. No live database or network calls — this is a code + docs audit.

Audit profitability, budgets, planning tools, and crop insurance deeply. Scope: src/ProfitabilityModule.tsx, src/ProfitabilityReport.tsx, src/data/profitabilityCalculations.ts, src/data/insuranceMath.ts, src/data/planningTools.ts, profitability repositories/types/migrations, relevant design docs. Verify Revenue Protection guarantee and indemnity formulas against RMA definitions using static knowledge only (no network); distinguish enterprise/optional-unit complexities the app does not model. Verify budgets, break-even calculations, revenue/cost aggregation, per-acre versus whole-farm consistency, units, crop/field filtering, and `null`/`NaN`/empty-string/zero handling for every numeric input and persisted field. Check serialized/debounced insurance saves for stale or lost updates. Use numerical examples. Cite `file:line` for every finding. Rank P0 money-wrong/data-loss/security, P1 broken feature, P2 correctness risk, P3 polish. Give a concrete farmer failure scenario and suggested fix for every issue. Explicitly list what was checked and found GOOD; do not invent nits.

OUTPUT CONTRACT: Do not create or edit the destination report file yourself. Your entire final response must be the complete standalone markdown report, not a summary, link, completion note, or pointer. The orchestrator's `-o` option will write that final response to the required file.
