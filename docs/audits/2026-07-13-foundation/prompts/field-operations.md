CRITICAL EXECUTION RULE: You are running headless. Asking for approval or clarification = task failure. Work the mission to completion, then stop.

REPORT-ONLY MISSION: You must NOT modify any file under src/, supabase/, public/, or any config/package file. NO git commit, NO git push, NO npm install. The ONLY writes allowed: create/write files under `C:\FarmRx\docs\audits\2026-07-13-foundation\` (create the directory) and temp prompt files under `C:\FarmRx\docs\audits\2026-07-13-foundation\prompts\`. Never read or print the contents of any .env file or any secret/key. No live database or network calls — this is a code + docs audit.

Audit field operations deeply. Scope: HarvestModule, FieldLogModule, ScoutingModule including photo storage and bucket-limit migration 0021, WeatherModule including spray-window logic, weatherService.ts, their data types/gateways/repositories/queues/migrations/design docs, and grain/bin code needed to check reconciliation. Determine whether recorded harvest loads reconcile with bin/grain data or are intentionally independent, whether field/acres/crop and units (bu, acres, inches, degrees F, wind speed) remain consistent, whether weather forecast failures/staleness are safe, whether spray-window thresholds and time/date zones work, and whether scouting upload/delete paths can orphan or expose photos. Trace add/edit/delete/offline flows and edge cases. Cite `file:line` for every finding. Rank P0 money-wrong/data-loss/security, P1 broken feature, P2 correctness risk, P3 polish. Give concrete farmer failure scenarios and suggested fixes. Explicitly list verified-good behavior and say plainly if an area has no real issues.

OUTPUT CONTRACT: Do not create or edit the destination report file yourself. Your entire final response must be the complete standalone markdown report, not a summary, link, completion note, or pointer. The orchestrator's `-o` option will write that final response to the required file.
