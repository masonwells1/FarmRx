# Sol authoritative adversarial release review 19 — offline startup closure

Act as a fresh-context, read-only release-gate orchestrator using actual `gpt-5.6-sol` at Extra High reasoning. Inspect base `48aad521bd1ecb4c5704ef2e6c5bb30e4d522685` through the current working tree directly. Do not trust summaries, prior verdicts, or reported PASS text. Do not edit/create files, change Git state, commit, push, deploy, call live services, run browser/Playwright/phone lanes, apply a migration to persistent storage, mutate persistent data, or expose credentials. The outer runner alone writes your final response.

Return `GO` only if no HIGH or MEDIUM correctness, data-integrity, permission, offline-recovery, concurrency, or proof-quality blocker remains. Review 18 is rejected historical evidence because it found a valid cached offline farm could be blocked by server-only Program or Equipment due generation. Review 19 is authoritative only if you independently verify that repair and the complete checkpoint.

Attack the production replay composition and its mounted non-browser regression. Prove a valid `source: "offline"` farm profile still executes every queue inspection/replay lane, installs retry actions, and can publish ready state, while neither Program nor Equipment server due generation runs. Prove a `source: "live"` profile still runs both strict due-generation paths; failures remain visible, blocking, and retryable without duplicate saves. Confirm explicit/manual Equipment retry retains combined queue inspection plus strict due generation. Look for source-check races, stale-profile publication, cancellation gaps, swallowed failures, duplicated calls, or a test that only proves injected fakes rather than the production composition.

Recheck prior repaired release invariants for regression: exact operation-context revalidation around all queued reads/writes; serialized retry and typed cancellation; account/farm capability fences; pure snapshots and durable clock fencing; strict queue parsing and write echoes; Fields relationships/flex validation; Equipment FIFO/rebasing/delete overlays; strict E2E fixtures; operational RLS; protected Program provenance with working trusted RPCs; service-log/meter exact provenance, historical refusal, reversals, recomputation, and races; atomic farm-selection rollback; no credential/debug leakage.

Independently inspect the disposable 0042 role proof. Every supported application RPC scenario must execute under real `authenticated` SQL role, including historical reversals, canonical save/replay/reversal, backdated/calendar flows, and both sides of the dblink race. Owner-only blocks may cover migration/private/catalog/deferred-constraint coordination but must not masquerade as application proof.

Reconcile exactly 43 non-audit implementation/config/test files, unchanged ordered 18/18 routes, staged count zero, HEAD/base equality, and Option 2 SHA-256 `D62CF7297313C1D4AA622CEB19C543B9ACFA92E1D493127FA49FDE109EA10D38`.

Fresh outer proof on the repaired bytes reports: focused mounted offline regression PASS; forced TypeScript PASS; standalone E2E TypeScript PASS; all 39 regression lanes PASS; production build PASS with only the existing chunk-size warning; dependency audit 0 vulnerabilities; targeted static guards 11/11; foundation static guards PASS; controlled mutation drills 11/11; credential scan 43/0; diff/scope/routes/hash/staged gates PASS; all nine disposable PostgreSQL 17 probes 0033, 0034, 0035, 0036, 0037, 0039, 0040, 0041, and 0042 PASS. Browser/Playwright/phone remain deliberately excluded. Rerun concise read-only non-browser probes as useful.

Report findings first with exact evidence and a reachable failure sequence. End with categorical `GO` or `NO-GO`, LOW follow-ups, actual model/effort if visible, scope reconciliation, probes run, skipped-lane residual limits, and `External mutation: no` only if true.
