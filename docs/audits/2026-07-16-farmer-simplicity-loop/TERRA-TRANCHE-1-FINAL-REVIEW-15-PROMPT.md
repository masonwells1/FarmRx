# Terra final cross-check 15 — repaired 43-file Option 2 checkpoint

Use actual gpt-5.6-terra at Medium reasoning as an independent read-only reviewer. Inspect base `48aad521bd1ecb4c5704ef2e6c5bb30e4d522685` through the current working tree. Do not trust summaries or PASS text. Do not edit/create files, change Git, commit, push, deploy, call live services, run browser/Playwright/phone lanes, apply persistent migrations, mutate persistent data, or expose credentials. The outer runner alone writes your response.

Return GO only if no HIGH or MEDIUM blocker remains. Reconcile the exact 43-file scope, ordered 18 routes, and Option 2 hash.

Review 14 is rejected because Sol found one HIGH and three MEDIUM issues. Independently attack the repairs: read-only operational RLS and service RPC denial; manual-only direct meter source; Program generic-writer and database provenance guards including downgrade/GUC bypass; trusted Program RPC compatibility and owner invariant; revoked private service core/linker; locked definer wrapper allowing both-new/idempotent exact pairs while blocking historical attachment; atomic farm storage rollback and fail-closed rollback failure; strict startup Program generation and retry without duplicate save; null flex-formula queue rejection; and the 0042 role attacks/full snapshots/metadata/race proof.

Spot-check all prior offline durability, cancellation, replay authorization, pure snapshot, exact echo, field/flex, interval reversal/recomputation, missing-field recovery, truthful sync status, capability route, strict mock, and credential-isolation invariants.

Fresh outer proof reports forced and standalone-E2E TypeScript PASS, 39/39 regressions, production build, audit 0, targeted guards 11/11, foundation static and mutation drills 11/11, credential scan 43/0, diff/staged/scope/routes/hash gates, and 9/9 PostgreSQL 17 disposable probes. Browser/Playwright/phone are excluded. Inspect directly and rerun concise read-only non-browser checks as useful.

Report findings first, categorical GO/NO-GO, actual model/effort, scope result, proof run, skipped-lane limits, and `External mutation: no` only if true.
