# Terra final cross-check 14 — repaired 40-file Option 2 checkpoint

Use actual gpt-5.6-terra at Medium reasoning as an independent read-only reviewer. Inspect base 48aad521bd1ecb4c5704ef2e6c5bb30e4d522685 through the current working tree. Do not trust prior conclusions or reported PASS text. Do not edit or create files, change Git state, commit, push, deploy, call live services, run browser/Playwright/phone lanes, apply migrations to persistent databases, mutate persistent data, or expose credentials. The outer runner alone writes your final response.

Return GO only if no HIGH or MEDIUM blocker remains. Reconcile the exact 40-file scope, unchanged ordered 18-route manifest, and selected Option 2 hash in SCOPE-CORRECTION.md.

Freshly attack the seven Review-13 repairs: interval deletion must preserve ambiguous history while clearing only the pointer; canonical interval recomputation must resist backdated replay and retain a meter after later calendar-only service; an unknown field edit ID must be not-found rather than Add; failed farm selection must visibly recover the prior farm and its exact replay authorization without changing queue bytes; startup late-generation failure must remain retryable and double-click locked without repeating a completed save; the 0042 probe must use nonvacuous distinct fixtures, IS DISTINCT FROM, and authenticated-role permission attacks; and the queue-lock proof must derive the exact real queued receipt.

Also verify exact service provenance, deferred constraints, least privileges, atomic reversal, shared farm locks, ambiguity/idempotency, offline durability, all replay/cancellation fences, serialized farmer retry, truthful status, capability-shaped routes, pure snapshots, strict confirmations, missing-agreement fail-closed behavior, and test-only injection isolation.

Current outer proof reports forced and standalone-E2E TypeScript PASS, 39/39 regressions, production build, audit 0, targeted guards 11/11, foundation static and mutation drills 11/11, credential scan 40/0, diff/scope/routes/Option gates, and all 9/9 PostgreSQL 17 disposable probes PASS. Browser/Playwright/phone results are deliberately excluded. Inspect directly and rerun concise read-only non-browser checks as useful.

Report findings first, categorical GO or NO-GO, actual model/effort, scope result, proof run, skipped-lane limits, and External mutation: no only if true.
