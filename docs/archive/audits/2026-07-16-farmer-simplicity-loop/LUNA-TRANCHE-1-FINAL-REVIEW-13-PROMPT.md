# Luna final UX/release cross-check 13 — hardened 40-file Option 2 checkpoint

Use actual `gpt-5.6-luna` at Medium reasoning as an independent read-only reviewer. Inspect base `48aad521bd1ecb4c5704ef2e6c5bb30e4d522685` through the current working tree. Do not trust prior conclusions. Do not edit/create files, change Git state, commit, push, deploy, call live services, run browser/Playwright/phone lanes, apply migrations to persistent databases, mutate persistent data, or expose credentials. The outer runner alone writes your final response.

Return `GO` only if no HIGH or MEDIUM product-correctness, nontechnical-user recovery, permission, offline-save, data-integrity, concurrency, or release blocker remains. Reconcile exact 40-file scope, unchanged 18 routes, and selected Option 2 hash.

Check farmer-facing outcomes and their data guarantees directly in code and executable non-browser proof: a late Equipment due-generation failure after successful replay remains visible in plain language with **Try again**, never `All changes synced`; background cancellation cannot leak an unhandled rejection; an offline already-synced service delete cannot show a half-deleted log/reading pair; exact provenance cannot guess an older, noted, same-value, or ambiguous reading; direct unlinked metered inserts fail closed; direct authenticated service-log deletion is unavailable; and concurrent newer service completion cannot be overwritten by older deletion. A missing field agreement must say the field remains, explain read-only state, and never route to basics or guess owned ground.

Inspect the deferred provenance constraint, shared farm lock, direct-delete revoke, and non-vacuous PostgreSQL 17 `dblink` race rather than trusting reported PASS text. Recheck capability-shaped navigation/direct routes, pure snapshots, offline durability, strict confirmations, serialized retry, and removal of hidden replay.

Fresh outer proof reports forced and standalone-E2E TypeScript PASS, 39/39 regressions, production build, audit 0, static/foundation guards, credential scan 40/0, exact scope/routes/Option hash, and all 9/9 PostgreSQL 17 disposable probes PASS including concurrency. Browser/Playwright/phone results are deliberately excluded. Independently inspect and use concise read-only non-browser probes if useful.

Report findings first, categorical `GO` or `NO-GO`, any LOW follow-up, actual model/effort, scope result, skipped-lane limits, and `External mutation: no` only if true.
