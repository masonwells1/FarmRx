# Sol authoritative adversarial release review 20 — exact offline/auth closure

Act as a fresh-context, read-only release-gate orchestrator using actual `gpt-5.6-sol` at Extra High reasoning. Inspect base `48aad521bd1ecb4c5704ef2e6c5bb30e4d522685` through the current working tree directly. Do not trust summaries, prior verdicts, or reported PASS text. Do not edit/create files, change Git state, commit, push, deploy, call live services, run browser/Playwright/phone lanes, apply a migration to persistent storage, mutate persistent data, or expose credentials. The outer runner alone writes your final response.

Return `GO` only if no HIGH or MEDIUM correctness, data-integrity, permission, offline-recovery, concurrency, auth-isolation, or proof-quality blocker remains. Review 19 and all earlier reviews are historical evidence; Review 20 is authoritative only if you independently verify the exact 44-file checkpoint.

Attack the complete offline/weak-signal startup and retry composition. Verify production access/profile/auth restoration has one bounded 10-second deadline for Supabase and injected collaborators, aborts supported queries, falls back only for definite transport failures, and never turns permission/auth denials into offline access. Verify an offline-ready session is bound to exact active/access/profile/fence/epoch bytes, the persisted auth user and JWT subject, a seven-day limit, and durable clock high-water; farm switches, sign-out, cross-tab changes, cache deletion, expiry, and clock rollback must synchronously close it. Verify all eleven queue lanes inspect/replay offline without server-only Program or Equipment due generation, while strict live startup/revalidation still runs both due paths and exposes failures.

Attack `AuthProvider` as a hostile async state machine. Cover a half-open account-A restore timing out into exact offline access, local sign-out, account-B password sign-in, late account-A events before and after B succeeds, rejected/thrown sign-in, a superseded action, and auth storage rewritten by an old refresh. Direct local sign-out must clear auth and farm authorization without queueing auth-js cleanup behind the old lock. Every tab that sees auth-key deletion must block stale `TOKEN_REFRESHED`/`USER_UPDATED`, yet accept an exact persisted `SIGNED_IN` from a later deliberate password sign-in in another tab without deleting the new shared session. Look for any sequence that renders, persists, or restores the wrong account.

Attack the farmer-facing recovery split. Gate-level **Try again** may reopen a valid offline farm; saved-work **Check signal** must require strict live revalidation, serialize double clicks, and retry later after failure. Offline startup followed by an ordinary save must remain queue-only even when `navigator.onLine` says true. Confirm status text never claims synced after a failed retry and the implementation uses the existing visual system without new routes.

Recheck every earlier release invariant for regression: exact operation-context revalidation around queued reads/writes; serialized retry and typed cancellation; capability fences; pure snapshots and clock fencing; strict parsers and write echoes; Fields relationships/flex validation; Equipment FIFO/rebasing/delete overlays; operational RLS; protected Program provenance and trusted RPCs; service-log/meter exact provenance, historical refusal, reversals, recomputation, and races; atomic farm-selection rollback; strict E2E fixtures; no credential/debug leakage.

Reconcile exactly 44 non-audit implementation/config/test files, unchanged ordered 18/18 routes, staged count zero, HEAD/base equality, and Option 2 SHA-256 `D62CF7297313C1D4AA622CEB19C543B9ACFA92E1D493127FA49FDE109EA10D38`.

Fresh outer proof on the exact bytes reports: focused Farm Access and mounted queued/auth regressions PASS; forced TypeScript PASS; standalone E2E TypeScript PASS; all 39 regression lanes PASS; production build PASS with only the existing chunk-size warning; dependency audit 0 vulnerabilities; targeted static guards 11/11; foundation static guards PASS; controlled mutation drills 11/11; credential scan 44/0; diff/scope/routes/hash/staged gates PASS; all nine disposable PostgreSQL 17 probes 0033, 0034, 0035, 0036, 0037, 0039, 0040, 0041, and 0042 PASS. Browser/Playwright/phone remain deliberately excluded. Rerun concise read-only non-browser probes as useful.

Report findings first with exact evidence and a reachable failure sequence. End with categorical `GO` or `NO-GO`, LOW follow-ups, actual model/effort if visible, scope reconciliation, probes run, skipped-lane residual limits, and `External mutation: no` only if true.
