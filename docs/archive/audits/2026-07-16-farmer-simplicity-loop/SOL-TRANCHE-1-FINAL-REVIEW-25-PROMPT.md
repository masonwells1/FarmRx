# Sol authoritative adversarial release review 25 — serialized auth tuple closure

Act as a fresh-context, read-only release-gate orchestrator using actual `gpt-5.6-sol` at Extra High reasoning. Inspect base `48aad521bd1ecb4c5704ef2e6c5bb30e4d522685` through the current working tree directly. Distrust all earlier reviews and PASS claims. Do not edit/create files, alter Git, commit, push, deploy, call live services, run browser/Playwright/phone lanes, apply persistent migrations, mutate persistent data, or expose credentials. The outer runner alone writes your response.

Return `GO` only if no HIGH or MEDIUM correctness, security, account-isolation, concurrency, rural-recovery, data, permission, or proof-quality blocker remains. Reattack the two Review-24 blockers and the complete repair:

- Every app-controlled mutation of the persisted auth session tuple and matching intent must execute under one cross-tab device transaction. The password network request must remain outside the lock so another tab can supersede it, while begin, commit, rollback, adoption, event cleanup, and explicit sign-out transitions are serialized.
- Attack the exact prior exploit: tab A checks ownership, restores prior session bytes, checks again, and is about to restore prior intent while tab B accepts session C. Prove B cannot enter that window and A cannot overwrite B. Inspect the mounted regression that starts B from the exact old-intent `setItem` hook and requires final exact C/new-session bytes and accepted lineage.
- Adoption after a lost nonce or failed commit may use only a freshly reread coherent shared session+accepted-intent tuple. It must never restore provider-local `acceptedSession` or `trustedAuthSnapshot` merely because shared intent appears to match. Incoherent accepted bytes must fail closed rather than guess.
- A non-null malformed intent must not be treated as absent legacy intent during rollback capture. Verify both mounted returned-error and rejected-promise paths replace malformed tuple state with a durable signed-out fence.
- Delayed historical auth-key deletion and bare historical `SIGNED_OUT` events must not erase a newer coherent accepted tuple. Genuine current deletion plus sign-out must fence. Pending sign-in may retain its nonce but must remove early/stale auth-js session bytes.
- Recheck nonce ABA, simultaneous returned errors and throws, early auth-js success, commit failure, delayed farm-cache cleanup, explicit sign-out, startup restore generation, exact access/refresh/JWT session lineage, and reload behavior.

Then spot-check the wider Farmer Simplicity release: bounded transport-only offline auth/access/profile restore; exact user/JWT/farm/fence/epoch bytes; seven-day and clock rollback; all eleven queue lanes without offline server due generation; gate `Try again` versus strict-live `Check signal`; serialized retries; queue-only offline-ready save; truthful retry state; pure snapshots; farm rollback; Fields/flex; Equipment FIFO/service reversal and provenance; Program provenance; operational RLS; strict mocks; credential hygiene.

Reconcile exactly 47 non-audit files (43 tracked plus 4 untracked), zero staged, HEAD equal to base, exact ordered 18/18 routes, and Option 2 SHA-256 `D62CF7297313C1D4AA622CEB19C543B9ACFA92E1D493127FA49FDE109EA10D38`.

Fresh outer proof reports: focused mounted auth regression PASS; forced and production-build TypeScript PASS; standalone foundation E2E TypeScript PASS; all 39 regressions PASS; production build PASS with only the known chunk warning; dependency audit 0; foundation static PASS; controlled mutations 11/11; credential scan 47/0; diff/scope/routes/hash/staged gates PASS; and disposable PostgreSQL probes 0033, 0034, 0035, 0036, 0037, 0039, 0040, 0041, 0042 plus RLS role matrix PASS. Browser/Playwright/phone are deliberately excluded. Rerun concise read-only non-browser probes as useful.

Report findings first with exact file/line evidence, reachable sequence, business impact, smallest correction, and proof to add. End with categorical `GO` or `NO-GO`, LOW follow-ups, actual model/effort if visible, scope/proof reconciliation, skipped-lane limits, and `External mutation: no` only if true.
