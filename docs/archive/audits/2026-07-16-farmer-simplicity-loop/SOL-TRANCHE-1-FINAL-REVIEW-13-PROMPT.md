# Sol final adversarial release review 13 — hardened 40-file Option 2 checkpoint

Act as the fresh-context, read-only release-gate orchestrator using actual `gpt-5.6-sol` at Extra High reasoning. Inspect base `48aad521bd1ecb4c5704ef2e6c5bb30e4d522685` through the current working tree directly. Do not trust prior summaries, proof claims, or reviewer verdicts. Do not edit/create files, change Git state, commit, push, deploy, call live services, run browser/Playwright/phone lanes, apply a migration to a persistent database, mutate persistent data, or print credentials. The outer runner alone writes your final response.

Return `GO` only if no HIGH or MEDIUM correctness, data-integrity, data-isolation, permission, offline-queue, nontechnical-user recovery, migration-safety, concurrency, or release blocker remains. Otherwise return `NO-GO` with exact file/line evidence, reachable failure sequence, business impact, smallest correction, and a non-vacuous proof. Record LOW follow-ups separately.

Reconcile the exact 40-file checkpoint documented in `SCOPE-CORRECTION.md`. Audit files are evidence-only. Confirm unchanged 18/18 route declarations and selected Option 2 SHA-256 `D62CF7297313C1D4AA622CEB19C543B9ACFA92E1D493127FA49FDE109EA10D38`.

First adversarially falsify the database closure that supersedes Review 12:

1. Historical provenance must not guess a same-value lookalike. Backfill, private exact linking, reversal validation, and the deferred constraint must require `notes IS NULL` plus exact `created_at`, along with the correct farm, equipment, read date, value, source, and creator. Ambiguous or unrelated history must remain intact.
2. A surviving metered service log must be impossible without exact provenance even if an authenticated caller invokes an insert/core path directly. Inspect the deferred constraint trigger, transaction timing, created-then-atomically-reversed behavior, function ownership/search paths, schema/function privileges, PUBLIC/anon grants, and the authenticated direct-table permission surface.
3. Authenticated direct `DELETE` on `equipment_service_log` must be revoked so deletion cannot bypass exact atomic reversal. The public reversal RPC must fail closed for unproven history and be idempotent for already-deleted exact pairs without partial state.
4. Save and delete must take the same farm advisory lock, with delete locking before it reads the service log or recomputes the interval. Confirm the two-session `dblink` probe is non-vacuous: a newer concurrent save must survive, the older exact pair must be removed, and the interval must point to the newer reading.
5. The disposable 0042 probe must genuinely attack noted/older lookalikes, ambiguity, direct unlinked inserts, privilege metadata, trigger metadata, explicit new reading IDs, older identical history, and concurrency on PostgreSQL 17.

Then falsify the earlier closures: already-synced offline Equipment delete retains log and reading until atomic server confirmation; same-queue add/delete hides only its exact queued pair; mounted `SyncNotice` executes the real **Try again** path through the production Equipment factory and surfaces a late due-generation failure without `All changes synced` or an unhandled rejection; test gateway injection cannot affect production; missing field agreement remains distinct/read-only and existing-field save cannot synthesize owned ground; the nonempty queue-lock cancellation proof preserves exact durable bytes, receipt, cache, writer, and status.

Recheck all prior repaired invariants for regression: eleven replay entrypoints revalidate exact operation context after lock acquisition and before queue reads/status; synchronous farm-switch/sign-out cancellation; serialized aggregate retry; background-only rejection sinks; capability-shaped navigation/direct routes; pure snapshots and durable clock fences; strict queue parsing/write echoes; Fields relationships/flex validation; Equipment FIFO/rebasing/nested confirmation; exact field-location echoes; strict E2E mocks; no hidden replay; no credential/debug leakage.

Fresh outer proof on these bytes: forced TypeScript PASS; standalone E2E TypeScript PASS; all 39 regression lanes PASS; production build PASS with only the existing chunk-size warning; dependency audit 0 vulnerabilities; targeted guards 11/11; foundation static guards and 11/11 controlled mutation drills PASS; credential scan 40/0; `git diff --check` PASS apart from line-ending notices; exact scope 40/40; routes 18/18; Option 2 hash exact; all nine disposable PostgreSQL 17 probes PASS, including the two-session 0042 race. Browser/Playwright/phone results are deliberately excluded. Independently inspect the bytes and rerun concise read-only non-browser probes as useful.

End with findings first, categorical verdict, actual model and reasoning effort, scope reconciliation, proof/probes run, skipped-lane residual limits, and `External mutation: no` only if true.
