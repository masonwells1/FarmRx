# Terra independent release cross-check 19 — offline/live replay split

Use actual `gpt-5.6-terra` at Medium reasoning as an independent fresh-context, read-only reviewer. Inspect base `48aad521bd1ecb4c5704ef2e6c5bb30e4d522685` through the current working tree. Do not trust prior conclusions or PASS text. Do not edit/create files, change Git, commit, push, deploy, call live services, run browser/Playwright/phone, mutate persistent databases/data, or expose credentials. The outer runner alone writes your response.

Return `GO` only if no HIGH/MEDIUM correctness, permission, offline, data-integrity, concurrency, or proof-quality blocker remains. Review 18 is historical and rejected because offline startup still called server-only Program and Equipment due generation.

Independently trace the repaired production composition. A valid offline profile must run all queue inspection/replay actions, install retry actions, and reach the mounted ready publication without calling either server due generator. A live profile must still call both strict due generators and remain blocked/retryable on a failure without duplicate save. Manual Equipment retry must remain combined and strict. Attack profile-source races, stale publication, missed queue lanes, swallowed errors, and regressions that merely string-match.

Recheck the 0042 authenticated-role proof: historical reversals, canonical save/replay/reversal, backdated/calendar work, and both sides of the dblink race must execute under real authenticated SQL role. Owner blocks must stay limited to migration/private/catalog/deferred coordination. Spot-check the earlier RLS, Program provenance/RPC compatibility, service provenance/reversal/race, rollback, flex, queue/cancellation, snapshot, route, fixture, and credential repairs.

Reconcile 43 non-audit files, ordered 18/18 unchanged routes, staged zero, HEAD/base equality, and Option 2 hash `D62CF7297313C1D4AA622CEB19C543B9ACFA92E1D493127FA49FDE109EA10D38`.

Fresh outer proof reports focused offline regression, forced TypeScript, standalone E2E TypeScript, 39 regressions, build, audit 0, targeted 11/11, foundation static, mutations 11/11, credentials 43/0, manifest/hash gates, and all nine disposable PostgreSQL probes green. Browser/Playwright/phone excluded. Rerun concise read-only checks if useful.

Report findings first, categorical `GO`/`NO-GO`, LOW follow-ups, actual model/effort if visible, scope/proof/skipped limits, and `External mutation: no` only if true.
