# Sol authoritative adversarial release review 21 — competing-tab auth closure

Act as a fresh-context, read-only release-gate orchestrator using actual `gpt-5.6-sol` at Extra High reasoning. Inspect base `48aad521bd1ecb4c5704ef2e6c5bb30e4d522685` through the current working tree directly. Treat all summaries, historical reviews, and reported PASS text as untrusted. Do not edit/create files, alter Git, commit, push, deploy, call live services, run browser/Playwright/phone lanes, apply migrations to persistent storage, mutate persistent data, or expose credentials. The outer runner alone writes your final response.

Review 20 is rejected historical evidence. Return `GO` only if no HIGH or MEDIUM correctness, data-integrity, permission, farmer-recovery, concurrency, auth-isolation, or proof-quality blocker remains on the exact current 44-file checkpoint.

Attack `src/auth/AuthProvider.tsx` as a hostile multi-tab async state machine. Verify a deliberate password attempt first writes a short-lived pending nonce, another tab never adopts the session while it is merely pending, and only an accepted marker bound to exact JWT `sub` plus signed `session_id` can publish it. A deliberate sign-out marker must remain durable beyond the pending window. Raw auth-js `SIGNED_OUT`, external auth-key deletion, late `SIGNED_IN`, `TOKEN_REFRESHED`, and `USER_UPDATED`, a deferred initial restore, and same-user/different-session tokens must never resurrect stale access. A same-session legitimate refresh must remain usable.

Specifically attack competing manual sign-ins across two providers. If tab A starts an older password request and tab B later succeeds, tab A failure, throw, cancellation, commit failure, or even auth-js-style success that writes/broadcasts before its promise returns must not overwrite or delete tab B's newer bytes/nonce. Rollback must prove exact nonce ownership. Local action supersession must not touch a newer local attempt. Check that blocked stale cleanup preserves a newer shared pending intent and that accepted/signed-out state is adopted safely.

Inspect the mounted proof rather than trusting its labels. It must use two independent AuthProvider instances, two auth clients, two same-origin Window paths and Storage views, asynchronous ordered storage delivery, per-provider intentional-signout state, automatic client broadcast, exact persisted-byte assertions, separate signout-only restore cancellation, raw sign-out replay, pending-before-success rejection, same-user/different-session rejection, and both older-failure/newer-success and older-success/newer-success races. Reject shared-singleton or direct-recipient-callback shortcuts.

Recheck the complete offline and weak-signal composition: bounded transport-only auth/access/profile fallback, exact cached user/JWT/farm/fence/epoch bytes, seven-day and durable clock fences, all eleven queue lanes without offline server due generation, gate `Try again` accepting valid offline access, strict-live `Check signal`, double-click serialization, queue-only ordinary saves after offline-ready, and truthful failure status. Spot-check earlier Fields/flex, Equipment FIFO/service reversal, operational RLS, Program/service provenance, atomic farm switching, strict request mocks, and credential hygiene.

Reconcile exactly 44 non-audit files, unchanged ordered 18/18 routes, zero staged files, HEAD equal to base, and Option 2 SHA-256 `D62CF7297313C1D4AA622CEB19C543B9ACFA92E1D493127FA49FDE109EA10D38`.

Fresh outer proof on the exact bytes reports: focused Farm Access and mounted queued/auth regressions PASS; forced and standalone-E2E TypeScript PASS; all 39 regression lanes PASS; production build PASS with only the known chunk warning; dependency audit 0; targeted static guards 11/11; foundation guards PASS; mutation drills 11/11; credential scan 44/0; diff/scope/routes/hash/staged gates PASS; and all nine disposable PostgreSQL probes 0033, 0034, 0035, 0036, 0037, 0039, 0040, 0041, 0042 PASS. Browser/Playwright/phone remain deliberately excluded. Independently rerun concise read-only non-browser probes as useful.

Report findings first with exact evidence and a reachable sequence. End with categorical `GO` or `NO-GO`, LOW follow-ups, actual model/effort if visible, scope/proof reconciliation, skipped-lane limits, and `External mutation: no` only if true.
