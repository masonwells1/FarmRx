'use strict';

// Retained as an inert placeholder because FarmRx's foundation gate rejects
// tracked-file deletion. The automatic review requester has been retired.
module.exports = Object.freeze({ retired: true });
