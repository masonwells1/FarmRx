import { isTransportFailure } from './QueuedFieldsRepository'
import { NotificationsWriteQueue, notificationsWriteQueueKey, type NotificationsQueueEntryV1 } from './notificationsWriteQueue'
import { setModuleSyncRetryAction, setModuleSyncStatus } from './syncStatus'
import type { MarkReadResult, NotificationsData, NotificationsRepository } from './notifications'
import type { StorageLike } from './writeQueue'
import type { SupabaseNotificationsRepository } from './SupabaseNotificationsRepository'
import { operationalCacheMaxAgeMs, readWorkspaceCache, writeWorkspaceCache } from './workspaceCache'
import { queueTransaction, subscribeQueueTransactions } from './queueTransaction'

const blocked = 'Saved changes on this device need attention. Nothing was deleted.'
export class QueuedNotificationsRepository implements NotificationsRepository {
  constructor(private readonly live: SupabaseNotificationsRepository, private readonly d: { getContext: () => Promise<{ userId: string; farmId: string }>; projectRef: string; storage: StorageLike; createId: () => string; clock: () => string; isOffline: () => boolean }) { setModuleSyncRetryAction('notifications', () => this.inspectAndReplay()); if (typeof window !== 'undefined') window.addEventListener('online', () => { void this.inspectAndReplay() }); subscribeQueueTransactions((key) => { void this.source().then(({ queue }) => { if (queue.key === key) void this.inspectAndReplay() }).catch(() => undefined) }) }
  private async source() { const context = await this.d.getContext(); return { context, queue: new NotificationsWriteQueue(this.d.storage, notificationsWriteQueueKey(this.d.projectRef, context.userId, context.farmId)) } }
  private locked<T>(queue: NotificationsWriteQueue, task: (verify: () => void) => Promise<T>) { return queueTransaction(queue.key, this.d.storage, this.d.createId, task) }
  async getData() { const { context, queue } = await this.source(); const cacheScope = { projectRef: this.d.projectRef, ...context, module: 'notifications' }; try { await this.inspectAndReplay(); const data = await this.live.getData(); await writeWorkspaceCache(cacheScope, data); return data } catch (error) { if (!isTransportFailure(error, this.d.isOffline())) throw error; const cached = await readWorkspaceCache<NotificationsData>(cacheScope, operationalCacheMaxAgeMs); if (!cached) throw error; const data = structuredClone(cached.data); for (const entry of queue.read().entries) { const ids = new Set(entry.ids); data.notifications = data.notifications.map((row) => ids.has(row.id) ? { ...row, read_at: row.read_at ?? entry.enqueuedAt } : row) } data.unreadCount = data.notifications.filter((row) => row.read_at === null).length; return data } }
  async markRead(ids: string[]): Promise<MarkReadResult> { const { context, queue } = await this.source(); const entry: NotificationsQueueEntryV1 = { version: 1, module: 'notifications', kind: 'markRead', operationId: this.d.createId(), userId: context.userId, farmId: context.farmId, enqueuedAt: this.d.clock(), ids: [...new Set(ids)] }; if (!entry.ids.length) throw new Error(blocked); const result = await this.locked(queue, async (verify): Promise<MarkReadResult> => { verify(); const enqueue = () => { verify(); const next = queue.append(entry); setModuleSyncStatus('notifications', { kind: 'pending', pending: next.entries.length }); return { kind: 'pending' as const } }; if (this.d.isOffline() || queue.read().entries.length) return enqueue(); try { const saved = await this.live.markReadOperation(entry.ids); verify(); setModuleSyncStatus('notifications', { kind: 'synced', pending: 0 }); return saved } catch (error) { if (!isTransportFailure(error, this.d.isOffline())) throw error; return enqueue() } }); if (result.kind === 'pending') void this.inspectAndReplay(); return result }
  async raiseNotification(...args: Parameters<NotificationsRepository['raiseNotification']>) { return this.live.raiseNotification(...args) }
  async savePushSubscription(...args: Parameters<NotificationsRepository['savePushSubscription']>) { return this.live.savePushSubscription(...args) }
  async deletePushSubscription(...args: Parameters<NotificationsRepository['deletePushSubscription']>) { return this.live.deletePushSubscription(...args) }
  async inspectAndReplay() { let source: Awaited<ReturnType<QueuedNotificationsRepository['source']>>; try { source = await this.source() } catch { return }; const { context, queue } = source; try { await this.locked(queue, async (verify) => { let envelope = queue.read(); if (!envelope.entries.length) { setModuleSyncStatus('notifications', { kind: 'synced', pending: 0 }); return } if (this.d.isOffline()) { setModuleSyncStatus('notifications', { kind: 'pending', pending: envelope.entries.length }); return } while (envelope.entries.length) { const entry = envelope.entries[0]; if (entry.userId !== context.userId || entry.farmId !== context.farmId) throw new Error(blocked); setModuleSyncStatus('notifications', { kind: 'syncing', pending: envelope.entries.length }); try { await this.live.markReadOperation(entry.ids); verify(); envelope = queue.removeConfirmedHead(entry.operationId) } catch (error) { if (isTransportFailure(error, this.d.isOffline())) setModuleSyncStatus('notifications', { kind: 'pending', pending: envelope.entries.length }); else setModuleSyncStatus('notifications', { kind: 'blocked', pending: envelope.entries.length, message: blocked }); return } } setModuleSyncStatus('notifications', { kind: 'synced', pending: 0 }) }) } catch { setModuleSyncStatus('notifications', { kind: 'blocked', pending: 0, message: blocked }) } }
}
