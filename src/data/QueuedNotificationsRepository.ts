import { isTransportFailure } from './QueuedFieldsRepository'
import { NotificationsWriteQueue, notificationsWriteQueueKey, type NotificationsQueueEntryV1 } from './notificationsWriteQueue'
import { setModuleSyncRetryAction, setModuleSyncStatus } from './syncStatus'
import type { MarkReadResult, NotificationsRepository } from './notifications'
import type { StorageLike } from './writeQueue'
import type { SupabaseNotificationsRepository } from './SupabaseNotificationsRepository'

const blocked = 'Saved changes on this device need attention. Nothing was deleted.'
export class QueuedNotificationsRepository implements NotificationsRepository {
  constructor(private readonly live: SupabaseNotificationsRepository, private readonly d: { getContext: () => Promise<{ userId: string; farmId: string }>; projectRef: string; storage: StorageLike; createId: () => string; clock: () => string; isOffline: () => boolean }) { setModuleSyncRetryAction('notifications', () => { void this.inspectAndReplay() }); if (typeof window !== 'undefined') window.addEventListener('online', () => { void this.inspectAndReplay() }) }
  private async source() { const context = await this.d.getContext(); return { context, queue: new NotificationsWriteQueue(this.d.storage, notificationsWriteQueueKey(this.d.projectRef, context.userId, context.farmId)) } }
  async getData() { return this.live.getData() }
  async markRead(ids: string[]): Promise<MarkReadResult> { const { context, queue } = await this.source(); const entry: NotificationsQueueEntryV1 = { version: 1, module: 'notifications', kind: 'markRead', operationId: this.d.createId(), userId: context.userId, farmId: context.farmId, enqueuedAt: this.d.clock(), ids: [...new Set(ids)] }; if (!entry.ids.length) throw new Error(blocked); if (this.d.isOffline() || queue.read().entries.length) { const next = queue.append(entry); setModuleSyncStatus('notifications', { kind: 'pending', pending: next.entries.length }); void this.inspectAndReplay(); return { kind: 'pending' } } try { const result = await this.live.markReadOperation(entry.ids); setModuleSyncStatus('notifications', { kind: 'synced', pending: 0 }); return result } catch (error) { if (!isTransportFailure(error, this.d.isOffline())) throw error; const next = queue.append(entry); setModuleSyncStatus('notifications', { kind: 'pending', pending: next.entries.length }); void this.inspectAndReplay(); return { kind: 'pending' } } }
  async raiseNotification(...args: Parameters<NotificationsRepository['raiseNotification']>) { return this.live.raiseNotification(...args) }
  async savePushSubscription(...args: Parameters<NotificationsRepository['savePushSubscription']>) { return this.live.savePushSubscription(...args) }
  async deletePushSubscription(...args: Parameters<NotificationsRepository['deletePushSubscription']>) { return this.live.deletePushSubscription(...args) }
  async inspectAndReplay() { let source: Awaited<ReturnType<QueuedNotificationsRepository['source']>>; try { source = await this.source() } catch { return }; const { context, queue } = source; let envelope: ReturnType<NotificationsWriteQueue['read']>; try { envelope = queue.read() } catch { setModuleSyncStatus('notifications', { kind: 'blocked', pending: 0, message: blocked }); return } if (!envelope.entries.length) { setModuleSyncStatus('notifications', { kind: 'synced', pending: 0 }); return } if (this.d.isOffline()) { setModuleSyncStatus('notifications', { kind: 'pending', pending: envelope.entries.length }); return } while (envelope.entries.length) { const entry = envelope.entries[0]; if (entry.userId !== context.userId || entry.farmId !== context.farmId) { setModuleSyncStatus('notifications', { kind: 'blocked', pending: envelope.entries.length, message: blocked }); return } setModuleSyncStatus('notifications', { kind: 'syncing', pending: envelope.entries.length }); try { await this.live.markReadOperation(entry.ids); envelope = queue.removeConfirmedHead(entry.operationId) } catch (error) { if (isTransportFailure(error, this.d.isOffline())) setModuleSyncStatus('notifications', { kind: 'pending', pending: envelope.entries.length }); else setModuleSyncStatus('notifications', { kind: 'blocked', pending: envelope.entries.length, message: blocked }); return } } setModuleSyncStatus('notifications', { kind: 'synced', pending: 0 }) }
}
