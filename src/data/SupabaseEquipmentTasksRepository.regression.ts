import { fieldsSeedForRegression } from './MockFieldsRepository'
import { moduleBackends } from './backends'
import { filterTasksForBoard, programTaskBoardBehavior } from '../EquipmentTasksModule'
import type { EquipmentTasksDataGateway, EquipmentTasksRowBundle } from './EquipmentTasksDataGateway'
import type { EquipmentTasksWorkspace, EquipmentWrite, IntervalWrite, MeterReadingWrite, ServiceLogEntryInput, ServiceLogWrite, TaskWrite } from './equipmentTasks'
import { QueuedEquipmentTasksRepository } from './QueuedEquipmentTasksRepository'
import { EquipmentTasksWriteQueue, equipmentTasksWriteQueueKey, parseEquipmentTasksQueue, type EquipmentTasksQueueEntryV1 } from './equipmentTasksWriteQueue'
import { SupabaseEquipmentTasksRepository } from './SupabaseEquipmentTasksRepository'
import type { FieldsData, FieldsRepository } from './fields'
import type { StorageLike } from './writeQueue'
import { readNeedsAttention } from './needsAttentionStore'
import { getSyncStatus } from './syncStatus'
import { captureFarmRevocationFence, resetFarmGrantFromLive } from './farmRevocationFence'
import { getWorkspaceCacheNotices } from './workspaceCache'
import { observeDeviceTime } from './deviceClockFence'

const uid = (n: number) => `00000000-0000-4000-8000-${String(n).padStart(12, '0')}`
const farm = uid(1); const actor = uid(2); const other = uid(3); const micro = '2026-07-12T12:30:00.123456+00:00'
const operationContext = { projectRef: 'test', userId: actor, farmId: farm, generation: 1, token: uid(999), serverEpoch: 1 }
function assert(value: unknown, message: string): asserts value { if (!value) throw new Error(message) }
async function rejects(action: () => Promise<unknown>, message: string) { let failed = false; try { await action() } catch { failed = true }; assert(failed, message) }
const row = (value: unknown) => value as Record<string, unknown>
const audit = { created_by: actor, created_at: micro, updated_at: micro }
const machineId = uid(10); const intervalId = uid(11); const taskId = uid(12)
function equipment(id = machineId, name = 'Truck 7') { return { id, farm_id: farm, name, category: 'truck', make: 'Case', model: 'Magnum', model_year: 2020, serial_or_vin: null, purchase_date: null, purchase_price: null, meter_unit: 'miles', warranty_expires_on: null, warranty_notes: null, status: 'active', notes: null, ...audit } }
function interval(id = intervalId, overrides: Record<string, unknown> = {}) { return { id, farm_id: farm, equipment_id: machineId, name: 'Oil change', every_meter: 100, every_months: null, last_done_on: null, last_done_reading: null, is_active: true, ...audit, ...overrides } }
function task(id = taskId, overrides: Record<string, unknown> = {}) { return { id, farm_id: farm, title: 'Service Truck 7', details: null, status: 'todo', priority: 'high', assigned_to: actor, due_on: '2026-07-15', field_id: null, equipment_id: machineId, source: 'manual', interval_id: null, interval_cycle_key: null, program_assigned_pass_id: null, program_cycle_key: null, completed_by: null, completed_at: null, ...audit, ...overrides } }
function fieldsFor(farmId: string): FieldsData { const data = fieldsSeedForRegression(); data.farm.id = farmId; data.fields.forEach((field) => { field.farm_id = farmId }); data.crop_assignments.forEach((assignment) => { assignment.farm_id = farmId }); return data }
function canonicalFixture(): EquipmentTasksRowBundle { return { viewer: { role: 'owner' }, equipment: [equipment()], meter_readings: [], intervals: [interval()], service_log: [], service_due: [], members: [{ farm_id: farm, user_id: actor, display_name: 'Current Operator' }, { farm_id: farm, user_id: other, display_name: 'Other Operator' }], tasks: [task()] } }
type Mutators = { equipment?: (reply: unknown) => unknown; meter?: (reply: unknown) => unknown; interval?: (reply: unknown) => unknown; service?: (reply: unknown) => unknown; task?: (reply: unknown) => unknown; delete?: (reply: unknown) => unknown }
class FakeGateway implements EquipmentTasksDataGateway {
  state = canonicalFixture(); fail = false; persistMeter = true; mutate: Mutators = {}; calls = { status: 0, load: 0, generated: 0, equipment: 0, meter: 0, interval: 0, service: 0, task: 0, delete: 0 }; private readingIds = new Map<string, string>(); beforeLoad: (() => Promise<void>) | null = null
  dueStatus: unknown = { has_due: true, task_needed: true, notification_needed: true, local_date: '2026-07-12' }; dueStatusFailure: Error | null = null; dueV2Failure: Error | null = null; dueV2Inputs: Array<{ farmId: string; operationId: string }> = []
  async getDueServiceGenerationStatus(_farmId: string) { this.calls.status++; if (this.dueStatusFailure) throw this.dueStatusFailure; return this.dueStatus }
  dueV2Result: unknown = { operation_kind: 'generate_due_service_tasks_v2', task_created_count: 1, notification_created_count: 1, local_date: '2026-07-12' }
  async generateDueServiceTasksV2(farmId: string, operationId: string) { this.calls.generated++; this.dueV2Inputs.push({ farmId, operationId }); if (this.dueV2Failure) throw this.dueV2Failure; return this.dueV2Result }
  async generateDueServiceTasks(_farmId: string) { this.calls.generated++; return { created_count: 0 } }
  async loadWorkspace(_farmId: string, _viewerId: string) { this.calls.load++; await this.beforeLoad?.(); if (this.fail) throw new Error('network timeout'); return structuredClone(this.state) }
  async saveEquipment(farmId: string, value: EquipmentWrite) { this.calls.equipment++; const saved = { ...value, farm_id: farmId, ...audit }; this.state.equipment = [...this.state.equipment.filter((item) => row(item).id !== value.id), saved]; return structuredClone(this.mutate.equipment ? this.mutate.equipment(saved) : saved) }
  async addMeterReading(farmId: string, value: MeterReadingWrite) { this.calls.meter++; const saved = { ...value, farm_id: farmId, ...audit }; if (this.persistMeter) this.state.meter_readings = [...this.state.meter_readings.filter((item) => row(item).id !== value.id), saved]; return structuredClone(this.mutate.meter ? this.mutate.meter(saved) : saved) }
  async saveInterval(farmId: string, value: IntervalWrite) { this.calls.interval++; const saved = { ...value, farm_id: farmId, ...audit }; this.state.intervals = [...this.state.intervals.filter((item) => row(item).id !== value.id), saved]; return structuredClone(this.mutate.interval ? this.mutate.interval(saved) : saved) }
  async addServiceLogEntry(farmId: string, value: ServiceLogWrite) {
    this.calls.service++; const prior = this.readingIds.get(value.id); if (prior && prior !== value.reading_id) throw new Error('service replay used a different reading id'); if (value.meter_reading !== null) { assert(value.reading_id !== null, 'Service writes with readings must carry a stable reading ID.'); this.readingIds.set(value.id, value.reading_id) }
    const log = { id: value.id, farm_id: farmId, equipment_id: value.equipment_id, service_date: value.service_date, work_performed: value.work_performed, parts: value.parts, vendor: value.vendor, cost: value.cost, meter_reading: value.meter_reading, interval_id: value.interval_id, ...audit }
    this.state.service_log = [...this.state.service_log.filter((item) => row(item).id !== value.id), log]
    const reading = value.meter_reading === null ? null : { id: value.reading_id, farm_id: farmId, equipment_id: value.equipment_id, reading: value.meter_reading, read_on: value.service_date, source: 'service', notes: null, ...audit }
    if (reading) this.state.meter_readings = [...this.state.meter_readings.filter((item) => row(item).id !== reading.id), reading]
    let completed: Record<string, unknown> | null = null
    let intervalCompletion: Record<string, unknown> | null = null
    let intervalReading: Record<string, unknown> | null = null
    if (value.interval_id) {
      const ordered = this.state.service_log
        .filter((item) => row(item).interval_id === value.interval_id)
        .map(row)
        .sort((a, b) => String(b.service_date).localeCompare(String(a.service_date)) || String(b.created_at).localeCompare(String(a.created_at)) || String(b.id).localeCompare(String(a.id)))
      const completionLog = ordered[0]!
      const readingLog = ordered.find((item) => item.meter_reading !== null)
      const priorInterval = row(this.state.intervals.find((item) => row(item).id === value.interval_id))
      completed = { ...priorInterval, last_done_on: completionLog.service_date, last_done_reading: readingLog?.meter_reading ?? null, updated_at: micro }
      intervalCompletion = { service_log_id: completionLog.id, service_date: completionLog.service_date }
      intervalReading = readingLog ? { service_log_id: readingLog.id, meter_reading: readingLog.meter_reading } : null
      this.state.intervals = this.state.intervals.map((item) => row(item).id === value.interval_id ? completed! : item)
    }
    const reply = { log, reading, interval: completed, interval_completion: intervalCompletion, interval_reading: intervalReading }; return structuredClone(this.mutate.service ? this.mutate.service(reply) : reply)
  }
  async saveTask(farmId: string, value: TaskWrite) { this.calls.task++; const saved = { ...value, farm_id: farmId, completed_by: value.status === 'done' ? actor : null, completed_at: value.status === 'done' ? micro : null, ...audit }; this.state.tasks = [...this.state.tasks.filter((item) => row(item).id !== value.id), saved]; return structuredClone(this.mutate.task ? this.mutate.task(saved) : saved) }
  private deleted(id: string) { this.calls.delete++; const reply = { id }; return structuredClone(this.mutate.delete ? this.mutate.delete(reply) : reply) }
  async deleteTask(_farmId: string, id: string) { this.state.tasks = this.state.tasks.filter((item) => row(item).id !== id); return this.deleted(id) }
  async deleteServiceLogEntry(_farmId: string, id: string) { const readingId = this.readingIds.get(id); this.state.service_log = this.state.service_log.filter((item) => row(item).id !== id); if (readingId) this.state.meter_readings = this.state.meter_readings.filter((item) => row(item).id !== readingId); this.readingIds.delete(id); return this.deleted(id) }
  async deleteInterval(_farmId: string, id: string) { this.state.intervals = this.state.intervals.filter((item) => row(item).id !== id); return this.deleted(id) }
}
function repo(gateway: FakeGateway, fieldsRepository: FieldsRepository = { getData: async () => structuredClone(fieldsFor(farm)), getSnapshot: async (context) => ({ data: structuredClone(fieldsFor(context.farmId)), source: 'live', capturedAt: micro }), saveField: async () => { throw new Error('not used') } }, createId?: () => string) { let next = 900; return new SupabaseEquipmentTasksRepository({ gateway, fieldsRepository, getFarmId: async () => farm, getUserId: async () => actor, getOperationContext: async () => operationContext, verifyOperationContext: async () => undefined, verifySnapshotContext: () => undefined, createId: createId ?? (() => uid(next++)), clock: () => micro }) }
function memory(): StorageLike & { values: Map<string, string>; writes: number } { const values = new Map<string, string>(); const result = { values, writes: 0, getItem: (key: string) => values.get(key) ?? null, setItem: (key: string, value: string) => { values.set(key, value); result.writes += 1 }, removeItem: (key: string) => { values.delete(key); result.writes += 1 } }; return result }
const equipmentWrite = (id = uid(30), name = 'New Truck'): EquipmentWrite => ({ id, farm_id: farm, name, category: 'truck', make: 'Case', model: 'Magnum', model_year: 2020, serial_or_vin: null, purchase_date: null, purchase_price: null, meter_unit: 'miles', warranty_expires_on: null, warranty_notes: null, status: 'active', notes: null })
const meterWrite = (id = uid(20)): MeterReadingWrite => ({ id, equipment_id: machineId, reading: 1200, read_on: '2026-07-12', source: 'manual', notes: null })
const intervalWrite = (id = uid(21)): IntervalWrite => ({ id, equipment_id: machineId, name: 'Grease', every_meter: 50, every_months: null, last_done_on: null, last_done_reading: null, is_active: true })
const serviceWrite = (id = uid(22), withReading = true): ServiceLogEntryInput => ({ id, equipment_id: machineId, service_date: '2026-07-12', work_performed: 'Oil change', parts: null, vendor: null, cost: 50, meter_reading: withReading ? 1200 : null, interval_id: intervalId })
const taskWrite = (id = uid(23), status: TaskWrite['status'] = 'todo'): TaskWrite => ({ id, title: 'Service Truck 7', details: null, status, priority: 'high', assigned_to: actor, due_on: '2026-07-15', field_id: null, equipment_id: machineId, source: 'manual', interval_id: null, interval_cycle_key: null, program_assigned_pass_id: null, program_cycle_key: null })

function existingReadonlyDatabase(databaseName: string, values: Map<string, unknown>) {
  const prior = Object.getOwnPropertyDescriptor(globalThis, 'indexedDB'); const calls = { open: 0, upgrade: 0 }
  const database = { objectStoreNames: { contains: (name: string) => name === 'workspaces' }, transaction: () => ({ objectStore: () => ({ get: (key: string) => { const request: { result?: unknown; onsuccess?: () => void; onerror?: () => void } = {}; queueMicrotask(() => { request.result = values.get(key); request.onsuccess?.() }); return request } }) }), close: () => undefined }
  const factory = { databases: async () => [{ name: databaseName }], open: (name: string) => { calls.open += 1; const request: { result?: typeof database; onsuccess?: () => void; onerror?: () => void; onblocked?: () => void; onupgradeneeded?: () => void } = {}; queueMicrotask(() => { if (name !== databaseName) { request.onerror?.(); return }; request.result = database; request.onsuccess?.() }); return request } }
  Object.defineProperty(globalThis, 'indexedDB', { configurable: true, value: factory })
  return { calls, restore: () => { if (prior) Object.defineProperty(globalThis, 'indexedDB', prior); else Reflect.deleteProperty(globalThis, 'indexedDB') } }
}
class VersionedGateway extends FakeGateway {
  private version = 1
  readonly expected = { equipment: [] as Array<string | null | undefined>, interval: [] as Array<string | null | undefined>, task: [] as Array<string | null | undefined> }
  readonly savedVersions = { equipment: [] as string[], interval: [] as string[], task: [] as string[] }
  private nextVersion() { return `2026-07-12T12:31:${String(this.version++).padStart(2, '0')}.000000+00:00` }
  private assertExpected(group: 'equipment' | 'interval' | 'task', value: { id: string; expected_updated_at?: string | null }, rows: unknown[]) {
    const expected = value.expected_updated_at; this.expected[group].push(expected)
    const current = rows.find((item) => row(item).id === value.id); assert(current && expected === row(current).updated_at, `${group} replay used a stale expected version.`)
  }
  private publish<T>(group: 'equipment' | 'interval' | 'task', saved: T, rows: unknown[]): T { const version = this.nextVersion(); row(saved).updated_at = version; const current = rows.find((item) => row(item).id === row(saved).id); if (current) row(current).updated_at = version; this.savedVersions[group].push(version); return saved }
  override async saveEquipment(farmId: string, value: EquipmentWrite) { this.assertExpected('equipment', value, this.state.equipment); return this.publish('equipment', await super.saveEquipment(farmId, value), this.state.equipment) }
  override async saveInterval(farmId: string, value: IntervalWrite) { this.assertExpected('interval', value, this.state.intervals); return this.publish('interval', await super.saveInterval(farmId, value), this.state.intervals) }
  override async saveTask(farmId: string, value: TaskWrite) { this.assertExpected('task', value, this.state.tasks); return this.publish('task', await super.saveTask(farmId, value), this.state.tasks) }
}

async function run() {
  const gateway = new FakeGateway(); const live = repo(gateway); const workspace = await live.getWorkspace()
  assert(workspace.viewer.user_id === actor && workspace.viewer.role === 'owner' && workspace.equipment[0].created_at === micro, 'Workspace loading must map the signed-in viewer and microsecond offset audit stamps.')
  assert(gateway.calls.generated === 0, 'A normal Equipment/Tasks read generated due work.'); await live.generateDueTasks(); assert(Number(gateway.calls.generated) === 1, 'The explicit authorized due-generation path did not reach the gateway exactly once.')
  let falseIds = 0; const falseDueGateway = new FakeGateway(); falseDueGateway.dueStatus = { has_due: false, task_needed: false, notification_needed: false, local_date: '2026-07-12' }; await repo(falseDueGateway, undefined, () => { falseIds += 1; return uid(890) }).generateDueTasks(); assert(falseIds === 0 && falseDueGateway.calls.generated === 0, 'A false Equipment preflight allocated an ID or invoked v2 generation.')
  let trueIds = 0; const trueDueGateway = new FakeGateway(); await repo(trueDueGateway, undefined, () => { trueIds += 1; return uid(891) }).generateDueTasks(); assert(trueIds === 1 && trueDueGateway.dueV2Inputs.length === 1 && trueDueGateway.dueV2Inputs[0]?.farmId === farm && trueDueGateway.dueV2Inputs[0]?.operationId === uid(891), 'A true Equipment preflight did not invoke v2 exactly once with the allocated ID.')
  const malformedDueGateway = new FakeGateway(); malformedDueGateway.dueStatus = { has_due: false, task_needed: true, notification_needed: false, local_date: '2026-07-12' }; await rejects(() => repo(malformedDueGateway).generateDueTasks(), 'Malformed Equipment status did not fail closed.'); assert(malformedDueGateway.calls.generated === 0, 'Malformed Equipment status reached v2 generation.')
  for (const malformedReceipt of [null, { operation_kind: 'wrong', task_created_count: 1, notification_created_count: 1, local_date: '2026-07-12' }, { operation_kind: 'generate_due_service_tasks_v2', task_created_count: 1.5, notification_created_count: 1, local_date: '2026-07-12' }, { operation_kind: 'generate_due_service_tasks_v2', task_created_count: 1, notification_created_count: 1, local_date: '2026-02-30' }]) { const failed = new FakeGateway(); failed.dueV2Result = malformedReceipt; await rejects(() => repo(failed).generateDueTasks(), 'Malformed Equipment v2 receipt did not fail closed.') }
  for (const stage of ['status', 'v2'] as const) { const failed = new FakeGateway(); if (stage === 'status') failed.dueStatusFailure = new Error('status offline'); else failed.dueV2Failure = new Error('v2 offline'); await rejects(() => repo(failed).generateDueTasks(), `Equipment ${stage} failure was swallowed.`) }
  for (const failVerifyCall of [2, 3, 4, 5]) { const switched = new FakeGateway(); let verifies = 0; let ids = 0; const fieldsRepository: FieldsRepository = { getData: async () => fieldsFor(farm), saveField: async () => { throw new Error('not used') } }; const repository = new SupabaseEquipmentTasksRepository({ gateway: switched, fieldsRepository, getFarmId: async () => farm, getUserId: async () => actor, getOperationContext: async () => operationContext, verifyOperationContext: async () => { verifies += 1; if (verifies === failVerifyCall) throw new Error('context switched') }, verifySnapshotContext: () => undefined, createId: () => { ids += 1; return uid(892) }, clock: () => micro }); await rejects(() => repository.generateDueTasks(), `Equipment context switch at verification ${failVerifyCall} was accepted.`); assert(failVerifyCall < 5 ? ids === 0 && switched.calls.generated === 0 : ids === 1 && switched.calls.generated === 1, `Equipment context switch at verification ${failVerifyCall} crossed the wrong stage.`) }
  const generatedBeforeSnapshot = gateway.calls.generated; const mutationsBeforeSnapshot = gateway.calls.equipment + gateway.calls.meter + gateway.calls.interval + gateway.calls.service + gateway.calls.task + gateway.calls.delete
  const pureWorkspace = await live.getSnapshot(operationContext)
  assert(pureWorkspace.data.tasks.length === workspace.tasks.length && pureWorkspace.source === 'live' && gateway.calls.generated === generatedBeforeSnapshot, 'The live Equipment/Tasks snapshot generated due tasks or changed the loaded workspace.')
  assert(gateway.calls.equipment + gateway.calls.meter + gateway.calls.interval + gateway.calls.service + gateway.calls.task + gateway.calls.delete === mutationsBeforeSnapshot, 'The live Equipment/Tasks snapshot invoked a mutation gateway.')
  const noPureFields: FieldsRepository = { getData: async () => fieldsFor(farm), saveField: async () => { throw new Error('not used') } }; const refusedGateway = new FakeGateway()
  await rejects(() => repo(refusedGateway, noPureFields).getSnapshot(operationContext), 'Equipment/Tasks snapshot fell back to mutation-capable Fields getData().')
  assert(refusedGateway.calls.load === 0 && refusedGateway.calls.generated === 0, 'Equipment/Tasks queried or generated data after its pure Fields dependency was refused.')

  const snapshotRef = 'snapshot-equipment'; const snapshotStore = memory(); resetFarmGrantFromLive(snapshotStore, { projectRef: snapshotRef, userId: actor, farmId: farm }, 1, micro); const snapshotContext = captureFarmRevocationFence(snapshotStore, { projectRef: snapshotRef, userId: actor, farmId: farm })
  const snapshotGateway = new FakeGateway(); let snapshotIds = 0; let snapshotContextResolutions = 0; const snapshotQueued = new QueuedEquipmentTasksRepository(repo(snapshotGateway), { getContext: async () => { snapshotContextResolutions += 1; throw new Error('pure snapshot resolved mutable farm access') }, projectRef: snapshotRef, storage: snapshotStore, createId: () => { snapshotIds += 1; return uid(800 + snapshotIds) }, clock: () => micro, isOffline: () => false })
  const pendingTask = taskWrite(uid(81)); new EquipmentTasksWriteQueue(snapshotStore, equipmentTasksWriteQueueKey(snapshotRef, actor, farm)).append({ version: 1, module: 'equipment_tasks', kind: 'saveTask', operationId: uid(810), userId: actor, farmId: farm, enqueuedAt: micro, value: pendingTask })
  const writesBeforeQueuedSnapshot = snapshotStore.writes; const idsBeforeQueuedSnapshot = snapshotIds; const syncBeforeQueuedSnapshot = getSyncStatus(); const queuedSnapshot = await snapshotQueued.getSnapshot(snapshotContext)
  assert(queuedSnapshot.data.tasks.filter((item) => item.id === pendingTask.id && item.title === pendingTask.title).length === 1, 'Queued Equipment/Tasks work did not appear exactly once in the pure snapshot.')
  assert(snapshotContextResolutions === 0 && snapshotGateway.calls.generated === 0 && snapshotStore.writes === writesBeforeQueuedSnapshot && snapshotIds === idsBeforeQueuedSnapshot && getSyncStatus() === syncBeforeQueuedSnapshot, 'Queued Equipment/Tasks snapshot resolved access, generated work, wrote storage, created an ID, or changed sync status.')
  const pureStateRef = 'snapshot-equipment-state'; const pureStateStore = memory(); resetFarmGrantFromLive(pureStateStore, { projectRef: pureStateRef, userId: actor, farmId: farm }, 1, micro); const pureStateContext = captureFarmRevocationFence(pureStateStore, { projectRef: pureStateRef, userId: actor, farmId: farm }); const pureStateGateway = new FakeGateway(); let pureStateId = 825
  const pureStateRepository = new QueuedEquipmentTasksRepository(repo(pureStateGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: pureStateRef, storage: pureStateStore, createId: () => uid(pureStateId++), clock: () => micro, isOffline: () => false })
  await pureStateRepository.getSnapshot(pureStateContext); pureStateGateway.fail = true
  await rejects(() => pureStateRepository.getWorkspace(), 'A pure Equipment snapshot changed the later normal-read fallback state.')

  const offlineStore = memory(); resetFarmGrantFromLive(offlineStore, { projectRef: snapshotRef, userId: actor, farmId: farm }, 1, micro); const offlineContext = captureFarmRevocationFence(offlineStore, { projectRef: snapshotRef, userId: actor, farmId: farm }); const offlineGateway = new FakeGateway(); let offlineMode = false; let offlineIds = 0; let offlineNow = micro
  const offlineQueued = new QueuedEquipmentTasksRepository(repo(offlineGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: snapshotRef, storage: offlineStore, createId: () => { offlineIds += 1; return uid(850 + offlineIds) }, clock: () => offlineNow, isOffline: () => offlineMode })
  await offlineQueued.getWorkspace(); await new Promise((resolve) => setTimeout(resolve, 0))
  const offlineTask = taskWrite(uid(82)); new EquipmentTasksWriteQueue(offlineStore, equipmentTasksWriteQueueKey(snapshotRef, actor, farm)).append({ version: 1, module: 'equipment_tasks', kind: 'saveTask', operationId: uid(820), userId: actor, farmId: farm, enqueuedAt: micro, value: offlineTask })
  offlineMode = true; offlineGateway.fail = true
  const writesBeforeOfflineSnapshot = offlineStore.writes; const idsBeforeOfflineSnapshot = offlineIds; const syncBeforeOfflineSnapshot = getSyncStatus(); const offlineSnapshot = await offlineQueued.getSnapshot(offlineContext)
  assert(offlineSnapshot.source === 'offline' && offlineSnapshot.data.tasks.filter((item) => item.id === offlineTask.id).length === 1, 'Offline Equipment/Tasks snapshot did not retain and overlay queued work exactly once.')
  assert(offlineStore.writes === writesBeforeOfflineSnapshot && offlineIds === idsBeforeOfflineSnapshot && getSyncStatus() === syncBeforeOfflineSnapshot, 'Offline Equipment/Tasks snapshot wrote storage, created an ID, or changed sync status.')
  offlineNow = '2026-07-20T12:30:01.000000+00:00'; await rejects(() => offlineQueued.getSnapshot(offlineContext), 'An expired retained Equipment/Tasks snapshot was shown as current offline data.')

  const rollbackRef = 'snapshot-equipment-rollback'; const rollbackStore = memory(); resetFarmGrantFromLive(rollbackStore, { projectRef: rollbackRef, userId: actor, farmId: farm }, 1, micro); const rollbackContext = captureFarmRevocationFence(rollbackStore, { projectRef: rollbackRef, userId: actor, farmId: farm }); const rollbackGateway = new FakeGateway(); let rollbackOffline = false; let rollbackNow = micro
  let rollbackId = 875; const rollbackRepository = new QueuedEquipmentTasksRepository(repo(rollbackGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: rollbackRef, storage: rollbackStore, createId: () => uid(rollbackId++), clock: () => rollbackNow, isOffline: () => rollbackOffline })
  await rollbackRepository.getWorkspace(); rollbackGateway.fail = true; rollbackOffline = true; rollbackNow = '2026-07-18T12:30:00.000000+00:00'; assert((await rollbackRepository.getSnapshot(rollbackContext)).source === 'offline', 'A day-six retained Equipment snapshot was not available offline.'); observeDeviceTime(rollbackStore, { projectRef: rollbackRef, userId: actor }, rollbackNow); const rollbackWrites = rollbackStore.writes; rollbackNow = micro; await rejects(() => rollbackRepository.getSnapshot(rollbackContext), 'Clock rollback extended a retained Equipment snapshot.'); assert(rollbackStore.writes === rollbackWrites, 'Clock rollback changed storage from the pure Equipment snapshot path.')

  const missingFenceStore = memory(); const missingFenceQueued = new QueuedEquipmentTasksRepository(repo(new FakeGateway()), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: snapshotRef, storage: missingFenceStore, createId: () => { throw new Error('snapshot created an ID') }, clock: () => micro, isOffline: () => false })
  await rejects(() => missingFenceQueued.getSnapshot({ projectRef: snapshotRef, userId: actor, farmId: farm, generation: 1, token: uid(997), serverEpoch: 1 }), 'Equipment/Tasks snapshot initialized a missing fence instead of failing closed.')
  assert(missingFenceStore.writes === 0, 'Equipment/Tasks snapshot wrote missing fence or queue metadata.')

  const invalidOverlayRef = 'snapshot-equipment-invalid'; const invalidOverlayStore = memory(); resetFarmGrantFromLive(invalidOverlayStore, { projectRef: invalidOverlayRef, userId: actor, farmId: farm }, 1, micro); const invalidOverlayContext = captureFarmRevocationFence(invalidOverlayStore, { projectRef: invalidOverlayRef, userId: actor, farmId: farm })
  new EquipmentTasksWriteQueue(invalidOverlayStore, equipmentTasksWriteQueueKey(invalidOverlayRef, actor, farm)).append({ version: 1, module: 'equipment_tasks', kind: 'saveTask', operationId: uid(811), userId: actor, farmId: farm, enqueuedAt: micro, value: { ...taskWrite(uid(83)), field_id: uid(997) } })
  const invalidOverlay = new QueuedEquipmentTasksRepository(repo(new FakeGateway()), { getContext: async () => { throw new Error('snapshot resolved context') }, projectRef: invalidOverlayRef, storage: invalidOverlayStore, createId: () => { throw new Error('snapshot created an ID') }, clock: () => micro, isOffline: () => false })
  await rejects(() => invalidOverlay.getSnapshot(invalidOverlayContext), 'A queued task with a dangling field reference survived post-overlay validation.')

  const deleteOverlayRef = 'equipment-delete-overlay'; const deleteOverlayStore = memory(); resetFarmGrantFromLive(deleteOverlayStore, { projectRef: deleteOverlayRef, userId: actor, farmId: farm }, 1, micro); const deleteOverlayGateway = new FakeGateway(); deleteOverlayGateway.state.tasks = [task(uid(835), { source: 'service_interval', interval_id: intervalId, interval_cycle_key: 'meter:100', equipment_id: machineId })]; deleteOverlayGateway.state.service_due = [{ farm_id: farm, equipment_id: machineId, interval_id: intervalId, reason: 'meter', overdue_amount: 1 }]; deleteOverlayGateway.state.service_log = [{ id: uid(836), farm_id: farm, equipment_id: machineId, service_date: '2026-07-12', work_performed: 'Oil', parts: null, vendor: null, cost: null, meter_reading: 900, interval_id: intervalId, ...audit }]; let deleteOverlayOffline = false
  const deleteOverlayRepository = new QueuedEquipmentTasksRepository(repo(deleteOverlayGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: deleteOverlayRef, storage: deleteOverlayStore, createId: (() => { let value = 837; return () => uid(value++) })(), clock: () => micro, isOffline: () => deleteOverlayOffline }); await deleteOverlayRepository.getWorkspace(); deleteOverlayOffline = true; await deleteOverlayRepository.deleteInterval(intervalId); deleteOverlayGateway.fail = true; const deletedOverlay = await deleteOverlayRepository.getWorkspace(); const normalizedTask = deletedOverlay.tasks[0]
  assert(deletedOverlay.intervals.length === 0 && deletedOverlay.service_due.length === 0 && deletedOverlay.service_log[0]?.interval_id === null && normalizedTask?.source === 'manual' && normalizedTask.interval_id === null && normalizedTask.interval_cycle_key === null, 'Deleting an interval offline left invalid source, cycle, log, or due links in the queued overlay.')

  const coldRef = 'snapshot-equipment-cold'; const coldStore = memory(); resetFarmGrantFromLive(coldStore, { projectRef: coldRef, userId: actor, farmId: farm }, 1, micro); const coldContext = captureFarmRevocationFence(coldStore, { projectRef: coldRef, userId: actor, farmId: farm }); const coldKey = `${coldRef}:${actor}:${farm}:equipment_tasks`; const coldValues = new Map<string, unknown>([[coldKey, { version: 2, key: coldKey, projectRef: coldRef, userId: actor, farmId: farm, module: 'equipment_tasks', generation: coldContext.generation, fenceToken: coldContext.token, serverEpoch: coldContext.serverEpoch, cachedAt: micro, data: workspace }]])
  const fakeIdb = existingReadonlyDatabase(`farm-rx-offline-v1-${coldRef}`, coldValues); const coldNotices = JSON.stringify(getWorkspaceCacheNotices()); const coldWrites = coldStore.writes
  const coldWriter = { getSnapshot: async () => { throw new TypeError('Failed to fetch') } }
  try {
    const freshColdRepository = new QueuedEquipmentTasksRepository(coldWriter as never, { getContext: async () => { throw new Error('cold snapshot resolved context') }, projectRef: coldRef, storage: coldStore, createId: () => { throw new Error('cold snapshot created an ID') }, clock: () => micro, isOffline: () => true })
    const coldSnapshot = await freshColdRepository.getSnapshot(coldContext)
    assert(coldSnapshot.source === 'offline' && coldSnapshot.data.equipment[0]?.id === machineId, 'A fresh Equipment repository did not reopen the existing IndexedDB cache.')
    assert(coldStore.writes === coldWrites && JSON.stringify(getWorkspaceCacheNotices()) === coldNotices && fakeIdb.calls.open === 1 && fakeIdb.calls.upgrade === 0, 'Cold Equipment snapshot created/upgraded storage, published a notice, or wrote device state.')
    const corruptCases: Array<[string, (value: EquipmentTasksWorkspace) => void]> = [
      ['cross-farm row', (value) => { value.equipment[0]!.farm_id = other }],
      ['invalid viewer', (value) => { (value.viewer as unknown as Record<string, unknown>).role = 'admin' }],
      ['duplicate equipment ID', (value) => { value.equipment.push(structuredClone(value.equipment[0]!)) }],
      ['malformed equipment row', (value) => { (value.equipment[0] as unknown as Record<string, unknown>).name = 42 }],
      ['equipment numeric scale overflow', (value) => { value.equipment[0]!.purchase_price = 1.001 }],
      ['equipment numeric range overflow', (value) => { value.equipment[0]!.purchase_price = 100_000_000_000_000 }],
      ['dangling equipment', (value) => { value.intervals[0]!.equipment_id = uid(990) }],
      ['dangling interval', (value) => { value.service_log.push({ id: uid(991), farm_id: farm, equipment_id: machineId, service_date: '2026-07-10', work_performed: 'Oil', parts: null, vendor: null, cost: null, meter_reading: null, interval_id: uid(992), ...audit }) }],
      ['dangling member', (value) => { value.tasks[0]!.assigned_to = uid(993) }],
      ['malformed meter row', (value) => { value.meter_readings.push({ id: uid(930), farm_id: farm, equipment_id: machineId, reading: -1, read_on: '2026-07-12', source: 'manual', notes: null, ...audit }) }],
      ['duplicate meter ID', (value) => { const reading = { id: uid(931), farm_id: farm, equipment_id: machineId, reading: 10, read_on: '2026-07-12', source: 'manual' as const, notes: null, ...audit }; value.meter_readings.push(reading, structuredClone(reading)) }],
      ['nonboolean interval state', (value) => { (value.intervals[0] as unknown as Record<string, unknown>).is_active = 'false' }],
      ['duplicate interval ID', (value) => { value.intervals.push(structuredClone(value.intervals[0]!)) }],
      ['malformed service log row', (value) => { value.service_log.push({ id: uid(932), farm_id: farm, equipment_id: machineId, service_date: '2026-02-31', work_performed: 'Oil', parts: null, vendor: null, cost: null, meter_reading: null, interval_id: null, ...audit }) }],
      ['duplicate service log ID', (value) => { const log = { id: uid(933), farm_id: farm, equipment_id: machineId, service_date: '2026-07-12', work_performed: 'Oil', parts: null, vendor: null, cost: null, meter_reading: null, interval_id: null, ...audit }; value.service_log.push(log, structuredClone(log)) }],
      ['forged manual task linkage', (value) => { value.tasks[0]!.interval_id = intervalId; value.tasks[0]!.interval_cycle_key = 'forged' }],
      ['duplicate task ID', (value) => { value.tasks.push(structuredClone(value.tasks[0]!)) }],
      ['duplicate member', (value) => { value.members.push(structuredClone(value.members[0]!)) }],
      ['invalid due row', (value) => { value.service_due.push({ farm_id: farm, equipment_id: machineId, interval_id: intervalId, reason: 'surprise' as never, overdue_amount: 1 }) }],
      ['duplicate due row', (value) => { const due = { farm_id: farm, equipment_id: machineId, interval_id: intervalId, reason: 'meter' as const, overdue_amount: 1 }; value.service_due.push(due, structuredClone(due)) }],
      ['dangling due interval', (value) => { value.service_due.push({ farm_id: farm, equipment_id: machineId, interval_id: uid(934), reason: 'meter', overdue_amount: 1 }) }],
      ['malformed nested Fields row', (value) => { (value.fields.fields[0] as unknown as Record<string, unknown>).name = 42 }],
      ['duplicate nested Fields ID', (value) => { value.fields.fields.push(structuredClone(value.fields.fields[0]!)) }],
      ['dangling nested Fields entity', (value) => { value.fields.fields[0]!.operating_entity_id = uid(994) }],
      ['invalid nested Fields arrangement', (value) => { value.fields.arrangements[0]!.cash_rent_per_acre = 250 }],
      ['nested crop acres over field acres', (value) => { const crop = value.fields.crop_assignments[0]!; crop.planted_acres = value.fields.fields.find((field) => field.id === crop.field_id)!.total_acres + 1 }],
    ]
    for (const [label, mutate] of corruptCases) {
      const corrupt = structuredClone(workspace); mutate(corrupt); coldValues.set(coldKey, { version: 2, key: coldKey, projectRef: coldRef, userId: actor, farmId: farm, module: 'equipment_tasks', generation: coldContext.generation, fenceToken: coldContext.token, serverEpoch: coldContext.serverEpoch, cachedAt: micro, data: corrupt })
      const freshCorruptRepository = new QueuedEquipmentTasksRepository(coldWriter as never, { getContext: async () => { throw new Error('corrupt snapshot resolved context') }, projectRef: coldRef, storage: coldStore, createId: () => { throw new Error('corrupt snapshot created an ID') }, clock: () => micro, isOffline: () => true })
      await rejects(() => freshCorruptRepository.getSnapshot(coldContext), `An Equipment cache with a ${label} survived fresh-instance validation.`)
    }
    assert(coldStore.writes === coldWrites && JSON.stringify(getWorkspaceCacheNotices()) === coldNotices && fakeIdb.calls.upgrade === 0, 'Corrupt cold Equipment cases wrote storage, published notices, or upgraded IndexedDB.')
  } finally { fakeIdb.restore() }

  const delayedGateway = new FakeGateway(); let releaseLoad!: () => void; let loadStarted!: () => void; const loadIsStarted = new Promise<void>((resolve) => { loadStarted = resolve }); delayedGateway.beforeLoad = () => { loadStarted(); return new Promise<void>((resolve) => { releaseLoad = resolve }) }
  let currentContext = operationContext; const delayedRepository = new SupabaseEquipmentTasksRepository({ gateway: delayedGateway, fieldsRepository: { getData: async () => fieldsFor(farm), getSnapshot: async () => ({ data: fieldsFor(farm), source: 'live', capturedAt: micro }), saveField: async () => { throw new Error('not used') } }, getFarmId: async () => currentContext.farmId, getUserId: async () => currentContext.userId, getOperationContext: async () => currentContext, verifyOperationContext: async (expected) => { if (expected !== currentContext) throw new Error('context changed') }, verifySnapshotContext: (expected) => { if (expected !== currentContext) throw new Error('context changed') }, createId: () => { throw new Error('snapshot created an ID') }, clock: () => micro })
  const delayedEquipmentSnapshot = delayedRepository.getSnapshot(currentContext); await loadIsStarted; currentContext = { ...operationContext, farmId: other, token: uid(998) }; releaseLoad()
  await rejects(() => delayedEquipmentSnapshot, 'A delayed Equipment/Tasks snapshot published after the farm context changed.')
  gateway.fail = true; await rejects(() => live.getWorkspace(), 'A gateway failure must reject without a fallback.'); gateway.fail = false
  const foreign = canonicalFixture(); row(foreign.equipment[0]).farm_id = other; gateway.state = foreign; await rejects(() => live.getWorkspace(), 'Cross-farm equipment rows must fail closed.'); gateway.state = canonicalFixture()
  const badMember = canonicalFixture(); row(badMember.members[0]).display_name = ''; gateway.state = badMember; await rejects(() => live.getWorkspace(), 'Invalid member-name rows must fail closed.'); gateway.state = canonicalFixture()
  const badViewer = canonicalFixture(); badViewer.viewer = { role: 'admin' }; gateway.state = badViewer; await rejects(() => live.getWorkspace(), 'Unknown viewer roles must fail closed.'); gateway.state = canonicalFixture()
  const calendarIntervalId = uid(13); const dueRows = canonicalFixture(); dueRows.intervals.push(interval(calendarIntervalId, { every_meter: null, every_months: 6 })); dueRows.service_due = [{ farm_id: farm, equipment_id: machineId, interval_id: intervalId, reason: 'meter', overdue_amount: 10 }, { farm_id: farm, equipment_id: machineId, interval_id: calendarIntervalId, reason: 'calendar', overdue_amount: 2 }]; gateway.state = dueRows; await live.getWorkspace()
  const wrongDueRule = structuredClone(dueRows); row(wrongDueRule.service_due[0]).interval_id = calendarIntervalId; gateway.state = wrongDueRule; await rejects(() => live.getWorkspace(), 'A meter-due row without a meter interval was accepted.')
  const inactiveDueInterval = structuredClone(dueRows); row(inactiveDueInterval.intervals[0]).is_active = false; gateway.state = inactiveDueInterval; await rejects(() => live.getWorkspace(), 'A due row for an inactive interval was accepted.')
  const inactiveDueMachine = structuredClone(dueRows); row(inactiveDueMachine.equipment[0]).status = 'retired'; gateway.state = inactiveDueMachine; await rejects(() => live.getWorkspace(), 'A due row for an inactive machine was accepted.')
  row(dueRows.service_due[0]).reason = 'surprise'; gateway.state = dueRows; await rejects(() => live.getWorkspace(), 'Unknown due-view reasons must fail closed.'); gateway.state = canonicalFixture()
  const titleCaps = canonicalFixture(); row(titleCaps.tasks[0]).title = 'x'.repeat(500); gateway.state = titleCaps; await live.getWorkspace(); row(titleCaps.tasks[0]).title = 'x'.repeat(501); await rejects(() => live.getWorkspace(), 'A 501-character task title must fail closed after accepting the DB-legal 500-character title.'); gateway.state = canonicalFixture()
  const oversizedWorkspaceCases: Array<[string, (value: EquipmentTasksRowBundle) => void]> = [
    ['purchase price', (value) => { row(value.equipment[0]).purchase_price = 100_000_000_000_000 }],
    ['meter reading', (value) => { value.meter_readings = [{ id: uid(880), farm_id: farm, equipment_id: machineId, reading: 10_000_000_000_000_000, read_on: '2026-07-12', source: 'manual', notes: null, ...audit }] }],
    ['service interval meter', (value) => { row(value.intervals[0]).every_meter = 10_000_000_000_000_000 }],
    ['service cost', (value) => { value.service_log = [{ id: uid(881), farm_id: farm, equipment_id: machineId, service_date: '2026-07-12', work_performed: 'Oil', parts: null, vendor: null, cost: 100_000_000_000_000, meter_reading: null, interval_id: null, ...audit }] }],
  ]
  for (const [label, mutate] of oversizedWorkspaceCases) { const invalid = canonicalFixture(); mutate(invalid); gateway.state = invalid; await rejects(() => live.getWorkspace(), `An out-of-range live ${label} was accepted.`) }
  const orphanService = canonicalFixture(); orphanService.tasks = [task(uid(882), { source: 'service_interval', interval_id: null, interval_cycle_key: 'deleted-interval-history' })]; gateway.state = orphanService; assert((await live.getWorkspace()).tasks[0]?.source === 'manual', 'A DB-legal service task orphaned by interval deletion was not safely canonicalized to manual history.')
  const orphanScouting = canonicalFixture(); orphanScouting.tasks = [task(uid(884), { source: 'scouting', field_id: null })]; gateway.state = orphanScouting; assert((await live.getWorkspace()).tasks[0]?.source === 'manual', 'A DB-legal scouting task orphaned by field deletion was not safely canonicalized to manual history.')
  const orphanProgram = canonicalFixture(); orphanProgram.tasks = [task(uid(885), { source: 'program', program_assigned_pass_id: null, program_cycle_key: 'deleted-program-history' })]; gateway.state = orphanProgram; assert((await live.getWorkspace()).tasks[0]?.source === 'manual', 'A DB-legal Program task orphaned by pass deletion was not safely canonicalized to manual history.')
  const malformedService = canonicalFixture(); malformedService.tasks = [task(uid(883), { source: 'service_interval', interval_id: intervalId, interval_cycle_key: null })]; gateway.state = malformedService; await rejects(() => live.getWorkspace(), 'A linked service task without its cycle key was accepted.'); gateway.state = canonicalFixture()
  const nonbooleanInterval = canonicalFixture(); row(nonbooleanInterval.intervals[0]).is_active = 'false'; gateway.state = nonbooleanInterval; await rejects(() => live.getWorkspace(), 'A nonboolean live interval state was accepted.'); gateway.state = canonicalFixture()

  await live.saveEquipment(equipmentWrite(uid(30), 'New Truck'))
  await live.addMeterReading(meterWrite(uid(31))); await live.saveInterval(intervalWrite(uid(32))); await live.addServiceLogEntry(serviceWrite(uid(33))); await live.saveTask(taskWrite(uid(34))); await live.deleteTask(uid(34)); await live.deleteServiceLogEntry(uid(33)); await live.deleteInterval(uid(32))
  assert(gateway.calls.equipment === 1 && gateway.calls.meter === 1 && gateway.calls.interval === 1 && gateway.calls.service === 1 && gateway.calls.task === 1 && gateway.calls.delete === 3, 'Every equipment/tasks operation must reach the canonical fake gateway end-to-end.')
  const badEquipment = new FakeGateway(); badEquipment.mutate.equipment = (reply) => ({ ...row(reply), name: 'Wrong' }); await rejects(() => repo(badEquipment).saveEquipment(equipmentWrite(uid(40))), 'Wrong equipment echoes must be rejected.')
  const badMeter = new FakeGateway(); badMeter.mutate.meter = (reply) => ({ ...row(reply), reading: 999 }); await rejects(() => repo(badMeter).addMeterReading(meterWrite(uid(41))), 'Wrong meter echoes must be rejected.')
  const badInterval = new FakeGateway(); badInterval.mutate.interval = (reply) => ({ ...row(reply), name: 'Wrong' }); await rejects(() => repo(badInterval).saveInterval(intervalWrite(uid(42))), 'Wrong interval echoes must be rejected.')
  const badService = new FakeGateway(); badService.mutate.service = (reply) => ({ ...row(reply), log: { ...row(row(reply).log), cost: 99 } }); await rejects(() => repo(badService).addServiceLogEntry(serviceWrite(uid(43))), 'Wrong service-log echoes must be rejected.')
  const badTask = new FakeGateway(); badTask.mutate.task = (reply) => ({ ...row(reply), title: 'Wrong' }); await rejects(() => repo(badTask).saveTask(taskWrite(uid(44))), 'Wrong task echoes must be rejected.')
  for (const kind of ['deleteTask', 'deleteServiceLogEntry', 'deleteInterval'] as const) { const badDelete = new FakeGateway(); badDelete.mutate.delete = () => ({ id: uid(999) }); await rejects(() => repo(badDelete)[kind](uid(45)), `Wrong ${kind} echoes must be rejected.`) }

  const calendarGateway = new FakeGateway(); calendarGateway.state.intervals = [interval(intervalId, { every_meter: null, every_months: 6, last_done_reading: 875 })]; await repo(calendarGateway).addServiceLogEntry(serviceWrite(uid(50), false)); assert(calendarGateway.calls.service === 1, 'A calendar service completion without a new reading must accept the retained interval reading.')
  const wrongReading = new FakeGateway(); wrongReading.mutate.service = (reply) => ({ ...row(reply), reading: { ...row(row(reply).reading), id: uid(999) } }); await rejects(() => repo(wrongReading).addServiceLogEntry(serviceWrite(uid(51))), 'A service-reading echo with the wrong stable ID must be rejected.')
  const wrongStamp = new FakeGateway(); wrongStamp.mutate.service = (reply) => ({ ...row(reply), interval: { ...row(row(reply).interval), last_done_on: '2026-07-11' } }); await rejects(() => repo(wrongStamp).addServiceLogEntry(serviceWrite(uid(52))), 'A wrong interval-completion stamp echo must be rejected.')
  const wrongCompletionEvidence = new FakeGateway(); wrongCompletionEvidence.mutate.service = (reply) => ({ ...row(reply), interval_completion: { ...row(row(reply).interval_completion), service_date: '2026-07-11' } }); await rejects(() => repo(wrongCompletionEvidence).addServiceLogEntry(serviceWrite(uid(521))), 'Interval completion evidence older than the submitted service was accepted.')
  const wrongReadingEvidence = new FakeGateway(); wrongReadingEvidence.mutate.service = (reply) => ({ ...row(reply), interval_reading: { ...row(row(reply).interval_reading), meter_reading: 1199 } }); await rejects(() => repo(wrongReadingEvidence).addServiceLogEntry(serviceWrite(uid(522))), 'Interval meter evidence that disagreed with the canonical interval was accepted.')
  const nestedServiceEchoCases: Array<[string, (reply: unknown) => unknown]> = [
    ['reading farm', (reply) => ({ ...row(reply), reading: { ...row(row(reply).reading), farm_id: other } })],
    ['reading equipment', (reply) => ({ ...row(reply), reading: { ...row(row(reply).reading), equipment_id: other } })],
    ['reading date', (reply) => ({ ...row(reply), reading: { ...row(row(reply).reading), read_on: '2026-07-11' } })],
    ['reading source', (reply) => ({ ...row(reply), reading: { ...row(row(reply).reading), source: 'manual' } })],
    ['reading notes', (reply) => ({ ...row(reply), reading: { ...row(row(reply).reading), notes: 'unexpected' } })],
    ['interval farm', (reply) => ({ ...row(reply), interval: { ...row(row(reply).interval), farm_id: other } })],
    ['interval equipment', (reply) => ({ ...row(reply), interval: { ...row(row(reply).interval), equipment_id: other } })],
    ['interval reading', (reply) => ({ ...row(reply), interval: { ...row(row(reply).interval), last_done_reading: 1199 } })],
  ]
  for (const [label, mutate] of nestedServiceEchoCases) { const corrupt = new FakeGateway(); corrupt.mutate.service = mutate; await rejects(() => repo(corrupt).addServiceLogEntry(serviceWrite(uid(520))), `A wrong ${label} echo must be rejected.`) }
  const backdated = new FakeGateway(); const backdatedRepo = repo(backdated); await backdatedRepo.addServiceLogEntry({ ...serviceWrite(uid(523)), service_date: '2026-07-16', meter_reading: 1300 }); await backdatedRepo.addServiceLogEntry({ ...serviceWrite(uid(524)), service_date: '2026-07-15', meter_reading: 1200 }); const backdatedInterval = row(backdated.state.intervals[0]); assert(backdatedInterval.last_done_on === '2026-07-16' && backdatedInterval.last_done_reading === 1300 && backdated.state.service_log.length === 2, 'A backdated offline service replay moved the interval completion backward or lost history.')
  await backdatedRepo.addServiceLogEntry({ ...serviceWrite(uid(525), false), service_date: '2026-07-17' }); const calendarAfterMeter = row(backdated.state.intervals[0]); assert(calendarAfterMeter.last_done_on === '2026-07-17' && calendarAfterMeter.last_done_reading === 1300, 'A later calendar service erased the latest canonical meter completion.')
  await live.addMeterReading({ ...meterWrite(uid(53)), reading: 20 }); assert(Number(gateway.calls.meter) === 2, 'Backwards meter readings remain accepted by the client contract.')
  const missingCompletion = new FakeGateway(); missingCompletion.mutate.task = (reply) => ({ ...row(reply), completed_by: null, completed_at: null }); await rejects(() => repo(missingCompletion).saveTask(taskWrite(uid(54), 'done')), 'Done-task echoes without server completion stamps must fail closed.')
  await live.saveTask(taskWrite(uid(55), 'done')); await live.saveTask(taskWrite(uid(55), 'todo')); assert(row(gateway.state.tasks.find((item) => row(item).id === uid(55))).completed_by === null, 'Reopen echoes with completion stamps cleared must be accepted.')

  const store = memory(); const key = equipmentTasksWriteQueueKey('project', actor, farm); const queue = new EquipmentTasksWriteQueue(store, key); let offline = true; let next = 600; const replayGateway = new FakeGateway(); const replayLive = repo(replayGateway); const queued = new QueuedEquipmentTasksRepository(replayLive, { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: 'project', storage: store, createId: () => uid(next++), clock: () => micro, isOffline: () => offline })
  assert(await queued.getNeedsAttentionQueueKey() === key, 'Equipment attention discovery must use only the exact current user and farm queue key.')
  await queued.addServiceLogEntry(serviceWrite(uid(60))); const serviceEntry = queue.read().entries[0] as Extract<EquipmentTasksQueueEntryV1, { kind: 'addServiceLogEntry' }>; queue.append(serviceEntry); assert(queue.read().entries.length === 1 && serviceEntry.value.reading_id === uid(601), 'Appending the same Equipment Tasks operationId twice must retain exactly one queue entry.')
  offline = false; await queued.inspectAndReplay(); queue.append(serviceEntry); await queued.inspectAndReplay(); assert(queue.read().entries.length === 0 && replayGateway.calls.service === 2, 'Exact service-log replay must reuse the same reading ID and be idempotent.')

  const atomicDeleteRef = 'equipment-atomic-service-delete'; const atomicDeleteStore = memory(); resetFarmGrantFromLive(atomicDeleteStore, { projectRef: atomicDeleteRef, userId: actor, farmId: farm }, 1, micro); const atomicDeleteGateway = new FakeGateway(); const atomicDeleteLogId = uid(63); await repo(atomicDeleteGateway).addServiceLogEntry(serviceWrite(atomicDeleteLogId)); const atomicDeleteReadingId = String(row(atomicDeleteGateway.state.meter_readings[0]).id); let atomicDeleteOffline = false; let atomicDeleteId = 630
  const atomicDeleteQueue = new EquipmentTasksWriteQueue(atomicDeleteStore, equipmentTasksWriteQueueKey(atomicDeleteRef, actor, farm)); const atomicDeleteRepository = new QueuedEquipmentTasksRepository(repo(atomicDeleteGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: atomicDeleteRef, storage: atomicDeleteStore, createId: () => uid(atomicDeleteId++), clock: () => micro, isOffline: () => atomicDeleteOffline })
  await atomicDeleteRepository.getWorkspace(); atomicDeleteOffline = true; await atomicDeleteRepository.deleteServiceLogEntry(atomicDeleteLogId); atomicDeleteGateway.fail = true
  const pendingAtomicDelete = await atomicDeleteRepository.getWorkspace()
  assert(pendingAtomicDelete.service_log.some((item) => item.id === atomicDeleteLogId) && pendingAtomicDelete.meter_readings.some((item) => item.id === atomicDeleteReadingId) && atomicDeleteQueue.read().entries.length === 1, 'An offline delete hid only part of an already-synced service event before exact server provenance was confirmed.')
  atomicDeleteGateway.fail = false; atomicDeleteOffline = false; await atomicDeleteRepository.inspectAndReplay(); const confirmedAtomicDelete = await atomicDeleteRepository.getWorkspace()
  assert(!confirmedAtomicDelete.service_log.some((item) => item.id === atomicDeleteLogId) && !confirmedAtomicDelete.meter_readings.some((item) => item.id === atomicDeleteReadingId) && atomicDeleteQueue.read().entries.length === 0 && atomicDeleteGateway.calls.delete === 1, 'A confirmed service deletion did not remove the log and its exact meter reading together exactly once.')

  const sameQueueDeleteRef = 'equipment-same-queue-service-delete'; const sameQueueDeleteStore = memory(); resetFarmGrantFromLive(sameQueueDeleteStore, { projectRef: sameQueueDeleteRef, userId: actor, farmId: farm }, 1, micro); const sameQueueDeleteGateway = new FakeGateway(); let sameQueueDeleteOffline = true; let sameQueueDeleteId = 660; const sameQueueDeleteLogId = uid(65)
  const sameQueueDeleteQueue = new EquipmentTasksWriteQueue(sameQueueDeleteStore, equipmentTasksWriteQueueKey(sameQueueDeleteRef, actor, farm)); const sameQueueDeleteRepository = new QueuedEquipmentTasksRepository(repo(sameQueueDeleteGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: sameQueueDeleteRef, storage: sameQueueDeleteStore, createId: () => uid(sameQueueDeleteId++), clock: () => micro, isOffline: () => sameQueueDeleteOffline })
  await sameQueueDeleteRepository.addServiceLogEntry(serviceWrite(sameQueueDeleteLogId)); const sameQueueReadingId = (sameQueueDeleteQueue.read().entries[0] as Extract<EquipmentTasksQueueEntryV1, { kind: 'addServiceLogEntry' }>).value.reading_id!; await sameQueueDeleteRepository.deleteServiceLogEntry(sameQueueDeleteLogId); const hiddenSameQueueDelete = await sameQueueDeleteRepository.getWorkspace()
  assert(!hiddenSameQueueDelete.service_log.some((item) => item.id === sameQueueDeleteLogId) && !hiddenSameQueueDelete.meter_readings.some((item) => item.id === sameQueueReadingId) && sameQueueDeleteQueue.read().entries.length === 2, 'A same-queue add then delete did not hide its exactly known service log and reading atomically.')
  await new Promise<void>((resolve) => setImmediate(resolve)); sameQueueDeleteOffline = false; await sameQueueDeleteRepository.inspectAndReplay()
  assert(sameQueueDeleteQueue.read().entries.length === 0 && !sameQueueDeleteGateway.state.service_log.some((item) => row(item).id === sameQueueDeleteLogId) && !sameQueueDeleteGateway.state.meter_readings.some((item) => row(item).id === sameQueueReadingId) && sameQueueDeleteGateway.calls.delete === 1, 'A same-queue add then delete did not replay both operations and remove only the exact paired reading.')

  const taskEntry: EquipmentTasksQueueEntryV1 = { version: 1, module: 'equipment_tasks', kind: 'saveTask', operationId: uid(610), userId: actor, farmId: farm, enqueuedAt: micro, value: taskWrite(uid(61)) }; queue.append(taskEntry); await queued.inspectAndReplay(); queue.append(taskEntry); await queued.inspectAndReplay(); assert(queue.read().entries.length === 0 && replayGateway.calls.task === 2, 'Exact queued task replay must also be idempotent.')
  const rebaseRef = 'equipment-rebase'; const rebaseStore = memory(); resetFarmGrantFromLive(rebaseStore, { projectRef: rebaseRef, userId: actor, farmId: farm }, 1, micro); let rebaseOffline = false; let rebaseId = 640; const rebaseGateway = new VersionedGateway(); const rebaseQueue = new EquipmentTasksWriteQueue(rebaseStore, equipmentTasksWriteQueueKey(rebaseRef, actor, farm)); const rebaseRepository = new QueuedEquipmentTasksRepository(repo(rebaseGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: rebaseRef, storage: rebaseStore, createId: () => uid(rebaseId++), clock: () => micro, isOffline: () => rebaseOffline })
  await rebaseRepository.getWorkspace(); rebaseOffline = true
  await rebaseRepository.saveEquipment({ ...equipmentWrite(machineId, 'First offline machine edit'), expected_updated_at: micro }); await rebaseRepository.saveEquipment({ ...equipmentWrite(machineId, 'Second offline machine edit'), expected_updated_at: micro })
  await rebaseRepository.saveInterval({ ...intervalWrite(intervalId), name: 'First offline interval edit', expected_updated_at: micro }); await rebaseRepository.saveInterval({ ...intervalWrite(intervalId), name: 'Second offline interval edit', expected_updated_at: micro })
  await rebaseRepository.saveTask({ ...taskWrite(taskId), title: 'First offline task edit', expected_updated_at: micro }); await rebaseRepository.saveTask({ ...taskWrite(taskId), title: 'Second offline task edit', expected_updated_at: micro })
  assert(rebaseQueue.read().entries.length === 6, 'Consecutive offline Equipment edits were not retained FIFO.'); rebaseOffline = false; await rebaseRepository.inspectAndReplay()
  for (const group of ['equipment', 'interval', 'task'] as const) assert(rebaseGateway.expected[group].length === 2 && rebaseGateway.expected[group][0] === micro && rebaseGateway.expected[group][1] === rebaseGateway.savedVersions[group][0], `${group} replay did not rebase the second offline edit to the first confirmed server timestamp.`)
  assert(rebaseQueue.read().entries.length === 0 && readNeedsAttention(rebaseStore, rebaseQueue.key).length === 0, 'Consecutive offline Equipment edits were parked instead of replaying cleanly.')
  const meterConflictStore = memory(); let meterConflictOffline = true; const meterConflictGateway = new FakeGateway(); const meterConflictQueue = new EquipmentTasksWriteQueue(meterConflictStore, equipmentTasksWriteQueueKey('meter-conflict', actor, farm)); const meterConflictRepository = new QueuedEquipmentTasksRepository(repo(meterConflictGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: 'meter-conflict', storage: meterConflictStore, createId: (() => { let id = 620; return () => uid(id++) })(), clock: () => micro, isOffline: () => meterConflictOffline }); await meterConflictRepository.addMeterReading(meterWrite(uid(62))); const meterConflictEntry = meterConflictQueue.read().entries[0]!; meterConflictGateway.persistMeter = false; meterConflictGateway.mutate.meter = (reply) => ({ ...row(reply), id: uid(999) }); meterConflictOffline = false; await meterConflictRepository.inspectAndReplay(); const meterConflictParked = readNeedsAttention(meterConflictStore, meterConflictQueue.key); assert(meterConflictQueue.read().entries.length === 0 && meterConflictParked.length === 1 && meterConflictParked[0]?.id === meterConflictEntry.operationId, 'A conflicting meter-reading replay must drain the queue and park exactly one needs-attention record for that operation.')
  await rejects(async () => parseEquipmentTasksQueue('{bad'), 'Corrupt queue bytes must fail closed.'); await rejects(async () => parseEquipmentTasksQueue('{"version":1,"entries":[{"version":1,"module":"equipment_tasks","kind":"addServiceLogEntry","operationId":"00000000-0000-4000-8000-000000000001","userId":"00000000-0000-4000-8000-000000000002","farmId":"00000000-0000-4000-8000-000000000001","enqueuedAt":"2026-07-12T12:30:00.123456+00:00","value":{}}]}'), 'Service queue entries without a stable reading ID must fail closed.')
  const duplicateGuardStore = memory(); const duplicateGuardQueue = new EquipmentTasksWriteQueue(duplicateGuardStore, equipmentTasksWriteQueueKey('duplicate-guard', actor, farm)); const duplicateOperation = uid(699); duplicateGuardQueue.append({ version: 1, module: 'equipment_tasks', kind: 'saveEquipment', operationId: duplicateOperation, userId: actor, farmId: farm, enqueuedAt: micro, value: equipmentWrite(uid(698)) }); const duplicateBytes = duplicateGuardStore.getItem(duplicateGuardQueue.key); const duplicateWrites = duplicateGuardStore.writes; await rejects(async () => { duplicateGuardQueue.append({ version: 1, module: 'equipment_tasks', kind: 'saveEquipment', operationId: duplicateOperation, userId: actor, farmId: farm, enqueuedAt: micro, value: {} } as never) }, 'A malformed duplicate operation bypassed Equipment queue validation.'); assert(duplicateGuardStore.getItem(duplicateGuardQueue.key) === duplicateBytes && duplicateGuardStore.writes === duplicateWrites, 'A malformed duplicate operation changed Equipment queue bytes before validation.')
  for (const kind of ['saveEquipment', 'addMeterReading', 'saveInterval', 'saveTask'] as const) {
    const malformed = { version: 1, entries: [{ version: 1, module: 'equipment_tasks', kind, operationId: uid(700), userId: actor, farmId: farm, enqueuedAt: micro, value: {} }] }
    await rejects(async () => parseEquipmentTasksQueue(JSON.stringify(malformed)), `Malformed ${kind} queue values must fail closed.`)
  }
  const common = { version: 1 as const, module: 'equipment_tasks' as const, operationId: uid(701), userId: actor, farmId: farm, enqueuedAt: micro }
  const semanticFailures: Array<[string, unknown]> = [
    ['blank equipment name', { ...common, kind: 'saveEquipment', value: { ...equipmentWrite(uid(702)), name: '' } }],
    ['oversized equipment text', { ...common, kind: 'saveEquipment', value: { ...equipmentWrite(uid(703)), make: 'x'.repeat(161) } }],
    ['equipment price range', { ...common, kind: 'saveEquipment', value: { ...equipmentWrite(uid(704)), purchase_price: 100_000_000_000_000 } }],
    ['equipment price scale', { ...common, kind: 'saveEquipment', value: { ...equipmentWrite(uid(705)), purchase_price: 1.001 } }],
    ['cross-farm equipment', { ...common, kind: 'saveEquipment', value: { ...equipmentWrite(uid(706)), farm_id: other } }],
    ['negative meter', { ...common, kind: 'addMeterReading', value: { ...meterWrite(uid(707)), reading: -1 } }],
    ['meter range', { ...common, kind: 'addMeterReading', value: { ...meterWrite(uid(708)), reading: 10_000_000_000_000_000 } }],
    ['meter date', { ...common, kind: 'addMeterReading', value: { ...meterWrite(uid(709)), read_on: '2026-02-31' } }],
    ['missing interval rule', { ...common, kind: 'saveInterval', value: { ...intervalWrite(uid(710)), every_meter: null, every_months: null } }],
    ['interval meter range', { ...common, kind: 'saveInterval', value: { ...intervalWrite(uid(711)), every_meter: 10_000_000_000_000_000 } }],
    ['interval month range', { ...common, kind: 'saveInterval', value: { ...intervalWrite(uid(712)), every_meter: null, every_months: 2_147_483_648 } }],
    ['service date', { ...common, kind: 'addServiceLogEntry', value: { ...serviceWrite(uid(713), false), service_date: '2026-02-31' } }],
    ['blank service work', { ...common, kind: 'addServiceLogEntry', value: { ...serviceWrite(uid(714), false), work_performed: '' } }],
    ['service cost range', { ...common, kind: 'addServiceLogEntry', value: { ...serviceWrite(uid(715), false), cost: 100_000_000_000_000 } }],
    ['service reading mismatch', { ...common, kind: 'addServiceLogEntry', value: { ...serviceWrite(uid(716), false), reading_id: uid(717) } }],
    ['blank task title', { ...common, kind: 'saveTask', value: { ...taskWrite(uid(718)), title: '' } }],
    ['task date', { ...common, kind: 'saveTask', value: { ...taskWrite(uid(719)), due_on: '2026-02-31' } }],
    ['missing program linkage', { ...common, kind: 'saveTask', value: { ...taskWrite(uid(720)), source: 'program', program_assigned_pass_id: null, program_cycle_key: null } }],
    ['missing service linkage', { ...common, kind: 'saveTask', value: { ...taskWrite(uid(721)), source: 'service_interval', interval_id: null, interval_cycle_key: null } }],
    ['missing scouting field', { ...common, kind: 'saveTask', value: { ...taskWrite(uid(722)), source: 'scouting', field_id: null } }],
    ['manual task with interval linkage', { ...common, kind: 'saveTask', value: { ...taskWrite(uid(723)), interval_id: intervalId, interval_cycle_key: 'forged' } }],
    ['unexpected value key', { ...common, kind: 'saveEquipment', value: { ...equipmentWrite(uid(724)), extra: true } }],
  ]
  for (const [label, malformed] of semanticFailures) await rejects(async () => parseEquipmentTasksQueue(JSON.stringify({ version: 1, entries: [malformed] })), `The queue accepted ${label}.`)
  const onlineValidationRef = 'equipment-online-validation'; const onlineValidationStore = memory(); resetFarmGrantFromLive(onlineValidationStore, { projectRef: onlineValidationRef, userId: actor, farmId: farm }, 1, micro); const onlineValidationGateway = new FakeGateway(); let onlineValidationId = 760
  const onlineValidationRepository = new QueuedEquipmentTasksRepository(repo(onlineValidationGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: onlineValidationRef, storage: onlineValidationStore, createId: () => uid(onlineValidationId++), clock: () => micro, isOffline: () => false })
  await rejects(() => onlineValidationRepository.saveEquipment({ ...equipmentWrite(uid(761)), purchase_price: 1.001 }), 'A 3-decimal Equipment price reached the online writer.')
  await rejects(() => onlineValidationRepository.addMeterReading({ ...meterWrite(uid(762)), reading: 1.001 }), 'A 3-decimal meter reading reached the online writer.')
  await rejects(() => onlineValidationRepository.saveInterval({ ...intervalWrite(uid(763)), every_meter: 1.001 }), 'A 3-decimal interval meter reached the online writer.')
  await rejects(() => onlineValidationRepository.addServiceLogEntry({ ...serviceWrite(uid(764), false), cost: 1.001 }), 'A 3-decimal service cost reached the online writer.')
  await rejects(() => onlineValidationRepository.saveTask({ ...taskWrite(uid(765)), interval_id: intervalId, interval_cycle_key: 'forged' }), 'A manual task with forged interval linkage reached the online writer.')
  assert(onlineValidationGateway.calls.equipment === 0 && onlineValidationGateway.calls.meter === 0 && onlineValidationGateway.calls.interval === 0 && onlineValidationGateway.calls.service === 0 && onlineValidationGateway.calls.task === 0, 'Invalid online Equipment/Task inputs invoked a writer before validation.')
  assert(onlineValidationStore.getItem(equipmentTasksWriteQueueKey(onlineValidationRef, actor, farm)) === null, 'Invalid online Equipment/Task inputs were persisted to the queue.')
  const forgedGateway = new FakeGateway(); const forgedLive = repo(forgedGateway); const fieldId = fieldsFor(farm).fields[0]!.id; const forgedTasks: Array<[string, TaskWrite]> = [
    ['service', { ...taskWrite(uid(766)), source: 'service_interval', interval_id: intervalId, interval_cycle_key: 'forged-cycle' }],
    ['scouting', { ...taskWrite(uid(767)), source: 'scouting', field_id: fieldId, equipment_id: null }],
    ['program', { ...taskWrite(uid(768)), source: 'program', field_id: fieldId, equipment_id: null, program_assigned_pass_id: uid(769), program_cycle_key: 'forged-cycle' }],
  ]
  for (const [label, forged] of forgedTasks) await rejects(() => forgedLive.saveTask(forged), `A forged ${label} task link reached the direct writer.`)
  assert(forgedGateway.calls.task === 0, 'A forged linked task invoked the direct task gateway.')
  const forgedQueueRef = 'equipment-forged-links'; const forgedQueueStore = memory(); resetFarmGrantFromLive(forgedQueueStore, { projectRef: forgedQueueRef, userId: actor, farmId: farm }, 1, micro); let forgedOffline = false; const forgedQueueGateway = new FakeGateway(); const forgedQueued = new QueuedEquipmentTasksRepository(repo(forgedQueueGateway), { getContext: async () => ({ userId: actor, farmId: farm }), projectRef: forgedQueueRef, storage: forgedQueueStore, createId: (() => { let value = 770; return () => uid(value++) })(), clock: () => micro, isOffline: () => forgedOffline }); await forgedQueued.getWorkspace(); forgedOffline = true
  for (const [label, forged] of forgedTasks) await rejects(() => forgedQueued.saveTask(forged), `A forged ${label} task link entered the offline queue.`)
  assert(forgedQueueGateway.calls.task === 0 && forgedQueueStore.getItem(equipmentTasksWriteQueueKey(forgedQueueRef, actor, farm)) === null, 'A forged linked task invoked a writer or changed the offline queue.')
  const filters = [task(uid(70), { status: 'todo', assigned_to: actor }), task(uid(71), { status: 'doing', assigned_to: other }), task(uid(72), { status: 'done', assigned_to: actor, completed_by: actor, completed_at: micro }), task(uid(73), { status: 'todo', assigned_to: other, due_on: '2026-07-01' })] as never
  assert(filterTasksForBoard(filters, actor, null, '2026-07-12').length === 4 && filterTasksForBoard(filters, actor, 'open', '2026-07-12').length === 3 && filterTasksForBoard(filters, actor, 'mine', '2026-07-12').length === 2 && filterTasksForBoard(filters, actor, 'overdue', '2026-07-12').length === 1 && filterTasksForBoard(filters, actor, 'done', '2026-07-12').length === 1, 'Task board filtering must keep the default Done column, use the signed-in viewer for Mine, and isolate each KPI filter.')
  // Group 10: a tracker-owned card is tap-only and every write must return to
  // Season progress; neither a same-source status change nor a manual downgrade
  // may reach the generic Tasks writer.
  const programGateway = new FakeGateway(); const programPassId = uid(74); const programTaskId = uid(75); const programFieldId = fieldsFor(farm).fields[0]!.id; programGateway.state.tasks = [task(programTaskId, { title: 'Corn Program — Post — North 80', source: 'program', due_on: '2026-07-12', field_id: programFieldId, equipment_id: null, program_assigned_pass_id: programPassId, program_cycle_key: `due:${programPassId}:2026-07-12` })]; const programLive = repo(programGateway); const openProgramCard = (await programLive.getWorkspace()).tasks[0]!; const behavior = programTaskBoardBehavior(openProgramCard); assert(behavior.trackerOwned && behavior.href === `/programs?pass=${programPassId}`, 'A Program card must route to its exact Season progress pass and be treated as tracker-owned, so the board does not render Edit or Delete.'); const programWrite = { id: openProgramCard.id, title: openProgramCard.title, details: openProgramCard.details, status: 'done' as const, priority: openProgramCard.priority, assigned_to: openProgramCard.assigned_to, due_on: openProgramCard.due_on, field_id: openProgramCard.field_id, equipment_id: openProgramCard.equipment_id, source: openProgramCard.source, interval_id: openProgramCard.interval_id, interval_cycle_key: openProgramCard.interval_cycle_key, program_assigned_pass_id: openProgramCard.program_assigned_pass_id, program_cycle_key: openProgramCard.program_cycle_key }; await rejects(() => programLive.saveTask(programWrite), 'A Program status change reached the generic Tasks writer.'); await rejects(() => programLive.saveTask({ ...programWrite, source: 'manual', program_assigned_pass_id: null, program_cycle_key: null }), 'A Program task was downgraded to manual through the generic Tasks writer.'); const preservedProgramTask = row(programGateway.state.tasks[0]); assert(programGateway.calls.task === 0 && preservedProgramTask.source === 'program' && preservedProgramTask.status === 'todo', 'A rejected Program task write changed its source, links, status, or invoked the gateway.')
  assert(moduleBackends.equipment_tasks === 'supabase', 'The backend manifest must select the live equipment/tasks repository.')
  console.log('SupabaseEquipmentTasksRepository regression passed (10 coverage groups)')
}
void run()
