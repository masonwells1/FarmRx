import { isSoilRxUuid, normalizeSoilTestDraft, soilMeasurementKeys, validateSoilTestDraft, type SoilTestDraft } from './soilRx'
import type { StorageLike } from './writeQueue'
export type QueuedSoilTestDraft = SoilTestDraft & { id: string }
export interface SoilRxQueueEntryV1 { version: 1; module: 'soilRx'; kind: 'saveTest'; operationId: string; userId: string; farmId: string; enqueuedAt: string; draft: QueuedSoilTestDraft }
export interface SoilRxQueueEnvelopeV1 { version: 1; entries: SoilRxQueueEntryV1[] }
const blocked = 'Saved Soil Rx changes on this device need attention. Nothing was deleted.'
const draftKeys = ['id', 'field_id', 'sample_date', 'lab_name', ...soilMeasurementKeys]
const exact = (value: Record<string, unknown>, keys: readonly string[]) => Object.keys(value).length === keys.length && keys.every((key) => Object.hasOwn(value, key))
function isDraft(value: unknown): value is QueuedSoilTestDraft { if (!value || typeof value !== 'object' || Array.isArray(value)) return false; const row = value as Record<string, unknown>; if (!exact(row, draftKeys)) return false; return isSoilRxUuid(row.id) && validateSoilTestDraft(normalizeSoilTestDraft(row as unknown as QueuedSoilTestDraft)) === null }
function isEntry(value: unknown): value is SoilRxQueueEntryV1 { if (!value || typeof value !== 'object' || Array.isArray(value)) return false; const row = value as Record<string, unknown>; return exact(row, ['version', 'module', 'kind', 'operationId', 'userId', 'farmId', 'enqueuedAt', 'draft']) && row.version === 1 && row.module === 'soilRx' && row.kind === 'saveTest' && isSoilRxUuid(row.operationId) && isSoilRxUuid(row.userId) && isSoilRxUuid(row.farmId) && typeof row.enqueuedAt === 'string' && !Number.isNaN(Date.parse(row.enqueuedAt)) && isDraft(row.draft) }
export function parseSoilRxQueue(serialized: string): SoilRxQueueEnvelopeV1 { let value: unknown; try { value = JSON.parse(serialized) } catch { throw new Error(blocked) } if (!value || typeof value !== 'object' || Array.isArray(value) || !exact(value as Record<string, unknown>, ['version', 'entries']) || (value as { version?: unknown }).version !== 1 || !Array.isArray((value as { entries?: unknown }).entries) || !(value as { entries: unknown[] }).entries.every(isEntry)) throw new Error(blocked); return value as SoilRxQueueEnvelopeV1 }
export class SoilRxWriteQueue { constructor(private readonly storage: StorageLike, readonly key: string) {} read() { const raw = this.storage.getItem(this.key); return raw === null ? { version: 1 as const, entries: [] } : parseSoilRxQueue(raw) } private persist(value: SoilRxQueueEnvelopeV1) { const bytes = JSON.stringify(value); parseSoilRxQueue(bytes); this.storage.setItem(this.key, bytes); if (this.storage.getItem(this.key) !== bytes) throw new Error('This Soil Rx entry could not be saved on this device. Keep this screen open and try again.'); parseSoilRxQueue(bytes) } append(entry: SoilRxQueueEntryV1) { parseSoilRxQueue(JSON.stringify({ version: 1, entries: [entry] })); const next = { version: 1 as const, entries: [...this.read().entries, entry] }; this.persist(next); return next } removeConfirmedHead(operationId: string) { const current = this.read(); if (current.entries[0]?.operationId !== operationId) throw new Error(blocked); const next = { version: 1 as const, entries: current.entries.slice(1) }; this.persist(next); return next } }
export function soilRxWriteQueueKey(projectRef: string, userId: string, farmId: string) { return `farm-rx-soil-rx-write-queue:v1:${projectRef}:${userId}:${farmId}` }
