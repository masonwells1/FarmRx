import { QueuedProgramsRepository } from './QueuedProgramsRepository'
import { SupabaseProgramsDataGateway } from './SupabaseProgramsDataGateway'
import { SupabaseProgramsRepository } from './SupabaseProgramsRepository'
import type { StorageLike } from './writeQueue'
import { captureFarmOperationContext, verifyFarmOperationContext } from './farmOperationContext'
export function createSupabaseProgramsServices(d: { getFarmId: () => Promise<string>; getUserId: () => Promise<string>; getContext: () => Promise<{ userId: string; farmId: string }>; projectRef: string; storage: StorageLike; createId: () => string; isOffline: () => boolean }) { const getOperationContext = async () => captureFarmOperationContext(d.storage, d.projectRef, await d.getContext()); const verifyOperationContext = async (expected: Awaited<ReturnType<typeof getOperationContext>>) => verifyFarmOperationContext(d.storage, expected, await getOperationContext()); const live = new SupabaseProgramsRepository({ gateway: new SupabaseProgramsDataGateway(), getFarmId: d.getFarmId, getUserId: d.getUserId, getOperationContext, verifyOperationContext, createId: d.createId }); const queued = new QueuedProgramsRepository(live, { getContext: d.getContext, projectRef: d.projectRef, storage: d.storage, createId: d.createId, clock: () => new Date().toISOString(), isOffline: d.isOffline }); return { programsRepository: queued, replayProgramsQueue: () => queued.inspectAndReplay() } }
