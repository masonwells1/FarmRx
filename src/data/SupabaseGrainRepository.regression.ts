import type { GrainDataGateway, GrainRowBundle, ReplaceMarketingPlanInput } from './GrainDataGateway'
import { productionActualColumns } from './SupabaseGrainDataGateway'
import { GrainWriteQueue, grainWriteQueueKey, parseGrainQueue } from './grainWriteQueue'
import { QueuedGrainRepository } from './QueuedGrainRepository'
import { fieldsSeedForRegression } from './MockFieldsRepository'
import { SupabaseGrainRepository } from './SupabaseGrainRepository'
import { moduleBackends } from './backends'
import { supabaseConfig } from '../lib/supabaseConfig'
import { getSyncStatus } from './syncStatus'
import { getSaveReceipt } from '../lib/saveReceipt'
import { readNeedsAttention } from './needsAttentionStore'
import { isMarsBid, latestBasis } from './basisMath'
import { farmerError } from '../lib/farmerErrors'
import { PRE_BASELINE_BIN_MOVEMENT_MESSAGE } from './binLedger'
import { deriveBinOnHand } from './binLedger'
import { FILLED_OFFER_DELETE_MESSAGE } from './firmOffers'
import type { FieldsRepository } from './fields'
import type { FarmOperationContext } from './farmOperationContext'
import type { BinTransaction, CashBid, FirmOffer, GrainAlertSettings, GrainBin, GrainContract, GrainContractDelivery, GrainWorkspace, MarketingAlertRule, MarketingPlanTarget, ProductionEstimate } from './grain'
import type { StorageLike } from './writeQueue'
import { resetFarmGrantFromLive } from './farmRevocationFence'

const stamp = '2026-07-11T00:00:00.000Z'; const uid = (n: number) => `00000000-0000-4000-8000-${String(n).padStart(12, '0')}`
const operationContext = (farmId: string): FarmOperationContext => ({ projectRef: 'test', userId: uid(10), farmId, generation: 1, token: uid(999), serverEpoch: 1 })
function assert(value: unknown, message: string): asserts value { if (!value) throw new Error(message) }
async function rejects(action: () => Promise<unknown>, message: string) { let failed = false; try { await action() } catch { failed = true }; assert(failed, message) }
function fixture() {
  const fields = fieldsSeedForRegression(); const farm = fields.farm.id; const commodity = fields.commodities[0].id; const scope = { farm_id: farm, crop_year: 2026, commodity_id: commodity, operating_entity_id: null, enterprise_label: null }; const base = { ...scope, created_at: stamp, updated_at: stamp }
  const production = { ...base, id: uid(1), planted_acres: '999', aph_yield: '200', expected_bushels: '1', actual_bushels: null, drives_math: 'projected', notes: null }
  const contract = { ...base, id: uid(2), contract_type: 'forward_cash', buyer: 'Buyer', bushels: '100', futures_price: null, basis: null, cash_price: '4.50', delivery_start: '2026-09-01', delivery_end: '2026-10-01', contract_number: null, premium_cents_per_bu: '0', notes: null }
  const target = { ...base, id: uid(3), target_month: '2026-09-01', target_pct_of_production: '20', target_price: '5', breakeven_relative_pct: null, deadline: '2026-09-15', notes: null }
  const insurance = { ...base, id: uid(4), unit_name: 'Enterprise', insured_acres: '10', aph: '200', coverage_level_pct: '80', revenue_guarantee_per_acre: '500', guarantee_per_bu: '2.5', notes: null }
  const bin = { id: uid(5), farm_id: farm, name: 'North', capacity_bu: '1000', location_type: 'on_farm', location_name: null, notes: null, moisture_pct: '15', moisture_checked_on: '2026-07-10', created_at: stamp, updated_at: stamp }
  const inventory = { id: uid(6), farm_id: farm, grain_bin_id: bin.id, crop_year: '2026', commodity_id: commodity, bushels: '600', committed_bushels: '100', measured_at: stamp, notes: null, created_at: stamp, updated_at: stamp }
  const bid = { id: uid(7), farm_id: farm, elevator: 'Iowa pilot [USDA MARS 2850]', commodity_id: commodity, bid_date: '2026-07-10', basis: '-0.2', cash_price: '4.3', delivery_start: null, delivery_end: null, notes: '[USDA MARS 2850]', created_at: stamp, updated_at: stamp }
  const report = { id: uid(8), report_name: 'WASDE', report_date: '2026-08-12', release_at: null, source_url: null, notes: null, created_at: stamp, updated_at: stamp }
  return { fields, scope, bundle: { production_estimates: [production], grain_contracts: [contract], grain_contract_deliveries: [], marketing_plan_targets: [target], insurance_units: [insurance], grain_bins: [bin], bin_inventory: [inventory], bin_transactions: [] as unknown[], cash_bids: [bid], usda_report_dates: [report], marketing_alert_rules: [], firm_offers: [], grain_alert_settings: null } }
}
/** Lets tests perturb what the "server" hands back, independent of what was sent, to prove the repository
 * confirms the canonical response rather than trusting its own request. Unset (null) by default so every
 * existing test keeps its original round-trip behavior. */
type ResponseMutators = { production?: (value: ProductionEstimate) => ProductionEstimate; contract?: (value: GrainContract) => GrainContract; plan?: (value: MarketingPlanTarget[]) => MarketingPlanTarget[]; bid?: (value: CashBid) => CashBid }
class FakeGateway implements GrainDataGateway {
  readonly state: { fields: ReturnType<typeof fixture>['fields']; scope: ReturnType<typeof fixture>['scope']; bundle: GrainRowBundle } = fixture(); fail = false; productionInputs: ProductionEstimate[] = []; productionActualInputs: Array<{ farmId: string; id: string; actualBushels: number; expectedUpdatedAt: string }> = []; contractInputs: GrainContract[] = []; planInputs: ReplaceMarketingPlanInput[] = []; bidInputs: CashBid[] = []; offerInputs: FirmOffer[] = []; fillInputs: Array<{ farmId: string; offerId: string; contract: GrainContract }> = []; deleteOfferIds: string[] = []; binInputs: GrainBin[] = []; movementInputs: BinTransaction[] = []
  mutate: ResponseMutators = {}; throwError: Error | null = null; movementError: Error | null = null; fillError: Error | null = null; fillResponse: { contract: GrainContract; offer: FirmOffer } | null = null
  afterOnlineMutation: (() => void) | null = null; beforeProduction: (() => Promise<void>) | null = null; beforeProductionActual: (() => Promise<void>) | null = null; beforeContract: (() => Promise<void>) | null = null; beforeBin: (() => Promise<void>) | null = null; beforeMovement: (() => Promise<void>) | null = null; beforeDelivery: (() => Promise<void>) | null = null
  throwAfterProductionPersist = false; afterProductionPersist: (() => void) | null = null; throwAfterContractPersist = false; afterContractPersist: (() => void) | null = null; throwAfterBinPersist = false; afterBinPersist: (() => void) | null = null; throwAfterMovementPersist = false; afterMovementPersist: (() => void) | null = null; throwAfterDeliveryPersist = false; afterDeliveryPersist: (() => void) | null = null
  private guard() { if (this.throwError) throw this.throwError }
  async loadWorkspace(_farmId: string): Promise<GrainRowBundle> { if (this.fail) throw new Error('network timeout'); return structuredClone(this.state.bundle) }
  async upsertProductionEstimate(_farm: string, row: ProductionEstimate) { this.guard(); this.productionInputs.push(structuredClone(row)); await this.beforeProduction?.(); this.state.bundle.production_estimates = this.state.bundle.production_estimates.some((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === row.id) ? this.state.bundle.production_estimates.map((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === row.id ? structuredClone(row) : item) : [...this.state.bundle.production_estimates, structuredClone(row)]; if (this.throwAfterProductionPersist) { this.afterProductionPersist?.(); throw new TypeError('lost production response') }; const response = structuredClone(row); return this.mutate.production ? this.mutate.production(response) : response }
  async updateProductionActual(farmId: string, id: string, actualBushels: number, expectedUpdatedAt: string) { this.guard(); this.productionActualInputs.push({ farmId, id, actualBushels, expectedUpdatedAt }); await this.beforeProductionActual?.(); const current = this.state.bundle.production_estimates.find((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === id) as ProductionEstimate | undefined; if (!current) throw new Error('missing production estimate'); const saved = { ...current, actual_bushels: actualBushels, drives_math: 'actual' as const, updated_at: stamp }; this.state.bundle.production_estimates = this.state.bundle.production_estimates.map((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === id ? saved : item); this.afterOnlineMutation?.(); return structuredClone(saved) }
  async upsertContract(_farm: string, row: GrainContract) { this.guard(); this.contractInputs.push(structuredClone(row)); await this.beforeContract?.(); this.state.bundle.grain_contracts = this.state.bundle.grain_contracts.some((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === row.id) ? this.state.bundle.grain_contracts.map((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === row.id ? structuredClone(row) : item) : [...this.state.bundle.grain_contracts, structuredClone(row)]; if (this.throwAfterContractPersist) { this.afterContractPersist?.(); throw new TypeError('lost contract response') }; const response = structuredClone(row); return this.mutate.contract ? this.mutate.contract(response) : response }
  async replaceMarketingPlan(input: ReplaceMarketingPlanInput) { this.guard(); this.planInputs.push(structuredClone(input)); const response = structuredClone(input.targets); return this.mutate.plan ? this.mutate.plan(response) : response }
  async upsertCashBid(_farm: string, row: CashBid) { this.guard(); this.bidInputs.push(structuredClone(row)); const response = structuredClone(row); return this.mutate.bid ? this.mutate.bid(response) : response }
  async upsertMarketingAlertRule(_farm: string, row: MarketingAlertRule) { this.guard(); return structuredClone(row) }
  async deleteMarketingAlertRule(_farm: string, _id: string) { this.guard() }
  async upsertFirmOffer(_farm: string, row: FirmOffer) { this.guard(); this.offerInputs.push(structuredClone(row)); return structuredClone(row) }
  async fillFirmOffer(farmId: string, offerId: string, row: GrainContract) { this.fillInputs.push({ farmId, offerId, contract: structuredClone(row) }); if (this.fillResponse) { const response = structuredClone(this.fillResponse); this.afterOnlineMutation?.(); return response } throw this.fillError ?? Object.assign(new Error('function public.fill_firm_offer(uuid,jsonb) does not exist'), { code: '42883' }) }
  async deleteFirmOffer(_farm: string, id: string) { this.guard(); this.deleteOfferIds.push(id) }
  async upsertGrainBin(_farm: string, row: GrainBin) { this.guard(); this.binInputs.push(structuredClone(row)); await this.beforeBin?.(); this.state.bundle.grain_bins = this.state.bundle.grain_bins.some((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === row.id) ? this.state.bundle.grain_bins.map((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === row.id ? structuredClone(row) : item) : [...this.state.bundle.grain_bins, structuredClone(row)]; if (this.throwAfterBinPersist) { this.afterBinPersist?.(); throw new TypeError('lost bin response') }; return structuredClone(row) }
  async appendBinTransactionRpc(_farm: string, row: BinTransaction) { this.guard(); this.movementInputs.push(structuredClone(row)); await this.beforeMovement?.(); if (this.movementError) throw this.movementError; const existing = this.state.bundle.bin_transactions.find((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === row.id) as BinTransaction | undefined; if (existing) { if (existing.farm_id === row.farm_id && existing.grain_bin_id === row.grain_bin_id && existing.direction === row.direction && existing.bushels === row.bushels && existing.commodity_id === row.commodity_id && existing.occurred_on === row.occurred_on && existing.note === row.note && existing.source_kind === row.source_kind) { const response = structuredClone(existing); this.afterOnlineMutation?.(); return response } throw new Error('Movement id was already used with different content.') } this.state.bundle.bin_transactions = [...this.state.bundle.bin_transactions, structuredClone(row)]; if (this.throwAfterMovementPersist) { this.afterMovementPersist?.(); throw new TypeError('lost movement response') }; const response = structuredClone(row); this.afterOnlineMutation?.(); return response }
  async appendContractDeliveryRpc(_farm: string, row: { id: string; farm_id: string; grain_contract_id: string; bushels: number; delivered_on: string; note: string | null }, _allow: boolean) { this.guard(); await this.beforeDelivery?.(); const existing = this.state.bundle.grain_contract_deliveries.find((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === row.id) as typeof row | undefined; if (existing) { if (existing.farm_id === row.farm_id && existing.grain_contract_id === row.grain_contract_id && existing.bushels === row.bushels && existing.delivered_on === row.delivered_on && existing.note === row.note) { const response = structuredClone(existing); this.afterOnlineMutation?.(); return response } throw new Error('Delivery id was already used with different content.') } this.state.bundle.grain_contract_deliveries = [...this.state.bundle.grain_contract_deliveries, structuredClone(row)]; if (this.throwAfterDeliveryPersist) { this.afterDeliveryPersist?.(); throw new TypeError('lost delivery response') }; const response = structuredClone(row); this.afterOnlineMutation?.(); return response }
  async finalizeContractPriceLegRpc(_farm: string, contractId: string, leg: 'futures_price' | 'basis', value: number) { const raw = this.state.bundle.grain_contracts.find((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === contractId) as Record<string, unknown> | undefined; if (!raw) throw new Error('missing contract'); if (raw[leg] !== null) throw new Error('already finalized'); const future = leg === 'futures_price' ? value : Number(raw.futures_price); const basis = leg === 'basis' ? value : Number(raw.basis); const saved = { ...raw, futures_price: future, basis, cash_price: future + basis + Number(raw.premium_cents_per_bu) / 100, updated_at: stamp }; this.state.bundle.grain_contracts = this.state.bundle.grain_contracts.map((item) => item && typeof item === 'object' && (item as { id?: unknown }).id === contractId ? saved : item); const response = structuredClone(saved); this.afterOnlineMutation?.(); return response }
  async upsertGrainAlertSettings(_farm: string, row: GrainAlertSettings) { this.guard(); return structuredClone(row) }
}
function repository(gateway: FakeGateway, verifyOperationContext: (context: ReturnType<typeof operationContext>) => Promise<void> = async () => undefined) { const fields = gateway.state.fields; const fieldsRepository: FieldsRepository = { getData: async () => structuredClone(fields), saveField: async () => { throw new Error('not used') } }; return new SupabaseGrainRepository({ gateway, fieldsRepository, getFarmId: async () => fields.farm.id, getOperationContext: async () => operationContext(fields.farm.id), verifyOperationContext, createId: () => uid(99), clock: () => stamp }) }
async function run() {
  const gateway = new FakeGateway(); const repo = repository(gateway); const data = await repo.getData()
  // 1-5: eight row sets map strictly, numeric strings/nulls survive, reconcile from injected Fields.
  assert(data.production_estimates.length === 1 && data.grain_bins.length === 1 && data.usda_report_dates.length === 1 && data.grain_contracts[0].cash_price === 4.5, 'All Grain result sets must map numeric strings exactly.')
  const acres = gateway.state.fields.crop_assignments.filter((row) => row.crop_year === 2026 && row.commodity_id === data.production_estimates[0].commodity_id).reduce((sum, row) => sum + row.planted_acres, 0); assert(data.production_estimates[0].planted_acres === acres && data.production_estimates[0].expected_bushels === acres * 200, 'Production was not reconciled from injected Fields.')
  gateway.fail = true; await rejects(() => repo.getData(), 'Partial gateway failure must reject.'); gateway.fail = false
  const bad = structuredClone(gateway.state.bundle) as { grain_contracts: Array<Record<string, unknown>> }; bad.grain_contracts[0].contract_type = 'mystery'; gateway.state.bundle.grain_contracts = bad.grain_contracts; await rejects(() => repo.getData(), 'Unknown enum must fail closed.'); gateway.state.bundle.grain_contracts = fixture().bundle.grain_contracts
  const firstInventory = gateway.state.bundle.bin_inventory[0] as Record<string, unknown>; firstInventory.farm_id = uid(55); await rejects(() => repo.getData(), 'Cross-farm private rows must reject.'); firstInventory.farm_id = gateway.state.fields.farm.id
  // 6-10: all persistence shapes bind the farm and preserve client IDs.
  const production = data.production_estimates[0]; await repo.saveProductionEstimate({ ...production, farm_id: uid(88), planted_acres: 1, expected_bushels: 1 }); assert(gateway.productionInputs[0].farm_id === data.fields.farm.id && gateway.productionInputs[0].id === production.id && gateway.productionInputs[0].expected_bushels === acres * production.aph_yield, 'Production save did not bind farm and derived totals.')
  await repo.reconcileHarvestActual(production, 50_000); assert(JSON.stringify(gateway.productionActualInputs[0]) === JSON.stringify({ farmId: data.fields.farm.id, id: production.id, actualBushels: 50_000, expectedUpdatedAt: production.updated_at }), 'Harvest reconciliation must send only identity, actual bushels, and the optimistic version to the gateway.')
  assert(JSON.stringify(productionActualColumns(50_000)) === JSON.stringify({ actual_bushels: 50_000, drives_math: 'actual' }), 'The final Harvest gateway patch must contain only Grain actual and its math basis.')
  await repo.saveContract(data.grain_contracts[0]); assert(gateway.contractInputs[0].id === data.grain_contracts[0].id, 'Contract upsert lost its stable id.')
  await rejects(() => repo.saveContract({ ...data.grain_contracts[0], cash_price: null }), 'Invalid contract shape was accepted.')
  const changed: MarketingPlanTarget = { ...data.marketing_plan_targets[0], target_pct_of_production: 25 }; await repo.saveMarketingPlanTarget(changed); assert(gateway.planInputs.at(-1)?.targets.length === 1 && gateway.planInputs.at(-1)?.targets[0].id === changed.id, 'Single target edit did not become a complete scoped plan.')
  await repo.replaceMarketingPlanTargets(data.marketing_plan_targets[0], []); assert(gateway.planInputs.at(-1)?.targets.length === 0, 'Empty plan replacement was not passed to the RPC.')
  await repo.saveCashBid(data.cash_bids[0]); assert(gateway.bidInputs[0].farm_id === data.fields.farm.id && gateway.bidInputs[0].notes === '[USDA MARS 2850]', 'Cash bid save lost farm/provenance.')
  // 11: live failure stays live and no mock storage is involved.
  gateway.fail = true; await rejects(() => repo.getData(), 'Live failure must never become a mock success.'); gateway.fail = false
  const queueKey = grainWriteQueueKey(supabaseConfig.projectRef, uid(10), data.fields.farm.id); assert(queueKey.startsWith('farm-rx-grain-write-queue:v1:') && !queueKey.includes('farm-rx-write-queue:v1:'), 'Grain queue key is not isolated.')
  await rejects(async () => { parseGrainQueue('{bad') }, 'Corrupt Grain queue bytes were accepted.')
  await rejects(async () => { parseGrainQueue(JSON.stringify({ version: 2, entries: [] })) }, 'Unknown Grain queue version was accepted.')
  // 12: all four strict entry kinds round-trip before any bytes are written.
  const memory: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }
  const queue = new GrainWriteQueue(memory, queueKey)
  const productionRow = data.production_estimates[0]; const contractRow = data.grain_contracts[0]; const targetRow = data.marketing_plan_targets[0]; const bidRow = data.cash_bids[0]
  const common = (kind: 'saveProductionEstimate' | 'saveContract' | 'replaceMarketingPlan' | 'saveCashBid', n: number) => ({ version: 1 as const, module: 'grain' as const, kind, operationId: uid(100 + n), userId: uid(10), farmId: data.fields.farm.id, enqueuedAt: stamp })
  const productionEntry = { ...common('saveProductionEstimate', 1), kind: 'saveProductionEstimate' as const, row: productionRow }; queue.append(productionEntry)
  queue.append({ ...common('saveContract', 2), kind: 'saveContract', row: contractRow })
  queue.append({ ...common('replaceMarketingPlan', 3), kind: 'replaceMarketingPlan', scope: gateway.state.scope, targets: [targetRow] })
  queue.append({ ...common('saveCashBid', 4), kind: 'saveCashBid', row: bidRow })
  queue.append(productionEntry)
  assert(queue.read().entries.length === 4, 'Appending the same Grain operationId twice must retain exactly one queue entry.')
  const beforeBad = memory.getItem(queueKey); await rejects(async () => { parseGrainQueue(JSON.stringify({ version: 1, entries: [{ ...common('saveContract', 5), kind: 'saveContract', row: { ...contractRow, extra: true } }] })) }, 'Queue accepted an extra row field.'); assert(memory.getItem(queueKey) === beforeBad, 'Invalid bytes replaced a valid queue.')
  // 13-15: the real queued repository keeps context keys isolated, overlays FIFO writes, and replays confirmed heads only.
  const overlayStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }
  const offlineDependencies = { getContext: async () => ({ userId: uid(10), farmId: data.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: overlayStorage, createId: (() => { let n = 200; return () => uid(n++) })(), clock: () => stamp, isOffline: () => true }
  const queued = new QueuedGrainRepository(repo, offlineDependencies)
  assert(await queued.getNeedsAttentionQueueKey() === queueKey, 'Grain attention discovery must use only the exact current user and farm queue key.')
  await queued.saveCashBid({ ...bidRow, id: uid(50) }); const overlaid = await queued.getData(); assert(overlaid.cash_bids.some((row) => row.id === uid(50)), 'Queued cash bid was not optimistically overlaid.')
  const other = grainWriteQueueKey(supabaseConfig.projectRef, uid(11), data.fields.farm.id); assert(other !== queueKey && memory.getItem(other) === null, 'Queue context isolation failed.')
  const replayStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }
  const replayGateway = new FakeGateway(); const replayRepo = repository(replayGateway); let online = false; const replay = new QueuedGrainRepository(replayRepo, { ...offlineDependencies, storage: replayStorage, isOffline: () => !online })
  await replay.saveContract({ ...data.grain_contracts[0], id: uid(60) }); await replay.saveCashBid({ ...data.cash_bids[0], id: uid(61) }); online = true; await replay.inspectAndReplay(); assert(replayGateway.contractInputs[0]?.id === uid(60) && replayGateway.bidInputs[0]?.id === uid(61), 'FIFO replay did not preserve operation order and IDs.')
  // 16: movements must never enter the offline queue; capacity needs the locked RPC.
  const binQueueKey = grainWriteQueueKey(supabaseConfig.projectRef, uid(10), data.fields.farm.id)
  const offlineBinStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }
  const offlineBinGateway = new FakeGateway(); const offlineBinWriter = repository(offlineBinGateway); let binsOnline = false
  const offlineBins = new QueuedGrainRepository(offlineBinWriter, { ...offlineDependencies, storage: offlineBinStorage, isOffline: () => !binsOnline })
  const queuedMovement: BinTransaction = { id: uid(62), farm_id: data.fields.farm.id, grain_bin_id: data.grain_bins[0].id, direction: 'in', bushels: 25, commodity_id: data.fields.commodities[0].id, occurred_on: '2026-07-12', note: null, source_kind: 'manual entry', created_at: stamp }
  let offlineMovementMessage = ''
  try { await offlineBins.appendBinTransaction(queuedMovement) } catch (error) { offlineMovementMessage = error instanceof Error ? error.message : '' }
  assert(offlineMovementMessage === 'Bin movements need a connection.' && new GrainWriteQueue(offlineBinStorage, binQueueKey).read().entries.length === 0, 'Offline movements must be refused and never queued.')
  binsOnline = false
  const queuedBin: GrainBin = { ...data.grain_bins[0], id: uid(63), name: 'Queued bin' }
  await offlineBins.upsertGrainBin(queuedBin)
  assert((await offlineBins.getData()).grain_bins.filter((row) => row.id === queuedBin.id).length === 1 && new GrainWriteQueue(offlineBinStorage, binQueueKey).read().entries.length === 1, 'An offline bin upsert must overlay and queue once.')
  binsOnline = true; await offlineBins.inspectAndReplay()
  assert(offlineBinGateway.binInputs.some((row) => row.id === queuedBin.id) && new GrainWriteQueue(offlineBinStorage, binQueueKey).read().entries.length === 0, 'A queued bin upsert must replay and drain.')
  // 18: canonical-confirmation rejections — the repository must confirm the server's echoed rows, never trust its own request.
  const baseTarget = data.marketing_plan_targets[0]
  gateway.mutate.plan = (rows) => rows.map((row) => ({ ...row, id: uid(940) }))
  await rejects(() => repo.replaceMarketingPlanTargets(gateway.state.scope, [baseTarget]), 'A plan response with a wrong id must reject.')
  gateway.mutate = {}
  gateway.mutate.plan = (rows) => rows.map((row) => ({ ...row, commodity_id: `${row.commodity_id}-other` }))
  await rejects(() => repo.replaceMarketingPlanTargets(gateway.state.scope, [baseTarget]), 'A plan response with a wrong scope must reject.')
  gateway.mutate = {}
  const twoTargets: MarketingPlanTarget[] = [{ ...baseTarget, id: uid(941), target_month: '2026-09-01', target_pct_of_production: 10 }, { ...baseTarget, id: uid(942), target_month: '2026-10-01', target_pct_of_production: 10 }]
  gateway.mutate.plan = (rows) => rows.slice(0, 1)
  await rejects(() => repo.replaceMarketingPlanTargets(gateway.state.scope, twoTargets), 'An incomplete plan response (fewer rows than submitted) must reject.')
  gateway.mutate = {}
  gateway.mutate.plan = (rows) => [...rows, ...rows]
  await rejects(() => repo.replaceMarketingPlanTargets(gateway.state.scope, [baseTarget]), 'A duplicated plan response (repeated rows) must reject.')
  gateway.mutate = {}
  // 17: a poisoned replay is parked locally and later FIFO entries are not wedged.
  const retentionStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }
  const retentionGateway = new FakeGateway(); const retentionWriter = repository(retentionGateway)
  let retentionOnline = false
  const retentionKey = grainWriteQueueKey(supabaseConfig.projectRef, uid(10), data.fields.farm.id)
  const retentionRepo = new QueuedGrainRepository(retentionWriter, { getContext: async () => ({ userId: uid(10), farmId: data.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: retentionStorage, createId: (() => { let n = 300; return () => uid(n++) })(), clock: () => stamp, isOffline: () => !retentionOnline })
  await retentionRepo.saveContract({ ...data.grain_contracts[0], id: uid(950) })
  assert(new GrainWriteQueue(retentionStorage, retentionKey).read().entries.length === 1, 'Setup for the queue-retention test failed to enqueue offline.')
  retentionGateway.mutate.contract = (row) => ({ ...row, id: uid(951) })
  retentionOnline = true
  await retentionRepo.inspectAndReplay()
  const afterWrongIdReplay = new GrainWriteQueue(retentionStorage, retentionKey).read().entries
  assert(afterWrongIdReplay.length === 0, 'A canonical-confirmation rejection must not wedge the live FIFO queue.')
  assert((readNeedsAttention(retentionStorage, retentionKey)[0]?.entry as { row: { id: string } }).row.id === uid(950), 'A canonical-confirmation rejection must be retained in the needs-attention receipt store.')
  { const status = getSyncStatus(); assert(status.kind === 'blocked' && status.pending === 1 && status.message === '1 saves need attention.', 'A parked operation must be shown as one save needing attention, not synced.') }
  // 18: a permission-shaped (403) gateway error must classify as blocked, not be retried as a transport failure.
  const permissionStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }
  const permissionGateway = new FakeGateway(); const permissionWriter = repository(permissionGateway)
  let permissionOnline = false
  const permissionKey = grainWriteQueueKey(supabaseConfig.projectRef, uid(10), data.fields.farm.id)
  const permissionRepo = new QueuedGrainRepository(permissionWriter, { getContext: async () => ({ userId: uid(10), farmId: data.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: permissionStorage, createId: (() => { let n = 400; return () => uid(n++) })(), clock: () => stamp, isOffline: () => !permissionOnline })
  await permissionRepo.saveCashBid({ ...data.cash_bids[0], id: uid(960) })
  assert(new GrainWriteQueue(permissionStorage, permissionKey).read().entries.length === 1, 'Setup for the 403 test failed to enqueue offline.')
  permissionGateway.throwError = Object.assign(new Error('permission denied for table grain_cash_bids'), { status: 403 })
  permissionOnline = true
  await permissionRepo.inspectAndReplay()
  const afterForbiddenReplay = new GrainWriteQueue(permissionStorage, permissionKey).read().entries
  assert(afterForbiddenReplay.length === 0, 'A non-transport failure must be parked instead of blocking later saves.')
  assert((readNeedsAttention(permissionStorage, permissionKey)[0]?.entry as { row: { id: string } }).row.id === uid(960), 'A 403/permission-shaped failure must remain available for farmer review.')
  { const status = getSyncStatus(); assert(status.kind === 'blocked' && status.pending === 1 && status.message === '1 saves need attention.', 'A parked failure must be shown as one save needing attention, not synced.') }
  // 19: plan validation must reject over-100% totals and duplicate months before ever reaching the gateway.
  const planCallsBeforeValidation = gateway.planInputs.length
  const overTotalTargets: MarketingPlanTarget[] = [{ ...baseTarget, id: uid(970), target_month: '2026-09-01', target_pct_of_production: 60 }, { ...baseTarget, id: uid(971), target_month: '2026-10-01', target_pct_of_production: 45 }]
  await rejects(() => repo.replaceMarketingPlanTargets(gateway.state.scope, overTotalTargets), 'An over-100% marketing plan must reject.')
  assert(gateway.planInputs.length === planCallsBeforeValidation, 'An over-100% marketing plan reached the gateway; it must reject before that call.')
  const dupMonthTargets: MarketingPlanTarget[] = [{ ...baseTarget, id: uid(972), target_month: '2026-09-01', target_pct_of_production: 10 }, { ...baseTarget, id: uid(973), target_month: '2026-09-01', target_pct_of_production: 10 }]
  await rejects(() => repo.replaceMarketingPlanTargets(gateway.state.scope, dupMonthTargets), 'A duplicate-month marketing plan must reject.')
  assert(gateway.planInputs.length === planCallsBeforeValidation, 'A duplicate-month marketing plan reached the gateway; it must reject before that call.')
  // 20: the extracted basisMath module must exclude MARS feed rows and use manual bids, even when MARS is the newest row.
  const marsRow: CashBid = { ...data.cash_bids[0], id: uid(980), bid_date: '2026-07-20', basis: -0.05 }
  const manualRow: CashBid = { ...data.cash_bids[0], id: uid(981), bid_date: '2026-07-12', basis: -0.18, notes: null }
  const olderManualRow: CashBid = { ...data.cash_bids[0], id: uid(982), bid_date: '2026-07-05', basis: -0.3, notes: null }
  const basisWorkspace: GrainWorkspace = { ...data, cash_bids: [marsRow, manualRow, olderManualRow] }
  assert(isMarsBid(marsRow) && !isMarsBid(manualRow) && !isMarsBid(olderManualRow), 'isMarsBid must classify MARS-tagged and manual rows correctly.')
  assert(latestBasis(basisWorkspace, gateway.state.scope) === manualRow.basis, 'latestBasis must use the latest manual bid and exclude the MARS feed row even when MARS is newest.')
  // 21: firm offers use the same bound-farm save/delete gateway seam and reject an invalid DB-check shape first.
  const offer: FirmOffer = { ...gateway.state.scope, id: uid(990), buyer: 'Buyer', offer_type: 'cash', bushels: 1000, price: 4.5, basis: null, contract_month: null, expires_on: null, delivery_location: null, notes: null, status: 'open', filled_contract_id: null, created_at: stamp, updated_at: stamp }
  let offlineFillMessage = ''
  try { await queued.fillFirmOffer(offer, { ...data.grain_contracts[0], id: uid(994) }) } catch (error) { offlineFillMessage = error instanceof Error ? error.message : '' }
  assert(offlineFillMessage === 'Connect to the internet before filling this offer.' && farmerError(new Error(offlineFillMessage), 'record this firm-offer sale') === offlineFillMessage, 'Offline firm-offer fills must preserve the farmer-facing connection message.')
  await repo.saveFirmOffer(offer)
  assert(gateway.offerInputs.length === 1 && gateway.offerInputs[0].farm_id === data.fields.farm.id && gateway.offerInputs[0].id === offer.id, 'Firm-offer save must bind the active farm and preserve the client ID.')
  const rpcContract = { ...data.grain_contracts[0], id: uid(995) }
  gateway.fillResponse = { contract: rpcContract, offer: { ...offer, status: 'filled', filled_contract_id: rpcContract.id } }
  const fallbackContractWrites = gateway.contractInputs.length; const fallbackOfferWrites = gateway.offerInputs.length
  const rpcFilled = await repo.fillFirmOffer(offer, rpcContract)
  assert(rpcFilled.contract.id === rpcContract.id && rpcFilled.offer.filled_contract_id === rpcContract.id && gateway.fillInputs.length === 1 && gateway.fillInputs[0].farmId === data.fields.farm.id && gateway.contractInputs.length === fallbackContractWrites && gateway.offerInputs.length === fallbackOfferWrites, 'A successful fill_firm_offer RPC must return its contract and offer without any fallback writes.')
  gateway.fillResponse = null
  let missingRpc = ''
  try { await repo.fillFirmOffer(offer, { ...data.grain_contracts[0], id: uid(995) }) } catch (error) { missingRpc = error instanceof Error ? error.message : '' }
  assert(missingRpc === 'FIRM_OFFER_FILL_RPC_UNAVAILABLE', 'Only the specific absent fill_firm_offer RPC error may activate the safe two-write fallback.')
  gateway.fillError = Object.assign(new Error('function public.fill_firm_offer(uuid,jsonb) does not exist'), { code: '500' })
  let serverFailure = ''
  try { await repo.fillFirmOffer(offer, { ...data.grain_contracts[0], id: uid(996) }) } catch (error) { serverFailure = error instanceof Error ? error.message : '' }
  assert(serverFailure !== 'FIRM_OFFER_FILL_RPC_UNAVAILABLE', 'A code-500 error with matching text must not activate the fallback.')
  gateway.fillError = null
  await repo.deleteFirmOffer(offer.id)
  assert(gateway.deleteOfferIds[0] === offer.id, 'Firm-offer delete must target the requested ID.')
  // Audit P3-01: a filled offer is offer-to-contract history and can never be deleted.
  const filledOffer: FirmOffer = { ...offer, id: uid(997), status: 'filled', filled_contract_id: uid(2) }
  ;(gateway.state.bundle.firm_offers as unknown[]).push(structuredClone(filledOffer))
  let filledDeleteMessage = ''
  try { await repo.deleteFirmOffer(filledOffer.id) } catch (error) { filledDeleteMessage = error instanceof Error ? error.message : '' }
  assert(filledDeleteMessage === FILLED_OFFER_DELETE_MESSAGE && !gateway.deleteOfferIds.includes(filledOffer.id), 'Deleting a filled offer must stop with the shared farmer message before any gateway delete.')
  ;(gateway.state.bundle.firm_offers as unknown[]).pop()
  await rejects(() => repo.saveFirmOffer({ ...offer, id: uid(991), price: null }), 'Cash firm offer without price must reject before the gateway.')
  assert(gateway.offerInputs.length === 1, 'Invalid firm offer reached the gateway.')
  // 22: bins are mutable, but movements use the dedicated insert-only append seam.
  const savedBin: GrainBin = data.grain_bins[0]
  await repo.upsertGrainBin({ ...savedBin, moisture_pct: 15.5, moisture_checked_on: '2026-07-12' })
  assert(gateway.binInputs.length === 1 && gateway.binInputs[0].farm_id === data.fields.farm.id && gateway.binInputs[0].moisture_pct === 15.5, 'Bin upsert must bind the active farm and preserve moisture fields.')
  const movement: BinTransaction = { id: uid(992), farm_id: uid(88), grain_bin_id: savedBin.id, direction: 'in', bushels: 25, commodity_id: gateway.state.scope.commodity_id, occurred_on: '2026-07-12', note: 'Scale ticket', source_kind: 'manual entry', created_at: stamp }
  await repo.appendBinTransaction(movement)
  assert(gateway.movementInputs.length === 1 && gateway.movementInputs[0].farm_id === data.fields.farm.id && gateway.movementInputs[0].direction === 'in', 'Movement append must bind the active farm and use the insert-only gateway seam.')
  await repo.appendBinTransaction(movement)
  assert((await repo.getData()).bin_transactions.filter((row) => row.id === movement.id).length === 1, 'Same movement UUID must replay idempotently.')
  await rejects(() => repo.appendBinTransaction({ ...movement, bushels: 26 }), 'A reused movement UUID with changed content must reject.')
  await rejects(() => repo.appendBinTransaction({ ...movement, id: uid(993), bushels: 0 }), 'Zero-bushel movement must reject before the gateway.')
  assert(gateway.movementInputs.filter(() => true).length === 3, 'Invalid movement reached the gateway.')
  // 23: capability errors fail closed with the exact farmer message; no direct-table fallback remains.
  const absentMovementGateway = new FakeGateway(); Object.defineProperty(absentMovementGateway, 'appendBinTransactionRpc', { value: undefined }); let absentMovement = ''
  try { await repository(absentMovementGateway).appendBinTransaction({ ...movement, id: uid(994) }) } catch (error) { absentMovement = error instanceof Error ? error.message : '' }
  assert(absentMovement === 'Bin movements arrive with the next database update.' && absentMovementGateway.movementInputs.length === 0 && farmerError(new Error(absentMovement), 'add this movement') === absentMovement, 'Absent movement RPC must disable the unsafe fallback with the exact message.')
  const absentDeliveryGateway = new FakeGateway(); Object.defineProperty(absentDeliveryGateway, 'appendContractDeliveryRpc', { value: undefined }); let absentDelivery = ''
  try { await repository(absentDeliveryGateway).recordContractDelivery({ id: uid(995), farm_id: data.fields.farm.id, grain_contract_id: data.grain_contracts[0].id, bushels: 1, delivered_on: '2026-07-12', note: null, created_at: stamp }) } catch (error) { absentDelivery = error instanceof Error ? error.message : '' }
  assert(absentDelivery === 'Delivery tracking arrives with the next database update.' && farmerError(new Error(absentDelivery), 'record this delivery') === absentDelivery, 'Missing delivery table/RPC must never leak a retryable generic error.')
  const absentFinalGateway = new FakeGateway(); Object.defineProperty(absentFinalGateway, 'finalizeContractPriceLegRpc', { value: undefined }); let absentFinal = ''
  try { await repository(absentFinalGateway).finalizeContractPriceLeg(data.grain_contracts[0].id, 'basis', -0.2) } catch (error) { absentFinal = error instanceof Error ? error.message : '' }
  assert(absentFinal === 'Price finalization arrives with the next database update.', 'Missing CAS RPC must disable price finalization.')
  // 24: an RPC-capable fake proves signed basis, blank/invalid guards, CAS, and exact replay behavior.
  const casGateway = new FakeGateway(); const rawHta = casGateway.state.bundle.grain_contracts[0] as Record<string, unknown>; rawHta.contract_type = 'hta'; rawHta.futures_price = '5'; rawHta.basis = null; rawHta.cash_price = null
  const casRepo = repository(casGateway); await casRepo.finalizeContractPriceLeg(String(rawHta.id), 'basis', -0.2); assert((casGateway.state.bundle.grain_contracts[0] as Record<string, unknown>).cash_price === 4.8, 'CAS finalization must preserve a negative basis and compute cash price atomically.')
  await rejects(() => casRepo.finalizeContractPriceLeg(String(rawHta.id), 'basis', -0.2), 'CAS must reject an already-finalized leg.')
  await rejects(() => casRepo.finalizeContractPriceLeg(String(rawHta.id), 'futures_price', 0), 'Blank/zero futures finalization must reject before the RPC.')
  const deliveryRepo = repository(new FakeGateway()); const deliveryRow = { id: uid(996), farm_id: data.fields.farm.id, grain_contract_id: data.grain_contracts[0].id, bushels: 25, delivered_on: '2026-07-12', note: null, created_at: stamp }; await deliveryRepo.recordContractDelivery(deliveryRow); await deliveryRepo.recordContractDelivery(deliveryRow); assert((await deliveryRepo.getData()).grain_contract_deliveries.filter((row) => row.id === deliveryRow.id).length === 1, 'Same delivery UUID must replay idempotently.')
  await rejects(() => deliveryRepo.recordContractDelivery({ ...deliveryRow, bushels: 26 }), 'A reused delivery UUID with changed content must reject.')
  const baselineGateway = new FakeGateway(); const baselineRepo = repository(baselineGateway); const baselineMovement = { ...movement, id: uid(997), occurred_on: '2026-07-11' }; let preBaselineMessage = ''; try { await baselineRepo.appendBinTransaction(baselineMovement) } catch (error) { preBaselineMessage = error instanceof Error ? error.message : '' }; assert(preBaselineMessage === PRE_BASELINE_BIN_MOVEMENT_MESSAGE && baselineGateway.movementInputs.length === 0, 'Repository must return the shared pre-baseline message before its RPC.')

  // 25 (FRX-D8-003): all four online-only Grain RPCs must re-check the captured
  // access fence after the server accepts the mutation and before publishing success.
  const changedFence = async (revoked: () => boolean) => { if (revoked()) throw new Error('The signed-in account or selected farm changed before this operation could finish.') }
  const fenceHarvestGateway = new FakeGateway(); let harvestRevoked = false; fenceHarvestGateway.afterOnlineMutation = () => { harvestRevoked = true }
  const fenceHarvestRepo = repository(fenceHarvestGateway, () => changedFence(() => harvestRevoked)); const fenceHarvestProduction = (await fenceHarvestRepo.getData()).production_estimates[0]
  await rejects(() => fenceHarvestRepo.reconcileHarvestActual(fenceHarvestProduction, 50_000), 'Harvest reconciliation published success after its access fence changed.')
  assert(harvestRevoked && fenceHarvestGateway.productionActualInputs.length === 1, 'The Harvest fence regression did not reach an accepted narrow server mutation.')
  const fenceDeliveryGateway = new FakeGateway(); let deliveryRevoked = false; fenceDeliveryGateway.afterOnlineMutation = () => { deliveryRevoked = true }
  const fenceDeliveryRepo = repository(fenceDeliveryGateway, () => changedFence(() => deliveryRevoked)); const fencedDelivery = { id: uid(998), farm_id: fenceDeliveryGateway.state.fields.farm.id, grain_contract_id: String((fenceDeliveryGateway.state.bundle.grain_contracts[0] as { id: string }).id), bushels: 5, delivered_on: '2026-07-12', note: null, created_at: stamp }
  await rejects(() => fenceDeliveryRepo.recordContractDelivery(fencedDelivery), 'A delivery RPC published success after its access fence changed.')
  assert(deliveryRevoked && fenceDeliveryGateway.state.bundle.grain_contract_deliveries.length === 1, 'The delivery fence regression did not reach an accepted server mutation.')

  const fencePriceGateway = new FakeGateway(); const fencePriceRaw = fencePriceGateway.state.bundle.grain_contracts[0] as Record<string, unknown>; fencePriceRaw.contract_type = 'hta'; fencePriceRaw.futures_price = '5'; fencePriceRaw.basis = null; fencePriceRaw.cash_price = null; let priceRevoked = false; fencePriceGateway.afterOnlineMutation = () => { priceRevoked = true }
  const fencePriceRepo = repository(fencePriceGateway, () => changedFence(() => priceRevoked)); await rejects(() => fencePriceRepo.finalizeContractPriceLeg(String(fencePriceRaw.id), 'basis', -0.2), 'A price-finalization RPC published success after its access fence changed.')
  assert(priceRevoked && Number((fencePriceGateway.state.bundle.grain_contracts[0] as Record<string, unknown>).basis) === -0.2, 'The price fence regression did not reach an accepted server mutation.')

  const fenceFillGateway = new FakeGateway(); const fenceFillOffer: FirmOffer = { ...fenceFillGateway.state.scope, id: uid(999), buyer: 'Fence Buyer', offer_type: 'cash', bushels: 100, price: 4.5, basis: null, contract_month: null, expires_on: null, delivery_location: null, notes: null, status: 'open', filled_contract_id: null, created_at: stamp, updated_at: stamp }; const fenceFillContract = { ...data.grain_contracts[0], id: uid(1000) }
  fenceFillGateway.fillResponse = { contract: fenceFillContract, offer: { ...fenceFillOffer, status: 'filled', filled_contract_id: fenceFillContract.id } }; let fillRevoked = false; fenceFillGateway.afterOnlineMutation = () => { fillRevoked = true }
  const fenceFillRepo = repository(fenceFillGateway, () => changedFence(() => fillRevoked)); await rejects(() => fenceFillRepo.fillFirmOffer(fenceFillOffer, fenceFillContract), 'A firm-offer fill RPC published success after its access fence changed.')
  assert(fillRevoked && fenceFillGateway.fillInputs.length === 1, 'The firm-offer fence regression did not reach an accepted server mutation.')

  const fenceMovementGateway = new FakeGateway(); let movementRevoked = false; fenceMovementGateway.afterOnlineMutation = () => { movementRevoked = true }
  const fenceMovementRepo = repository(fenceMovementGateway, () => changedFence(() => movementRevoked)); const fencedMovement: BinTransaction = { id: uid(1001), farm_id: fenceMovementGateway.state.fields.farm.id, grain_bin_id: String((fenceMovementGateway.state.bundle.grain_bins[0] as { id: string }).id), direction: 'in', bushels: 5, commodity_id: fenceMovementGateway.state.scope.commodity_id, occurred_on: '2026-07-12', note: null, source_kind: 'manual entry', created_at: stamp }
  await rejects(() => fenceMovementRepo.appendBinTransaction(fencedMovement), 'A bin-movement RPC published success after its access fence changed.')
  assert(movementRevoked && fenceMovementGateway.movementInputs.length === 1, 'The bin-movement fence regression did not reach an accepted server mutation.')
  // 26: production receipts stay bound to the estimate ID across creation/replay, while online-only Harvest reconciliation never queues or touches bins.
  const receiptStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }
  let receiptOnline = true; let receiptSequence = 1100; const receiptGateway = new FakeGateway(); const receiptWriter = repository(receiptGateway); const receiptQueue = new QueuedGrainRepository(receiptWriter, { getContext: async () => ({ userId: uid(10), farmId: receiptGateway.state.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: receiptStorage, createId: () => uid(receiptSequence++), clock: () => stamp, isOffline: () => !receiptOnline })
  const createdId = uid(1101); const created = { ...(await receiptWriter.getData()).production_estimates[0], id: createdId, actual_bushels: null, drives_math: 'projected' as const }; let releaseCreate!: () => void; const createGate = new Promise<void>((resolve) => { releaseCreate = resolve }); receiptGateway.beforeProduction = () => createGate
  const delayedCreate = receiptQueue.saveProductionEstimate(created); await new Promise<void>((resolve) => setTimeout(resolve, 0)); assert(getSaveReceipt(createdId) === 'saving' && receiptGateway.productionInputs.length === 1, 'A delayed production create must publish Saving under the exact estimate ID before one durable write completes.'); releaseCreate(); await delayedCreate; assert(getSaveReceipt(createdId) === 'saved' && receiptGateway.productionInputs.length === 1, 'A completed production create must publish Saved with one stable estimate ID and one write.')
  const offlineId = uid(1102); const offlineCreated = { ...created, id: offlineId }; receiptOnline = false; await receiptQueue.saveProductionEstimate(offlineCreated); const receiptQueueKey = grainWriteQueueKey(supabaseConfig.projectRef, uid(10), receiptGateway.state.fields.farm.id); const receiptEntries = new GrainWriteQueue(receiptStorage, receiptQueueKey).read().entries; assert(getSaveReceipt(offlineId) === 'queued offline' && receiptEntries.length === 1 && receiptEntries[0].kind === 'saveProductionEstimate' && (receiptEntries[0].row as ProductionEstimate).id === offlineId, 'An offline production create must publish Queued offline and retain the exact estimate ID for replay.'); await new Promise<void>((resolve) => setTimeout(resolve, 0)); receiptOnline = true; await receiptQueue.inspectAndReplay(); await receiptQueue.inspectAndReplay(); assert(receiptGateway.productionInputs.filter((row) => row.id === offlineId).length === 1 && new GrainWriteQueue(receiptStorage, receiptQueueKey).read().entries.length === 0 && getSaveReceipt(offlineId) === 'saved', 'Replay must use the same estimate ID once, drain its queue entry, and publish Saved without a duplicate create.')
  const terminalId = uid(1103); receiptOnline = false; await receiptQueue.saveProductionEstimate({ ...created, id: terminalId }); const terminalEntry = structuredClone(new GrainWriteQueue(receiptStorage, receiptQueueKey).read().entries[0]); const remoteBeforeTerminal = structuredClone(receiptGateway.state.bundle); await new Promise<void>((resolve) => setTimeout(resolve, 0)); receiptGateway.throwError = new Error('validation failed'); receiptOnline = true; await receiptQueue.inspectAndReplay(); const parkedTerminal = readNeedsAttention(receiptStorage, receiptQueueKey)[0]?.entry; assert(getSaveReceipt(terminalId) === 'needs attention' && JSON.stringify(parkedTerminal) === JSON.stringify(terminalEntry) && JSON.stringify(receiptGateway.state.bundle) === JSON.stringify(remoteBeforeTerminal), 'A terminal production replay must publish Needs attention, park the full exact queued entry, and leave remote state unchanged.'); receiptGateway.throwError = null
  const lostId = uid(1104); const unrelatedBeforeLost = structuredClone(receiptGateway.state.bundle.grain_bins); receiptGateway.throwAfterProductionPersist = true; receiptGateway.afterProductionPersist = () => { receiptOnline = false }; await receiptQueue.saveProductionEstimate({ ...created, id: lostId }); const lostEntry = structuredClone(new GrainWriteQueue(receiptStorage, receiptQueueKey).read().entries[0]); await new Promise<void>((resolve) => setTimeout(resolve, 0)); receiptGateway.throwAfterProductionPersist = false; receiptOnline = true; await receiptQueue.inspectAndReplay(); assert(lostEntry.operationId.length > 0 && receiptGateway.productionInputs.filter((row) => row.id === lostId).length === 2 && (receiptGateway.state.bundle.production_estimates as ProductionEstimate[]).filter((row) => row.id === lostId).length === 1 && JSON.stringify(receiptGateway.state.bundle.grain_bins) === JSON.stringify(unrelatedBeforeLost) && getSaveReceipt(lostId) === 'saved', 'A lost production response must replay the same queued operation in two gateway attempts, converge on one logical persist, leave unrelated bins unchanged, and publish Saved.')
  const reconcileEstimate = (await receiptWriter.getData()).production_estimates[0]; let releaseReconcile!: () => void; const reconcileGate = new Promise<void>((resolve) => { releaseReconcile = resolve }); receiptGateway.beforeProductionActual = () => reconcileGate; const delayedReconcile = receiptQueue.reconcileHarvestActual(reconcileEstimate, 50_000); await new Promise<void>((resolve) => setTimeout(resolve, 0)); assert(getSaveReceipt(reconcileEstimate.id) === 'saving' && receiptGateway.productionActualInputs.length === 1, 'A delayed Harvest reconciliation must publish Saving under the exact existing estimate ID before its online RPC returns.'); releaseReconcile(); await delayedReconcile; const reconciled = receiptGateway.state.bundle.production_estimates.find((row) => (row as ProductionEstimate).id === reconcileEstimate.id) as ProductionEstimate; assert(getSaveReceipt(reconcileEstimate.id) === 'saved' && reconciled.actual_bushels === 50_000 && reconciled.drives_math === 'actual' && JSON.stringify(receiptGateway.state.bundle.grain_bins) === JSON.stringify(unrelatedBeforeLost), 'Harvest reconciliation must publish Saved and change only Grain actual and its math basis, never bins.')
  const offlineBinsBefore = structuredClone(receiptGateway.state.bundle.grain_bins); const reconciliationCalls = receiptGateway.productionActualInputs.length; receiptOnline = false; let offlineReconcileMessage = ''; try { await receiptQueue.reconcileHarvestActual(reconciled, 51_000) } catch (error) { offlineReconcileMessage = error instanceof Error ? error.message : '' }; assert(offlineReconcileMessage === 'Connect to the internet before using the harvest total.' && receiptGateway.productionActualInputs.length === reconciliationCalls && getSaveReceipt(reconcileEstimate.id) === 'needs attention' && JSON.stringify(receiptGateway.state.bundle.grain_bins) === JSON.stringify(offlineBinsBefore), 'Offline Harvest reconciliation must make zero server writes, retain bins, and expose its exact connection error with Needs attention.'); receiptOnline = true; await receiptQueue.reconcileHarvestActual(reconciled, 51_000); assert(receiptGateway.productionActualInputs.length === reconciliationCalls + 1 && getSaveReceipt(reconcileEstimate.id) === 'saved' && new GrainWriteQueue(receiptStorage, receiptQueueKey).read().entries.length === 0, 'Harvest reconciliation must remain a direct online retry: one fresh retry, no operation receipt queue entry, and Saved on recovery.')
  const staleStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }; const staleGateway = new FakeGateway(); const staleId = uid(1105); const staleEstimate = { ...(await repository(staleGateway).getData()).production_estimates[0], id: staleId }; staleGateway.state.bundle.production_estimates = [...staleGateway.state.bundle.production_estimates, staleEstimate]; const staleScope = { projectRef: supabaseConfig.projectRef, userId: uid(10), farmId: staleGateway.state.fields.farm.id }; resetFarmGrantFromLive(staleStorage, staleScope, 1, stamp); let staleRemoteAccepted = false; staleGateway.afterOnlineMutation = () => { staleRemoteAccepted = true; resetFarmGrantFromLive(staleStorage, staleScope, 2, '2026-07-12T00:00:00.000Z') }; const staleQueue = new QueuedGrainRepository(repository(staleGateway), { getContext: async () => ({ userId: uid(10), farmId: staleGateway.state.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: staleStorage, createId: () => uid(1110), clock: () => stamp, isOffline: () => false }); await rejects(() => staleQueue.reconcileHarvestActual(staleEstimate, 50_000), 'A post-remote context fence failure must rethrow.'); assert(staleRemoteAccepted && staleGateway.productionActualInputs.length === 1 && getSaveReceipt(staleId) === 'saving', 'A production-equivalent revocation fence change after remote acceptance must preserve the narrow write but publish neither Saved nor Needs attention.')

  // 27 (November): the farmer's contract/bin saves keep one durable receipt through delay, offline replay,
  // terminal parking, and a lost response. The test uses the real queue rather than a substitute writer.
  const novemberStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }
  let novemberOnline = true; let novemberSequence = 1200; const novemberGateway = new FakeGateway(); const novemberWriter = repository(novemberGateway); const novemberQueue = new QueuedGrainRepository(novemberWriter, { getContext: async () => ({ userId: uid(10), farmId: novemberGateway.state.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: novemberStorage, createId: () => uid(novemberSequence++), clock: () => '2027-11-10T17:10:00.000Z', isOffline: () => !novemberOnline })
  const novemberQueueKey = grainWriteQueueKey(supabaseConfig.projectRef, uid(10), novemberGateway.state.fields.farm.id)
  const novemberContract = async (id: string): Promise<GrainContract> => ({ ...(await novemberWriter.getData()).grain_contracts[0], id, crop_year: 2027, delivery_start: '2027-11-10', delivery_end: '2027-11-30', created_at: '2027-11-10T17:10:00.000Z', updated_at: '2027-11-10T17:10:00.000Z' })
  const novemberBin = async (id: string): Promise<GrainBin> => ({ ...(await novemberWriter.getData()).grain_bins[0], id, name: `November ${id.slice(-4)}`, capacity_bu: 60_000, created_at: '2027-11-10T17:10:00.000Z', updated_at: '2027-11-10T17:10:00.000Z' })
  const delayedContract = await novemberContract(uid(1201)); let releaseContract!: () => void; const contractGate = new Promise<void>((resolve) => { releaseContract = resolve }); novemberGateway.beforeContract = () => contractGate
  const delayedContractSave = novemberQueue.saveContract(delayedContract); await new Promise<void>((resolve) => setTimeout(resolve, 0)); assert(getSaveReceipt(delayedContract.id) === 'saving' && novemberGateway.contractInputs.filter((row) => row.id === delayedContract.id).length === 1, 'A delayed November contract must publish Saving before exactly one durable attempt.'); releaseContract(); await delayedContractSave; novemberGateway.beforeContract = null; assert(getSaveReceipt(delayedContract.id) === 'saved' && novemberGateway.state.bundle.grain_contracts.filter((row) => (row as GrainContract).id === delayedContract.id).length === 1, 'A saved November contract must keep its stable id and exactly one logical persist.')
  const offlineContract = await novemberContract(uid(1202)); novemberOnline = false; await novemberQueue.saveContract(offlineContract); const queuedContract = structuredClone(new GrainWriteQueue(novemberStorage, novemberQueueKey).read().entries.at(-1)!); assert(getSaveReceipt(offlineContract.id) === 'queued offline' && queuedContract.kind === 'saveContract' && JSON.stringify((queuedContract as { row: GrainContract }).row) === JSON.stringify(offlineContract), 'An offline November contract must retain its full exact queue entry.'); novemberOnline = true; await novemberQueue.inspectAndReplay(); assert(novemberGateway.contractInputs.filter((row) => row.id === offlineContract.id).length === 1 && new GrainWriteQueue(novemberStorage, novemberQueueKey).read().entries.length === 0 && getSaveReceipt(offlineContract.id) === 'saved', 'An offline November contract must replay once with the original id and drain only after confirmation.')
  const terminalContract = await novemberContract(uid(1203)); novemberOnline = false; await novemberQueue.saveContract(terminalContract); const terminalContractEntry = structuredClone(new GrainWriteQueue(novemberStorage, novemberQueueKey).read().entries.at(-1)!); const contractBeforeTerminal = structuredClone(novemberGateway.state.bundle); novemberGateway.throwError = new Error('contract validation failed'); novemberOnline = true; await novemberQueue.inspectAndReplay(); assert(getSaveReceipt(terminalContract.id) === 'needs attention' && JSON.stringify(readNeedsAttention(novemberStorage, novemberQueueKey).at(-1)?.entry) === JSON.stringify(terminalContractEntry) && JSON.stringify(novemberGateway.state.bundle) === JSON.stringify(contractBeforeTerminal), 'A terminal November contract must park the exact full entry and leave the remote bundle byte-stable.'); novemberGateway.throwError = null
  const lostContract = await novemberContract(uid(1204)); novemberGateway.throwAfterContractPersist = true; novemberGateway.afterContractPersist = () => { novemberOnline = false }; await novemberQueue.saveContract(lostContract); assert(getSaveReceipt(lostContract.id) === 'queued offline' && new GrainWriteQueue(novemberStorage, novemberQueueKey).read().entries.some((entry) => entry.kind === 'saveContract' && entry.row.id === lostContract.id), 'A lost November contract response must queue the original operation for reconciliation.'); novemberGateway.throwAfterContractPersist = false; novemberGateway.afterContractPersist = null; novemberOnline = true; await novemberQueue.inspectAndReplay(); assert(novemberGateway.contractInputs.filter((row) => row.id === lostContract.id).length === 2 && novemberGateway.state.bundle.grain_contracts.filter((row) => (row as GrainContract).id === lostContract.id).length === 1 && getSaveReceipt(lostContract.id) === 'saved', 'A lost November contract response must make two attempts, one logical persist, and end Saved.')
  const delayedBin = await novemberBin(uid(1211)); let releaseBin!: () => void; const binGate = new Promise<void>((resolve) => { releaseBin = resolve }); novemberGateway.beforeBin = () => binGate
  const delayedBinSave = novemberQueue.upsertGrainBin(delayedBin); await new Promise<void>((resolve) => setTimeout(resolve, 0)); assert(getSaveReceipt(delayedBin.id) === 'saving' && novemberGateway.binInputs.filter((row) => row.id === delayedBin.id).length === 1, 'A delayed November bin must publish Saving before exactly one durable attempt.'); releaseBin(); await delayedBinSave; novemberGateway.beforeBin = null; assert(getSaveReceipt(delayedBin.id) === 'saved' && novemberGateway.state.bundle.grain_bins.filter((row) => (row as GrainBin).id === delayedBin.id).length === 1, 'A saved November bin must keep its stable id and exactly one logical persist.')
  const offlineBin = await novemberBin(uid(1212)); novemberOnline = false; await novemberQueue.upsertGrainBin(offlineBin); const queuedBinEntry = structuredClone(new GrainWriteQueue(novemberStorage, novemberQueueKey).read().entries.at(-1)!); assert(getSaveReceipt(offlineBin.id) === 'queued offline' && queuedBinEntry.kind === 'upsertGrainBin' && JSON.stringify((queuedBinEntry as { row: GrainBin }).row) === JSON.stringify(offlineBin), 'An offline November bin must retain its full exact queue entry.'); novemberOnline = true; await novemberQueue.inspectAndReplay(); assert(novemberGateway.binInputs.filter((row) => row.id === offlineBin.id).length === 1 && new GrainWriteQueue(novemberStorage, novemberQueueKey).read().entries.length === 0 && getSaveReceipt(offlineBin.id) === 'saved', 'An offline November bin must replay once with the original id and drain only after confirmation.')
  const terminalBin = await novemberBin(uid(1213)); novemberOnline = false; await novemberQueue.upsertGrainBin(terminalBin); const terminalBinEntry = structuredClone(new GrainWriteQueue(novemberStorage, novemberQueueKey).read().entries.at(-1)!); const binBeforeTerminal = structuredClone(novemberGateway.state.bundle); novemberGateway.throwError = new Error('bin validation failed'); novemberOnline = true; await novemberQueue.inspectAndReplay(); assert(getSaveReceipt(terminalBin.id) === 'needs attention' && JSON.stringify(readNeedsAttention(novemberStorage, novemberQueueKey).at(-1)?.entry) === JSON.stringify(terminalBinEntry) && JSON.stringify(novemberGateway.state.bundle) === JSON.stringify(binBeforeTerminal), 'A terminal November bin must park the exact full entry and leave the remote bundle byte-stable.'); novemberGateway.throwError = null
  const lostBin = await novemberBin(uid(1214)); novemberGateway.throwAfterBinPersist = true; novemberGateway.afterBinPersist = () => { novemberOnline = false }; await novemberQueue.upsertGrainBin(lostBin); assert(getSaveReceipt(lostBin.id) === 'queued offline' && new GrainWriteQueue(novemberStorage, novemberQueueKey).read().entries.some((entry) => entry.kind === 'upsertGrainBin' && entry.row.id === lostBin.id), 'A lost November bin response must queue the original operation for reconciliation.'); novemberGateway.throwAfterBinPersist = false; novemberGateway.afterBinPersist = null; novemberOnline = true; await novemberQueue.inspectAndReplay(); assert(novemberGateway.binInputs.filter((row) => row.id === lostBin.id).length === 2 && novemberGateway.state.bundle.grain_bins.filter((row) => (row as GrainBin).id === lostBin.id).length === 1 && getSaveReceipt(lostBin.id) === 'saved', 'A lost November bin response must make two attempts, one logical persist, and end Saved.')

  // Direct November movement and delivery writes remain independent append-only records. A delivery never
  // subtracts a bin, and an Out movement never implies a contract delivery.
  const directStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }; let directOnline = true; const directGateway = new FakeGateway(); const directInventory = directGateway.state.bundle.bin_inventory[0] as { bushels: string; committed_bushels: string }; directInventory.bushels = '30000'; directInventory.committed_bushels = '0'; (directGateway.state.bundle.grain_bins[0] as { capacity_bu: string }).capacity_bu = '60000'; (directGateway.state.bundle.grain_contracts[0] as { bushels: string }).bushels = '10000'
  const directQueue = new QueuedGrainRepository(repository(directGateway), { getContext: async () => ({ userId: uid(10), farmId: directGateway.state.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: directStorage, createId: () => uid(1220), clock: () => '2027-11-10T17:10:00.000Z', isOffline: () => !directOnline }); const directFarm = directGateway.state.fields.farm.id; const directBin = String((directGateway.state.bundle.grain_bins[0] as { id: string }).id); const directContract = String((directGateway.state.bundle.grain_contracts[0] as { id: string }).id); const directCommodity = directGateway.state.scope.commodity_id
  const novemberIn: BinTransaction = { id: uid(1221), farm_id: directFarm, grain_bin_id: directBin, direction: 'in', bushels: 800, commodity_id: directCommodity, occurred_on: '2027-11-10', note: 'November load', source_kind: 'manual entry', created_at: '2027-11-10T17:10:00.000Z' }; await directQueue.appendBinTransaction(novemberIn); const balanceAfterIn = deriveBinOnHand((await repository(directGateway).getData()).bin_inventory.find((row) => row.grain_bin_id === directBin), (await repository(directGateway).getData()).bin_transactions.filter((row) => row.grain_bin_id === directBin)).onHand; assert(directGateway.movementInputs.filter((row) => row.id === novemberIn.id).length === 1 && balanceAfterIn === 30_800, 'A direct November In movement must append once under its stable id and make the bin balance 30,800 bu.')
  const novemberDelivery: GrainContractDelivery = { id: uid(1222), farm_id: directFarm, grain_contract_id: directContract, bushels: 5_000, delivered_on: '2027-11-10', note: 'November contract delivery', created_at: '2027-11-10T17:10:00.000Z' }; const binBeforeDelivery = structuredClone(directGateway.state.bundle.bin_transactions); await directQueue.recordContractDelivery(novemberDelivery); assert(directGateway.state.bundle.grain_contract_deliveries.filter((row) => (row as GrainContractDelivery).id === novemberDelivery.id).length === 1 && JSON.stringify(directGateway.state.bundle.bin_transactions) === JSON.stringify(binBeforeDelivery) && getSaveReceipt(novemberDelivery.id) === 'saved', 'A direct November delivery must append once, show Saved, and never alter the bin ledger.')
  const novemberOut: BinTransaction = { ...novemberIn, id: uid(1223), direction: 'out', bushels: 5_000, occurred_on: '2027-11-11', note: 'November unload', created_at: '2027-11-11T17:10:00.000Z' }; const deliveredBeforeOut = structuredClone(directGateway.state.bundle.grain_contract_deliveries); await directQueue.appendBinTransaction(novemberOut); const balanceAfterOut = deriveBinOnHand((await repository(directGateway).getData()).bin_inventory.find((row) => row.grain_bin_id === directBin), (await repository(directGateway).getData()).bin_transactions.filter((row) => row.grain_bin_id === directBin)).onHand; assert(directGateway.movementInputs.filter((row) => row.id === novemberOut.id).length === 1 && balanceAfterOut === 25_800 && JSON.stringify(directGateway.state.bundle.grain_contract_deliveries) === JSON.stringify(deliveredBeforeOut), 'A direct November Out movement must append once, make the bin balance 25,800 bu, and never change deliveries.')
  directGateway.throwAfterMovementPersist = true; directGateway.afterMovementPersist = () => { directOnline = false }; const lostMovement: BinTransaction = { ...novemberIn, id: uid(1224), bushels: 1, occurred_on: '2027-11-12' }; await rejects(() => directQueue.appendBinTransaction(lostMovement), 'A lost direct movement response must make the farmer retry.'); assert(getSaveReceipt(lostMovement.id) === 'confirmation needed' && getSaveReceipt(lostMovement.id) !== 'needs attention' && directGateway.movementInputs.filter((row) => row.id === lostMovement.id).length === 1, 'A lost direct movement response must truthfully retain Confirmation needed after the accepted first attempt.'); directGateway.throwAfterMovementPersist = false; directGateway.afterMovementPersist = null; directOnline = true; await directQueue.appendBinTransaction(lostMovement); assert(directGateway.movementInputs.filter((row) => row.id === lostMovement.id).length === 2 && directGateway.state.bundle.bin_transactions.filter((row) => (row as BinTransaction).id === lostMovement.id).length === 1 && getSaveReceipt(lostMovement.id) === 'saved', 'A retried direct movement must use the stable id for two attempts and one logical persist.')
  directGateway.throwAfterDeliveryPersist = true; directGateway.afterDeliveryPersist = () => { directOnline = false }; const lostDelivery: GrainContractDelivery = { ...novemberDelivery, id: uid(1225), delivered_on: '2027-11-12' }; await rejects(() => directQueue.recordContractDelivery(lostDelivery), 'A lost direct delivery response must make the farmer retry.'); assert(getSaveReceipt(lostDelivery.id) === 'confirmation needed' && getSaveReceipt(lostDelivery.id) !== 'needs attention' && directGateway.state.bundle.grain_contract_deliveries.filter((row) => (row as GrainContractDelivery).id === lostDelivery.id).length === 1, 'A lost direct delivery response must truthfully retain Confirmation needed after the accepted first attempt.'); directGateway.throwAfterDeliveryPersist = false; directGateway.afterDeliveryPersist = null; directOnline = true; await directQueue.recordContractDelivery(lostDelivery); assert(directGateway.state.bundle.grain_contract_deliveries.filter((row) => (row as GrainContractDelivery).id === lostDelivery.id).length === 1 && getSaveReceipt(lostDelivery.id) === 'saved', 'A retried direct delivery must use its stable id for one logical persist.')
  const rejectedMovement: BinTransaction = { ...novemberIn, id: uid(1228), occurred_on: '2027-11-14' }; directGateway.movementError = new Error('movement validation failed'); await rejects(() => directQueue.appendBinTransaction(rejectedMovement), 'A canonical movement rejection must fail directly.'); directGateway.movementError = null; const existingDelivery: GrainContractDelivery = { ...novemberDelivery, id: uid(1229), delivered_on: '2027-11-14' }; directGateway.state.bundle.grain_contract_deliveries = [...directGateway.state.bundle.grain_contract_deliveries, existingDelivery]; const rejectedDelivery = { ...existingDelivery, bushels: existingDelivery.bushels + 1 }; await rejects(() => directQueue.recordContractDelivery(rejectedDelivery), 'A canonical conflicting delivery must fail directly.'); assert(getSaveReceipt(rejectedMovement.id) === 'needs attention' && getSaveReceipt(rejectedDelivery.id) === 'needs attention', 'Definite canonical movement and delivery failures must remain Needs attention, never Confirmation needed.')
  directOnline = false; const offlineDirectMovement: BinTransaction = { ...novemberIn, id: uid(1226), occurred_on: '2027-11-13' }; const offlineDirectDelivery: GrainContractDelivery = { ...novemberDelivery, id: uid(1227), delivered_on: '2027-11-13' }; await rejects(() => directQueue.appendBinTransaction(offlineDirectMovement), 'An offline movement must be refused before a server call.'); await rejects(() => directQueue.recordContractDelivery(offlineDirectDelivery), 'An offline delivery must be refused before a server call.'); assert(directGateway.movementInputs.filter((row) => row.id === offlineDirectMovement.id).length === 0 && directGateway.state.bundle.grain_contract_deliveries.filter((row) => (row as GrainContractDelivery).id === offlineDirectDelivery.id).length === 0 && getSaveReceipt(offlineDirectMovement.id) === 'needs attention' && getSaveReceipt(offlineDirectDelivery.id) === 'needs attention', 'Offline November direct writes must make zero server calls and publish Needs attention.'); directOnline = true
  const reverseGateway = new FakeGateway(); const reverseInventory = reverseGateway.state.bundle.bin_inventory[0] as { bushels: string; committed_bushels: string }; reverseInventory.bushels = '30000'; reverseInventory.committed_bushels = '0'; (reverseGateway.state.bundle.grain_bins[0] as { capacity_bu: string }).capacity_bu = '60000'; (reverseGateway.state.bundle.grain_contracts[0] as { bushels: string }).bushels = '10000'; const reverseStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }; const reverseQueue = new QueuedGrainRepository(repository(reverseGateway), { getContext: async () => ({ userId: uid(10), farmId: reverseGateway.state.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: reverseStorage, createId: () => uid(1230), clock: () => '2027-11-10T17:10:00.000Z', isOffline: () => false }); const reverseFarm = reverseGateway.state.fields.farm.id; const reverseBin = String((reverseGateway.state.bundle.grain_bins[0] as { id: string }).id); const reverseContract = String((reverseGateway.state.bundle.grain_contracts[0] as { id: string }).id); await reverseQueue.recordContractDelivery({ ...novemberDelivery, id: uid(1231), farm_id: reverseFarm, grain_contract_id: reverseContract }); await reverseQueue.appendBinTransaction({ ...novemberIn, id: uid(1232), farm_id: reverseFarm, grain_bin_id: reverseBin }); await reverseQueue.appendBinTransaction({ ...novemberOut, id: uid(1233), farm_id: reverseFarm, grain_bin_id: reverseBin }); const reverseWorkspace = await repository(reverseGateway).getData(); assert(deriveBinOnHand(reverseWorkspace.bin_inventory.find((row) => row.grain_bin_id === reverseBin), reverseWorkspace.bin_transactions.filter((row) => row.grain_bin_id === reverseBin)).onHand === 25_800 && reverseWorkspace.grain_contract_deliveries.reduce((sum, row) => sum + row.bushels, 0) === 5_000, 'November movement and delivery order must be independent: no automatic coupling can create or erase either record.')

  // A remote acceptance followed by a new grant must leave one accepted mutation but never tell the farmer Saved.
  const fenceDirectStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }; const fenceDirectGateway = new FakeGateway(); const fenceDirectScope = { projectRef: supabaseConfig.projectRef, userId: uid(10), farmId: fenceDirectGateway.state.fields.farm.id }; resetFarmGrantFromLive(fenceDirectStorage, fenceDirectScope, 1, stamp); fenceDirectGateway.afterOnlineMutation = () => { resetFarmGrantFromLive(fenceDirectStorage, fenceDirectScope, 2, '2027-11-10T17:10:01.000Z') }; const fenceDirectQueue = new QueuedGrainRepository(repository(fenceDirectGateway), { getContext: async () => ({ userId: uid(10), farmId: fenceDirectGateway.state.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: fenceDirectStorage, createId: () => uid(1240), clock: () => '2027-11-10T17:10:00.000Z', isOffline: () => false }); const fenceMovement: BinTransaction = { ...novemberIn, id: uid(1241), farm_id: fenceDirectGateway.state.fields.farm.id, grain_bin_id: String((fenceDirectGateway.state.bundle.grain_bins[0] as { id: string }).id) }; await rejects(() => fenceDirectQueue.appendBinTransaction(fenceMovement), 'A post-remote November movement grant change must cancel publication.'); assert(fenceDirectGateway.movementInputs.filter((row) => row.id === fenceMovement.id).length === 1 && fenceDirectGateway.state.bundle.bin_transactions.filter((row) => (row as BinTransaction).id === fenceMovement.id).length === 1 && getSaveReceipt(fenceMovement.id) === 'saving', 'A post-remote November movement grant change must keep exactly one accepted mutation and publish neither Saved nor Needs attention.')
  const novemberFenceDeliveryStorage: StorageLike & { values: Map<string, string> } = { values: new Map(), getItem(key) { return this.values.get(key) ?? null }, setItem(key, value) { this.values.set(key, value) }, removeItem(key) { this.values.delete(key) } }; const novemberFenceDeliveryGateway = new FakeGateway(); const novemberFenceDeliveryScope = { projectRef: supabaseConfig.projectRef, userId: uid(10), farmId: novemberFenceDeliveryGateway.state.fields.farm.id }; resetFarmGrantFromLive(novemberFenceDeliveryStorage, novemberFenceDeliveryScope, 1, stamp); novemberFenceDeliveryGateway.afterOnlineMutation = () => { resetFarmGrantFromLive(novemberFenceDeliveryStorage, novemberFenceDeliveryScope, 2, '2027-11-10T17:10:01.000Z') }; const novemberFenceDeliveryQueue = new QueuedGrainRepository(repository(novemberFenceDeliveryGateway), { getContext: async () => ({ userId: uid(10), farmId: novemberFenceDeliveryGateway.state.fields.farm.id }), projectRef: supabaseConfig.projectRef, storage: novemberFenceDeliveryStorage, createId: () => uid(1242), clock: () => '2027-11-10T17:10:00.000Z', isOffline: () => false }); const novemberFenceDelivery: GrainContractDelivery = { ...novemberDelivery, id: uid(1243), farm_id: novemberFenceDeliveryGateway.state.fields.farm.id, grain_contract_id: String((novemberFenceDeliveryGateway.state.bundle.grain_contracts[0] as { id: string }).id) }; await rejects(() => novemberFenceDeliveryQueue.recordContractDelivery(novemberFenceDelivery), 'A post-remote November delivery grant change must cancel publication.'); assert(novemberFenceDeliveryGateway.state.bundle.grain_contract_deliveries.filter((row) => (row as GrainContractDelivery).id === novemberFenceDelivery.id).length === 1 && getSaveReceipt(novemberFenceDelivery.id) === 'saving', 'A post-remote November delivery grant change must keep exactly one accepted mutation and publish neither Saved nor Needs attention.')
  assert(moduleBackends.fields === 'supabase' && moduleBackends.grain === 'supabase', 'Release composition did not select live Fields and Grain.')
  console.log('SupabaseGrainRepository regressions passed.')
}
void run().catch((error: unknown) => { console.error(error); process.exitCode = 1 })
