import { QueuedEquipmentTasksRepository } from './QueuedEquipmentTasksRepository'
import { SupabaseEquipmentTasksDataGateway } from './SupabaseEquipmentTasksDataGateway'
import { SupabaseEquipmentTasksRepository } from './SupabaseEquipmentTasksRepository'
import type { FieldsRepository } from './fields'
import type { StorageLike } from './writeQueue'
import { captureFarmOperationContext, verifyFarmOperationContext } from './farmOperationContext'
export function createSupabaseEquipmentTasksServices(d: { fieldsRepository: FieldsRepository; getFarmId: () => Promise<string>; getContext: () => Promise<{ userId: string; farmId: string }>; projectRef: string; storage: StorageLike; createId: () => string; isOffline: () => boolean }) { const getOperationContext = async () => captureFarmOperationContext(d.storage, d.projectRef, await d.getContext()); const verifyOperationContext = async (expected: Awaited<ReturnType<typeof getOperationContext>>) => verifyFarmOperationContext(d.storage, expected, await getOperationContext()); const live = new SupabaseEquipmentTasksRepository({ gateway: new SupabaseEquipmentTasksDataGateway(), fieldsRepository: d.fieldsRepository, getFarmId: d.getFarmId, getUserId: async () => (await d.getContext()).userId, getOperationContext, verifyOperationContext, createId: d.createId }); const queued = new QueuedEquipmentTasksRepository(live, { ...d, clock: () => new Date().toISOString() }); return { equipmentTasksRepository: queued, replayEquipmentTasksQueue: () => queued.inspectAndReplay() } }
