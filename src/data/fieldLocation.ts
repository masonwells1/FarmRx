import { supabase } from '../lib/supabaseClient'
import { isTransportFailure } from './QueuedFieldsRepository'
import { setModuleSyncStatus } from './syncStatus'
import { isFarmReplayContextChangedError, launchReplayInBackground, type StorageLike } from './writeQueue'
import { queueTransaction } from './queueTransaction'
import { bindFarmOperationRequest, type FarmOperationContext } from './farmOperationContext'
import { captureQueuedOperationContext, verifyQueuedOperationContext } from './queuedOperationGuard'

export type LocationSource = 'gps' | 'manual'
export interface FieldLocation { id: string; farm_id: string; latitude: number; longitude: number; location_source: LocationSource }
export interface FieldLocationGateway { setFieldLocation(input: { farmId: string; fieldId: string; latitude: number; longitude: number; source: LocationSource }, context: FarmOperationContext): Promise<unknown> }
export interface FieldLocationClient { saveLocation(fieldId: string, latitude: number, longitude: number, source: LocationSource): Promise<FieldLocation | null>; replay(): Promise<void> }
type Context = { userId: string; farmId: string }
type QueueEntry = { version: 1; module: 'weather'; kind: 'setFieldLocation'; operationId: string; userId: string; farmId: string; fieldId: string; latitude: number; longitude: number; source: LocationSource; enqueuedAt: string }
export type FieldLocationQueueEnvelopeV1 = { version: 1; entries: QueueEntry[] }
const uuid = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i
const key = (projectRef: string, userId: string, farmId: string) => `farm-rx-field-location-queue:v1:${projectRef}:${userId}:${farmId}`
const blocked = 'Saved changes on this device need attention. Nothing was deleted.'
const valid = (entry: unknown): entry is QueueEntry => { if (!entry || typeof entry !== 'object' || Array.isArray(entry)) return false; const value = entry as Record<string, unknown>; return Object.keys(value).length === 11 && value.version === 1 && value.module === 'weather' && value.kind === 'setFieldLocation' && [value.operationId, value.userId, value.farmId, value.fieldId].every((id) => typeof id === 'string' && uuid.test(id)) && typeof value.latitude === 'number' && Number.isFinite(value.latitude) && value.latitude >= -90 && value.latitude <= 90 && typeof value.longitude === 'number' && Number.isFinite(value.longitude) && value.longitude >= -180 && value.longitude <= 180 && (value.source === 'gps' || value.source === 'manual') && typeof value.enqueuedAt === 'string' && !Number.isNaN(Date.parse(value.enqueuedAt)) }
export function parseFieldLocationQueue(serialized: string): FieldLocationQueueEnvelopeV1 { let parsed: unknown; try { parsed = JSON.parse(serialized); if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error(); const value = parsed as Record<string, unknown>; if (Object.keys(value).length !== 2 || value.version !== 1 || !Array.isArray(value.entries) || !value.entries.every(valid)) throw new Error(); return value as unknown as FieldLocationQueueEnvelopeV1 } catch { throw new Error(blocked) } }
function read(storage: StorageLike, storageKey: string): FieldLocationQueueEnvelopeV1 { const raw = storage.getItem(storageKey); return raw === null ? { version: 1, entries: [] } : parseFieldLocationQueue(raw) }
function persist(storage: StorageLike, storageKey: string, next: FieldLocationQueueEnvelopeV1) { const raw = JSON.stringify(next); parseFieldLocationQueue(raw); storage.setItem(storageKey, raw); if (storage.getItem(storageKey) !== raw) throw new Error('This location could not be saved on this device. Keep this screen open and try again.') }
export function mapFieldLocationEcho(value: unknown, expected: { farmId: string; fieldId: string; latitude: number; longitude: number; source: LocationSource }): FieldLocation { if (!value || typeof value !== 'object' || Array.isArray(value)) throw new Error('Farm Rx could not confirm the field location.'); const row = value as Record<string, unknown>; const latitude = typeof row.latitude === 'number' ? row.latitude : typeof row.latitude === 'string' ? Number(row.latitude) : NaN; const longitude = typeof row.longitude === 'number' ? row.longitude : typeof row.longitude === 'string' ? Number(row.longitude) : NaN; if (row.id !== expected.fieldId || row.farm_id !== expected.farmId || latitude !== expected.latitude || longitude !== expected.longitude || row.location_source !== expected.source) throw new Error('Farm Rx could not confirm the field location.'); return { id: expected.fieldId, farm_id: expected.farmId, latitude, longitude, location_source: expected.source } }
export class SupabaseFieldLocationGateway implements FieldLocationGateway { async setFieldLocation(input: { farmId: string; fieldId: string; latitude: number; longitude: number; source: LocationSource }, context: FarmOperationContext) { const { data, error } = await bindFarmOperationRequest(supabase.rpc('set_field_location', { p_farm_id: input.farmId, p_field_id: input.fieldId, p_latitude: input.latitude, p_longitude: input.longitude, p_source: input.source }), context); if (error) throw error; return data } }
export function createFieldLocationClient(d: { gateway: FieldLocationGateway; getContext: () => Promise<Context>; projectRef: string; storage: StorageLike; createId: () => string; clock: () => string; isOffline: () => boolean }): FieldLocationClient {
  const source = async () => { const operationContext = await captureQueuedOperationContext(d); const context = { userId: operationContext.userId, farmId: operationContext.farmId }; return { context, operationContext, storageKey: key(d.projectRef, context.userId, context.farmId) } }
  const send = async (entry: QueueEntry, operationContext: FarmOperationContext) => { await verifyQueuedOperationContext(d, operationContext, entry); const saved = await d.gateway.setFieldLocation({ farmId: entry.farmId, fieldId: entry.fieldId, latitude: entry.latitude, longitude: entry.longitude, source: entry.source }, operationContext); await verifyQueuedOperationContext(d, operationContext, entry); return mapFieldLocationEcho(saved, { farmId: entry.farmId, fieldId: entry.fieldId, latitude: entry.latitude, longitude: entry.longitude, source: entry.source }) }
  const enqueue = (storageKey: string, entry: QueueEntry) => { const current = read(d.storage, storageKey); const entries = [...current.entries.filter((item) => item.fieldId !== entry.fieldId), entry]; persist(d.storage, storageKey, { version: 1, entries }); setModuleSyncStatus('weather', { kind: 'pending', pending: entries.length }) }
  const locked = <T,>(storageKey: string, task: (verify: () => void) => Promise<T>) => queueTransaction(storageKey, d.storage, d.createId, task)
  async function replay() { let state: Awaited<ReturnType<typeof source>>; try { state = await source() } catch (error) { if (isFarmReplayContextChangedError(error)) throw error; return }; try { await locked(state.storageKey, async (verify) => { await verifyQueuedOperationContext(d, state.operationContext, state.context); verify(); let current = read(d.storage, state.storageKey); if (!current.entries.length) { setModuleSyncStatus('weather', { kind: 'synced', pending: 0 }); return } if (d.isOffline()) { setModuleSyncStatus('weather', { kind: 'pending', pending: current.entries.length }); return } while (current.entries.length) { const entry = current.entries[0]; if (entry.userId !== state.context.userId || entry.farmId !== state.context.farmId) throw new Error(blocked); await verifyQueuedOperationContext(d, state.operationContext, entry); setModuleSyncStatus('weather', { kind: 'syncing', pending: current.entries.length }); try { await send(entry, state.operationContext); verify(); await verifyQueuedOperationContext(d, state.operationContext, entry); const latest = read(d.storage, state.storageKey); const entries = latest.entries.filter((item) => item.operationId !== entry.operationId); if (entries.length === latest.entries.length) throw new Error(blocked); const next = { version: 1 as const, entries }; await verifyQueuedOperationContext(d, state.operationContext, entry); persist(d.storage, state.storageKey, next); current = next } catch (error) { await verifyQueuedOperationContext(d, state.operationContext, entry); if (isTransportFailure(error, d.isOffline())) { setModuleSyncStatus('weather', { kind: 'pending', pending: current.entries.length }); return } setModuleSyncStatus('weather', { kind: 'blocked', pending: current.entries.length, message: blocked }); return } } setModuleSyncStatus('weather', { kind: 'synced', pending: 0 }) }) } catch (error) { if (isFarmReplayContextChangedError(error)) throw error; setModuleSyncStatus('weather', { kind: 'blocked', pending: 0, message: blocked }) } }
  return { async saveLocation(fieldId, latitude, longitude, sourceValue) { if (!uuid.test(fieldId) || !Number.isFinite(latitude) || latitude < -90 || latitude > 90 || !Number.isFinite(longitude) || longitude < -180 || longitude > 180) throw new Error('Enter a valid latitude and longitude.'); const state = await source(); const entry: QueueEntry = { version: 1, module: 'weather', kind: 'setFieldLocation', operationId: d.createId(), userId: state.context.userId, farmId: state.context.farmId, fieldId, latitude, longitude, source: sourceValue, enqueuedAt: d.clock() }; const result = await locked(state.storageKey, async (verify) => { const verifyOperation = async () => { verify(); await verifyQueuedOperationContext(d, state.operationContext, entry) }; await verifyOperation(); if (d.isOffline() || read(d.storage, state.storageKey).entries.length) { await verifyOperation(); enqueue(state.storageKey, entry); return null } try { const saved = await send(entry, state.operationContext); await verifyOperation(); setModuleSyncStatus('weather', { kind: 'synced', pending: 0 }); return saved } catch (error) { await verifyQueuedOperationContext(d, state.operationContext, entry); if (!isTransportFailure(error, d.isOffline())) throw error; await verifyOperation(); enqueue(state.storageKey, entry); return null } }); if (result === null) launchReplayInBackground(replay); return result }, replay }
}
